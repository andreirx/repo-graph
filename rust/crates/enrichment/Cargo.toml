[package]
name = "enrichment"
version = "0.1.0"
edition = "2021"
description = "Compiler-assisted enrichment subsystem for receiver type resolution and edge promotion"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"
uuid = { version = "1.0", features = ["v4", "v5"] }

[dev-dependencies]
