[package]
name = "repo-graph-module-queries"
version = "0.1.0"
edition = "2021"
description = "Module graph query orchestration for repo-graph"

[dependencies]
repo-graph-storage = { path = "../storage" }
repo-graph-classification = { path = "../classification" }
thiserror = "1.0"

[dev-dependencies]
# Storage-backed tests for ModuleQueryContext::load() are in
# rust/crates/rgr/tests/modules_list_command.rs as integration tests.
