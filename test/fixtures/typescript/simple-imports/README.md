# simple-imports fixture

Minimal TypeScript fixture for exercising the graph extractor,
indexer, and CLI surfaces. Contains a handful of files with
cross-module imports and exported symbols.

This README is also used to exercise the annotations extraction
layer (module_readme kind, repo-level attribution).
