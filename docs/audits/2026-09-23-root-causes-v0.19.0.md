# Root causes — audit round seven (v0.19.0), 2026-09-23

Status per entry: VERIFIED = read from source and reproduced on the retained root by the operator; OBSERVED = the graders' capture + ground truth + cited mechanism, code-level cause still to be established before a packet (the human's binding rule: root-cause before packeting).

## RC-1 (VERIFIED) — `rmap docs list` fails outright on a repository with one unreadable document → DOCS-UNREADABLE-DECODE-1
Symptom: poco `docs list` and `docs list --json` exit 2 with `error: failed to parse response: missing field content_hash` (smoke + uncontended retest). Mechanism: `legacy-codebases/poco/packaging/README.txt` is Windows-1252 (byte 0x92 at offset 232); admitted by DOCS-DISCOVERY-1's stem rule; `doc-facts/src/lib.rs:484-493 read_and_hash` uses `fs::read_to_string`, fails on invalid UTF-8, the entry is admitted with `content_hash: None` and counted `unreadable` (RG-REQ-008-L05's contract); `DocInventoryEntry.content_hash: Option<String>` with `skip_serializing_if` omits the key; `rgr/src/presentation/docs.rs:192 DocEntry.content_hash: String` is required, so the single decode in `rgr/src/commands/docs.rs` fails for the whole response in both modes. Never worked (the required field predates every slice; no corpus had an unreadable admitted doc until the widened rule). Requirements violated in the field: RG-REQ-008-L05, L08. Smallest fix: `Option<String>` with `#[serde(default)]`, renderer and `filtered_json_view` re-emit only when present, a fixture with an absent hash on an unreadable-counted entry, a poco live proof (`+1 unreadable, counted`); regression watch: every other corpus byte-identical.

## RC-2 (VERIFIED) — Python `obj.method()` binds by unique method name with the receiver ignored → PYTHON-RECEIVER-BINDING-1
Symptom: `callers ListMixin.extend` on django → `271 callers found` (ListMixin is `django/contrib/gis/geos/mutable_list.py:16`; `extend` at :192); `callees BaseHandler.load_middleware` names `ListMixin.insert` for `self._view_middleware.insert(` (base.py:80); `explain MigrationAutodetector._get_dependencies_for_model` callee `extend (django/contrib/gis/geos)` for `autodetector.py:1737 dependencies.extend(` on `:1733 dependencies = []`. Mechanism: `indexer/src/resolver.rs resolve_call_target` (:910) — after the C++ receiver stage (C++ carrier only), the namespace stage and the Python self/cls stage, the "Dotted name fallback" (:1012-1050) takes a 2-part `obj.method`, skips only Default-import receivers and the `this.field.method` shape, and binds `pick_unambiguous(nodes_by_name.get(method_name))` — the bare method name, unique in the corpus, whatever `obj` is. CALL-BINDING-RECEIVER-1's invariant was implemented on the C++ carrier; Python/TS/JS reach the fallback with no receiver disposition. Never worked. Requirements: RG-REQ-005-L01/L02 (bind on evidence; a name-only pool is stated, never a certain edge). Smallest fix: a 2-part key whose receiver is a bare local name (not self/cls, not a resolvable module alias or class) never binds by method name — unresolved with basis `receiver_untyped_name_only_pool`, candidates persisted; measure the movement first (seam over django's store: CALLS edges from this fallback with a non-self 2-part key); regression watch: PYTHON-SELF-BINDING-1's bindings, FRAKTAG's `this.field.method` shape, leveldb/vcmi byte-identical.

## RC-3 (OBSERVED, judge D4) — test-source edges promoted to production structure → TEST-EDGE-SCOPE-1 (+ IS-TEST-CPPUNIT-1)
poco `Foundation → CppUnit (415)` comes solely from 281 files under `Foundation/testsuite/` (0 in src/include); leveldb's `db -> table -> db` cycle exists only via `table/table_test.cc:11-13`. Cited mechanism: the cycles kernel classifies test-only per module (`agent/src/cycle_composition.rs:42`), module edges are not partitioned by the importing file's stored `is_test`. Surfaces: modules list, cycles, orient, explain's Import cycles. Establish: where module edges are derived (`compute_module_stats`/module-pairs CTE over `edges` joined to `files.is_test`) and whether `is_test` is available on the edge's source file at derivation time.

## RC-4 (OBSERVED, matrix N3 / judge D8) — C++ field with a trailing attribute macro extracted as a METHOD (decl) → CPP-ATTRIBUTE-MACRO-1
`db/db_impl.h:189 SnapshotList snapshots_ GUARDED_BY(mutex_);` → `leveldb::DBImpl::GUARDED_BY [METHOD] (decl)` (×4 in DBImpl, ×4 in `util/cache.cc` LRUCache, 13 facts in all); they rank in `find DBImpl::Recover` seeds and appear in `explain leveldb::DBImpl` Members (73). Control: vcmi `Q_OBJECT` body macros are not extracted. Cited mechanism: the C++ extractor parses `declarator MACRO(args)` as a function declarator. Establish: the tree-sitter node shape (`field_declaration` with a `function_declarator` whose declarator is an identifier followed by a macro call) at the extractor's method-emission site; the fix is an exclusion on that shape, never a macro name list.

## RC-5 (OBSERVED, matrix N5 / judge D3) — nginx (and sqlite ext/) module graph empty: single-segment angle includes never resolve → CPP-INCLUDE-BASENAME-1 (DECISION for the human)
nginx: 1,142 `#include <ngx_*.h>` (369 `<ngx_config.h>`, 367 `<ngx_core.h>` …), headers under `src/core`, `src/event`, `src/http` …, no `include/` directory; every specifier is single-segment and basename-unique. The ratified CPP-INCLUDE-SUFFIX-1 (≥ 2 segments) cannot recover it. Options: a unique-basename fallback bound to a uniqueness proof (one file with that basename in the repo), or reading `auto/sources`. RG-REQ-006-L03 forbids guessing; a uniqueness-bound rule is a requirement change → the human decides.

## RC-6 (OBSERVED, matrix N6 / judge D1) — npm-workspace package imports produce no module edge → TS-WORKSPACE-RESOLUTION-1
`FRAKTAG/packages/api/src/server.ts:6 import { Fraktag } from '@fraktag/engine'` (`package.json:5 "workspaces": ["packages/*"]`, `packages/engine/package.json:2 "name": "@fraktag/engine"`): `deps list` counts it as a first-party dep, `modules`/`cycles`/`trust` see no edge and call `packages/engine` isolated. TS-ALIAS-RESOLUTION-1 (tsconfig `paths`) does not cover workspace names; amodx (333 + 83 unresolved), glamCRM (231) show the same. Establish: the TS import resolver's stages and where a package-name specifier could be mapped to a workspace member's entry (`main`/`exports`).

## RC-7 (OBSERVED, matrix N4 / judge D7) — Python `from <package> import <module>` resolves to the package `__init__.py` → PYTHON-SUBMODULE-IMPORT-1
`django/core/handlers/wsgi.py:5 from django.core.handlers import base` → IMPORTS `django/core/handlers/__init__.py`, not `base.py`; `explain BaseHandler` `Referenced by (1 files)` omits `wsgi.py:113 class WSGIHandler(base.BaseHandler)` and `asgi.py:150`; 101 such lines under `django/django`. Establish: the Python import stage's target selection when the imported name is a sibling module file.

## RC-8 (OBSERVED, judge D2) — kafka's root buildscript classpath attributed to every module; `libs.*` catalog declarations unparsed → DEPS-GRADLE-CATALOG-1
61/61 modules `used 0 · no static import found 1` — the 1 is `kafka/build.gradle:27 classpath "org.ajoberstar.grgit…"` (buildscript); real deps `build.gradle:1879-1884 implementation libs.zstd/lz4/snappy/slf4jApi` (version-catalog aliases) unparsed; own package `org.apache.kafka.common.header.Header` flagged undeclared. Establish: the Gradle reader's scope handling (buildscript vs dependencies block; `libs.<alias>` → `gradle/libs.versions.toml`) and first-party package-prefix recognition.

## RC-9 (OBSERVED, judge D6) — django's root `LICENSE`/`LICENSE.python` absent; the only `license` entry is a nested Font Awesome asset
By the human's ruling of 2026-09-22 ("ignore license" — no `license` stem in RG-REQ-008-L01), root license files without a doc extension are outside the inventory; `docs/_theme/djangodocs/static/fontawesome/LICENSE.txt` is admitted (`.txt` inside a docs tree) and classified `license` by name. Recorded as a consequence of the ratified rule, not a defect; the vendored-theme-asset part belongs to DOC-RELEVANCE-STEMS-1 / the vendored predicate.

## Low observations (not root-caused; queued last)
N7 `nginx/auto/install` (a shell fragment) listed as a doc — the ratified bare `install` stem's known residual class (`debian/install`, human: keep as is); N8 `resource list` counts poco's vendored zlib generators (`dependencies/zlib/src/crc32.c:285 fopen("crc32.h","w")`) — the surface does not apply the shared vendored predicate; N9 smoke accounting (a `check --full` Busy exit 2 counted as passed); N10 module identity spaces (codegraph's two `codegraph-kernel` modules; storybook directory ids vs npm names; two modules named "Django"); N11 vscode `orient` 21–24 s at every budget (the budget bounds the output, not the work); N12 hadoop `src/main/proto2-generated/.../README.txt` admitted (the docs `generated` predicate is only rmap's own marker).

## Addendum 2026-09-23 (seam verification after the audit; append-only — corrects and extends the entries above)

### RC-1 — DOCS-UNREADABLE-DECODE-1: parked on CC-10 (catalog contradiction)
Three document reviews (PREP, PREP-2, PREP-3) blocked on one class: RG-REQ-008-L05 admits and counts a document whose bytes cannot be read, RG-REQ-008-L08 requires `content_hash` on every entry; no truthful hash exists for such a document. The byte-hash mechanism (hash the bytes, decode the text separately) repairs poco — `packaging/README.txt` is Windows-1252, its bytes are readable — and the reviewer confirmed it; the residual corner (permission/IO failure) is pre-existing and unchanged. Human ruling needed (reviewer's options A/B/C in `.agent-manager/slices/DOCS-UNREADABLE-DECODE-1-PREP-3/notes-for-human.md`). The "smallest fix" in RC-1 above (an `Option<String>` CLI decode) was withdrawn: it would have weakened L08 by operator fiat.

### RC-2 — verified at code level (seam read 2026-09-23)
`indexer/src/resolver.rs::resolve_call_target` (:907-1160) runs its stages in this order: namespace import (:931-979) → Python self/cls hierarchy (:980-1010) → **dotted-name fallback (:1012-1061)** → receiver-type binding (:1063-1091) → bare-name singleton (:1093-1099) → C-family enclosing-class preference (:1101-1124) → named import (:1125-1158). The fallback takes any key containing `.`, skips only an `ImportKind::Default` prefix (:1026-1043) and binds `pick_unambiguous(nodes_by_name.get(<last segment>))` (:1055-1060) — for a Calls edge the pool is every node with that bare name minus TYPE_ALIAS/INTERFACE minus forward decls (:1638-1684, :340-342): no file, language, kind or receiver filter. `receiver_disposition` (:930) is read only at :1078 and :1110 — the fallback consults no receiver evidence. The Python extractor emits only `Namespace`/`Named` import kinds (python-extractor/src/extractor.rs:619-636, :808-817), so the Default skip is a no-op for Python; a Python `x.m()` CALLS edge carries `metadata_json = None` (:1063-1098; only `self.`/`cls.` get `{"selfCall","enclosingClass"}`), so `call_receiver_info(None)` labels every Python call `Receiverless` (:1526-1528) — inert only because the enclosing-class stage is gated on `is_c_family_extractor` (:1109). C/C++ never reach the fallback because their `target_key` is the bare field name (cpp-extractor/src/extractor.rs:2018-2021), not because of a guard — incidental protection. Dead code: the `this.`-3-part branch (:1046-1052) is byte-identical to the general branch (:1055-1060). No positive test covers the 2-part bind; no parity fixture exercises `resolve_call_target` (indexer/tests/parity.rs dispatches only detect_language / is_test_file / categorize_unresolved_edge / build_invalidation_plan). Trust consumer: `call_resolution_rate` = `edges` COUNT (storage/src/call_resolution_reads.rs:205-210, trust_impl.rs:277) over resolved + internal-like unresolved (trust/src/service.rs:592-599); removing evidence-less binds lowers the numerator only; a persisted `snapshots.resolved_call_count` (trust_impl.rs:306) masks the drop on an old snapshot. **Requirement question for the human:** RG-REQ-005-L02 ratifies "exactly one candidate matches by name" as a binding basis; a fix that declines unique-name binding for an untyped receiver changes L02's text.

### RC-4 — verified at code level and on the store; the class is larger than the audit's 13 facts
Mechanism (tree-sitter-cpp 0.23.4, reproduced with `tree-sitter parse` on the pinned checkouts): a trailing attribute macro has no derivation in the grammar (`_declaration_specifiers` admits one type specifier; `_field_declarator` cannot be `identifier identifier`), so recovery moves the REAL declarator into an `ERROR` node that is a direct child of the member node, between `type` and `declarator`, and lets the macro call fill the required `declarator: function_declarator` slot. The extractor admits it through `has_function_declarator` (cpp-extractor/src/extractor.rs:1337-1345) at the class-body dispatch (:859-863), names it from the first direct-child `function_declarator` (:1648-1661), and `extract_function_name` (:2121-2194) finds neither of CPP-DECLARATORS-1's shapes (a) `macro_call_shape` :2200-2212 nor (b) `error_child_name` :2245-2260 — the ERROR here is the fd's SIBLING, the shape (b)'s doc (:2238-2244) deliberately declines — and falls through to the plain unwrap: name = the macro token, `macro_tokens` empty, `forward_decl: true` (:1684-1703). The leveldb store (fresh index, HEAD 3f812e3, `/private/tmp/CPP-ATTRIBUTE-MACRO-1-leveldb-before`) holds **35 macro-named SYMBOL/METHOD rows** (`name = upper(name)`, excluding the real `leveldb::Status::OK` status.h:37), in three recovery shapes:
- **(1) field + trailing macro** — 20 rows, e.g. `db/db_impl.h:189 SnapshotList snapshots_ GUARDED_BY(mutex_);` → `field_declaration(type, ERROR(identifier snapshots_), declarator: function_declarator(field_identifier GUARDED_BY, parameter_list(parameter_declaration(type: type_identifier mutex_))))` → phantom `leveldb::DBImpl::GUARDED_BY [METHOD] (decl)`; the pointer variant `db_impl.h:187 WriteBatch* tmp_batch_ GUARDED_BY(mutex_);` puts the ERROR inside the `pointer_declarator`, skips the method path and records the data member as `GUARDED_BY` in `class_field_types` (:826-832, :2602-2620). Rows: db_impl.h 183/186/189/203, util/cache.cc 184/189/193/195, benchmarks/db_bench.cc 348/349/358/359, db/db_test.cc 62, db/skiplist_test.cc 326/327, helpers/memenv/memenv.cc 145/148/149/383, util/env_test.cc 144. Consequence beyond the phantom: the member's declared type is never recorded, so `snapshots_.empty()`/`table_.Insert()`/`tmp_batch_->Clear()` stay `unresolved_edges` rows (2 + 3 + 1) although `SnapshotList::empty`, `HandleTable::Insert`, `WriteBatch::Clear` exist.
- **(2a) definition (or prototype) + trailing macro, the real declarator inside the ERROR** — e.g. `port/port_stdcxx.h:59 void Lock() EXCLUSIVE_LOCK_FUNCTION() { mu_.lock(); }` → `function_definition(type, ERROR(init_declarator(identifier Lock, argument_list)), declarator: function_declarator(identifier EXCLUSIVE_LOCK_FUNCTION, ()), body)`; with parameters the ERROR holds a `function_declarator` (probe `void F2(int x) MACRO(m) {}`); `util/mutexlock.h:28 ~MutexLock() UNLOCK_FUNCTION() {…}` (destructor). The REAL method is misnamed, not duplicated: `leveldb::port::Mutex::Lock/Unlock/AssertHeld` and `leveldb::MutexLock::MutexLock(port::Mutex*)/~MutexLock` do not exist in the graph; 25 `Lock`, 25 `Unlock`, 12 `AssertHeld` call sites are `unresolved_edges` rows (5+5 of them with `receiverType: port::Mutex`). Rows: port_stdcxx.h 59/60, db_test.cc 51/55, mutexlock.h 28.
- **(2b) split: the prototype keeps its name, a typeless `function_definition` named by the macro takes the body** — e.g. `db/db_test.cc:47 void IncrementBy(int count) LOCKS_EXCLUDED(mu_) {` → `field_declaration(type, fd(IncrementBy, (int count)))` (+ `MISSING ";"`) followed by `function_definition(declarator: fd(identifier LOCKS_EXCLUDED, (mu_)), body)`: the store has `AtomicCounter::IncrementBy :47 {"forward_decl":true}` AND `AtomicCounter::LOCKS_EXCLUDED :47-50` — the real method is demoted to a declaration and its body (calls) belongs to the phantom. The prototype node is `field_declaration` or `declaration` (`port_stdcxx.h:61`, `mutexlock.h:25` — a constructor with an initializer list; `memenv.cc:311-312` two-line). Rows: db_test.cc 47, fault_injection_test.cc 150/154, skiplist_test.cc 309/317, env_posix.cc 501/507, port_stdcxx.h 61, mutexlock.h 25, memenv.cc 312. The already-correct neighbour: `db_impl.h:113-114 Status Recover(…)\n EXCLUSIVE_LOCKS_REQUIRED(mutex_);` splits into the prototype + a bodiless `declaration` the dispatch ignores (:864-885) — the right result by accident.
Controls: vcmi `Q_OBJECT` (launcher/gamepadHandler.h:18) is absorbed as the `type:` of the next field and has no parameter list → not admitted (arity, not a rule). `grep -rl 'GUARDED_BY\|LOCKS_EXCLUDED\|EXCLUSIVE_LOCKS_REQUIRED\|LOCK_FUNCTION('` over OpenXcom, poco, vcmi: no file → byte-identical controls; hadoop's `gtest-all.cc:5561 GTEST_LOCK_EXCLUDED_(mutex_)` is a (2b) instance outside the proof corpus. **Ambiguity that needs a stated rule (D-CAM-01):** inside a class body `int MYAPI foo(int x);` (leading macro, a real prototype `foo`) and `int seed_ GUARDED_BY(mutex_);` (trailing macro, a field) produce IDENTICAL trees — `(type, ERROR(identifier), declarator: fd(field_identifier, params))`; only the parameter list separates them: the attribute's argument is a data member of the same class (`mutex_`), a prototype's parameter is a type. Evidence-based rule chosen by the operator (human may override): shape (1) applies only when every parameter of the macro call is an unnamed `parameter_declaration` whose text names a data member of the enclosing class (from the existing field pre-pass :816-833); `int x_ MACRO();` (no argument) stays today's behaviour and is a stated residual (0 occurrences on leveldb). No macro-name list, no ALL-CAPS lexical rule. Existing test `error_sibling_of_declarator_does_not_trigger_macro_name_extraction` (:3095-3129) pins the file-scope `int MYAPI foo(int x) {…}` case and its doc's claim "the reachable other ERROR is exactly this sibling shape" is falsified by (1)/(2a)/(2b); the test's assertion stays true. Requirement: RG-REQ-001-L01 ("named by its declarator identifier, never by a macro token"). Slice: CPP-ATTRIBUTE-MACRO-1.

### RC-7 — verified at code level (seam read 2026-09-23) → PYTHON-SUBMODULE-IMPORT-1
`python-extractor/src/extractor.rs:797-839 emit_from_import_binding`: the IMPORTS edge's `target_key` is `python_specifier_to_target_key(specifier, …)` — the MODULE part `X` only (`:512-560`, dots → slashes for absolute; `repo:<dir>:FILE` for relative); the imported name `Y` is stamped on the edge's `metadata_json.importedName` (:834) and on the `ImportBinding` (`imported_name`, `kind: Named`) but never on the key. `indexer/src/resolver.rs:661-724 resolve_import_target` then probes ONE candidate for a non-relative key (Stage 4, :711-717: `repo:X:FILE` through `file_resolution`), which `build_file_resolution_map` (:1802-1839) has bound to `X/__init__.py` whenever `X` is a package directory (:1831-1835, `or_insert`); `X/Y.py` and `X/Y/__init__.py` are never candidates. Relative keys hit Stage 2 (:675-680) on the same directory entry. `edge.metadata_json` is not passed into the ladder (:554-562); no Python-gated stage exists (only Rust :741-791 and Java :811). Python emits no IMPLEMENTS edge at all: `class WSGIHandler(base.BaseHandler)` is stored as node metadata `{"superclass":"base.BaseHandler"}` (:960-1004) and read only by `superclasses_from_metadata` (:138-164, keeps the last segment) for the self-call walk. `explain <Class>` "Referenced by" is `find_file_importers` (storage/src/agent_impl.rs:1601-1639): `e.type = 'IMPORTS' AND tgt_f.path = ?` — IMPORTS only, file-scoped — so the resolver fix alone restores it. Corpus: on django 4d455ae, 215 absolute `from <pkg> import <name>` lines name a sibling module or subpackage of a package that has an `__init__.py` (e.g. `django/__main__.py:7 from django.core import management`, `django/shortcuts.py:13 from django.template import loader`, `django/middleware/locale.py:5 from django.utils import translation`) — every one of them lands on the package `__init__.py`. Never worked (`docs/TECH-DEBT.md:487-488` records the `__init__.py` landing as intended for `from .pkg import X` — right when `X` is a symbol, wrong when `X` is a module file). No test pins either behaviour (`index_python_imports_resolve`, repo-index/tests/integration.rs:1266, asserts only `>=` counts; the fixture `tests/fixtures/python/simple-app` has no such line); no Python parity fixture exists. Smallest fix: a Python-gated candidate stage (extractor prefix `python-core:`, `importedName` a plain identifier) run BEFORE Stage 2 for Python edges: probe `repo:X/Y:FILE` in `file_resolution` (the extensionless entry → `X/Y.py`; the package-dir entry → `X/Y/__init__.py`); found → resolved by an existing-file proof; not found → today's ladder, byte-identical (`from pkg import SomeClass` keeps landing on `__init__.py`). Extractor untouched. Requirement: RG-REQ-006-L04.

### RC-6 — verified at code level (seam read 2026-09-23) → TS-WORKSPACE-RESOLUTION-1
TS-ALIAS-RESOLUTION-1 is not implemented (`grep -rn TS-ALIAS rust/crates` → 0; it is queued prose in TECH-DEBT), and FRAKTAG's `packages/api/tsconfig.json` has no `paths` — the audit's "does not cover" was moot. Mechanism: `ts-extractor/src/extractor.rs:1487-1531` resolves only RELATIVE specifiers by path arithmetic; a bare specifier emits an IMPORTS edge whose `target_key` is the RAW specifier (`@fraktag/engine`, no `resolvedPath`). `resolve_import_target` (indexer/src/resolver.rs:661-724) has no npm stage; Stage 4 constructs `fraktag:@fraktag/engine:FILE`, which no map holds → `ImportsFileNotFound` (:1702). The classifier then stamps it EXTERNAL: `classification/src/unresolved_classifier.rs:181-183` `resolve_declared_dependency` matches `packages/api/package.json`'s `"@fraktag/engine": "*"` under `dependencies` — `external_library_candidate` / `specifier_matches_package_dependency`, indistinguishable from `express`. The workspace-member set ALREADY exists at index time: `indexer/src/package_json.rs:101-198` parses root `workspaces` (array/object) and each member's `name`; `repo-index/src/compose.rs:945-1069` expands the globs (pnpm-workspace.yaml first) into `prepared.npm_modules` (:474, :686) and persists `module_candidates` (:3085-3130) — but only AFTER resolution, and only `declared_modules_from_cargo` (:770-784) feeds the resolver's `DeclaredModule` DTO (indexer/src/types.rs:553-562, whose doc names `ecosystem` as the extension point), at :3456 and :3928. `main`/`exports`/`types` are never read anywhere; FRAKTAG's `packages/engine/package.json` points them at unindexed `dist/`, its source entry is `src/index.ts` (present). Module edges: storage/src/trust_impl.rs:755-865 `compute_module_stats` reads `edges` (`type='IMPORTS' AND resolution='static'`) joined to `module_file_ownership` — ONE resolved edge `packages/api/src/server.ts → packages/engine/src/index.ts` makes `packages/engine` fan_in 1. The LiveGraph route already ranks workspace membership ahead of the declared-dependency check (repo-graph-import-resolver/src/lib.rs:105-112) — the SQLite route lacks it. Cycle check: `indexer → module-queries → storage → indexer` is a hard cycle, so the member set must reach the resolver through `repo-index::compose` (same crate/phase as the cargo DTO) — no new crate edge. The one existing cross-workspace module-edge test (`rgr/tests/trust_module_edges_seam.rs:142-171`) routes around the defect with a relative path. Smallest fix: `declared_modules_from_npm(&prepared.npm_modules)` beside the cargo one (both call sites); an `npm_package_roots` map in the orchestrator (:892 pattern, literal names — no `_`/`-` folding for npm); a Stage 3.6 in `resolve_import_target` gated on the TS extractor prefix: reduce the specifier with `classification::dep_reduce::npm_package_head`, then probe `<root>/<subpath>`, `<root>/src/index`, `<root>/index`, `<root>/src` against `file_resolution` (existing-file proof; `main`/`exports` reading is a later additive increment). Corpora: FRAKTAG (1 line: `packages/api/src/server.ts:6`), amodx (root `workspaces` with literal + glob members), glamCRM (NO root manifest — the stage is a no-op there, honestly). Requirement: RG-REQ-006-L04. Stale pins after the fix: docs/slices/alias-suspicion-1.md:189,339 and docs/assurance/ALIAS-SUSPICION-1/verification.json:431 list `packages/engine` among FRAKTAG's zero-connectivity modules.

### RC-8 — verified at code level (seam read 2026-09-23) → DEPS-GRADLE-CATALOG-1 (corrected)
Corrections: kafka (0dad6a7) has NO `gradle/libs.versions.toml`; its `libs.<alias>` map comes from `gradle/dependencies.gradle` (`libs += [ … lz4: "at.yawk.lz4:lz4-java:$versions.lz4" :225, zstd: "com.github.luben:zstd-jni:$versions.zstd" :247 …]`) applied by `build.gradle:23 apply from: "$rootDir/gradle/dependencies.gradle"`; kafka has exactly ONE build file (root) with 62 `project(':x') { … dependencies { implementation libs.zstd … } }` blocks. grpc-java (f430131) HAS `gradle/libs.versions.toml`, 67 per-module build files, and each opens with `buildscript { dependencies { classpath 'com.google.guava:guava:…' } }` — the same misattribution per module. Three independent predicates, all verified: (a) `repo-index/src/config.rs:545-553` — a brace scanner whose only rule is "the word before `{` is `dependencies`"; `buildscript { dependencies { classpath … } }` is mined like a project dependency (the deliberate no-verb-allowlist rule :484-488); kafka's ONE literal coordinate in the whole file is `build.gradle:27 classpath "org.ajoberstar.grgit:grgit-core:$versions.grgit"`, so the parsed declared set is `{org.ajoberstar.grgit}` — every Java file's nearest manifest walk-up (`manifest_deps.rs:42-92`) ends at the root script, and `module-queries/src/deps/compose.rs:264-282` unions per-file sets per module → 61/61 rows `no static import found 1`. (b) `config.rs:601-616 extract_gradle_coordinates` mines only QUOTED `group:artifact` tokens (`quoted_strings`); `implementation libs.zstd` has no quotes → nothing (documented non-feature :501-504; `gradle_non_literal_forms_not_fabricated` :1431 pins it). (c) `module-queries/src/deps/reconcile.rs:75-82, :122` — `declared_set.contains(package)` is EXACT string equality between Maven GROUP ids and Java package heads (no group → package-prefix mapping; even a parsed `com.github.luben` cannot match `com.github.luben.zstd`, and lz4's group `at.yawk.lz4` is unrelated to its package `net.jpountz.lz4`), and `is_self` compares against Gradle DISPLAY names (`kafka`, `clients`, `api` — `settings_gradle.rs:29`), never the repo's own `package …;` declarations → `org.apache.kafka.common.header` falls to :146 `ObservedButUndeclared`. Fix shapes: (a) a 3-state brace frame (suppress under `buildscript`/`pluginManagement`) — ships alone, ~10 lines; (b) an alias map from `[libraries]` of `libs.versions.toml` (grpc-java) AND from an `apply from:`-referenced Groovy `libs += [ … ]` map (kafka), threaded into `extract_gradle_dependencies`; (c) segment-anchored package-prefix containment for BOTH the declared side and a Java self-set derived from the repo's own FILE-node `package` declarations. (b) without (c) makes kafka LOUDER-wrong (`no static import found 4+`) — (b) and (c) ship together; spring-petclinic (literal coordinates, single module) is the no-regression baseline; hadoop is Maven (out of scope). Requirement: RG-REQ-006 (dependency attribution Ls — to be bound when the packet is cut).
