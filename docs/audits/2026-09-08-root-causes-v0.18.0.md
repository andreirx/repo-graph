# Root causes — audit round six (rmap v0.18.0), 2026-09-08

Each entry: render site → data path → exact predicate → regression-or-never-worked → smallest fix → verification → blast radius. Store evidence from read-only copies of ~/repo-graph-retained/audit-v0.17.0 and audit-v0.18.0 (`?immutable=1`). Investigators: six read-only agents; corpus counts by the operator. Adjudicated by Codex gpt-5.6-terra (standalone).

# RC-1 — C++ call binding ignores the receiver; enclosing-class preference fabricates self-loops (REGRESSION, CPP-DECLARATORS-1 5c3ec2d)

Outward surface: `callers leveldb::DBImpl::Recover` → one caller, itself; `callees` lists itself; `explain` shows the same; real caller `DB::Open` (db_impl.cc:1511) absent. Store proof on the retained roots: v0.17.0 leveldb self-loop CALLS = 0; v0.18.0 = 155 — ALL 155 carry a `receiver` (zero genuine recursion). 155 of the +879 CALLS "resolution gain" (17.6%) are fabricated.

1. Render site (faithful): rgr graph.rs:420 run_callers → dispatch.rs:1270 handle_callers (:1381 edge_types=["CALLS"]) → storage/queries.rs:741 find_direct_callers (no self-loop exclusion; printed anchor = caller node's line_start, not the callsite); callees :805 symmetric.
2. Data path: cpp-extractor/src/extractor.rs:1648 extract_call — for a field_expression call the target_key is the BARE field name; receiver stored only as metadata (:1710 `{"calleeName","receiver"}`, comment :1657 "NOT consumed here"). indexer/src/resolver.rs:497 → resolve_call_target: short-name global bind at :942 `pick_unambiguous(nodes_by_name.get(target_key), Calls, false)`; no receiver-type / qualified path. Unresolved calls are dropped from the served graph (edges.target_node_uid NOT NULL) into unresolved_edges. C extractor emits no receiver.
3. Predicates: (a) self-loop — resolver.rs:951-962 (CPP-DECLARATORS-1 §2.3 AMENDED): for c-family extractors, an ambiguous pool → enclosing_class_preference (:1113): pick the one candidate whose qualified container (rfind "::") equals the CALLER's container. `versions_->Recover` inside DBImpl::Recover: pool = {DBImpl::Recover, VersionSet::Recover} → caller container leveldb::DBImpl → itself. Never reads receiver="versions_". Store row cf304bc8… src=tgt=leveldb::DBImpl::Recover line 324 resolution=static metadata receiver "versions_".
   (b) missing DB::Open→DBImpl::Recover — caller container leveldb::DB matches no candidate → None → Unresolved: unresolved_edges aa754ba3… source=leveldb::DB::Open target_key='Recover' line 1511 receiver "impl" category=calls_function_ambiguous_or_missing. Unique-name calls from DB::Open resolved (impl->RemoveObsoleteFiles); only the ambiguous case is broken, in both directions.
4. Regression: `git log -S enclosing_class_preference` / `-S '"receiver"'` → exactly 5c3ec2d. Its ship line recorded the defect as a win ("callers DBImpl::Recover ambiguous→1" — the 1 IS the self-loop). Pre-5c3ec2d: ambiguous → unresolved → callers = 0 (honest silence). Tests resolver.rs:1723 (fictional `leveldb::DBImpl::Open` caller — leveldb has no such method) and :1763 (encodes defect (b) as intended). SYMBOL-IDENTITY-1 f5cfe1e did not touch call binding.
5. Smallest fix (index-time): (1) cpp-extractor :763 field_declaration arm — capture (class, member) → declared type (strip * & const templates) as an extractor fact; (2) generalise ctx.local_stream_types (CPP-SB-1 D3, cleared :1621) to any declared local type so `DBImpl* impl` binds; put `receiver_type` on edge metadata; (3) resolver: receiver-type stage BEFORE :942 — look up `<receiver_type>::<target_key>` (walk IMPLEMENTS bases when available); keep enclosing_class_preference ONLY for receiverless calls (implicit this->) with a hard invariant: never return the caller's own node for a call whose metadata carries a receiver (that guard alone removes all 155). Files: cpp-extractor/src/extractor.rs, indexer/src/resolver.rs, the two tests rewritten. resolve_call_target is the single binding path for every language; the gated block is C/C++-only; C unaffected (no receiver).
   Verify: two-class same-short-name fixture (A::run / B::run with `b_->run()`): callers A::run = B::run, no self-loop; leveldb isolated before/after: callers DBImpl::Recover = DB::Open (db_impl.cc:1503), callees include VersionSet::Recover and exclude itself; corpus assertion `SELECT count(*) FROM edges WHERE type='CALLS' AND source_node_uid=target_node_uid AND metadata_json LIKE '%receiver%'` = 0 (today 155); report resolved/unresolved CALLS both ways.
6. Blast radius: callers/callees/explain/path/map direct; `dead`/orient dead aggregator (queries.rs:1125 find_dead_nodes: call_fan_in>0 = live) — 155 leveldb symbols shielded by their own fabricated edge → fix INCREASES reported dead code correctly; trust "calls resolved %" (trust/service.rs:898-906) leveldb 25.3% → 34.7% at v0.18.0, ≥1.6 pts fabricated — the headline will FALL when fixed, say so; complexity/impact degrees; callgraph_cert/union_serve ledgers carry the same edges; cycles safe (scc.rs excludes size-1 self-loops). Class shape: delegation/decorator/iterator wrappers (`iter_->Next()` in DBIter::Next, `data_iter_->SeekToFirst()`, `shard_[s]->TotalCharge()`) — exactly what users ask callers/callees about. Re-run the self-loop count on vcmi/OpenXcom/poco/duckdb before sizing.

CORPUS SIZING (operator, 2026-09-08, read-only copies of both retained roots; query = receiver-bearing CALLS self-loops | CALLS edges | unresolved calls_*):
  repo       v0.17.0                 v0.18.0
  leveldb    0 | 2382 | 7031         155 | 3261 | 6152
  OpenXcom   0 | 6007 | 38793        497 | 16254 | 28546
  vcmi       0 | 18606 | 96058       836 | 33852 | 80812
  poco       0 | 21023 | 124377      1098 | 28129 | 117271
  duckdb     0 | 52431 | 177790      2522 | 71669 | 158552
  gstreamer  0 | 216159 | 477420     321 | 217593 | 475985   (C; the 321 come from its C++ parts)
  codegraph  —                       0 | 8531 | 26384        (TS+Rust control: unaffected)
Zero before, thousands after, on every C++ repo: a corpus-wide fabricated-edge regression from 5c3ec2d. Each self-loop also shields a symbol from `dead` (fan-in ≥ 1) and inflates trust's calls-resolved %.

ADJUDICATION CORRECTION (codex gpt-5.6-terra standalone, 2026-09-08): the invariant "never bind a receiver-bearing call to the caller itself" is OVER-BROAD — an explicit `this->m()` / `(*this).m()` receiver legitimately targets the enclosing class. Safe invariant: "do not bind an INDIRECT receiver (any receiver other than this/self) to the enclosing class without receiver-type evidence". The "≥1.6 pts fabricated" trust figure is derived (155 / 9413 leveldb CALLS universe) — record it as operator source-investigation evidence, not a reproduced measurement. CPP-DECLARATORS-1's IMPLEMENTS 2→852 win was NOT ground-truthed in round six (no capture exercises it) — provisional; RC-3 shows those edges are anchored on the FILE node, so the count is real but not queryable from a class focus.

# RC-4 — `explain` "Import cycles" fabricates arrows (NEVER WORKED; explain was never given the honest form)

Outward: explain CGHeroInstance (vcmi) "Cycle 1: AI/BattleAI -> AI/EmptyAI -> …", explain BaseHandler.get_response (django) "django -> django/apps -> django/conf -> …" — strictly alphabetical member sets drawn as arrows; grep EmptyAI in AI/BattleAI/ → 0.
Render site: rgr/src/presentation/explain_sections.rs:226-249 render_cycles (dispatched :90); the fabricating line :237-243: `cycle_str.join(" -> ")` over item["modules"].
Data path: agent/src/explain/mod.rs:562-588 (symbol module-context) and :882-906 (path focus) build CycleEvidence { length, modules, type_only, walk } from storage find_cycles_involving_module_cancellable / _involving_path_cancellable (storage/src/agent_impl.rs:1357 / :931); both set walk: None ("no walk precomputed on the focus/path-scoped read"); agent/src/ordering.rs::canonicalize_cycles does c.modules.sort() — the modules list is the alphabetically sorted MEMBER SET by construction. The LiveGraph route (daemon-runtime/src/explain_lg_serve.rs:133 serve_cycles) carries no walk either.
Exact predicate: join(" -> ") over a lexicographic member set with no check of walk and no edge set. Byte-for-byte matches the captures; walk is null on that leaf.
Never worked: the line exists since 82b6557 (2026-06-12, EXPLAIN-LIVEGRAPH-IMPL). The honest form was built TWICE elsewhere: CYCLE-HONESTY-1 (cycles/walk.rs:25-68 render_unordered — arrows only over verified edges) and COHERENCE-3 55ab942 (orient_sections.rs:369-395 + orient_guidance.rs:75, shared walk + format_cycle_anchor + unordered fallback); `git show --stat 55ab942` touches explain_sections.rs zero times.
Smallest fix: in render_cycles draw arrows only from item["walk"] via the shared format_cycle_anchor (strict; None on drift); when walk is absent/null/empty — every current explain serve — render the unordered form mirroring cycles/walk.rs::render_unordered: `- Cycle 1 (57 modules): members (unordered): AI/BattleAI, AI/EmptyAI, … (+ 49 more)`. ~15 lines, one function, no storage/agent/wire change. Optional follow-up: populate walk on the focus-scoped reads so explain can draw the real ring.
Verify: explain CGHeroInstance (vcmi) and explain BaseHandler.get_response (django) print "members (unordered)" with zero " -> " in the Import cycles block; `rmap cycles` on repo-graph byte-identical (its walk IS verified); unit test render_cycles walk:null → no arrow, valid 3-member walk → ring.
Blast radius: only this render site; explain --json already carries modules (set) + walk:null; both focus kinds and both serving engines route through this one function.

# RC-5 — `trust` zero-connectivity contradicts `modules list` (REGRESSION, 28126a2 ORIENT-BUG-1 2026-05-22; ONE root cause, all languages; grader hint CONFIRMED)

Outward: trust "Suspicious Modules (zero connectivity)" lists rust/crates/agent, boundary-interaction, contract-schema (repo-graph 48/61 candidates), hadoop-hdfs-project (vs modules list 11,874 imports → hadoop-common-project), kafka 65/65, FRAKTAG 3/4; drives `alias_resolution_suspicion` → Import-graph LOW / Change-impact LOW.
Render site: rgr/src/presentation/trust.rs:623-656 render_suspicious_modules (filter m.suspicious_zero_connectivity) + render_downgrades line + the reliability lines.
Data path: trust/src/service.rs:296-307 → count_suspicious_zero_connectivity_modules (trust/src/rules.rs:392); :525-546 stamps the per-row flag; input.module_stats ← TrustStorageRead::compute_module_stats = storage/src/trust_impl.rs:755-822 (a DIFFERENT query from stats' queries.rs:1581): rows FROM module_candidates (canonical_root_path e.g. rust/crates/agent); file_count FROM module_file_ownership (correct); fan_in/fan_out from edges type='IMPORTS' between nodes kind='MODULE' joined `LEFT JOIN nodes m ON m.kind='MODULE' AND m.qualified_name = mc.canonical_root_path`. MODULE nodes are PER-DIRECTORY (indexer/src/orchestrator.rs:1252-1290 create_module_nodes, subtype Directory, one per ancestor dir) and MODULE→MODULE IMPORTS edges attach between each file's IMMEDIATE PARENT directory (create_module_edges :1345-1425). modules list reads a different thing: module-queries/src/facts.rs:194-206 → classification::module_edges::derive_module_dependency_edges over module_file_ownership + resolved file-level imports keyed by canonical_root_path.
Exact predicate: `m.qualified_name = mc.canonical_root_path` (exact equality on the crate/module ROOT) against edges that only attach to the LEAF directory owning files: rust/crates/agent (root) owns 0 files, 0 IMPORTS; rust/crates/agent/src owns 15, fan_in 14 / fan_out 2. LEFT JOIN misses → COALESCE 0 → rules.rs:392 fires on file_count >= 2.
Measured (read-only copies, audit-v0.18.0): repo-graph 61 candidates / 48 flagged / rows with any fan > 0: 0; kafka 65/65/0; hadoop 9/8/0; FRAKTAG 4/3/0; django 2/0/0; vcmi 16/4/11 (only inferred dirs that directly own C++ sources score). On Cargo/Gradle/Maven/TS layouts the metric is STRUCTURALLY ALWAYS ZERO. Java is the same cause (hadoop-hdfs-project matches a dir node that owns nothing; edges sit on …/src/main/java/org/apache/hadoop/hdfs/…).
Regression: 28126a2 (2026-05-22, ORIENT-BUG-1) moved the row source from `FROM nodes m … kind='MODULE'` to `FROM module_candidates mc` while leaving the fan subqueries keyed on m.node_uid bridged by the new exact join; before it trust rows WERE the directory nodes (rust/crates/agent/src fan_in 14, self-consistent). a2dda68 (TRUST-FIRSTPARTY-1) touched the SQL without changing the join. IMPORT-RESOLUTION-RUST-1 c71879f is NOT the cause (trust never reads the derived edges, so it did not move). trust.rs:640-650 (CONTRADICTION-SWEEP-1) added the basis line "a finer-grained child module may still be connected — cross-check stats" with "Computation is UNCHANGED" — a wording patch over this defect that now names the wrong node identity out loud.
Smallest fix: ONE module-edge computation read by both — replace the fan subqueries in trust_impl.rs::compute_module_stats with counts over the SAME derived module-dependency edge set modules list renders (aggregate resolved file→file IMPORTS through module_file_ownership to module_candidate_uid on both endpoints; fan_in = COUNT(DISTINCT source candidate), fan_out = COUNT(DISTINCT target)); drop the nodes kind='MODULE' join (the longest-prefix ownership rule call_resolution_reads.rs already uses). One SQL statement + unit tests; no schema/reindex/wire change. REJECT a prefix-LIKE hack (double-counts nested candidates; wrong population).
Verify: repo-graph trust: no Suspicious Modules section (48 → 0), Triggered Downgrades no longer lists alias_resolution_suspicion; trust --json modules[] fan_in/fan_out match modules list/deps per module; cross-repo assertion rows-with-fan>0 > 0 on repo-graph/kafka/hadoop/FRAKTAG. The Import-graph verdict does NOT reach HIGH: rules.rs:215-243 returns LOW whenever unresolved_imports_count > 0 (repo-graph 4,903) → the line moves from "LOW (alias resolution suspected; 4903 unresolved imports)" to "LOW (4903 unresolved imports)"; Change-impact from "LOW (alias resolution suspected)" to "LOW (import_graph_reliability_low)" (rules.rs:319-350). The false REASON disappears; the level is separately gated — state this in the packet.
Blast radius: trust (suspicious section, downgrades, Import-graph + Change-impact lines, caveats), orient's reliability/coverage map (orient_reliability*.rs, orient_types.rs), stats (renders the same axes; NOTE stats' module rows come from queries.rs::compute_module_stats — the directory-node population — an identity mismatch with modules list this fix does not close), dead's per-candidate confidence (overlay.rs:312 unresolved_import_pressure — unchanged, level stays LOW), assess (inherits trust). modules list/deps/violations unaffected.

# RC-9 — complexity centers rank vendored/generated/test code, unmarked (NEVER WORKED; is_generated dead-false since f63a94e)

Outward: poco orient top-5 = dependencies/pcre2/src/* (1580/839/749/466) + Data/SQLParser/src/parser/bison_parser.cpp (391, "A Bison parser, made by GNU Bison 3.8.2"); codegraph #1 codegraph-kernel/grammars/lua/parser.c (322, line 1 "Automatically @generated by tree-sitter"); repo-graph #2 rust/crates/storage/tests/parity.rs (92, files.is_test = 1); vscode #1 extensions/copilot/.../multilineModelWeights.ts (3001).
Render: rgr/src/presentation/orient_sections.rs:199 complexity_line(); :252 complexity_breakdown_section (called orient.rs:487); both read signal_evidence("HIGH_COMPLEXITY")["top_complex"] — no classification field exists in the DTO.
Data path: measurements(kind='cyclomatic_complexity') → storage/src/agent_impl.rs:1552 query_high_complexity_symbols_cancellable (SELECT target_stable_key, n.name, f.path, n.line_start, value_json; LEFT JOIN nodes→files; ORDER BY value DESC) → agent/src/aggregators/complexity.rs:80 aggregate_with_threshold_cancellable (sort_complexity, truncate(max_complexity_centers)) → HighComplexityEvidence { high_complexity_count, threshold, top_complex } (agent/src/dto/signal.rs:712).
Exact predicate: the ONLY filter is `complexity >= min_threshold` (agent_impl.rs:1638; threshold 20, complexity.rs:15); ranking = complexity DESC then stable_key. No vendored/generated/test predicate in SQL, aggregator or renderer; the SELECT already joins files, so f.is_test / f.is_generated are one column away and not selected.
Existing predicates and why none reaches here: is_vendored_path / VENDORED_SEGMENTS (daemon-runtime/src/handlers/quality/support.rs:33-52: vendor, vendors, third_party, third-party, external, deps, node_modules, site-packages, dist-packages — `dependencies` NOT in the list; consumers hotspots --exclude-vendored opt-in, docs_list_overlay.rs:26, orient_additive_fields.rs:179, modules_method.rs; lives in an ADAPTER crate — core cannot import it). files.is_test real and populated (indexer/src/routing.rs:206; repo-graph 278, codegraph 305, poco 8). files.is_generated: column exists (storage/src/types.rs:346,370; bootstrap :1787), consumed by the seed corpus filter (seed_impl.rs:81 AND f.is_generated = 0) but EVERY construction site hard-codes false (indexer/src/orchestrator.rs:333,551,841,1897,2036; repo-index/src/compose.rs:1365,1450) — COUNT(is_generated=1) = 0 on poco/codegraph/repo-graph; no @generated sniffer, no .gitattributes linguist reader. docs' "1247 generated maps excluded" is the narrower doc-facts/src/self_generated.rs:40 GENERATED_MARKER "<!-- generated by rmap map" (rmap's own sidecars).
Never worked: complexity.rs added whole in 72940db with no exclusion; git log -S vendored / -S is_test over the aggregator + orient_sections → nothing; is_generated dead-false since f63a94e/55e6b2d.
Smallest fix (three parts, existing code): (1) index-time: set files.is_generated for real — first-8-lines marker sniff (@generated, "Automatically generated", "DO NOT EDIT", "made by GNU Bison", "generated by …") at orchestrator.rs:551 where is_test is computed; (2) widen VENDORED_SEGMENTS with `dependencies` (+ contrib) and MOVE is_vendored_path into an inner crate (classification) so core can call it; (3) filter in the AGGREGATOR (after the storage read, before truncate) — add is_test/is_generated to the SELECT + AgentComplexityMeasurement; drop vendored/generated/test rows; high_complexity_count from the FILTERED set; carry the excluded count so both renderers print "N generated/vendored/test symbols excluded — --include-all". The filter MUST NOT go into query_high_complexity_symbols: daemon-runtime/src/orient_lg_decisions/complexity_cert.rs compares that read's FULL set against LiveGraph's unfiltered set — filtering there turns the no-loss cert RED on every vendored repo.
Verify (simulated with this predicate on the retained DBs): poco 389 above threshold, 218 excluded, new top-5 Foundation/src/Windows1251Encoding.cpp — convert (345), Windows1250Encoding (225), Latin2Encoding (177), PDF/src/PDFException.cpp (107), Windows1252Encoding (87); codegraph 12 excluded, #1 src/mcp/tools.ts — handleExplore (256); repo-graph 2 excluded, parity.rs gone. vscode does NOT pass: multilineModelWeights.ts (12,317 lines, one exported function, 313 KB) carries no marker, no vendored segment, no repo-declared exclusion (.gitattributes/.gitignore/eslint.config.js checked) — indistinguishable by any fact in the index; leave it ranked with the exclusion line, or a separate "one symbol spanning a ≥N-line file" fact — a second decision.
Blast: orient headline + section + every consumer of high_complexity_count ("+N more above threshold", budget.rs:66); hotspots unchanged; LiveGraph cert untouched only if the filter stays agent-side; is_generated becoming non-zero also changes the SEED corpus (seed_impl.rs:81) — a real behaviour change for seeds/find, stated; requires a reindex.

# RC-10 — C/C++ includes don't resolve through per-module include/ roots (NEVER WORKED; explicitly deferred v1.3) + gstreamer = a SECOND cause

Outward: poco modules list "No cross-module dependencies detected … 13703 imports did not resolve to a file" (faithful render of an empty edge array).
Data path: #include → cpp-extractor/src/extractor.rs:521 extract_include (and the C twin) emit IMPORTS with target_key = specifier verbatim ("Poco/Exception.h"), metadata rawPath for quoted → indexer/src/orchestrator.rs:867-874 IncludeResolverConfig { configured_roots: c_include_roots, conventional_roots: ["include","inc","src/include"] } → include_resolver.rs::build_include_resolution_map → resolver.rs:432-440. Module edges derive ONLY from resolved file→file imports: classification/src/module_edges.rs:152 derive_module_dependency_edges → module_rollup → modules list.
Exact predicate: indexer/src/include_resolver.rs:96-180 IncludeResolver::resolve: (1) same-dir join (quoted only); (2) each configured root (--include-root, empty by default); (3) each conventional root — REPO-ROOT-ANCHORED literals "include", "inc", "src/include" (:62). Net/src/HTTPClientSession.cpp including "Poco/Exception.h": same-dir Net/src/Poco/Exception.h ✗; include/Poco/Exception.h ✗ (poco has zero files under a root include/) → Unresolved. The header is at Foundation/include/Poco/Exception.h; poco has 35 */include directories, none reachable.
Never worked, deferred: include_resolver.rs added whole in 55f1ac6 (2026-04-23) with that literal list, unchanged since; docs/milestones/c-include-resolution-v1.1.md:290-302 defers v1.2 compile_commands.json / v1.3 build-system detection (CMake, Meson) — v1.2 shipped as the angle-bracket repair for nginx instead; no build-system reader exists (grep target_include_directories → 0).
Measured recovery (retained v0.18.0 copies; rule 1 = any directory named include/inc at any depth is a candidate root; rule 2 = unique ≥2-segment path-suffix match, only where rule 1 yields nothing):
  repo       resolved now | unresolved | rule-1 roots | rule 1 resolves | ambiguous | rule 2 adds
  poco       1,702 | 13,703 | 35 | 9,893 | 368 | 69
  duckdb     22,958 | 8,692 | 24 | 2,277 | 0 | 1,618
  OpenXcom   3,983 | 945 | 1 | 153 | 0 | 0
  gstreamer  7,813 | 22,392 | 1 | 16 | 0 | 9,775
  vcmi       12,432 | 1,957 | 0 | 0 | 0 | 300
  leveldb    519 | 280 | 0 | 0 | 0 | 0
  nginx/swupdate/sqlite: 0 new roots, 0 change.
  poco's residual 3,442 are true system headers (vector 223, iostream 214, string 161 …) — correctly unresolved.
Module edges follow: of poco's 9,893 newly resolved includes, 3,538 are cross-module under the existing top-level attribution — Net → Foundation 616, Data → Foundation 455, Foundation → CppUnit 415, Util → Foundation 187, NetSSL_OpenSSL → Net 114 — matching the ground-truth grep (112 Net files). derive_module_dependency_edges needs no change.
Smallest fix: rule 1 in include_resolver.rs alone — derive conventional roots from the indexed file list (any path segment include/inc → that prefix is a candidate root) instead of the three literals; build_include_resolution_map already receives file_paths. Pure, deterministic, ~15 lines, no build-system parsing. CMake/meson readers are strictly larger and recover LESS on this corpus.
gstreamer = a SECOND cause: headers at subprojects/gstreamer/gst/gst.h and subprojects/gst-plugins-base/gst-libs/gst/video/video.h — meson subproject roots / gst-libs dirs never named include; rule 1 recovers 16 of 22,392; a subprojects/* root rule only 3,447. What works: the unique multi-segment suffix fallback (the build_java_suffix_index shape shipped by IMPORT-RESOLUTION-JAVA-1) → 9,775. Restricted to ≥2-segment specifiers and a UNIQUE match it cannot regress (only runs on specs unresolved today; config.h / string.h have 0 candidates; gst/gst.h and gst/video/video.h match exactly one file). A second, separately-decidable increment.
Verify: after rule 1 poco modules list shows Net → Foundation (616) among ~50 pairs; unresolved 13,703 → 3,810 (3,442 system + 368 ambiguous); resolved IMPORTS 1,702 → 11,595; duckdb +2,277; OpenXcom +153; leveldb/vcmi/nginx/swupdate/sqlite byte-stable (provably: zero */include dirs beyond the root). After rule 2: gstreamer 22,392 → ~12,600 with cross-module edges; vcmi +300; duckdb +1,618.
Blast: every C/C++ repo's modules list edges, orient topology, unresolved counts feeding trust and the zero-state note; cycles and dead counts shift as file→file edges appear (poco ~9.9k new edges, ~6× IMPORTS — store-size/index-time cost to measure). Adding roots can only turn Unresolved into Resolved or Ambiguous, never flip an existing resolution (same-dir hits return first; no corpus repo with an existing root-based resolution gains a root). Requires reindex; --include-root stays as the escape hatch.

# RC-2 — Python `self.<method>()` never binds (own class OR base class) — NEVER WORKED

Outward: `explain BaseHandler.get_response` (django) → `Callers (0)`; truth: WSGIHandler.__call__ wsgi.py:124, ClientHandler.__call__ client.py:186 (`self.get_response(request)`).
Render site: agent/src/explain/mod.rs:498 EXPLAIN_CALLERS from storage.find_symbol_callers (resolved `edges` only); rendered explain_sections.rs:100 — faithful render of an empty edge set.
Data path: python-extractor/src/extractor.rs:1065 emit_call_edge → get_call_target_name :1094, "attribute" branch :1097-1105 formats `object.attribute` verbatim → target_key "self.get_response"; indexer/src/resolver.rs:826 resolve_call_target; failure → categorize_unresolved_call :1235 → unresolved_edges.
Exact predicates (in order): (1) resolver.rs:888-935 dotted-name fallback — the only receiver-aware branch is `if prefix == "this" && parts.len() >= 3` (:922); `self` is not spelled anywhere; only the bare-name lookup `pick_unambiguous(nodes_by_name.get("get_response"), Calls, false)` (:931) runs. (2) pick_unambiguous :1064-1078 returns Some only for a pool of exactly ONE; django has 8 nodes named get_response → None. (3) categorize_unresolved_call :1252 special-cases only `this.`; `self.` → generic `key.contains('.')` → CallsObjMethodNeedsTypeInfo.
Store decision (audit-v0.18.0 django copy, snapshot …/ed0a7b80): every ground-truth callsite is an unresolved_edges row target_key='self.get_response', category='calls_obj_method_needs_type_info', classification='unknown', basis_code='no_supporting_signal' — incl. wsgi.py:124 and client.py:186. Zero edges reach BaseHandler.get_response. Recorded unresolved, bound to nothing, never mis-bound. exception.py:43,56 are bare closure calls, also unresolved (coincidentally correct).
Hierarchy data: NO edge — extract_class (python-extractor :960-1026) stores bases only as metadata_json.superclass raw text (:998-1002); Python emits only Imports + Calls; django edge types CALLS/IMPORTS/INSTANTIATES/OWNS, zero IMPLEMENTS (Java emits IMPLEMENTS with the CLASS as source, java-extractor :1086-1112). The fact IS present as node metadata and query_resolver_nodes (storage/src/indexer_impl.rs:377-402) already SELECTs metadata_json (reads forward_decl from it). Stored: WSGIHandler → {"superclass":"base.BaseHandler"}, ClientHandler → {"superclass":"BaseHandler"}, StaticFilesHandler → {"superclass":"StaticFilesHandlerMixin, WSGIHandler"}.
Never worked: get_call_target_name's attribute branch is in the extractor's introducing commit 4c9071e (2026-04-28); `git log -S "fn get_call_target_name"` → that single commit.
Smallest fix (additive, gated to Python edges): (a) extractor — when key is `self.<m>`/`cls.<m>` and ctx.current_class == Some(C), keep the key and add metadata {"selfCall":true,"enclosingClass":C} (do NOT rewrite to `C.m` — the TS precedent ts-extractor :1311-1317 does that and it buys nothing: resolve_call_target has no qualified-name lookup at all). (b) resolver — add nodes_by_qualified_name to ResolverIndex and superclasses: Vec<String> to ResolverNode (parsed from the metadata already read; split on ',', drop kwargs/subscripts, map the head through the file's import bindings else last dotted segment); ONE step before the dotted fallback at :888, firing only on selfCall: BFS the ancestor closure from enclosingClass (self first); at each depth collect nodes_by_qualified_name["<Ancestor>.<m>"] subtype METHOD; first depth with exactly one hit wins; >1 at a depth (real MRO collision — StaticFilesHandler(StaticFilesHandlerMixin, WSGIHandler) both define get_response) → None, honestly unresolved; optionally a new CallsSelfMethodNeedsClassContext category at :1252. Files: python-extractor/src/extractor.rs, indexer/src/resolver.rs, storage/src/indexer_impl.rs (populate superclasses).
Verify: resolver unit fixtures (Base.run / Sub(Base) self.run() → one CALLS edge; two same-depth ancestors both defining run → unresolved); django before/after: explain BaseHandler.get_response Callers (0) → (2) = WSGIHandler.__call__ + ClientHandler.__call__; ASGIHandler must NOT appear (awaits get_response_async); exception.py:43,56 must NOT appear; non-Python edge counts byte-stable on leveldb + FRAKTAG.
Blast radius: django has 26,139 unresolved CALLS with a `self.` prefix + 322 `cls.` = 28.6% of 91,514 unresolved calls (60,414 resolved). Store simulation of the predicate (class-name-keyed upper bound): 2,544 bind on the own class + 1,093 uniquely on an ancestor, 385 MRO-ambiguous declined → ~3,600 new edges ≈ +6% resolved call graph; the remaining 22,098 are instance-attribute callables / un-indexed third-party bases, correctly refused. Python-only via the selfCall gate (resolve_call_target is the shared hot path for every language).

# RC-3 — `explain <Class>` renders nothing about the class (NEVER WORKED; language-wide) + C++ inheritance edges anchored on the FILE

Outward: `explain CGHeroInstance` (vcmi; used in 163 files) → Callers (0), Callees (0), an import cycle, trust — no members, bases, derived, referencing files.
Render site: rgr/src/presentation/explain_sections.rs:84-97 maps signal codes → sections; no members/bases/derived/referenced-by section and no producer. agent/src/explain/mod.rs:60-200 routes focus: exact file path → explain_file (:698); path prefix/module → explain_path (:827); else stable-key/resolve_symbol → explain_symbol (:467), which pushes exactly IDENTITY (:483), CALLERS (:498), CALLEES (:530), CYCLES (:582), TRUST (:649), MEASUREMENTS (:652, dormant) — for every language and subtype. A CLASS is never called → structurally 0/0.
(a) TS/Java/Python: the same nothing. The only methods listing is EXPLAIN_SYMBOLS at mod.rs:751 inside explain_file, fed by storage.list_symbols_in_file (storage/src/agent_impl.rs:1417, `WHERE f.path = ?` — a FILE listing), reachable only when the target string IS the repo-relative path (resolve_path_focus, agent_impl.rs:481-501, exact match). SYMBOL-IDENTITY-1's "explain ConversationManager lists its methods" was a FILE-focus answer (FRAKTAG has packages/engine/src/core/ConversationManager.ts). Not language-gated — absent everywhere.
(b) Store for CGHeroInstance (vcmi snapshot …/2834d519): edges in both directions EMPTY for the definition node (lib/mapObjects/CGHeroInstance.h:55) and all 71 nodes of that name; parent_node_uid children 0. Members EXIST as nodes: 133 METHOD + 1 CONSTRUCTOR + 1 DESTRUCTOR with qualified_name LIKE 'CGHeroInstance::%' (containment rides in the qualified name; parent_node_uid is None in every extractor — 196 non-null rows in all of vcmi). Bases: ZERO, not even unresolved — (i) cpp-extractor/src/extractor.rs:1261-1298 extract_base_clause sets source_node_uid: ctx.file_node_uid (:1288) — the inheritance edge is anchored on the FILE (vcmi IMPLEMENTS shapes: SOURCE→CLASS 691, TEST_FILE→CLASS 148, SOURCE→STRUCT 11, CLASS→INTERFACE 2 = java); (ii) the clause is lost on macro-decorated classes (:1276-1278 comment: base tokens sit in ERROR nodes) — CGHeroInstance metadata {"macro_tokens":["DLL_LINKAGE"]}; its 7 declared bases produce no edge; lib/ has 451 `class DLL_LINKAGE` vs 108 macro-free `class X :` and only 91 IMPLEMENTS edges. References: no such edge type in indexer (EdgeType types.rs:38 has no References; the IR variant is LiveGraph-only). What exists for "where used": 178 IMPORTS edges target the FILE node lib/mapObjects/CGHeroInstance.h.
(c) Cause: BOTH, unequally — (ii) explain-not-rendering is dominant and language-wide (mod.rs:467-697); member data (135 nodes) and referencing-file data (178 include edges) are ALREADY in the store → two sections are pure render work. (i) missing edges costs the base/derived sections via two C++ index-time bugs (wrong anchor :1288; macro-mangled clauses dropped).
Never worked: `source_node_uid: ctx.file_node_uid` in extract_base_clause dates to e0fa892 (C++ extractor's introducing commit); explain_symbol never emitted a members signal.
Smallest fix (ordered, independently shippable): 1. RENDER, language-agnostic: in explain_symbol, when the focus subtype is a type (is_type_subtype, mod.rs:324, exists), push EXPLAIN_MEMBERS from a new storage.list_members_of_type(snapshot_uid, qualified_name) (`LIKE '<qn>::%' OR '<qn>.%'` with no further separator, by line_start) and EXPLAIN_REFERENCED_BY from IMPORTS edges into the focus symbol's FILE node grouped by module; render both. 2. C++ ANCHOR: :1288 pass the type node's node_uid (the java-extractor :1101 shape) — bases/derived queryable both directions. 3. C++ MACRO RECOVERY: scan the ERROR sibling for the `: public X, public Y` token run, reusing CPP-DECLARATORS-1's macro-name recovery.
Verify: explain CGHeroInstance → Members ≥ 135 anchored at CGHeroInstance.h:<line>; Base classes = 7 declared (indexed ones resolved); Derived classes; Referenced by = 178 files, top modules lib/ client/ server/ AI/. Same command on django BaseHandler and FRAKTAG ConversationManager BY NAME must also list methods (proves step 1 not C++-gated). Guard: vcmi IMPLEMENTS 852 → higher and the shape histogram loses SOURCE→CLASS; leveldb/FRAKTAG byte-stable.
Blast radius: step 1 render-only but explain is the flagship deep-dive surface — every `explain <Type>` in every language gains sections; explain.rs output-contract tests move. Steps 2-3 index-time; step 2 re-points all ~850 cpp IMPLEMENTS edges from FILE to CLASS sources — consumers keyed on the FILE anchor (module-edge derivation via file_to_module) must be re-checked. Ship 1, then 2, then 3.

# RC-6 — `deps list --ecosystem npm` counts Python imports as undeclared npm packages (observed side never partitioned; visibility regressed via DEPS-CLASSIFIER-1B d333214)

Outward: django `--ecosystem npm` → "undeclared 102 … abc, argparse, asgiref.local"; package.json declares 6.
Render: rgr/src/presentation/deps_list.rs:262-264 (⚠ line) + per-module `undeclared N`; numbers from daemon-runtime/src/deps_headline.rs:326-365 compute_unattributed over compose_dependency_summaries.
Data path: dispatch.rs:6737 takes --ecosystem verbatim → ComposeDependenciesInput.ecosystem="npm" → module-queries/src/deps/compose.rs:181-230 groups EVERY external_library_candidate row (unresolved_edges ⋈ nodes, storage/src/crud/module_edges_support.rs:285-315) into the module bucket; reconcile.rs diffs against the npm declared set.
Exact predicate: the observed loop has NO source-file partition — its only gate admit_observed (compose.rs:652-671) → classify_observed (classify.rs:106-140) picks a NORMALIZER by ecosystem, never asks the importing file's language. The declared side DOES partition: compose.rs:240 `if path_ecosystem(&dep.file_path) != Some(input.ecosystem) { continue }` (helper :615-624). Declared = npm-only, observed = everything. Store (django snapshot …/ed0a7b80): 13,967 external refs — 13,956 from .py, 11 from .js (document.*, Object.hasOwn — Builtin). abc ×2, argparse ×17, asgiref.local ×11 all Python-sourced.
Regression-or-never: the missing observed filter is ORIGINAL (f634618, 2026-05-11); the declared filter arrived with DEPS-LIST-REWRITE-1 (4055d27) and fixed half. VISIBILITY regressed from DEPS-CLASSIFIER-1B (d333214): before it django's two root manifests both hashed to canonical path `.`, last-writer-wins put pyproject in module_keys and the npm prefix gate (compose.rs:299-305) dropped the row — the npm view rendered nothing; insert_module_key_preferring_ecosystem (:365-395) made it render, exposing the hole.
Smallest fix: symmetric partition on the observed loop — storage read carries the source file path (JOIN files f ON f.file_uid = n.file_uid, select f.path) into ExternalImportFact; compose.rs:181 skips when ecosystem_prefix.is_some() && path_ecosystem(source_file_path) != Some(input.ecosystem) — reusing the ONE predicate. The skipped refs are NAMED: `cross_ecosystem: usize` on ComposeDependenciesResult; compute_unattributed says "13,956 of 13,967 external references are Python-file imports, outside this npm view — see deps list --ecosystem python" (never folded into "outside a parsed manifest scope" — a second false reason). none-detected views exempt (ecosystem_prefix.is_none()) or gstreamer's 7 rows lose every import.
Verify: django npm: undeclared 102 → 0, builtins 10 → 10-11, `used 0 · no static import found 6` unchanged, ⚠ names the split. Byte-stability provable from stores: repo-graph cargo view 17,360 rust / 294 non-rust refs, headline already exactly "294 of 17654"; FRAKTAG "30 of 733" = its 30 Python refs; storybook 100% ts/tsx/js/jsx.
Blast: one SELECT + one struct field + one loop guard + one reason string; only mixed-language repos with a sub-dominant reader-bearing ecosystem move.

# RC-7 — gstreamer "no dependency-manifest reader for … JavaScript/Rust" while 13 manifests were parsed (NEVER WORKED; two predicates)

Render: deps_list.rs:262-264 prints unattributed_reason verbatim; built by reader_context.rs:134-147 deps_reader_context_note.
Data path: dispatch.rs:6741 dominant_deps_ecosystem(language_counts) → compute_unattributed (deps_headline.rs:347-354): if ecosystem == "none-detected" → deps_reader_context_note(repo_languages, total) where repo_languages (dispatch.rs:6734) = EVERY language token in the snapshot.
Exact predicates: (1) deps_reader_context_note joins display_language_names(languages) (reader_context.rs:79-89) — ALL repo languages, no check against language_deps_ecosystem; gstreamer tokens c 6125, cpp 732, python 275, rust 20, java 18, javascript 15 → "C/C++/Java/JavaScript/Python/Rust" — the list is simply unfiltered. (2) the §2.4 secondary-ecosystem line that would have named cargo is suppressed by secondary_material_ecosystems (deps_ecosystem_presence.rs:207-212): source_files[eco]*10 >= total_code; Rust 20/7185 = 0.28%, JS 0.21%, Python 3.8%, Java 0.25% — all dropped silently. Manifests WERE discovered and parsed: store 4c3ef543f43413bc.db latest snapshot extraction_diagnostics_json.deps_manifests_present = {"cargo":4,"npm":1,"python":0}; deps_manifests has parsed records for 4 Cargo.toml, 1 package.json, 8 build.gradle, all error: null — the sentence is false on stored evidence in the same row. Third consequence: with ecosystem="none-detected", attach_manifest_context (compose.rs:450-477) filters r.ecosystem == "none-detected" → never matches → every row prints `[no manifest — imports unattributed]` incl. subprojects/gst-examples/webrtc/janus/rust whose Cargo.toml is a parsed record at that dir; compose.rs:240 drops all declared deps → used/undeclared structurally 0 for all 7 rows.
Never worked: deps_reader_context_note shipped with HONEST-DEGRADATION-IMPL-2 (c8f1f1c, 2026-07-01) unfiltered; dominant_deps_ecosystem + none-detected branch with DEPS-LIST-REWRITE-1 (4055d27); the ≥10% gate with DEPS-ATTRIB-2 (a79fc19). No commit crossed the note against language_deps_ecosystem.
Smallest fix: (1) in deps_reader_context_note filter languages by language_deps_ecosystem(tok).is_none() → "no dependency-manifest reader for C/C++ on this build"; (2) append, from the provenance dispatch.rs:6748 already reads: "13 manifests of other ecosystems were parsed (4 cargo, 1 npm, 8 gradle) — see deps list --ecosystem cargo". No change to the ≥10% gate (a rendering budget, correctly conservative). The per-row [no manifest] lie is answered by the documented --ecosystem route (fixing it inside the none-detected view would require four composes — not the smallest change).
Verify: gstreamer header names C/C++ only and points at cargo/npm/gradle with counts; --ecosystem cargo renders the 4 Cargo modules with [<path>/Cargo.toml]. nginx/poco/OpenXcom/sqlite/leveldb byte-identical (empty deps_manifests → clause 2 emits nothing).
Blast: one predicate in a pure function + one appended clause; no index/store change.

# RC-8 — `docs list` misses whole documentation trees (hadoop 23/540; root README.txt/.rst; .adoc) — ONE allow-list family, four disjoint sets (NEVER WORKED)

Render: rgr/src/presentation/docs.rs:193-196 headline + :255-262 By kind; recommendation line daemon-runtime/src/modules_method.rs:463-472. All downstream of doc-facts.
Data path: QUERY TIME — dispatch handle_docs_list → storage/src/agent_orient_reads.rs:90 discover_doc_inventory → doc-facts/src/lib.rs:151 discovery::discover_doc_files → live filesystem walk. Nothing about docs is read from the store; no reindex involved.
Exact predicate: doc-facts/src/discovery.rs:120-160 is_doc_candidate — a file is a document ONLY if (A) name allow-list :126-136 = exactly README.md, README, ARCHITECTURE.md, CONTRIBUTING.md, CHANGELOG.md, docker-compose.{yml,yaml}, compose.{yml,yaml}; or (B) extension × doc-tree :157: DOC_EXTENSIONS [".md",".txt",".rst"] (:96) AND in_docs_tree (:107-111) — an ancestor component literally docs/doc/design (DOC_TREE_DIRS :103); or (C) an rmap map sidecar / .env*. Not a src/ exclusion, not depth (MAX_DEPTH 10; hadoop's deepest is depth 7), not the generated-maps filter (separate, correctly stated at docs.rs:200-210), not size. Two more disjoint sets: (D) kind allow-list classification.rs:50 `file_name == "readme.md" || "readme"` — README.rst/.txt can never be kind readme; (E) orientation stems modules_method.rs:447-457 is_named_orientation_file {architecture, design, overview, contributing} at depth ≤1, plus rule 1 :395 requiring kind == readme && !path.contains('/').
Ground truth: hadoop 520 .md under hadoop-*/src/site/markdown/ — only 3 have a docs/doc/design ancestor; 14 match (A) → blocked by (B); hadoop root README.txt (+ BUILDING.txt) not in (A) → never discovered → (E) rule 1 sees no root readme → "No README or architecture doc found"; django root README.rst, CONTRIBUTING.rst (also INSTALL, AUTHORS) → (A); django's 4 readmes are deep README.md's; docs/README.rst → doc via (D); buildroot docs/manual/*.adoc 73 files → .adoc ∉ DOC_EXTENSIONS (B).
Never worked: discovery.rs commits 83bfd8f (2026-04-26 original), b9cc7d5 (fmt), 4a771e9 SELF-POLLUTION-1 (added .txt/.rst and doc/design INSIDE trees). AUDIT5-MINORS-1 F1 (75fe165) touched classification.rs/lib.rs/lib_tests.rs/types.rs only — kind labels, never discovery, and not the readme rule :50.
Smallest fix — one shape: (1) DOC_NAME_STEMS = {readme, contributing, changelog, architecture, design, overview, install, building, authors, news} matched on STEM with optional doc extension or none — README.txt/.rst, CONTRIBUTING.rst, INSTALL enter; classification.rs:50 readme rule becomes stem == "readme" (fixes docs/README.rst); modules_method.rs is_named_orientation_file keeps its depth bound but reads the same constant. (2) DOC_EXTENSIONS += .adoc, .markdown; in_docs_tree also opens on the site convention: ancestor component `site` whose parent is `src` (src/site/**), whole-component match as classification.rs:110-119 does. Then state what still is not scanned beside the generated/vendored lines: "+N markdown/prose files outside a docs tree not scanned (--json for the rule)".
Verify (measured on tracked trees): hadoop 23 → ~545 (+520 src/site, +2 root stems), By kind gains doc, modules list prints "read: README.txt"; django 670 → ~674, readme 4 → 6, "read: README.rst, CONTRIBUTING.rst"; buildroot 4 → ~77 (+73 .adoc). Byte-stable elsewhere: src/site prose = 0 for django/kafka/grpc-java/langchain4j/vscode/storybook/gstreamer/repo-graph; .adoc/.markdown in doc trees = 0 for all; vscode/storybook/kafka/gstreamer/langchain4j/buildroot already match (A) at root.
Blast: two constants + two predicates in doc-facts, one stem comparison in classification.rs, one shared-constant read in modules_method.rs, one header line in rgr; query-time only. Risk inverts: .txt under a `doc` component can pull fixtures — keep the honest header so refusals are counted out loud.

# Operator field checks (2026-09-08, round six)
- Five smoke failures (repo-graph assess, codegraph assess, duckdb orient --budget large, gstreamer deps list, rabbitmq-tutorials assess) = typed Busy under batch load (3 named the enrich pass + age; 2 generic "a concurrent operation is holding it"); all five retested uncontended on the retained root → exit 0. Not product defects; the two generic messages are DAEMON-RESIDUALS-3 OpKind::Seed territory.
- DAEMON-RESIDUALS-2B doctor retention fields: DORMANT on the audit root (1 snapshot/repo) AND on the production daemon (`rmap doctor` 2026-09-08: "retention: cleanup: none yet", repo-graph "1 snapshot" — the store was rebuilt 2026-09-04, no prune has run since). The deep-vertical rule bites: the surface cannot be told working from missing until a prune runs. Follow-up: the retention line should always state the standing (snapshots held / cap / last pass: never) so a fresh store still renders the mechanism.
- AUDIT5-MINORS-1 F5 doctor `[note]` line: RENDERS in the field on the production daemon — "[note] vector store: degraded: seeds re-embedding for per-chunk facts (pending)" and the count line reads "26 ok · 1 note" (the audit root's seed store was healthy, hence no note there — correctly not-applicable, resolving the matrix grader's open item).
- CPP-DECLARATORS-1 IMPLEMENTS 2→852 (vcmi): not exercised by any round-six capture; RC-3 shows the edges are anchored SOURCE(file)→CLASS, so the count is real in the store but invisible from any class focus.


# RC-11 — `detect_alias_resolution_suspicion` attributes genuine module isolation to alias-resolution failure (found by TRUST-MODULE-EDGES-1's implementation, 2026-09-13)

Outward: after trust's fan counts are re-sourced from the derived module edge set (Q2), `trust` still renders `Import-graph: LOW (alias resolution suspected; …)` and the Change-impact downgrade on repo-graph (6 zero-connectivity candidates: `rust/` umbrella dir, `tools`, `scripts`, two test-fixture dirs, leaf crates `detectors`, `git`), kafka (5) and FRAKTAG (3 — zero cross-module resolved imports, 185 intra of 210); the downgrade clears on hadoop (2) and vcmi (2).
Predicate: `rust/crates/trust/src/rules.rs` `detect_alias_resolution_suspicion` fires when the count of zero-connectivity module candidates (file_count ≥ 2, fan_in = fan_out = 0) is ≥ 3. Calibrated in the all-zero world of RC-5; with true fans it cannot distinguish "this module's imports did not resolve" (alias suspicion) from "nothing imports this module and it imports nothing" (genuine isolation: umbrella/fixture directories, leaf crates nobody depends on). Never worked as a discriminator — the RC-5 regression masked it.
Smallest fix shape (its own slice, ALIAS-SUSPICION-1 — RG-REQ-009-L04 "a reason never outlives its cause", RG-REQ-002-L08 reader frame): count toward suspicion only zero-connectivity candidates that HAVE unresolved imports (the store knows per-module unresolved counts), or render genuine isolation as its own reader-frame fact ("6 modules import nothing and are imported by nothing: …") and reserve the alias reason for candidates whose imports failed to resolve. Verify on the five proof repos: the reason disappears where isolation is genuine and stays where unresolved imports exist; every trust LEVEL movement reported.
Blast radius: trust's Import-graph/Change-impact reasons on every repo; orient/assess inherit; `dead` overlay level unchanged while unresolved > 0. Preservation: RG-REQ-009-L02/L03/L06/L07, D-T6.
