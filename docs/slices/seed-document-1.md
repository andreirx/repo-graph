<!-- requirements-assurance-implementation-v1
{
  "formatVersion": 1,
  "kind": "implementation-allocation",
  "workItemId": "SEED-DOCUMENT-1",
  "baselinePath": "docs/requirements/baselines/SEED-DOCUMENT-1-INPUT-1.json",
  "parentRequirementIds": [
    "RG-REQ-010",
    "RG-REQ-011"
  ],
  "implements": [
    "RG-REQ-010-L10"
  ],
  "preserves": [
    "RG-REQ-010-L01",
    "RG-REQ-010-L03",
    "RG-REQ-010-L05",
    "RG-REQ-010-L06",
    "RG-REQ-010-L07",
    "RG-REQ-010-L08",
    "RG-REQ-011-L06"
  ],
  "preservationObligationIds": [
    "P-SD-01",
    "P-SD-02",
    "P-SD-03"
  ],
  "changes": [],
  "acceptanceBoundary": "The `find` outputs (human and --json) of the candidate rmap on two fresh isolated roots it indexed and seeded itself (FRAKTAG, leveldb) against the before binary on the two pre-provisioned seeded `-before` roots, plus cargo test on repo-graph-seed (whole crate), as named per check.",
  "candidatePaths": [
    "rust/crates/repo-graph-seed/src/document.rs",
    "rust/crates/repo-graph-seed/src/pass.rs",
    "rust/crates/repo-graph-seed/src/pass_tests.rs",
    "rust/crates/repo-graph-seed/src/lib.rs"
  ],
  "postReviewRecordPaths": [
    "docs/assurance/SEED-DOCUMENT-1/verification.json",
    "docs/assurance/SEED-DOCUMENT-1/implementation-review.json"
  ],
  "candidateExclusions": [
    {
      "pathPrefix": ".agent-manager/",
      "reason": "local relay state and raw run/progress evidence"
    },
    {
      "pathPrefix": "rust/target/",
      "reason": "reproducible Cargo build output"
    }
  ],
  "checks": [
    {
      "checkId": "SD-C01",
      "obligationIds": [
        "RG-REQ-010-L10"
      ],
      "owner": "builder",
      "method": {
        "kind": "command",
        "command": "cargo test -p repo-graph-seed --lib document 2>&1 | tee /tmp/sd-c01.txt | grep -E '^test result: ok\\.' && ! grep -E '^test .* FAILED$|^thread .* panicked at|^test result: FAILED' /tmp/sd-c01.txt && for t in enclosing_sentence_follows_the_qualified_name_and_precedes_the_own_doc enclosing_sentence_is_the_first_sentence_stripped_of_comment_markers enclosing_sentence_truncates_at_the_named_bound_on_a_char_boundary enclosing_sentence_is_none_for_an_empty_or_marker_only_doc no_enclosing_doc_embeds_exactly_as_before parent_qualified_name_splits_on_the_last_separator document_has_qualified_name_doc_and_body_in_order missing_qualified_name_and_doc_still_embeds_the_span empty_qualified_name_and_doc_are_not_blank_lines no_role_prefix_is_emitted body_capped_at_60_lines char_cap_never_splits_a_scalar_and_bounds_length query_is_verbatim_and_capped; do grep -qE \"^test .*$t .* ok$\" /tmp/sd-c01.txt || { echo \"MISSING $t\"; exit 1; }; done && grep -qE '^pub const MAX_ENCLOSING_SENTENCE_CHARS: usize = 160;' crates/repo-graph-seed/src/document.rs && ! grep -q 'This builder does NOT try to fix that geometry' crates/repo-graph-seed/src/document.rs",
        "cwd": "rust",
        "environment": "candidate tree",
        "inputs": "rust/crates/repo-graph-seed/src/document.rs: `pub const MAX_ENCLOSING_SENTENCE_CHARS: usize = 160;` (the slice-owned bound RG-REQ-010-L10 names); `pub fn enclosing_doc_sentence(doc: &str) -> Option<String>` — a PURE function: per line, trim, strip leading comment markers (`/**`, `/*`, `*/`, `*`, `///`, `//!`, `//`, `#`, `!`) and trim again; skip leading empty lines; stop at the first empty line, at a line starting with `@` (a JSDoc/Javadoc tag) or at the end; join the kept lines with one space; cut after the first `.` that is followed by whitespace or ends the text; truncate to `MAX_ENCLOSING_SENTENCE_CHARS` scalars on a char boundary (the `truncate_chars` helper :81-86); `None` when nothing remains (the stored `doc_comment` shapes differ by extractor — TypeScript keeps `/**\\n * text\\n */`, Rust keeps its raw `///` lines, Python stores the docstring with its quotes stripped, Java strips `/** */` and the leading `*` — so the stripping is the function's, not the extractors'); `pub fn parent_qualified_name(qn: &str) -> Option<&str>` — the prefix before the LAST `::` or `.` (`ConversationManager.createSession` → `ConversationManager`; `leveldb::DBImpl::Recover` → `leveldb::DBImpl`; `a.b.c` → `a.b`; `f` → None); `build_chunk_document(qualified_name, enclosing_sentence: Option<&str>, doc_comment, span_source)` gains the SECOND parameter and places it between the qualified name and the own doc (order: qualified name, enclosing sentence, own doc, first 60 body lines; the char cap unchanged); with `None` the output is byte-identical to today's (`no_enclosing_doc_embeds_exactly_as_before`); the module doc (:1-6) and the fn doc (:14-38 — the paragraph 'This builder does NOT try to fix that geometry … the compensation lives DOWNSTREAM' is FALSE after this slice and is rewritten to state the composition and the measured reason, citing docs/audits/2026-09-21-seed-document-spike-1.md; the negated grep). The seven existing tests keep their names and assertions verbatim (the six that call `build_chunk_document` — every one except `query_is_verbatim_and_capped` — gain `None` as the new second argument, so their expected bytes are unchanged); six NEW tests (names in the command; the marker test covers the TypeScript `/**\\n * ConversationManager handles conversation persistence.\\n * Each conversation …` shape → `ConversationManager handles conversation persistence.` and the Java/Python plain shape)"
      },
      "expected": "exit 0: the composition is pinned — the enclosing type's first sentence, bounded by the named constant, sits between the qualified name and the own doc; a chunk without a documented enclosing type embeds exactly as before (no behavior change for it)"
    },
    {
      "checkId": "SD-C02",
      "obligationIds": [
        "RG-REQ-010-L10",
        "RG-REQ-010-L01",
        "RG-REQ-010-L03",
        "RG-REQ-010-L05",
        "RG-REQ-010-L06",
        "RG-REQ-010-L07",
        "RG-REQ-010-L08",
        "P-SD-01",
        "P-SD-02"
      ],
      "owner": "builder",
      "method": {
        "kind": "command",
        "command": "cargo test -p repo-graph-seed  2>&1 | tee /tmp/sd-c02.txt | grep -E '^test result: ok\\.' && ! grep -E '^test .* FAILED$|^thread .* panicked at|^test result: FAILED' /tmp/sd-c02.txt && for t in enclosing_type_sentence_enters_the_method_document_and_its_reuse_key same_file_type_parent_wins_over_a_cross_file_one unique_cross_file_type_parent_contributes_its_sentence ambiguous_cross_file_type_parents_contribute_no_sentence same_file_name_parent_blocks_the_cross_file_fallback forward_declaration_is_never_an_enclosing_type_candidate unique_type_parent_beyond_the_corpus_cap_still_contributes_its_sentence second_type_parent_beyond_the_corpus_cap_makes_the_fallback_abstain corpus_cap_omits_the_remainder non_method_children_embed_without_the_enclosing_sentence documented_non_type_name_parent_gives_no_enclosing_sentence each_canonical_type_subtype_encloses_a_method copy_forward_reuses_unchanged_chunk_and_skips_the_embed model_change_forces_full_reembed copy_forward_reembeds_when_doc_comment_changed_though_file_hash_unchanged copy_forward_reembeds_a_legacy_null_document_hash_parent field_ranks_below_body_bearing_even_when_it_scores_higher production_ranks_above_test_even_when_test_scores_higher impl_outranks_its_own_decl_even_when_decl_scores_higher undocumented_one_liner_is_field_tier_by_span documented_one_line_constant_is_not_field_tiered_by_span_alone; do grep -qE \"^test .*$t .* ok$\" /tmp/sd-c02.txt || { echo \"MISSING $t\"; exit 1; }; done && git diff --quiet HEAD -- crates/repo-graph-seed/src/rank.rs crates/repo-graph-seed/src/classify.rs crates/repo-graph-seed/src/classify_tests.rs crates/repo-graph-seed/src/ports.rs crates/repo-graph-seed/src/hash.rs crates/storage/src/seed_impl.rs crates/daemon-runtime/src/seed crates/rgr/src/commands/find",
        "cwd": "rust",
        "environment": "candidate tree",
        "inputs": "the WHOLE repo-graph-seed crate. rust/crates/repo-graph-seed/src/pass.rs `build_store`: an entry ENCLOSES (can supply a sentence) only when it carries a non-empty doc AND its stored `subtype` is a canonical TYPE subtype — `ENCLOSING_TYPE_SUBTYPES = [\"CLASS\", \"INTERFACE\", \"STRUCT\", \"ENUM\"]`, a private const in pass.rs (review SD-F02: RG-REQ-010-L10 says the enclosing TYPE's doc). The set is the type-declaring variants of `indexer::types::NodeSubtype` (types.rs:143-159, serialized SCREAMING_SNAKE_CASE) as the extractors emit them: TypeScript class/interface/enum; Java class and record → CLASS (extractor.rs:594-632), interface and `@interface` → INTERFACE (:466-491, :668-688), enum → ENUM; Python class → CLASS; Rust struct → CLASS ('Closest mapping for structs', rust-extractor/src/extractor.rs:482), trait → INTERFACE (:534), enum → ENUM; C/C++ class → CLASS, struct → STRUCT, enum → ENUM. TYPE_ALIAS is excluded (an alias names a type, it declares no member scope; no FRAKTAG METHOD has a documented TYPE_ALIAS name-parent); FUNCTION, VARIABLE, CONSTANT, PROPERTY, METHOD and every other subtype never enclose; the FIRST such entry in corpus order winning (`entry(..).or_insert`) so a duplicated qualified name resolves deterministically (FRAKTAG: 0 duplicated (path, qualified_name) pairs) (`SeedCorpusEntry.qualified_name`/`doc_comment`, ports.rs:61/:67 — no DTO or SQL change: the enclosing type is found by qualified-name containment in the corpus already read); per chunk whose stored `subtype` is exactly `METHOD` (D-SD1-002 as revised by the document review's SD-DEC-01: RG-REQ-010-L10 says 'a method chunk' — properties, constructors, getters, variables and nested types embed exactly as today), `parent_qualified_name(chunk.qualified_name)` and then the LOOKUP RULE (METHOD chunks only; genuine forward declarations — `SeedForwardDecl::ForwardDecl` — are ignored throughout; both steps read the COMPLETE corpus `build_store` receives, before the corpus cap truncates it — only the emitted vectors are cap-limited): the parent qualified name is the chunk's minus its last `.`/`::` segment; (1) if the chunk's own file holds any entry with that qualified name, the enclosing type is decided in that file — the first entry in corpus order with a canonical type subtype (CLASS, INTERFACE, STRUCT, ENUM) and a non-empty doc supplies the sentence, otherwise there is no sentence and the cross-file branch is not consulted; (2) only if the chunk's file holds no such entry, the corpus-wide entries with that qualified name and a canonical type subtype are counted — exactly one, if documented, supplies its sentence; none, or two or more (ambiguous), supply no sentence. Mechanism (review F-SD-001 of PREP-3: both indexes are built ONCE from the COMPLETE `entries` vector BEFORE `entries.truncate(cfg.corpus_cap)` (pass.rs:127-133) — the storage read `seed_corpus` (storage/src/seed_impl.rs:50) applies no LIMIT, so the pass receives the whole corpus and the cap is the only truncation; building over the capped vector would make a same-named parent beyond the cap invisible, so a duplicate could read unique or a unique parent could not contribute): step (1) reads a PER-FILE index (path, qualified_name) → the doc of the first entry in corpus order with a canonical type subtype and a non-empty doc, or none, whose KEY SET records every (path, qualified_name) of an entry of ANY subtype, forward declarations skipped — key present ⇒ decided in that file (not a per-run map over the run at :171-196, which would miss a class cut from its run by the cap); step (2) reads a CORPUS-WIDE map over the entries with a canonical type subtype, forward declarations skipped: qualified_name → (the first such entry's doc, count of such entries), consulted only when count == 1; the capped run loop only CONSULTS the two indexes, so the admitted/emitted/`corpus_omitted` counts are unchanged (review SD-F03: L10 states no same-file limitation and the spike's measured lookup was same-file-or-unique-in-corpus — the C++ header/impl split: leveldb `db/db_impl.h:29` `class DBImpl : public DB {` with `db/db_impl.cc:292` `Status DBImpl::Recover(VersionEdit* edit, bool* save_manifest) {`; `db/db_iter.h:15` `class DBImpl;` is a forward declaration the C++ extractor stores with the same qualified name and `forward_decl: true` (cpp-extractor `forward_decl_class_is_flagged_definition_is_not`), so counting it would make every forward-declared class ambiguous and defeat the case the fallback exists for — hence ignored; and a same-file entry of any kind blocks the fallback because in that file it IS the method's name-parent, so a same-named type elsewhere is not its enclosing type — the spike's `by_file_qn` rule; leveldb stores 0 doc comments, so the fallback contributes nothing there); → `enclosing_doc_sentence(doc)` → passed to `build_chunk_document` (:252-256) (leveldb has 0 documented symbols, so the cross-file branch changes nothing there — stated; on FRAKTAG the 152 METHOD chunks with a documented same-file name-parent all sit under a CLASS (133) or an INTERFACE (19), so the kind gate changes no measured row — it makes the L10 invariant hold by construction rather than by corpus accident); `has_doc` (:237) and `is_field` (:241-246) are untouched (`non_method_children_embed_without_the_enclosing_sentence`: a PROPERTY and a CONSTRUCTOR of a documented class keep today's document and document_hash); the document hash (:257) is the copy-forward reuse key (:150-161, :272-289), so every METHOD chunk whose enclosing type carries a doc gets a NEW `document_hash` and is re-embedded ONCE on the first pass after this change (`reused` = 0 for those; the model_change test's mechanism, now triggered by composition) — the stated cost of RG-REQ-010-L10, reported per corpus in the evidence; pass_tests.rs helper `doc_hash` (:13-15; its `build_chunk_document` call is :14) gains the argument (its callers pass `None`; the existing tests' `chunk` helper builds FUNCTION entries, so their documents are unchanged). Eleven NEW pass tests, the eleven names in the command (a fake corpus: `enclosing_type_sentence_enters_the_method_document_and_its_reuse_key`: a documented class `C` with a method `C.m` in file `a.ts` → `C.m`'s embedded text contains the class sentence and its `document_hash` differs from the no-enclosing composition; `same_file_type_parent_wins_over_a_cross_file_one`: a class `D` documented in `a.ts` AND another documented `D` in `b.ts`, with `D.m` in `a.ts` → `D.m` carries `a.ts`'s sentence; `unique_cross_file_type_parent_contributes_its_sentence`: a class `E` documented only in `e.h` with `E.m` (subtype METHOD) in `e.cc` → `E.m` carries `E`'s sentence and its `document_hash` differs from the no-enclosing composition; `ambiguous_cross_file_type_parents_contribute_no_sentence`: a class `F` documented in both `f1.h` and `f2.h` with `F.m` in `f.cc` → `F.m` embeds without a sentence, document and hash byte-identical to today's; `same_file_name_parent_blocks_the_cross_file_fallback`: in `h.ts` an UNDOCUMENTED class `H` with METHOD `H.m` and a documented VARIABLE `v` with METHOD `v.m`, and in `other.ts` a documented class `H` and a documented class `v` → `H.m` and `v.m` byte-identical to the no-enclosing composition (step (1) decides in the chunk's file; no other file's type is attributed); `forward_declaration_is_never_an_enclosing_type_candidate`: a documented class `G` defined in `g.h`, a forward declaration of `G` documented with a DIFFERENT sentence (CLASS, `forward_decl: SeedForwardDecl::ForwardDecl`) in `g_fwd.h`, and METHOD `G::m` in `g.cc` → `G::m` carries `g.h`'s sentence (the forward declaration neither supplies nor makes the name ambiguous — the leveldb `db_iter.h:15` shape); and a class `K` present only as a documented forward declaration in `k_fwd.h` with METHOD `K::m` in `k.cc` → `K::m` byte-identical to the no-enclosing composition; `unique_type_parent_beyond_the_corpus_cap_still_contributes_its_sentence`: `corpus_cap: 1`, a METHOD `Z::m` in `a.cc` (first in (path, line) order, so inside the cap) and a documented class `Z` only in `z.h` (beyond the cap) → `Z::m`'s document carries `z.h`'s sentence and its `document_hash` differs from the no-enclosing composition, while `report.admitted` = 1 and `report.corpus_omitted` = 1 (the class's own chunk is still not emitted); `second_type_parent_beyond_the_corpus_cap_makes_the_fallback_abstain`: `corpus_cap: 2`, a METHOD `Y::m` in `a.cc` and a documented class `Y` in `b.h` (both inside the cap) and a second documented class `Y` in `z.h` (beyond the cap) → `Y::m` byte-identical to the no-enclosing composition (the out-of-cap duplicate makes the name ambiguous), `report.admitted` = 2, `report.corpus_omitted` = 1; the existing `corpus_cap_omits_the_remainder` stays green unchanged (the cap still bounds the emitted vectors); `non_method_children_embed_without_the_enclosing_sentence`: a one-line undocumented PROPERTY `C.p` and a CONSTRUCTOR `C.constructor` of the documented class → documents and `document_hash` byte-identical to today's, `is_field` true for `C.p`; `documented_non_type_name_parent_gives_no_enclosing_sentence`: a documented FUNCTION `f` and a documented VARIABLE `v` in `a.ts` with METHOD chunks `f.m` and `v.m` in the same file → both documents and `document_hash` byte-identical to the no-enclosing composition (`doc_hash(qn, None, own doc, span)`), i.e. no sentence enters from a non-type; `each_canonical_type_subtype_encloses_a_method`: one documented parent per subtype CLASS, INTERFACE, STRUCT, ENUM, each with a METHOD child in the same file → each child's embedded text contains its parent's first sentence). rank.rs (13 tests), classify.rs/classify_tests.rs (31), ports.rs, hash.rs, storage seed_impl.rs, daemon-runtime seed/, rgr find/ are byte-identical (the diff --quiet): the floor `SEED_SIMILARITY_FLOOR = 0.30` (rgr seed_render.rs:51), the model `minishlab/potion-code-16M-v2` (daemon-runtime seed/local_engine.rs:49), the tiers and the partition are untouched"
      },
      "expected": "exit 0: a method's document carries its enclosing TYPE's first sentence (a CLASS, INTERFACE, STRUCT or ENUM parent only — a documented non-type name-parent contributes nothing; decided in the method's own file when that file holds its name-parent, else only by a unique non-forward-declared type across the corpus) and that sentence is part of the reuse identity; non-method chunks, field tiering, ranking, the partition, the floor, the model and the copy-forward invariants remain exactly as before (no behavior change to them)"
    },
    {
      "checkId": "SD-C03",
      "obligationIds": [
        "P-SD-03"
      ],
      "owner": "builder",
      "method": {
        "kind": "command",
        "command": "cargo fmt --check -p repo-graph-seed && cargo clippy -p repo-graph-seed -p repo-graph-daemon-runtime --all-targets -- -D warnings > /tmp/sd-c03.txt 2>&1; rc=$?; tail -3 /tmp/sd-c03.txt; test $rc -eq 0",
        "cwd": "rust",
        "environment": "candidate tree",
        "inputs": "rustfmt on the touched crate; clippy (deny warnings, all targets) on repo-graph-seed and on its consumer repo-graph-daemon-runtime (the `build_store` caller compiles against the unchanged port)"
      },
      "expected": "exit 0: no formatting or lint debt enters with the candidate"
    },
    {
      "checkId": "SD-C04",
      "obligationIds": [
        "RG-REQ-010-L10",
        "RG-REQ-010-L01",
        "RG-REQ-010-L03",
        "P-SD-02"
      ],
      "owner": "builder",
      "method": {
        "kind": "command",
        "command": "rm -f /tmp/sd-frk-* /tmp/sd-fraktag-* && trap 'P=$(cat /private/tmp/SEED-DOCUMENT-1-fraktag-after/rmapd.pid 2>/dev/null); if [ -n \"$P\" ]; then kill \"$P\" 2>/dev/null; sleep 1; kill -9 \"$P\" 2>/dev/null; fi; rm -rf /private/tmp/SEED-DOCUMENT-1-fraktag-after' EXIT && rm -rf /private/tmp/SEED-DOCUMENT-1-fraktag-after && mkdir -p /private/tmp/SEED-DOCUMENT-1-fraktag-after && cp -R \"$HOME/Library/Application Support/repo-graph/seed-model-cache\" /private/tmp/SEED-DOCUMENT-1-fraktag-after/seed-model-cache && ( env -u RMAP_TRANSPORT RMAP_STATE_ROOT=/private/tmp/SEED-DOCUMENT-1-fraktag-after RMAP_SOCKET_PATH=/private/tmp/SEED-DOCUMENT-1-fraktag-after/d.sock RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmapd\" > /tmp/sd-fraktag-rmapd.log 2>&1 & echo $! > /private/tmp/SEED-DOCUMENT-1-fraktag-after/rmapd.pid ) && for i in $(seq 1 30); do test -S /private/tmp/SEED-DOCUMENT-1-fraktag-after/d.sock && break; sleep 1; done && test -S /private/tmp/SEED-DOCUMENT-1-fraktag-after/d.sock && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/FRAKTAG' && export RMAP_STATE_ROOT=/private/tmp/SEED-DOCUMENT-1-fraktag-after RMAP_SOCKET_PATH=/private/tmp/SEED-DOCUMENT-1-fraktag-after/d.sock RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && unset RMAP_TRANSPORT && \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" index > /tmp/sd-fraktag-index.txt 2>&1 && ready=0 && for i in $(seq 1 120); do \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" find zzz --json 2>/dev/null | python3 -c 'import json,sys; d=json.load(sys.stdin); v=d.get(\"value\", d); sys.exit(0 if v.get(\"seeds_available\") is True else 1)' && ready=1 && break; sleep 5; done; kill \"$(cat /private/tmp/SEED-DOCUMENT-1-fraktag-after/rmapd.pid)\"; sleep 1; kill -9 \"$(cat /private/tmp/SEED-DOCUMENT-1-fraktag-after/rmapd.pid)\" 2>/dev/null; rm -f /private/tmp/SEED-DOCUMENT-1-fraktag-after/rmapd.pid; test \"$ready\" = 1 ) || { echo \"SEED-NOT-READY /private/tmp/SEED-DOCUMENT-1-fraktag-after (socket, index or seed pass; see /tmp/sd-fraktag-rmapd.log)\"; exit 1; } && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/FRAKTAG' && export RMAP_STATE_ROOT=/private/tmp/SEED-DOCUMENT-1-fraktag-before RMAP_SOCKET_PATH=/private/tmp/SEED-DOCUMENT-1-fraktag-before/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && \"/private/tmp/SEED-DOCUMENT-1-before-bin/rmap\" find 'where are conversations persisted to disk' --json > /tmp/sd-frk-persist-before.json 2> /tmp/sd-frk-persist-before.json.err && \"/private/tmp/SEED-DOCUMENT-1-before-bin/rmap\" find 'where are conversations persisted to disk' > /tmp/sd-frk-persist-before.txt 2> /tmp/sd-frk-persist-before.txt.err ) || { echo \"RUN-FAILED /tmp/sd-frk-persist-before\"; exit 1; } && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/FRAKTAG' && export RMAP_STATE_ROOT=/private/tmp/SEED-DOCUMENT-1-fraktag-after RMAP_SOCKET_PATH=/private/tmp/SEED-DOCUMENT-1-fraktag-after/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" find 'where are conversations persisted to disk' --json > /tmp/sd-frk-persist-after.json 2> /tmp/sd-frk-persist-after.json.err && \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" find 'where are conversations persisted to disk' > /tmp/sd-frk-persist-after.txt 2> /tmp/sd-frk-persist-after.txt.err ) || { echo \"RUN-FAILED /tmp/sd-frk-persist-after\"; exit 1; } && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/FRAKTAG' && export RMAP_STATE_ROOT=/private/tmp/SEED-DOCUMENT-1-fraktag-before RMAP_SOCKET_PATH=/private/tmp/SEED-DOCUMENT-1-fraktag-before/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && \"/private/tmp/SEED-DOCUMENT-1-before-bin/rmap\" find 'how are content atoms hashed' --json > /tmp/sd-frk-hash-before.json 2> /tmp/sd-frk-hash-before.json.err && \"/private/tmp/SEED-DOCUMENT-1-before-bin/rmap\" find 'how are content atoms hashed' > /tmp/sd-frk-hash-before.txt 2> /tmp/sd-frk-hash-before.txt.err ) || { echo \"RUN-FAILED /tmp/sd-frk-hash-before\"; exit 1; } && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/FRAKTAG' && export RMAP_STATE_ROOT=/private/tmp/SEED-DOCUMENT-1-fraktag-after RMAP_SOCKET_PATH=/private/tmp/SEED-DOCUMENT-1-fraktag-after/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" find 'how are content atoms hashed' --json > /tmp/sd-frk-hash-after.json 2> /tmp/sd-frk-hash-after.json.err && \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" find 'how are content atoms hashed' > /tmp/sd-frk-hash-after.txt 2> /tmp/sd-frk-hash-after.txt.err ) || { echo \"RUN-FAILED /tmp/sd-frk-hash-after\"; exit 1; } && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/FRAKTAG' && export RMAP_STATE_ROOT=/private/tmp/SEED-DOCUMENT-1-fraktag-before RMAP_SOCKET_PATH=/private/tmp/SEED-DOCUMENT-1-fraktag-before/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && \"/private/tmp/SEED-DOCUMENT-1-before-bin/rmap\" find 'where are tree nodes saved to disk' --json > /tmp/sd-frk-tree-before.json 2> /tmp/sd-frk-tree-before.json.err && \"/private/tmp/SEED-DOCUMENT-1-before-bin/rmap\" find 'where are tree nodes saved to disk' > /tmp/sd-frk-tree-before.txt 2> /tmp/sd-frk-tree-before.txt.err ) || { echo \"RUN-FAILED /tmp/sd-frk-tree-before\"; exit 1; } && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/FRAKTAG' && export RMAP_STATE_ROOT=/private/tmp/SEED-DOCUMENT-1-fraktag-after RMAP_SOCKET_PATH=/private/tmp/SEED-DOCUMENT-1-fraktag-after/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" find 'where are tree nodes saved to disk' --json > /tmp/sd-frk-tree-after.json 2> /tmp/sd-frk-tree-after.json.err && \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" find 'where are tree nodes saved to disk' > /tmp/sd-frk-tree-after.txt 2> /tmp/sd-frk-tree-after.txt.err ) || { echo \"RUN-FAILED /tmp/sd-frk-tree-after\"; exit 1; } && grep -q 'ConversationManager.createSession' /tmp/sd-frk-persist-after.txt && ! grep -q 'ConversationManager.createSession' /tmp/sd-frk-persist-before.txt && python3 - <<'PY' > /tmp/sd-frk-after-vectors.txt || { echo \"ASSERT-FAILED human rows (createSession rendered after, absent before) or after-store read\"; exit 1; }\nimport sqlite3, glob\nk = sqlite3.connect('file:' + glob.glob('/private/tmp/SEED-DOCUMENT-1-fraktag-after/databases/*.db')[0] + '?mode=ro', uri=True)\nprint(k.execute('select count(*) from seed_vectors').fetchone()[0])\nPY\nrm -rf /private/tmp/SEED-DOCUMENT-1-fraktag-after && python3 - /tmp/sd-frk-persist-before.json /tmp/sd-frk-persist-after.json /tmp/sd-frk-hash-after.json /tmp/sd-frk-tree-after.json <<'PY'\nimport json, sys\ndef top(p):\n    d = json.load(open(p)); v = d.get('value', d); assert v['seeds_available'] is True, p\n    return v['candidates']\nb, a, h, t = (top(p) for p in sys.argv[1:5])\nnames_b = [c.get('qualified_name') for c in b]; names_a = [c.get('qualified_name') for c in a]\nprint('BEFORE', [(i + 1, round(c['score'], 3), c.get('qualified_name')) for i, c in enumerate(b)])\nprint('AFTER ', [(i + 1, round(c['score'], 3), c.get('qualified_name')) for i, c in enumerate(a)])\nassert 'ConversationManager.createSession' not in names_b, 'the before root must show the defect'\ncs = [c for c in a if c.get('qualified_name') == 'ConversationManager.createSession' and c['path'] == 'packages/engine/src/core/ConversationManager.ts']\nassert len(cs) == 1, ('createSession absent from the after top ten', names_a)\nassert cs[0]['score'] >= 0.30, ('createSession under the floor', cs[0]['score'])\nassert cs[0]['line'] == 71, cs[0]['line']\ncm = [i for i, c in enumerate(a) if c.get('qualified_name') == 'ConversationManager' and c['path'] == 'packages/engine/src/core/ConversationManager.ts']\nassert cm and cm[0] <= 2, ('owning class not in the top three', names_a)\nassert not any(c['is_field'] for c in a), [c.get('qualified_name') for c in a if c['is_field']]\nassert h[0].get('qualified_name') == 'ContentStore.findByHash' and h[0]['path'] == 'packages/engine/src/core/ContentStore.ts' and h[0]['line'] == 90 and h[0]['score'] >= 0.30, h[0]\nassert t[0].get('qualified_name') == 'TreeStore.saveNode' and t[0]['path'] == 'packages/engine/src/core/TreeStore.ts' and t[0]['line'] == 316 and t[0]['score'] >= 0.30, t[0]\nn = int(open('/tmp/sd-frk-after-vectors.txt').read().strip())\nprint('after seed_vectors', n, '| createSession', round(cs[0]['score'], 3), 'rank', names_a.index('ConversationManager.createSession') + 1, '| class rank', cm[0] + 1)\nassert n == 1291, n\nPY",
        "cwd": ".",
        "environment": "pre-provisioned isolated SEEDED root /private/tmp/SEED-DOCUMENT-1-fraktag-before (fresh index + seed pass of /Users/apple/Documents/APLICATII BIJUTERIE/FRAKTAG at its HEAD 9bea3a5 — the snapshot's `basis_commit`, unchanged since — by the repo-graph binary built at 12eecd5 on 2026-09-21 — 1291 seed vectors) served by the before binary /private/tmp/SEED-DOCUMENT-1-before-bin/rmap copied from rust/target/release on the CLEAN tree at step 0; NEW /private/tmp/SEED-DOCUMENT-1-fraktag-after indexed AND seeded by the candidate through a SOCKET daemon (candidate rmapd, model cache copied from the operator's `seed-model-cache`, spawned as `env -u RMAP_TRANSPORT … rmapd … &` so `env` execs rmapd and `$!` is rmapd's own PID (a backgrounded `export … && rmapd &` AND-list would record the PID of a forked subshell, and killing that PID would orphan the daemon); the socket polled with `test -S` before `rmap index` (the client never auto-spawns a socket daemon: in auto mode only EPERM/EACCES falls back to stdio); the PID killed after `seeds_available` turns true. A stdio daemon cannot do this: the stdio `rmapd` is a child of the one `rmap` process and exits when that client closes its stdin after `index` returns, taking the detached background seed thread with it, so no pass ever completes and every later stdio `find` starts a fresh daemon); queries under RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off; the after root is REMOVED by the check after its captures AND on every failure path by an EXIT trap that first kills the recorded daemon PID (SD-F01); every `/tmp/sd-frk-*`/`/tmp/sd-fraktag-*` capture is deleted at the start so a stale file from an earlier cycle can never satisfy an assertion; `rmap find --json` prints the daemon's FindResponse at top level (`seeds_available`, `candidates[]` with `qualified_name`, `path`, `line`, `score`, `is_field`; daemon-runtime seed/dto.rs; FIND_CANDIDATE_CAP = 10, dispatch_seed.rs:65) — the `d.get('value', d)` read accepts it and an enveloped form alike",
        "inputs": "FRAKTAG (TypeScript, 1291 chunks; 286 documented symbols). Code under analysis: `packages/engine/src/core/ConversationManager.ts:48-60` class doc `/**\\n * ConversationManager handles conversation persistence.\\n * Each conversation is its own Tree (conv-{uuid}), stored in Internal KB. …` → first sentence `ConversationManager handles conversation persistence.`; `:71 async createSession(` (own doc `Creates a new conversation session as a separate Tree.`) and `:133 async logTurn(` (`Logs a conversation turn into the session's tree.`) — neither contains persist/disk/store. BEFORE (`rmap find \"where are conversations persisted to disk\"`, recorded 2026-09-21): top ten 1 `Fraktag.listConversationSessions` 0.436 (index.ts:1746), 2 `ConversationManager` 0.387 (:61), 3 `Fraktag.getConversationTurns` 0.371, 4 `ConversationSession` 0.357 (:27), 5 `ConversationSession` 0.354 (ChatDialog.tsx:37), 6 `ConversationManager.listSessions` 0.333, 7 `Fraktag.deleteConversationSession` 0.329, 8 `ConversationTurn` 0.327, 9 `KnowledgeBase` 0.314, 10 `Fraktag.createConversationSession` 0.314; `createSession` 0.274 (rank 14), `logTurn` 0.155 — absent. AFTER (predicted by the spike's METHOD-only first-sentence variant, docs/audits/2026-09-21-seed-document-spike-1.md addendum 2026-09-23: createSession 0.333 at rank 9, `ConversationManager` rank 3 under the product's field tier, the constructor unchanged at 0.199; REPORTED, not asserted beyond the L10 criterion): `ConversationManager.createSession` (ConversationManager.ts:71) in the top ten with score ≥ 0.30, `ConversationManager` (:61) within the top three, 0 `is_field` rows; the controls keep their first row above the floor (`how are content atoms hashed` → `ContentStore.findByHash` ContentStore.ts:90, before 0.665; `where are tree nodes saved to disk` → `TreeStore.saveNode` TreeStore.ts:316, before 0.622); 1291 seed vectors in the after store (same checkout, same corpus). The fresh after root embeds all 1291 chunks, so it cannot observe the stated re-embed cost; that cost is the 152 FRAKTAG METHOD chunks whose same-file parent carries a doc (133 under a CLASS, 19 under an INTERFACE; counted read-only from a copy of the before store on 2026-09-23), whose `document_hash` changes and which an EXISTING store therefore re-embeds once (SD-C02 proves the mechanism); the seed pass wall time from the daemon log is REPORTED. `logTurn` is expected to stay under the floor (spike: 0.192) — reported, not a failure"
      },
      "expected": "exit 0: on the requirement's own query the method that does the work is now offered above the floor while the owning class stays in the top three and no field row leaks in; the two control queries keep their answers"
    },
    {
      "checkId": "SD-C05",
      "obligationIds": [
        "RG-REQ-010-L10",
        "RG-REQ-010-L03",
        "P-SD-01",
        "P-SD-02"
      ],
      "owner": "builder",
      "method": {
        "kind": "command",
        "command": "rm -f /tmp/sd-ldb-* /tmp/sd-leveldb-* && trap 'P=$(cat /private/tmp/SEED-DOCUMENT-1-leveldb-after/rmapd.pid 2>/dev/null); if [ -n \"$P\" ]; then kill \"$P\" 2>/dev/null; sleep 1; kill -9 \"$P\" 2>/dev/null; fi; rm -rf /private/tmp/SEED-DOCUMENT-1-leveldb-after' EXIT && rm -rf /private/tmp/SEED-DOCUMENT-1-leveldb-after && mkdir -p /private/tmp/SEED-DOCUMENT-1-leveldb-after && cp -R \"$HOME/Library/Application Support/repo-graph/seed-model-cache\" /private/tmp/SEED-DOCUMENT-1-leveldb-after/seed-model-cache && ( env -u RMAP_TRANSPORT RMAP_STATE_ROOT=/private/tmp/SEED-DOCUMENT-1-leveldb-after RMAP_SOCKET_PATH=/private/tmp/SEED-DOCUMENT-1-leveldb-after/d.sock RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmapd\" > /tmp/sd-leveldb-rmapd.log 2>&1 & echo $! > /private/tmp/SEED-DOCUMENT-1-leveldb-after/rmapd.pid ) && for i in $(seq 1 30); do test -S /private/tmp/SEED-DOCUMENT-1-leveldb-after/d.sock && break; sleep 1; done && test -S /private/tmp/SEED-DOCUMENT-1-leveldb-after/d.sock && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/leveldb' && export RMAP_STATE_ROOT=/private/tmp/SEED-DOCUMENT-1-leveldb-after RMAP_SOCKET_PATH=/private/tmp/SEED-DOCUMENT-1-leveldb-after/d.sock RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && unset RMAP_TRANSPORT && \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" index > /tmp/sd-leveldb-index.txt 2>&1 && ready=0 && for i in $(seq 1 120); do \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" find zzz --json 2>/dev/null | python3 -c 'import json,sys; d=json.load(sys.stdin); v=d.get(\"value\", d); sys.exit(0 if v.get(\"seeds_available\") is True else 1)' && ready=1 && break; sleep 5; done; kill \"$(cat /private/tmp/SEED-DOCUMENT-1-leveldb-after/rmapd.pid)\"; sleep 1; kill -9 \"$(cat /private/tmp/SEED-DOCUMENT-1-leveldb-after/rmapd.pid)\" 2>/dev/null; rm -f /private/tmp/SEED-DOCUMENT-1-leveldb-after/rmapd.pid; test \"$ready\" = 1 ) || { echo \"SEED-NOT-READY /private/tmp/SEED-DOCUMENT-1-leveldb-after (socket, index or seed pass; see /tmp/sd-leveldb-rmapd.log)\"; exit 1; } && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/leveldb' && export RMAP_STATE_ROOT=/private/tmp/SEED-DOCUMENT-1-leveldb-before RMAP_SOCKET_PATH=/private/tmp/SEED-DOCUMENT-1-leveldb-before/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && \"/private/tmp/SEED-DOCUMENT-1-before-bin/rmap\" find 'crash recovery' --json > /tmp/sd-ldb-before.json 2> /tmp/sd-ldb-before.json.err && \"/private/tmp/SEED-DOCUMENT-1-before-bin/rmap\" find 'crash recovery' > /tmp/sd-ldb-before.txt 2> /tmp/sd-ldb-before.txt.err ) || { echo \"RUN-FAILED /tmp/sd-ldb-before\"; exit 1; } && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/leveldb' && export RMAP_STATE_ROOT=/private/tmp/SEED-DOCUMENT-1-leveldb-after RMAP_SOCKET_PATH=/private/tmp/SEED-DOCUMENT-1-leveldb-after/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" find 'crash recovery' --json > /tmp/sd-ldb-after.json 2> /tmp/sd-ldb-after.json.err && \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" find 'crash recovery' > /tmp/sd-ldb-after.txt 2> /tmp/sd-ldb-after.txt.err ) || { echo \"RUN-FAILED /tmp/sd-ldb-after\"; exit 1; } && grep -v '^Snapshot' /tmp/sd-ldb-before.txt | sed -E 's/repo_[0-9a-z]+/repo_X/g' > /tmp/sd-ldb-b.txt && grep -v '^Snapshot' /tmp/sd-ldb-after.txt | sed -E 's/repo_[0-9a-z]+/repo_X/g' > /tmp/sd-ldb-a.txt && diff /tmp/sd-ldb-b.txt /tmp/sd-ldb-a.txt && python3 - <<'PY' > /tmp/sd-ldb-after-store.txt || { echo \"ASSERT-FAILED human output differs (diff above) or after-store read\"; exit 1; }\nimport sqlite3, glob\nk = sqlite3.connect('file:' + glob.glob('/private/tmp/SEED-DOCUMENT-1-leveldb-after/databases/*.db')[0] + '?mode=ro', uri=True)\nsnap = k.execute('select snapshot_uid from snapshots order by created_at desc limit 1').fetchone()[0]\nprint(k.execute(\"select count(*) from nodes where snapshot_uid = ? and kind = 'SYMBOL' and doc_comment is not null and doc_comment <> ''\", (snap,)).fetchone()[0], k.execute('select count(*) from seed_vectors').fetchone()[0])\nPY\nrm -rf /private/tmp/SEED-DOCUMENT-1-leveldb-after && python3 - /tmp/sd-ldb-before.json /tmp/sd-ldb-after.json <<'PY'\nimport json, sys\ndef top(p):\n    d = json.load(open(p)); v = d.get('value', d); assert v['seeds_available'] is True, p\n    return v['candidates']\nb, a = top(sys.argv[1]), top(sys.argv[2])\nassert [(c.get('qualified_name'), c['path'], c.get('line')) for c in b] == [(c.get('qualified_name'), c['path'], c.get('line')) for c in a], ([c.get('qualified_name') for c in b], [c.get('qualified_name') for c in a])\nfor x, y in zip(b, a):\n    assert abs(x['score'] - y['score']) < 1e-6, (x.get('qualified_name'), x['score'], y['score'])\nassert a[0].get('qualified_name') == 'leveldb::Iter::CorruptionError' and a[0]['path'] == 'table/block.cc' and a[0]['line'] == 242, a[0]\nassert a[1].get('qualified_name') == 'leveldb::RepairDB' and a[1]['path'] == 'db/repair.cc' and a[1]['line'] == 446, a[1]\ndocumented, vectors = open('/tmp/sd-ldb-after-store.txt').read().split()\nassert documented == '0' and vectors == '2112', (documented, vectors)\nprint('leveldb control ok: ten rows and scores identical;', [(round(c['score'], 3), c.get('qualified_name')) for c in a[:3]])\nPY",
        "cwd": ".",
        "environment": "pre-provisioned SEEDED root /private/tmp/SEED-DOCUMENT-1-leveldb-before (leveldb at its HEAD 7ee830d — the snapshot's `basis_commit`, unchanged since; 2112 seed vectors; repo-graph binary built at 12eecd5, 2026-09-21); NEW /private/tmp/SEED-DOCUMENT-1-leveldb-after indexed and seeded by the candidate (the SD-C04 socket-daemon recipe: `env`-exec'd rmapd, socket polled, PID killed); queries under RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off; the after root is REMOVED by the check after its captures AND on every failure path by the EXIT trap (SD-F01); stale `/tmp/sd-ldb-*`/`/tmp/sd-leveldb-*` captures are deleted first",
        "inputs": "leveldb (C++; 2112 chunks; 0 documented symbols in the store — C++ class docs are not captured as `doc_comment`, so no chunk gains an enclosing sentence and every document is byte-identical to today's): `rmap find \"crash recovery\"` — the ten rows and their scores identical (1 `leveldb::Iter::CorruptionError` table/block.cc:242 0.398, 2 `leveldb::RepairDB` db/repair.cc:446 0.333, 3 `leveldb_repair_db` db/c.cc:278, 4 `leveldb::Repairer::~Repairer`, 5 `leveldb::log::Reader::ReportCorruption` db/log_reader.cc:178, 6 its `(decl)` log_reader.h:85 …), the human output byte-identical apart from the per-index repo_uid in full cursors (normalized by the `repo_…` substitution; the `^Snapshot` filter is defensive — today's human `find` renderer prints no snapshot line); a human-output difference fails the check (the `diff` is inside the chain that guards the store read). The SEED-CHUNK-2 control the requirement names"
      },
      "expected": "exit 0: a corpus with no documented enclosing types is byte-stable (no behavior change where the composition has nothing to add), and the SEED-CHUNK-2 proof holds"
    },
    {
      "checkId": "SD-C06",
      "obligationIds": [
        "RG-REQ-011-L06",
        "P-SD-03"
      ],
      "owner": "builder",
      "method": {
        "kind": "command",
        "command": "test \"$(grep -E '^registry-before [0-9a-f]{64}$' .agent-manager/slices/SEED-DOCUMENT-1/build-progress.md | tail -1 | cut -d' ' -f2)\" = \"$(shasum -a 256 \"$HOME/Library/Application Support/repo-graph/registry.json\" | cut -d' ' -f1)\" && PIDS=$(pgrep -x rmapd | paste -sd, -); { [ -z \"$PIDS\" ] || ! ps -o command= -p \"$PIDS\" | grep -v '/\\.local/bin/rmapd' | grep -q .; } && ! test -d /private/tmp/SEED-DOCUMENT-1-before-bin && ls -d /private/tmp/SEED-DOCUMENT-1-fraktag-before /private/tmp/SEED-DOCUMENT-1-leveldb-before >/dev/null && ! test -d /private/tmp/SEED-DOCUMENT-1-fraktag-after && ! test -d /private/tmp/SEED-DOCUMENT-1-leveldb-after && ! git status --short | grep -vE '^[ MA?][ MA?] (rust/crates/repo\\-graph\\-seed/src/document\\.rs|rust/crates/repo\\-graph\\-seed/src/pass\\.rs|rust/crates/repo\\-graph\\-seed/src/pass_tests\\.rs|rust/crates/repo\\-graph\\-seed/src/lib\\.rs)$' | grep -q . && git diff --check",
        "cwd": ".",
        "environment": "candidate tree, after every other check",
        "inputs": "the full sha256 of the operator registry recorded as `registry-before <digest>` in build-progress.md at step 0 equals the current one; no isolated daemon survives (only the operator's `~/.local/bin/rmapd`); the before-binary directory removed; the two manager-owned `-before` roots kept; the two candidate-created `-after` roots REMOVED (CLAUDE.md relay rule: a builder deletes the isolated roots it created — SD-F01; their store facts were read into /tmp/sd-*-after-*.txt before removal); `git status --short` names only the four candidate paths; no trailing whitespace"
      },
      "expected": "exit 0: every proof was isolated (the operator's registry byte-identical — no behavior change outside the isolated roots), no daemon or root leaked, the tree holds only the allocated paths"
    }
  ]
}
-->

# SEED-DOCUMENT-1 — a method's embedded document carries its class's first sentence

Status: SPECIFIED and GROUNDED 2026-09-22; RE-VERIFIED 2026-09-23 on HEAD c39abf8f by the second document item SEED-DOCUMENT-1-PREP-2 (every seam read from the sources; the composition MEASURED before packeting with the product's model — docs/audits/2026-09-21-seed-document-spike-1.md — and the wording ratified by the human on that measurement, D-SD1-001, RG-BOOTSTRAP-INPUT-6) · Track: SEED-CHUNK-3's filed design cause (ROADMAP 2026-09-08); last item of the ratified order. CODE slice: repo-graph-seed only (document composition + the pass's enclosing-type lookup). Cost: every repository re-embeds every METHOD chunk whose enclosing type carries a doc on its next seed pass (the document hash is the reuse key) — FRAKTAG: 152 of 1,291 chunks through the same-file branch (counted 2026-09-23; the cross-file branch's additions are not counted by any document item — INFERRED ≈ 0 on TypeScript, where a method's class lives in its own file); leveldb: 0. Builder: claude / claude-opus-5-5 (human trial directive 2026-09-22); reviewer: codex / gpt-5.6-terra.

## 0. Requirements allocation (the machine-readable block above is binding; this section explains it)

**Implements:** RG-REQ-010-L10 (revised 2026-09-21, D-SD1-001) — the embedded document of a method chunk carries, besides its qualified name, doc comment and leading body lines, the FIRST SENTENCE of its enclosing type's doc comment truncated at `repo-graph-seed::document::MAX_ENCLOSING_SENTENCE_CHARS` (160); no identifier summary; measured on `rmap find "where are conversations persisted to disk"` on FRAKTAG with the floor and model unchanged (SD-C01/C02/C04).

**Changes:** none allocated. REPORTED consequences: FRAKTAG's persistence query gains `ConversationManager.createSession` above the floor (spike: 0.274 → 0.333, rank 9); `logTurn` stays under the floor (0.192); the first seed pass after the change re-embeds every METHOD chunk with a documented enclosing type (FRAKTAG 152 through the same-file branch, the cross-file branch's additions not counted — INFERRED ≈ 0 on TypeScript; leveldb 0; a fresh index embeds everything anyway).

**Preserves:** RG-REQ-010-L01 (candidate generator, never answer — the seed block's framing and the facts section untouched; SD-C02 byte-identity of rgr find/), L03 (deterministic tie-break; rank.rs untouched), L05/L06/L07/L08 (partition, tiers, floor, model — rank.rs, classify.rs, seed_render.rs, local_engine.rs untouched; SD-C02/C05), RG-REQ-011-L06 (isolation — SD-C06).

**Decision D-SD1-002 (operator; REVISED 2026-09-23 by the document review's SD-DEC-01, whose recommendation is adopted; kind gate added 2026-09-23 by the second document review's SD-F02; cross-file fallback added 2026-09-23 by the same review's SD-F03; the human may override at closeout) — scope of the lookup.** LOOKUP RULE (METHOD chunks only; genuine forward declarations — `SeedForwardDecl::ForwardDecl` — are ignored throughout; both steps read the COMPLETE corpus `build_store` receives, before the corpus cap truncates it — only the emitted vectors are cap-limited): the parent qualified name is the chunk's minus its last `.`/`::` segment; (1) if the chunk's own file holds any entry with that qualified name, the enclosing type is decided in that file — the first entry in corpus order with a canonical type subtype (CLASS, INTERFACE, STRUCT, ENUM) and a non-empty doc supplies the sentence, otherwise there is no sentence and the cross-file branch is not consulted; (2) only if the chunk's file holds no such entry, the corpus-wide entries with that qualified name and a canonical type subtype are counted — exactly one, if documented, supplies its sentence; none, or two or more (ambiguous), supply no sentence. The canonical type subtypes are mapped from the extractors in SD-C02; ONLY chunks whose stored `subtype` is `METHOD` receive the sentence — RG-REQ-010-L10 says 'a method chunk', read literally. Re-measured (spike addendum 2026-09-23): METHOD-only gives the same target outcome (createSession 0.333, rank 9; the owning class rank 3 once the product's field tier is applied; controls first) and drops the every-child variant's one visible side effect (a 5-line constructor echo row at rank 8). Rejected: every child kind (the first draft — changes non-method Layer-3 rows without a requirement); METHOD + CONSTRUCTOR + FUNCTION (the constructor echo row; a free FUNCTION has no enclosing type); a same-file-ONLY lookup (the second draft — L10 states no such limitation and the spike's measured lookup was same-file-or-unique-in-corpus; revised per review SD-F03 to the LOOKUP RULE above — the C++ header/impl split is the demonstrated case (leveldb `db/db_impl.h:29` `class DBImpl`, `db/db_impl.cc:292` `DBImpl::Recover`), though leveldb itself stores 0 doc comments, so the fallback contributes nothing there); a same-file-documented-only step (1) (the SD-F03 draft — an UNDOCUMENTED same-file class, or a same-file non-type name-parent, fell through to a same-named documented type in another file, attributing a type that is not the method's enclosing one; the spike's `by_file_qn` step blocks on any same-file entry); a fallback counting forward declarations (leveldb's `db/db_iter.h:15` `class DBImpl;` is stored as a CLASS with the same qualified name, so every forward-declared class would read ambiguous).

*Challenge (SEED-DOCUMENT-1-PREP-2, 2026-09-23).* Against L10's wording ("the embedded document of a method chunk shall carry … the FIRST SENTENCE of its enclosing type's doc comment") METHOD-only is the literal reading, and every shipped extractor that emits callables inside a type stamps them `METHOD` (TypeScript, Rust, Java, Python, C++), so no language is silently excluded by the subtype gate. Against the addendum's table the target row is identical to the every-child variant (createSession 0.333, rank 9; logTurn 0.192), the owning class reaches rank 3 only through the product's field tier (`Fraktag.conversationManager`, index.ts:79, is stored `is_field = 1` in the before store — so the tier the prediction relies on is a stored fact, not a spike approximation), and the constructor stays at today's 0.199. The decision holds. Residual (a) of this challenge — the name-containment lookup was not kind-gated, so a documented non-type name-parent could have supplied text — was raised by the second document review as SD-F02 and is CLOSED by the kind gate above (rule: only a CLASS/INTERFACE/STRUCT/ENUM entry encloses; proven by `documented_non_type_name_parent_gives_no_enclosing_sentence` and `each_canonical_type_subtype_encloses_a_method`); on FRAKTAG all 152 METHOD chunks with a documented same-file name-parent sit under a CLASS (133) or an INTERFACE (19), so no measured row moves. Stated residual (b): a duplicated parent qualified name in one file resolves to the first documented entry in corpus order (FRAKTAG: 0 duplicates) — deterministic, per RG-REQ-010-L03.

**Preservation obligations:**

| Id | Obligation | Proof |
|---|---|---|
| P-SD-01 | a corpus with no documented enclosing types is byte-stable (documents, vectors, ranks) | SD-C02 (`no_enclosing_doc_embeds_exactly_as_before`, copy-forward tests); SD-C05 |
| P-SD-02 | ranking, tiers, partition, floor, model, the find surfaces and the storage/daemon seed code are untouched; non-method chunks, and methods whose name-parent is not a type, embed as today | SD-C02 (diff --quiet; `non_method_children_embed_without_the_enclosing_sentence`, `documented_non_type_name_parent_gives_no_enclosing_sentence`, `same_file_name_parent_blocks_the_cross_file_fallback`); SD-C04 (0 field rows; controls) |
| P-SD-03 | isolation, cleanup, hygiene | SD-C03; SD-C06 |

## 1. Problem (ROOT-CAUSED by SEED-CHUNK-3 §2.6, 2026-09-08; measured 2026-09-21)

`rust/crates/repo-graph-seed/src/document.rs:39-71 build_chunk_document(qualified_name, doc_comment, span_source)` embeds a method as its qualified name + own doc + first 60 body lines. `ConversationManager.createSession` (FRAKTAG packages/engine/src/core/ConversationManager.ts:71; doc "Creates a new conversation session as a separate Tree.") and `logTurn` (:133) contain no "persist"/"disk"/"store", so on `find "where are conversations persisted to disk"` they score 0.274 / 0.155 — under the 0.30 floor — while their class (:61, doc "ConversationManager handles conversation persistence. …") ranks 2nd. The spike (`docs/audits/2026-09-21-seed-document-spike-1.md`; potion-code-16M-v2 from the local cache; scores equal the product's to three decimals) measured: the class doc alone lifts createSession to 0.333 (first sentence) … 0.372 (whole doc); the identifier summary the original L10 named never raises a target and lowers two; the whole class doc dilutes precise matches on grpc-java (`AbstractStub.withDeadline` rank 2 → 9) and turns short members into class-doc echoes (a logger field at rank 1). The human chose the first sentence (D-SD1-001).

## 2. Contract

### 2.1 The fix, at the cause — one bounded sentence, one kind-gated lookup (decided in the method's own file when it holds the name-parent, else a unique type in the corpus), one new document parameter

1. **document.rs** (SD-C01): `MAX_ENCLOSING_SENTENCE_CHARS = 160`; `enclosing_doc_sentence(doc) -> Option<String>` (marker stripping, first paragraph, first sentence, char-bounded); `parent_qualified_name(qn) -> Option<&str>`; `build_chunk_document(qualified_name, enclosing_sentence, doc_comment, span_source)` — order: qualified name, enclosing sentence, own doc, body; `None` → byte-identical to today. The fn doc's "this builder does NOT try to fix that geometry" paragraph is rewritten (it is false after this slice).
2. **pass.rs** (SD-C02): LOOKUP RULE (METHOD chunks only; genuine forward declarations — `SeedForwardDecl::ForwardDecl` — are ignored throughout; both steps read the COMPLETE corpus `build_store` receives, before the corpus cap truncates it — only the emitted vectors are cap-limited): the parent qualified name is the chunk's minus its last `.`/`::` segment; (1) if the chunk's own file holds any entry with that qualified name, the enclosing type is decided in that file — the first entry in corpus order with a canonical type subtype (CLASS, INTERFACE, STRUCT, ENUM) and a non-empty doc supplies the sentence, otherwise there is no sentence and the cross-file branch is not consulted; (2) only if the chunk's file holds no such entry, the corpus-wide entries with that qualified name and a canonical type subtype are counted — exactly one, if documented, supplies its sentence; none, or two or more (ambiguous), supply no sentence. Mechanism: both indexes built ONCE from the complete `entries` before the corpus-cap truncation — a per-file index ((path, qualified_name) of every non-forward-declared entry → the first documented canonical-type entry's doc, if any) and a corpus-wide map (qualified_name → first doc, count) over the non-forward-declared canonical-type entries; the capped run loop only consults them; other subtypes: `None`; `has_doc`/`is_field` unchanged; the document hash (reuse key) now covers the sentence.
3. **lib.rs**: the crate doc's `document` bullet (:9-11) names the enclosing sentence.
4. **pass_tests.rs**: `doc_hash` helper + eleven tests.
5. **Nothing else moves:** no DTO, no SQL, no migration, no daemon/rgr change, no floor/model/tier change.

### 2.2 Evidence taxonomy (one row = one bound test)

| Input | Outcome | Bound test / check |
|---|---|---|
| qn + enclosing sentence + own doc + body | that order, each on its own line | SD-C01 `enclosing_sentence_follows_the_qualified_name_and_precedes_the_own_doc` |
| TypeScript `/**\n * S1.\n * S2 …\n */`, plain `S1. S2`, a `@tag` line | `S1.` | SD-C01 `enclosing_sentence_is_the_first_sentence_stripped_of_comment_markers` |
| a 400-char first sentence with multi-byte scalars | 160 scalars, valid UTF-8 | SD-C01 `enclosing_sentence_truncates_at_the_named_bound_on_a_char_boundary` |
| `/** */`, empty, whitespace | `None` | SD-C01 `enclosing_sentence_is_none_for_an_empty_or_marker_only_doc` |
| `None` enclosing | today's bytes | SD-C01 `no_enclosing_doc_embeds_exactly_as_before` |
| `a.b.c`, `x::y::z`, `f` | `a.b`, `x::y`, None | SD-C01 `parent_qualified_name_splits_on_the_last_separator` |
| documented class + method, same file | sentence in the method's document; new document_hash | SD-C02 `enclosing_type_sentence_enters_the_method_document_and_its_reuse_key` |
| class documented in the same file AND in another file | the same-file sentence | SD-C02 `same_file_type_parent_wins_over_a_cross_file_one` |
| class documented only in a header, method in the `.cc` | the header's sentence; new document_hash | SD-C02 `unique_cross_file_type_parent_contributes_its_sentence` |
| class documented in two other files, method in a third | no sentence; document and hash byte-identical | SD-C02 `ambiguous_cross_file_type_parents_contribute_no_sentence` |
| an UNDOCUMENTED same-file class, or a documented same-file VARIABLE, as name-parent; a documented same-named class in another file | no sentence; document and hash byte-identical (the other file is not consulted) | SD-C02 `same_file_name_parent_blocks_the_cross_file_fallback` |
| a class defined in `g.h` and forward-declared in `g_fwd.h`, method in `g.cc`; a class only forward-declared, method in `k.cc` | `g.h`'s sentence; for `K::m` no sentence | SD-C02 `forward_declaration_is_never_an_enclosing_type_candidate` |
| method inside the corpus cap, its only documented class beyond it | the class's sentence; the class chunk still not emitted | SD-C02 `unique_type_parent_beyond_the_corpus_cap_still_contributes_its_sentence` |
| method and one documented class inside the cap, a second same-named class beyond it | no sentence (ambiguous); byte-identical | SD-C02 `second_type_parent_beyond_the_corpus_cap_makes_the_fallback_abstain` |
| a PROPERTY and a CONSTRUCTOR of a documented class | documents and hashes byte-identical to today; `is_field` unchanged | SD-C02 `non_method_children_embed_without_the_enclosing_sentence` |
| a METHOD whose same-file name-parent is a documented FUNCTION or VARIABLE | no sentence; document and hash byte-identical to today | SD-C02 `documented_non_type_name_parent_gives_no_enclosing_sentence` |
| a METHOD under a documented CLASS, INTERFACE, STRUCT, ENUM | the parent's first sentence in each | SD-C02 `each_canonical_type_subtype_encloses_a_method` |
| corpora: FRAKTAG (target + two controls), leveldb (byte-stable control) | §2.3 | SD-C04/C05 |

### 2.3 Outward proof (what a user of the product gains)

FRAKTAG `rmap find "where are conversations persisted to disk"`: `ConversationManager.createSession` (ConversationManager.ts:71) enters the top ten above the 0.30 floor (spike: 0.333, rank 9); `ConversationManager` stays in the top three; no `[field]` row. Controls: `how are content atoms hashed` → `ContentStore.findByHash` first; `where are tree nodes saved to disk` → `TreeStore.saveNode` first. leveldb `crash recovery`: byte-identical.

## 3. Regression watch

| Preserved | What would regress | Proof |
|---|---|---|
| RG-REQ-010-L01/L03 | the seed block's framing or tie-break | SD-C02 (rgr find/ and rank.rs byte-identical); SD-C05 (leveldb's ten rows and scores identical — the tie-break's output unchanged) |
| RG-REQ-010-L05/L06/L07/L08, P-SD-02 | partition, tiers, floor, model | SD-C02 (diff --quiet; 13 rank + 31 classify tests); SD-C04 (0 field rows) |
| P-SD-01 | a corpus without documented types moving | SD-C05 (leveldb ten rows and scores identical) |
| copy-forward invariants | reuse of a vector whose document changed | SD-C02 (`copy_forward_*`, `model_change_forces_full_reembed`) |
| RG-REQ-011-L06, P-SD-03 | isolation | SD-C06 |

## 4. Stop conditions

Frozen: `SEED_SIMILARITY_FLOOR = 0.30`, the model, the tiers, the partition, the tie-break, the wire shape, the storage schema and the seed DTOs (no migration — the hash change re-embeds silently, stated). The bound is `MAX_ENCLOSING_SENTENCE_CHARS = 160` — never tuned to the query. No identifier summary (measured harmful). The LOOKUP RULE (METHOD chunks only; genuine forward declarations — `SeedForwardDecl::ForwardDecl` — are ignored throughout; both steps read the COMPLETE corpus `build_store` receives, before the corpus cap truncates it — only the emitted vectors are cap-limited): the parent qualified name is the chunk's minus its last `.`/`::` segment; (1) if the chunk's own file holds any entry with that qualified name, the enclosing type is decided in that file — the first entry in corpus order with a canonical type subtype (CLASS, INTERFACE, STRUCT, ENUM) and a non-empty doc supplies the sentence, otherwise there is no sentence and the cross-file branch is not consulted; (2) only if the chunk's file holds no such entry, the corpus-wide entries with that qualified name and a canonical type subtype are counted — exactly one, if documented, supplies its sentence; none, or two or more (ambiguous), supply no sentence. The cross-file fallback is UNIQUE-only — never a heuristic choice among ambiguous parents. Nothing outside the four candidate paths. STANDING HONESTY RULES. If createSession does not reach the floor on the after root, STOP and report the measured table (the prediction is the spike's; the product is the truth) — never adjust the floor or the bound. Unmet DoD → STOP. Do NOT commit. Run every check to completion in the FOREGROUND; end your turn only with the evidence object.

## 5. Validation (ORDERED; `build-progress.md` after EACH step)

0. On the CLEAN tree: record `git rev-parse HEAD`; `(cd rust && cargo build --release --bin rmap --bin rmapd)` → `mkdir -p /private/tmp/SEED-DOCUMENT-1-before-bin && cp rust/target/release/rmap rust/target/release/rmapd /private/tmp/SEED-DOCUMENT-1-before-bin/`; `registry-before <full sha256>` in build-progress.md; verify the two seeded `-before` roots exist (§7) and the operator's `seed-model-cache` exists.
1. Failing tests FIRST (§2.2 names): document.rs → pass.rs.
2. SD-C01 → SD-C02 (whole crate, foreground) → SD-C03 — NEVER `cargo test --workspace`.
3. `(cd rust && cargo build --release --bin rmap --bin rmapd)` (candidate) → SD-C04 (FRAKTAG: index + seed ≈ 1 min) → SD-C05 (leveldb ≈ 1 min); every `/tmp/sd-*` capture from THIS cycle's run.
4. Remove `/private/tmp/SEED-DOCUMENT-1-before-bin`, THEN SD-C06 (it asserts that directory's absence; the two `-before` roots stay, the two `-after` roots are already gone — SD-C04/C05 remove them on success and, via their EXIT trap, on failure), hand-off with the evidence object (each check's outcome NESTED under `outcome`; `supportingEvidence` non-empty).

## 6. Definition of done

All six checks pass; §2.3 holds; the report carries the code-under-analysis examples: FRAKTAG `ConversationManager.ts:48-60` class doc and its extracted first sentence, `:71 createSession` with its BEFORE score/rank (0.274 / 14) and AFTER, `:133 logTurn` BEFORE/AFTER (expected still under the floor — stated), the BEFORE/AFTER top-ten tables for the persistence query, the two control first rows, leveldb's identical top three, the after-store seed-vector counts and the seed pass wall times from the daemon logs.

## 7. Corpus roots (pre-provisioned by the manager on 2026-09-21 with the base-revision binary at 12eecd5; rebuild recipe)

- `/private/tmp/SEED-DOCUMENT-1-fraktag-before` (FRAKTAG at 9bea3a5), `/private/tmp/SEED-DOCUMENT-1-leveldb-before` (leveldb at 7ee830d): fresh `rmap index` + background seed pass through a socket daemon (`rmapd` with `RMAP_STATE_ROOT`/`RMAP_SOCKET_PATH` in the root, the model cache copied to `<root>/seed-model-cache`, readiness polled with `find zzz --json` → `seeds_available`, the daemon killed by its captured PID). The builder creates the `-after` roots the same way with the candidate binaries — the daemon started as `env -u RMAP_TRANSPORT RMAP_STATE_ROOT=… … rmapd &` so `$!` is rmapd's own PID (a backgrounded `export … && rmapd &` list records a forked shell's PID, and killing it orphans the daemon — demonstrated 2026-09-23 with a `sleep` stand-in), the socket polled with `test -S` before `rmap index` — and REMOVES them after capturing the evidence and, through an EXIT trap that kills the recorded PID first, on every failure path; the retained roots under `~/repo-graph-retained/` are never served; the operator registry is read only for its digest.

## 8. Follow-ups (not this slice)

SEED-CPP-CLASS-DOC-1: C++ class doc comments are not captured as `doc_comment` on the class node (leveldb: 0 documented symbols), so the composition has nothing to add on C++ corpora — an extractor fact. SEED-REEMBED-VISIBILITY-1: the first seed pass after a composition change re-embeds silently (`reused` = 0) — no surface tells the operator why a pass took longer. SEED-LOGTURN-1: `logTurn` stays under the floor on the target query (0.192) — its own text and its class sentence do not carry the query's words; no composition variant measured reaches it without diluting others. Stale doc comment outside the allocation, left byte-identical: `storage/src/migrations/migration_037.rs:5` still describes the document as `qualified_name + doc_comment + capped span` (SEED-DOC-COMMENTS-1, a comment-only follow-up). `git grep -n build_chunk_document -- rust` on c39abf8f names: document.rs:39 (definition) and its six test calls (:94, :108, :114, :120, :131, :140); pass.rs:12 (`use`) and :252 (the one production call); pass_tests.rs:5 (`use`), :11 (the helper's doc) and :14 (the helper's call); ports.rs:151 (a doc link that stays true — it names the assembled document, not its parts); migration_037.rs:5 (the stale comment) — no other crate calls it, so the four candidate paths are complete.

## 9. Baseline history

- INPUT-1 (2026-09-22; revised 2026-09-23 before approval): first admission baseline under RG-BOOTSTRAP-INPUT-7 (L10 wording from INPUT-6). Its first document review (codex gpt-5.6-terra) raised SD-DEC-01 — the draft gave the enclosing sentence to every child kind while L10 says 'a method chunk' — and SD-F01 (candidate-created `-after` roots must be removed by the builder). Both applied here before any approval: METHOD-only (re-measured, spike addendum 2026-09-23), after-root cleanup in SD-C04/C05/C06. The second document item (SEED-DOCUMENT-1-PREP-2, 2026-09-23) re-verified every seam and corrected, in this file only: SD-C04/C05 recorded the PID of a forked shell instead of rmapd's (killing it would orphan the daemon) — now `env`-exec'd, socket-polled; SD-F01's failure-path cleanup (an EXIT trap); two assertion lines whose failure the next script line masked (SD-C04's human-row greps, SD-C05's human `diff`) — now `|| exit 1`; stale `/tmp/sd-*` captures deleted first; `qualified_name` read with `.get` (the DTO skip-serializes it); the classify test count (31, not 39); the six (not three) existing document tests that gain the `None` argument; the re-embed cost stated as a count (152) the fresh after root cannot observe; the §8 grep inventory; §0's challenge record. Its review (review-0 of PREP-2) raised SD-F02 — the parent lookup was not kind-gated although L10 names the enclosing TYPE — applied in iteration 1: `ENCLOSING_TYPE_SUBTYPES` (CLASS/INTERFACE/STRUCT/ENUM, mapped from the extractors in SD-C02) gates the lookup; two new SD-C02 tests (the negative fixture and the per-subtype positive); the SEED-ENCLOSING-KIND-1 follow-up withdrawn (closed here). The third document item (PREP-3): SD-F03 applied by the manager — a same-file-only lookup contradicted L10 (no such limitation) and the spike's measured lookup; now same-file canonical-type parent first, else a unique corpus-wide one, an ambiguous name contributing nothing; three SD-C02 fixtures replace `enclosing_type_is_looked_up_in_the_same_file_only`. The PREP-3 author then corrected two defects of that application, in this file only, closing one class — a cross-file sentence attributed to a type that is not the method's enclosing type, or the fallback defeated by a non-defining declaration — with one rule stated verbatim in §0, §2.1, SD-C02 and §4 (the LOOKUP RULE): (i) step (1) was keyed on DOCUMENTED same-file types only, so an undocumented same-file class, or a same-file non-type name-parent, fell through to a same-named documented type in another file — now any same-file entry with the name-parent decides in that file (the spike's `by_file_qn` step; the reviewer's 'same-file canonical-type parent first'); (ii) the uniqueness count ran over documented entries and did not exclude forward declarations — now it counts every canonical-type entry (the reviewer's 'unique corpus-wide canonical-type parent') and ignores genuine forward declarations, which the C++ extractor stores with the class's qualified name (leveldb `db/db_iter.h:15` `class DBImpl;`) and which would otherwise make the header/impl case ambiguous; two SD-C02 fixtures bind them (`same_file_name_parent_blocks_the_cross_file_fallback`, `forward_declaration_is_never_an_enclosing_type_candidate`); the new-pass-test count is corrected to nine (it read five after SD-F03 had made it seven). Its review (review-0 of PREP-3) raised F-SD-001: the corpus-wide index was specified over the CAPPED `entries`, so a same-named parent beyond `corpus_cap` was invisible — applied in iteration 1 with one rule closing the class (a lookup index that sees less than the corpus it claims): every lookup index is built from the complete corpus before the cap truncates it, and only emitted vectors are cap-limited — stated in the LOOKUP RULE's head (all four copies) and SD-C02's mechanism (which also moves the same-file index off the per-run map, since the cap can cut a file's run); two cap-boundary fixtures added and the existing `corpus_cap_omits_the_remainder` bound; eleven new pass tests.
