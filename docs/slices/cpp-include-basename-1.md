<!-- requirements-assurance-implementation-v1
{
  "formatVersion": 1,
  "kind": "implementation-allocation",
  "workItemId": "CPP-INCLUDE-BASENAME-1",
  "baselinePath": "docs/requirements/baselines/CPP-INCLUDE-BASENAME-1-INPUT-1.json",
  "parentRequirementIds": [
    "RG-REQ-006",
    "RG-REQ-002",
    "RG-REQ-004",
    "RG-REQ-011"
  ],
  "implements": [
    "RG-REQ-006-L11",
    "RG-REQ-002-L11"
  ],
  "preserves": [
    "RG-REQ-006-L01",
    "RG-REQ-006-L02",
    "RG-REQ-006-L03",
    "RG-REQ-006-L04",
    "RG-REQ-006-L05",
    "RG-REQ-006-L06",
    "RG-REQ-006-L07",
    "RG-REQ-006-L08",
    "RG-REQ-006-L09",
    "RG-REQ-006-L10",
    "RG-REQ-006-L12",
    "RG-REQ-006-L13",
    "RG-REQ-002-L01",
    "RG-REQ-002-L03",
    "RG-REQ-002-L04",
    "RG-REQ-002-L05",
    "RG-REQ-002-L06",
    "RG-REQ-002-L07",
    "RG-REQ-002-L08",
    "RG-REQ-002-L09",
    "RG-REQ-002-L10",
    "RG-REQ-004-L12",
    "RG-REQ-004-L02",
    "RG-REQ-011-L06",
    "RG-REQ-011-L07"
  ],
  "preservationObligationIds": [
    "P-CIB-01",
    "P-CIB-02",
    "P-CIB-03",
    "P-CIB-04",
    "P-CIB-05"
  ],
  "changes": [
    "RG-REQ-002-L02"
  ],
  "acceptanceBoundary": "Gated on TOOLCHAIN-STALENESS-1's and TEST-EDGE-SCOPE-1B's accepted implementation-review records at HEAD (result accepted, verified in CIB-C01 and CIB-C05). The whole unit suites of the indexer, classification, repo-index, trust and storage crates and the --lib suites of rgr and daemon-runtime (named new tests, existing tests by name and unchanged body, floors), byte-identity of every other reader, store and extractor file, fmt/clippy over the seven touched crates, and the field proof: rg-store-diff between a before-binary built from HEAD and the candidate on nginx, leveldb and poco (isolated roots), one query over the moved rows' {basis, candidates} against the rule recomputed from the file list, and the nginx reader surfaces (`modules deps` default and --include-inferred, `imports <file>` human and JSON, trust's unresolved-import figure equal to the modules-list headline), as named per check.",
  "candidatePaths": [
    "rust/crates/indexer/src/include_resolver.rs",
    "rust/crates/indexer/src/resolver.rs",
    "rust/crates/indexer/src/orchestrator.rs",
    "rust/crates/repo-index/src/compose.rs",
    "rust/crates/repo-index/tests/cpp_include_roots.rs",
    "rust/crates/classification/src/types.rs",
    "rust/crates/trust/src/rules.rs",
    "rust/crates/trust/src/service.rs",
    "rust/crates/storage/src/queries.rs",
    "rust/crates/rgr/src/presentation/imports.rs",
    "rust/crates/daemon-runtime/src/livegraph_feed.rs"
  ],
  "postReviewRecordPaths": [
    "docs/assurance/CPP-INCLUDE-BASENAME-1/verification.json",
    "docs/assurance/CPP-INCLUDE-BASENAME-1/implementation-review.json"
  ],
  "candidateExclusions": [
    {
      "pathPrefix": ".agent-manager/",
      "reason": "local relay state and raw run/progress evidence"
    },
    {
      "pathPrefix": "rust/target/",
      "reason": "reproducible Cargo build output"
    }
  ],
  "checks": [
    {
      "checkId": "CIB-C01",
      "obligationIds": [
        "RG-REQ-006-L11",
        "RG-REQ-002-L11",
        "RG-REQ-006-L03",
        "RG-REQ-006-L05",
        "RG-REQ-006-L01",
        "RG-REQ-006-L02",
        "RG-REQ-006-L04",
        "RG-REQ-004-L02",
        "RG-REQ-002-L01",
        "RG-REQ-002-L04",
        "P-CIB-01",
        "P-CIB-02"
      ],
      "owner": "builder",
      "method": {
        "kind": "command",
        "command": "git cat-file -e HEAD:docs/assurance/TOOLCHAIN-STALENESS-1/implementation-review.json && git show HEAD:docs/assurance/TOOLCHAIN-STALENESS-1/implementation-review.json | python3 -c \"import json,sys; d=json.load(sys.stdin); sys.exit(0 if (d.get('workItemId'), d.get('result')) == ('TOOLCHAIN-STALENESS-1', 'accepted') else 1)\" && git cat-file -e HEAD:docs/assurance/TEST-EDGE-SCOPE-1B/implementation-review.json && git show HEAD:docs/assurance/TEST-EDGE-SCOPE-1B/implementation-review.json | python3 -c \"import json,sys; d=json.load(sys.stdin); sys.exit(0 if (d.get('workItemId'), d.get('result')) == ('TEST-EDGE-SCOPE-1B', 'accepted') else 1)\" && ( set -o pipefail; cargo test -p repo-graph-indexer 2>&1 | tee /tmp/cib-01.txt ) && grep -qE '^test result: ok\\.' /tmp/cib-01.txt && ! grep -E '^test .* FAILED$|^thread .* panicked at|^test result: FAILED|^error(\\[E[0-9]+\\])?: ' /tmp/cib-01.txt && for t in unique_multi_segment_suffix_matches_one_indexed_file non_unique_multi_segment_suffix_lists_every_candidate multi_segment_suffix_matches_only_at_a_directory_boundary single_segment_unique_basename_is_an_inferred_candidate_never_a_suffix_match single_segment_non_unique_basename_lists_every_candidate single_segment_with_no_indexed_basename_matches_nothing basename_match_is_exact_never_extension_completion dot_dot_or_stable_key_specifier_is_declined suffix_index_is_built_from_the_indexed_file_list_only include_unique_multi_segment_suffix_resolves_static_with_its_basis include_unique_basename_resolves_inferred_with_its_candidate_and_basis include_non_unique_suffix_or_basename_stays_unresolved_ambiguous_with_candidates include_suffix_stage_runs_only_after_every_earlier_stage_misses non_c_family_import_never_reaches_the_suffix_or_basename_stage include_with_malformed_metadata_declines_the_suffix_stage ambiguous_when_multiple_roots_have_same_header angle_bracket_ambiguous_when_multiple_matches angle_bracket_resolves_if_local_header_exists angle_bracket_resolves_via_conventional_root angle_bracket_skips_same_dir angle_bracket_unresolved_when_no_local_match configured_and_conventional_both_matching_is_ambiguous configured_root_resolves derived_root_ambiguous_when_two_derived_roots_hold_the_header derived_root_angle_bracket_resolves derived_root_ignores_a_file_named_include derived_root_is_case_sensitive derived_root_pooled_with_configured_root_is_ambiguous derived_root_resolves_header_under_nested_inc_dir derived_root_resolves_header_under_nested_include_dir derived_root_same_directory_hit_still_wins derived_roots_are_deterministic_regardless_of_file_order header_to_header_same_dir header_to_header_via_include_root inc_root_resolves include_root_resolves no_sibling_directory_magic no_suffix_guessing normalize_removes_dot normalize_removes_empty_segments normalize_resolves_dotdot quoted_still_checks_same_dir_first resolution_map_get_resolved_key same_directory_resolves same_directory_wins_over_include_root src_include_root_resolves subpath_does_not_match_basename subpath_include_resolves categorize_imports java_import_resolves_to_file_node java_import_wildcard_is_named_basis per_file_include_resolution_different_directory per_file_include_resolution_same_directory resolve_import_by_stable_key resolve_import_via_extensionless_file_resolution resolve_import_via_per_tu_include resolve_import_via_repo_prefix_fallback stage_3_5_gated_off_for_non_rust_imports; do grep -qE \"^test .*::$t \\.\\.\\. ok$\" /tmp/cib-01.txt || grep -qE \"^test $t \\.\\.\\. ok$\" /tmp/cib-01.txt || { echo \"MISSING $t\"; exit 1; }; done && test \"$(cargo test -p repo-graph-indexer -- --list 2>/dev/null | grep -c ': test$')\" -ge 457 && python3 - <<'PY'\nimport subprocess, re\ndef sh(*a): return subprocess.run(a, capture_output=True, text=True, check=True).stdout\ndef body(src, name, indent):\n    pad = \" \" * indent\n    key = pad + \"fn \" + name + \"(\"\n    i = src.find(key)\n    assert i >= 0 and src.find(key, i + 1) < 0, (\"exactly one test fn\", name)\n    j = src.find(\"\\n\" + pad + \"}\\n\", i)\n    assert j > i, (\"end of test fn\", name)\n    return src[i:j]\ndef same(path, names, indent):\n    head, cur = sh(\"git\", \"show\", \"HEAD:./\" + path), open(path, encoding=\"utf-8\").read()\n    for n in names:\n        assert body(head, n, indent) == body(cur, n, indent), (\"existing test body changed\", path, n)\ndef head_tests(path):\n    return re.findall(r\"#\\[test\\]\\s*\\n\\s*fn (\\w+)\\(\", sh(\"git\", \"show\", \"HEAD:./\" + path))\ndef changed(p): return [l for l in sh(\"git\", \"diff\", \"-U0\", \"HEAD\", \"--\", p).splitlines() if l[:1] in \"+-\" and not l.startswith((\"+++\", \"---\"))]\nsame(\"crates/indexer/src/include_resolver.rs\", ['ambiguous_when_multiple_roots_have_same_header', 'angle_bracket_ambiguous_when_multiple_matches', 'angle_bracket_resolves_if_local_header_exists', 'angle_bracket_resolves_via_conventional_root', 'angle_bracket_skips_same_dir', 'angle_bracket_unresolved_when_no_local_match', 'configured_and_conventional_both_matching_is_ambiguous', 'configured_root_resolves', 'derived_root_ambiguous_when_two_derived_roots_hold_the_header', 'derived_root_angle_bracket_resolves', 'derived_root_ignores_a_file_named_include', 'derived_root_is_case_sensitive', 'derived_root_pooled_with_configured_root_is_ambiguous', 'derived_root_resolves_header_under_nested_inc_dir', 'derived_root_resolves_header_under_nested_include_dir', 'derived_root_same_directory_hit_still_wins', 'derived_roots_are_deterministic_regardless_of_file_order', 'header_to_header_same_dir', 'header_to_header_via_include_root', 'inc_root_resolves', 'include_root_resolves', 'no_sibling_directory_magic', 'no_suffix_guessing', 'normalize_removes_dot', 'normalize_removes_empty_segments', 'normalize_resolves_dotdot', 'quoted_still_checks_same_dir_first', 'resolution_map_get_resolved_key', 'same_directory_resolves', 'same_directory_wins_over_include_root', 'src_include_root_resolves', 'subpath_does_not_match_basename', 'subpath_include_resolves'], 4)\nsame(\"crates/indexer/src/resolver.rs\", ['categorize_imports', 'java_import_resolves_to_file_node', 'java_import_wildcard_is_named_basis', 'per_file_include_resolution_different_directory', 'per_file_include_resolution_same_directory', 'resolve_import_by_stable_key', 'resolve_import_via_extensionless_file_resolution', 'resolve_import_via_per_tu_include', 'resolve_import_via_repo_prefix_fallback', 'stage_3_5_gated_off_for_non_rust_imports'], 4)\ndef ver(src):\n    m = re.findall(r'^const INDEXER_VERSION: &str = \"(indexer:(\\d+)\\.(\\d+)\\.(\\d+))\";$', src, re.M)\n    assert len(m) == 1, (\"exactly one INDEXER_VERSION constant\", m)\n    return m[0][0], tuple(int(x) for x in m[0][1:])\nORCH, COMP = \"crates/indexer/src/orchestrator.rs\", \"crates/repo-index/src/compose.rs\"\nH, hv = ver(sh(\"git\", \"show\", \"HEAD:./\" + ORCH))\nC, cv = ver(open(ORCH, encoding=\"utf-8\").read())\nassert cv == (hv[0], hv[1] + 1, 0), (\"INDEXER_VERSION moves up exactly one minor from HEAD's\", H, C)\nassert changed(ORCH) == ['-const INDEXER_VERSION: &str = \"%s\";' % H, '+const INDEXER_VERSION: &str = \"%s\";' % C], (\"orchestrator.rs differs from HEAD by exactly its version line\", changed(ORCH))\nassert [l for l in changed(COMP) if \"INDEXER_VERSION\" in l] == ['-    let extractor = \"%s\"; // Match INDEXER_VERSION in orchestrator.rs' % H, '+    let extractor = \"%s\"; // Match INDEXER_VERSION in orchestrator.rs' % C], (\"compose.rs moves its declared copy of the version\", changed(COMP))\nassert len(changed(COMP)) == 2, (\"compose.rs differs from HEAD by exactly its version line\", changed(COMP))\ndef grep_counts(*rev):\n    out = subprocess.run([\"git\", \"grep\", \"-c\", \"-F\", H, *rev, \"--\", \"*.rs\"], capture_output=True, text=True).stdout\n    return {l.rsplit(\":\", 1)[0][len(rev[0]) + 1 if rev else 0:]: int(l.rsplit(\":\", 1)[1]) for l in out.splitlines()}\nhc, wc = grep_counts(\"HEAD\"), grep_counts()\nwant = {p: n - (1 if p in (ORCH, COMP) else 0) for p, n in hc.items()}\nassert {p: n for p, n in wc.items() if n} == {p: n for p, n in want.items() if n}, (\"the old version literal leaves exactly the two declared sites and is added nowhere\", hc, wc)\nprint(\"CIB-C01 source assertions ok:\", H, \"->\", C)\nPY",
        "cwd": "rust",
        "environment": "candidate tree; fail-closed gate: TOOLCHAIN-STALENESS-1's and TEST-EDGE-SCOPE-1B's implementation-review records exist at HEAD with result accepted",
        "inputs": "The WHOLE indexer test run (442 listed at 9755ba47; floor >= 457 = 442 + 9 + 6), run in a subshell with pipefail so the && chain carries cargo's own status. THE RULE (RG-REQ-006-L11 as ratified; section 2.1): a pure suffix/basename lookup built ONCE per `IncludeResolutionMap` from the same indexed file list the include roots are derived from (`build_include_resolution_map`, include_resolver.rs:274), and a Stage 6 in `resolver.rs resolve_target`'s Imports arm that runs ONLY when the include resolver answered Unresolved AND every existing stage 1-4 of `resolve_import_target` missed (resolver.rs:514-577) AND the edge's extractor is C/C++ (`is_c_family_extractor`, resolver.rs:61-63). `IncludeResolver::resolve` (include_resolver.rs:100-187) is NOT changed: its 33 tests stay byte-identical by body. Nine NEW include_resolver tests and six NEW resolver tests as named; the 33 existing include_resolver tests and ten existing resolver import-stage tests by name AND unchanged body (`no_suffix_guessing`, include_resolver.rs:739, stays as found - it tests that extensionless `foo` never completes to `foo.h`, which the exact-basename rule preserves). VERSION (D-REFRESH-STALE-1 = B; D-CIB-L06-1 = B: a reported consequence, TOOLCHAIN-STALENESS-1 owns RG-REQ-001-L06): `INDEXER_VERSION` (orchestrator.rs:59) moves up exactly one minor from HEAD's value with its declared copy (compose.rs:1653); orchestrator.rs differs from HEAD by exactly its version line and compose.rs by exactly its copy; the old literal leaves exactly those two sites."
      },
      "expected": "exit 0: an include no existing stage resolves binds static only on a unique path suffix of two or more segments (basis unique_suffix) and inferred - marked, with its one candidate and the basis unique_basename, outside the persisted module graph - only on a unique basename; a non-unique match stays unresolved as imports_ambiguous_match with every candidate recorded; an extensionless, dot-dot or key-form specifier and every non-C/C++ import are untouched; every existing include-resolver and import-stage test is preserved by name and body - no behavior change for any include an earlier stage resolves (RG-REQ-006-L03 never-flip); the indexer version moves by exactly one minor"
    },
    {
      "checkId": "CIB-C02",
      "obligationIds": [
        "RG-REQ-006-L05",
        "RG-REQ-006-L06",
        "RG-REQ-006-L07",
        "RG-REQ-006-L08",
        "RG-REQ-006-L09",
        "RG-REQ-006-L10",
        "RG-REQ-006-L13",
        "RG-REQ-002-L03",
        "RG-REQ-002-L05",
        "RG-REQ-002-L06",
        "RG-REQ-002-L07",
        "RG-REQ-002-L08",
        "RG-REQ-002-L09",
        "RG-REQ-002-L10",
        "RG-REQ-004-L12",
        "RG-REQ-011-L07",
        "P-CIB-02",
        "P-CIB-05"
      ],
      "owner": "builder",
      "method": {
        "kind": "command",
        "command": "( set -o pipefail; cargo test -p repo-graph-classification 2>&1 | tee /tmp/cib-02.txt ) && grep -qE '^test result: ok\\.' /tmp/cib-02.txt && ! grep -E '^test .* FAILED$|^thread .* panicked at|^test result: FAILED|^error(\\[E[0-9]+\\])?: ' /tmp/cib-02.txt && test \"$(cargo test -p repo-graph-classification -- --list 2>/dev/null | grep -c ': test$')\" -ge 269 && git -C .. diff HEAD --quiet -- rust/crates/classification/src/unresolved_classifier.rs rust/crates/agent rust/crates/module-queries rust/crates/cpp-extractor rust/crates/c-extractor ':(exclude)rust/crates/storage/src/queries.rs' rust/crates/storage ':(exclude)rust/crates/trust/src/rules.rs' ':(exclude)rust/crates/trust/src/service.rs' rust/crates/trust ':(exclude)rust/crates/rgr/src/presentation/imports.rs' rust/crates/rgr ':(exclude)rust/crates/daemon-runtime/src/livegraph_feed.rs' rust/crates/daemon-runtime && python3 - <<'PY'\nimport subprocess, re\ndef sh(*a): return subprocess.run(a, capture_output=True, text=True, check=True).stdout\ndef body(src, name, indent):\n    pad = \" \" * indent\n    key = pad + \"fn \" + name + \"(\"\n    i = src.find(key)\n    assert i >= 0 and src.find(key, i + 1) < 0, (\"exactly one test fn\", name)\n    j = src.find(\"\\n\" + pad + \"}\\n\", i)\n    assert j > i, (\"end of test fn\", name)\n    return src[i:j]\ndef same(path, names, indent):\n    head, cur = sh(\"git\", \"show\", \"HEAD:./\" + path), open(path, encoding=\"utf-8\").read()\n    for n in names:\n        assert body(head, n, indent) == body(cur, n, indent), (\"existing test body changed\", path, n)\ndef head_tests(path):\n    return re.findall(r\"#\\[test\\]\\s*\\n\\s*fn (\\w+)\\(\", sh(\"git\", \"show\", \"HEAD:./\" + path))\ndef changed(p): return [l for l in sh(\"git\", \"diff\", \"-U0\", \"HEAD\", \"--\", p).splitlines() if l[:1] in \"+-\" and not l.startswith((\"+++\", \"---\"))]\nT = \"crates/classification/src/types.rs\"\nd = changed(T)\ncode = [l for l in d if l[1:].strip() and not l[1:].strip().startswith(\"//\")]\nassert d and not code, (\"types.rs changes only documentation\", code)\nfull = sh(\"git\", \"diff\", \"-U3\", \"HEAD\", \"--\", T)\nassert \"ImportsAmbiguousMatch\" in full and \"MODULES_LIST_UNRESOLVED_IMPORT_CATEGORIES\" in full, \"the edited doc comments are ImportsAmbiguousMatch's and the shared set's\"\ncur = open(T, encoding=\"utf-8\").read()\nassert \"pub const MODULES_LIST_UNRESOLVED_IMPORT_CATEGORIES: [UnresolvedEdgeCategory; 4] = [\" in cur, \"the shared set keeps its four entries\"\nprint(\"CIB-C02 ok: classification diff is documentation only (%d lines)\" % len(d))\nPY",
        "cwd": "rust",
        "environment": "candidate tree",
        "inputs": "The WHOLE classification test run (269 at 9755ba47; floor >= 269; no new test - the category set, the classifier and the headline set are unchanged). `classification/src/types.rs` changes ONLY documentation: `UnresolvedEdgeCategory::ImportsAmbiguousMatch` (types.rs:268-270) names the three C/C++ ambiguity shapes (root overlap; non-unique path suffix; non-unique basename - the shape in `metadata_json.basis`), and `MODULES_LIST_UNRESOLVED_IMPORT_CATEGORIES` (types.rs:325-352) names its second reader, trust's `sum_unresolved_imports`; no variant, serde name, `is_imports_category` arm or set entry changes. Byte-identity of every file outside the eleven candidate paths in the storage, trust, rgr and daemon-runtime crates (the modules-list headline reader dispatch.rs:9383-9403 included) and of the classifier, agent, module-queries and both C-family extractors."
      },
      "expected": "exit 0: the unresolved-category vocabulary, the classifier and every untouched reader file are byte-identical - no behavior change outside the allocated paths; the only classification edits are the honest documentation of the category that now also carries the suffix and basename ambiguity shapes and of the shared set's two readers"
    },
    {
      "checkId": "CIB-C03",
      "obligationIds": [
        "RG-REQ-006-L11",
        "RG-REQ-002-L11",
        "RG-REQ-006-L03",
        "RG-REQ-004-L02",
        "RG-REQ-002-L02",
        "P-CIB-01",
        "P-CIB-02",
        "P-CIB-05"
      ],
      "owner": "builder",
      "method": {
        "kind": "command",
        "command": "( set -o pipefail; cargo test -p repo-graph-repo-index 2>&1 | tee /tmp/cib-03.txt ) && grep -qE '^test result: ok\\.' /tmp/cib-03.txt && ! grep -E '^test .* FAILED$|^thread .* panicked at|^test result: FAILED|^error(\\[E[0-9]+\\])?: ' /tmp/cib-03.txt && for t in nginx_style_single_segment_include_resolves_inferred_by_unique_basename unique_multi_segment_suffix_include_resolves_static refresh_turns_an_inferred_unique_basename_include_ambiguous_when_a_second_header_is_added trust_unresolved_import_figure_equals_the_modules_list_headline_on_one_snapshot indexed_cpp_include_resolves_through_nested_include_root indexed_cpp_include_ambiguous_across_two_include_roots_is_counted_not_bound; do grep -qE \"^test .*::$t \\.\\.\\. ok$\" /tmp/cib-03.txt || grep -qE \"^test $t \\.\\.\\. ok$\" /tmp/cib-03.txt || { echo \"MISSING $t\"; exit 1; }; done && test \"$(cargo test -p repo-graph-repo-index -- --list 2>/dev/null | grep -c ': test$')\" -ge 413 && python3 - <<'PY'\nimport subprocess, re\ndef sh(*a): return subprocess.run(a, capture_output=True, text=True, check=True).stdout\ndef body(src, name, indent):\n    pad = \" \" * indent\n    key = pad + \"fn \" + name + \"(\"\n    i = src.find(key)\n    assert i >= 0 and src.find(key, i + 1) < 0, (\"exactly one test fn\", name)\n    j = src.find(\"\\n\" + pad + \"}\\n\", i)\n    assert j > i, (\"end of test fn\", name)\n    return src[i:j]\ndef same(path, names, indent):\n    head, cur = sh(\"git\", \"show\", \"HEAD:./\" + path), open(path, encoding=\"utf-8\").read()\n    for n in names:\n        assert body(head, n, indent) == body(cur, n, indent), (\"existing test body changed\", path, n)\ndef head_tests(path):\n    return re.findall(r\"#\\[test\\]\\s*\\n\\s*fn (\\w+)\\(\", sh(\"git\", \"show\", \"HEAD:./\" + path))\ndef changed(p): return [l for l in sh(\"git\", \"diff\", \"-U0\", \"HEAD\", \"--\", p).splitlines() if l[:1] in \"+-\" and not l.startswith((\"+++\", \"---\"))]\nsame(\"crates/repo-index/tests/cpp_include_roots.rs\", ['indexed_cpp_include_resolves_through_nested_include_root', 'indexed_cpp_include_ambiguous_across_two_include_roots_is_counted_not_bound'], 0)\nprint(\"CIB-C03 existing integration test bodies unchanged\")\nPY",
        "cwd": "rust",
        "environment": "candidate tree",
        "inputs": "The WHOLE repo-index test run (409 at 9755ba47; floor >= 413). Four NEW end-to-end tests in `repo-index/tests/cpp_include_roots.rs` through the real extract -> resolve -> store pipeline: `nginx_style_single_segment_include_resolves_inferred_by_unique_basename` (`src/core/ngx_core.h`; `src/event/ngx_event.c` with `#include <ngx_core.h>` and `#include <ngx_time.h>`; `src/os/unix/ngx_time.h` and `src/os/win32/ngx_time.h`; no `include/` directory: ONE IMPORTS edge `inferred` with `basis = unique_basename`, `candidates = [<src/core/ngx_core.h FILE key>]` and the extractor's keys kept; ONE unresolved row `imports_ambiguous_match` with `basis = ambiguous_basename` and both candidate keys; no MODULE->MODULE edge `src/event -> src/core`); `unique_multi_segment_suffix_include_resolves_static` (`app/main.cpp` with `#include \"util/foo.h\"`, the only `util/foo.h` at `lib/src/util/foo.h`: ONE IMPORTS edge `static`, `basis = unique_suffix`, and the MODULE->MODULE edge `app -> lib/src/util`); `refresh_turns_an_inferred_unique_basename_include_ambiguous_when_a_second_header_is_added` (D-CIB-REFRESH-1 = A: index `src/app/main.c` with `#include <foo.h>` and `src/lib/foo.h` -> inferred edge; add `src/other/foo.h`, leave main.c unchanged; `refresh_into_storage` -> main.c's content hash is identical across the two snapshots (it was copied forward, `query_file_version_hashes`) AND its include is now ONE unresolved `imports_ambiguous_match` row with `basis = ambiguous_basename` and both candidate keys, with no IMPORTS edge from main.c - refresh re-resolves every include of the snapshot, orchestrator.rs:1008-1039); `trust_unresolved_import_figure_equals_the_modules_list_headline_on_one_snapshot` (D-CIB-COUNT-1 = A: one index of a fixture with one C include of an unindexed header (`imports_file_not_found`), one C include whose basename exists twice (`imports_ambiguous_match`) and one Java `import com.acme.*;` (`imports_wildcard`): `assemble_trust_report`'s `summary.reliability.import_graph.reasons` holds `unresolved_imports=3`, and the sum of `count_unresolved_edges_by_classification` over `MODULES_LIST_UNRESOLVED_IMPORT_CATEGORIES` - the modules-list headline's read - is 3). The two existing tests by name and unchanged body."
      },
      "expected": "exit 0: both binding shapes and the ambiguous case hold end to end; a refresh re-resolves a copied-forward include against the current file list, so a second header of the same basename turns the inferred binding ambiguous; trust's unresolved-import figure and the modules-list headline are one number on one snapshot; the include-root behavior CPP-INCLUDE-ROOTS-1 shipped is preserved by name and body - no behavior change"
    },
    {
      "checkId": "CIB-C07",
      "obligationIds": [
        "RG-REQ-002-L02",
        "RG-REQ-002-L10",
        "P-CIB-05"
      ],
      "owner": "builder",
      "method": {
        "kind": "command",
        "command": "( set -o pipefail; cargo test -p repo-graph-trust 2>&1 | tee /tmp/cib-07.txt ) && grep -qE '^test result: ok\\.' /tmp/cib-07.txt && ! grep -E '^test .* FAILED$|^thread .* panicked at|^test result: FAILED|^error(\\[E[0-9]+\\])?: ' /tmp/cib-07.txt && for t in sum_unresolved_imports_sums_every_category_of_the_shared_imports_set a_lone_non_legacy_unresolved_import_category_lowers_import_graph_and_change_impact sum_unresolved_calls_sums_calls_family import_low_with_unresolved_imports minimal_input_produces_all_high_reliability caveats_for_non_high_levels; do grep -qE \"^test .*::$t \\.\\.\\. ok$\" /tmp/cib-07.txt || grep -qE \"^test $t \\.\\.\\. ok$\" /tmp/cib-07.txt || { echo \"MISSING $t\"; exit 1; }; done && test \"$(cargo test -p repo-graph-trust -- --list 2>/dev/null | grep -c ': test$')\" -ge 120 && python3 - <<'PY'\nimport subprocess, re\ndef sh(*a): return subprocess.run(a, capture_output=True, text=True, check=True).stdout\ndef body(src, name, indent):\n    pad = \" \" * indent\n    key = pad + \"fn \" + name + \"(\"\n    i = src.find(key)\n    assert i >= 0 and src.find(key, i + 1) < 0, (\"exactly one test fn\", name)\n    j = src.find(\"\\n\" + pad + \"}\\n\", i)\n    assert j > i, (\"end of test fn\", name)\n    return src[i:j]\ndef same(path, names, indent):\n    head, cur = sh(\"git\", \"show\", \"HEAD:./\" + path), open(path, encoding=\"utf-8\").read()\n    for n in names:\n        assert body(head, n, indent) == body(cur, n, indent), (\"existing test body changed\", path, n)\ndef head_tests(path):\n    return re.findall(r\"#\\[test\\]\\s*\\n\\s*fn (\\w+)\\(\", sh(\"git\", \"show\", \"HEAD:./\" + path))\ndef changed(p): return [l for l in sh(\"git\", \"diff\", \"-U0\", \"HEAD\", \"--\", p).splitlines() if l[:1] in \"+-\" and not l.startswith((\"+++\", \"---\"))]\nR = \"crates/trust/src/rules.rs\"\nold, new = \"sum_unresolved_imports_picks_imports_family\", \"sum_unresolved_imports_sums_every_category_of_the_shared_imports_set\"\ncur = open(R, encoding=\"utf-8\").read()\nassert (\"fn \" + old + \"(\") not in cur and (\"fn \" + new + \"(\") in cur, \"the test is renamed to what it proves\"\nsame(R, [t for t in head_tests(R) if t != old], 4)\ni = cur.find(\"pub(crate) fn sum_unresolved_imports(\"); j = cur.find(\"\\n}\\n\", i)\nf = cur[i:j]\nassert i >= 0 and \"MODULES_LIST_UNRESOLVED_IMPORT_CATEGORIES\" in f and '\"imports_file_not_found\"' not in f, (\"sum_unresolved_imports reads the shared set, never one literal key\", f)\nS = \"crates/trust/src/service.rs\"\nsame(S, head_tests(S), 4)\nassert not [l for l in changed(S) if l.startswith(\"-\")], (\"service.rs only gains lines\", changed(S))\nmark = \"#[cfg(test)]\\nmod tests {\"\nhs, cs = sh(\"git\", \"show\", \"HEAD:./\" + S), open(S, encoding=\"utf-8\").read()\nassert hs.count(mark) == 1 and cs.count(mark) == 1 and hs.split(mark)[0] == cs.split(mark)[0], \"service.rs production code is byte-identical; only its test module grows\"\nassert \"fn a_lone_non_legacy_unresolved_import_category_lowers_import_graph_and_change_impact(\" in cs.split(mark)[1], \"the propagation test lives in service.rs's test module\"\nprint(\"CIB-C07 ok: trust sums the shared IMPORTS set; every other rules.rs/service.rs test unchanged by body; service.rs production code unchanged\")\nPY",
        "cwd": "rust",
        "environment": "candidate tree",
        "inputs": "The WHOLE trust test run (119 at 9755ba47, 118 of them --lib; floor >= 120). `sum_unresolved_imports` (trust/src/rules.rs:390-399, doc 'Sum unresolved counts for IMPORTS-family categories') sums `unresolved_breakdown` over every category of `MODULES_LIST_UNRESOLVED_IMPORT_CATEGORIES` (each keyed by its serialized name, the key the indexer writes, orchestrator.rs:1091-1095) instead of the single key `imports_file_not_found`; its test `sum_unresolved_imports_picks_imports_family` (:994-1006, which asserted the narrow behavior under the family name) is renamed `sum_unresolved_imports_sums_every_category_of_the_shared_imports_set` and asserts that one count in each of the four categories plus a CALLS category sums to the four. F-CIB-TRUST-PROPAGATION: the sum feeds `compute_import_graph_reliability` (service.rs:370-374; rules.rs:228-253 - any unresolved import makes the axis LOW with reason `unresolved_imports=N`), whose level feeds `compute_change_impact_reliability` (service.rs:386-390; rules.rs:329-364 - LOW with reason `import_graph_reliability_low`) and the two caveats (service.rs:240-253); ONE NEW test in service.rs's test module, `a_lone_non_legacy_unresolved_import_category_lowers_import_graph_and_change_impact`, runs `compute_trust_report` on `minimal_input()` (all axes HIGH) with diagnostics whose ONLY unresolved category is `imports_wildcard: 2` and asserts import_graph = LOW with reasons exactly [`unresolved_imports=2`], change_impact = LOW with reasons exactly [`import_graph_reliability_low`], the two caveats 'Import-graph reliability is LOW' and 'Change-impact reliability is LOW' present, and call_graph and dead_code still HIGH - the transition HEAD's single-key sum missed (it reads 0 there and leaves both axes HIGH). Every other test of rules.rs and service.rs is unchanged by body; service.rs's production code is byte-identical (only its test module grows); `sum_unresolved_calls_sums_calls_family`, `import_low_with_unresolved_imports`, `minimal_input_produces_all_high_reliability` and `caveats_for_non_high_levels` pass unchanged."
      },
      "expected": "exit 0: trust's unresolved-import figure counts the whole IMPORTS family through the one shared set the modules-list headline reads (one definition, two readers), and every field derived from that figure - the import-graph level and reason, the change-impact level and reason, their caveats - moves with it, as tested; every trust field not derived from it (CALLS sums, call-graph and dead-code axes, category, classification and module rows) is preserved unchanged - no behavior change there; the renamed test states what it proves"
    },
    {
      "checkId": "CIB-C08",
      "obligationIds": [
        "RG-REQ-002-L11",
        "RG-REQ-006-L12",
        "RG-REQ-002-L04",
        "RG-REQ-002-L08",
        "P-CIB-04"
      ],
      "owner": "builder",
      "method": {
        "kind": "command",
        "command": "( set -o pipefail; cargo test -p repo-graph-storage 2>&1 | tee /tmp/cib-08s.txt ) && grep -qE '^test result: ok\\.' /tmp/cib-08s.txt && ! grep -E '^test .* FAILED$|^thread .* panicked at|^test result: FAILED|^error(\\[E[0-9]+\\])?: ' /tmp/cib-08s.txt && for t in find_imports_row_without_a_recorded_basis_carries_no_reason find_imports_bound_include_carries_its_basis_and_candidate_paths find_imports_malformed_reason_carrier_is_unreadable_never_dropped find_imports_row_of_another_basis_carries_no_reason find_imports_reason_that_contradicts_its_edge_is_unreadable_never_evidence find_imports_c_family_inferred_row_with_a_null_carrier_reports_its_reason_missing find_imports_c_family_inferred_row_whose_carrier_lacks_a_basis_reports_its_reason_missing; do grep -qE \"^test .*::$t \\.\\.\\. ok$\" /tmp/cib-08s.txt || grep -qE \"^test $t \\.\\.\\. ok$\" /tmp/cib-08s.txt || { echo \"MISSING $t\"; exit 1; }; done && test \"$(cargo test -p repo-graph-storage -- --list 2>/dev/null | grep -c ': test$')\" -ge 748 && ( set -o pipefail; cargo test -p repo-graph-rgr --lib 2>&1 | tee /tmp/cib-08r.txt ) && grep -qE '^test result: ok\\.' /tmp/cib-08r.txt && ! grep -E '^test .* FAILED$|^thread .* panicked at|^test result: FAILED|^error(\\[E[0-9]+\\])?: ' /tmp/cib-08r.txt && for t in inferred_unique_basename_row_renders_its_reason_and_candidate inferred_row_with_an_unreadable_reason_says_so_never_bare_inferred inferred_row_whose_reason_disagrees_with_the_row_is_unreadable_never_evidence row_without_a_rendered_reason_prints_exactly_as_before compare_listing_prints_the_same_rows_as_the_default_listing import_entry_decodes_a_reason_its_unreadable_form_its_missing_form_and_its_absence inferred_row_whose_reason_is_missing_for_a_null_carrier_says_re_index inferred_row_whose_reason_is_missing_for_a_carrier_without_basis_says_re_index c_family_inferred_row_without_a_reason_key_says_reason_missing_never_bare_inferred render_imports_shows_header render_imports_shows_count render_imports_singular_count render_imports_shows_entries render_imports_shows_depth render_imports_shows_resolution render_empty_imports compare_render_shows_sqlite_listing_and_summary compare_render_regression_is_loud; do grep -qE \"^test .*::$t \\.\\.\\. ok$\" /tmp/cib-08r.txt || grep -qE \"^test $t \\.\\.\\. ok$\" /tmp/cib-08r.txt || { echo \"MISSING $t\"; exit 1; }; done && test \"$(cargo test -p repo-graph-rgr --lib -- --list 2>/dev/null | grep -c ': test$')\" -ge 1277 && ( set -o pipefail; cargo test -p repo-graph-daemon-runtime --lib 2>&1 | tee /tmp/cib-08d.txt ) && grep -qE '^test result: ok\\.' /tmp/cib-08d.txt && ! grep -E '^test .* FAILED$|^thread .* panicked at|^test result: FAILED|^error(\\[E[0-9]+\\])?: ' /tmp/cib-08d.txt && for t in imports_auto_body_serves_livegraph_or_falls_back; do grep -qE \"^test .*::$t \\.\\.\\. ok$\" /tmp/cib-08d.txt || grep -qE \"^test $t \\.\\.\\. ok$\" /tmp/cib-08d.txt || { echo \"MISSING $t\"; exit 1; }; done && test \"$(cargo test -p repo-graph-daemon-runtime --lib -- --list 2>/dev/null | grep -c ': test$')\" -ge 786 && python3 - <<'PY'\nimport subprocess, re\ndef sh(*a): return subprocess.run(a, capture_output=True, text=True, check=True).stdout\ndef body(src, name, indent):\n    pad = \" \" * indent\n    key = pad + \"fn \" + name + \"(\"\n    i = src.find(key)\n    assert i >= 0 and src.find(key, i + 1) < 0, (\"exactly one test fn\", name)\n    j = src.find(\"\\n\" + pad + \"}\\n\", i)\n    assert j > i, (\"end of test fn\", name)\n    return src[i:j]\ndef same(path, names, indent):\n    head, cur = sh(\"git\", \"show\", \"HEAD:./\" + path), open(path, encoding=\"utf-8\").read()\n    for n in names:\n        assert body(head, n, indent) == body(cur, n, indent), (\"existing test body changed\", path, n)\ndef head_tests(path):\n    return re.findall(r\"#\\[test\\]\\s*\\n\\s*fn (\\w+)\\(\", sh(\"git\", \"show\", \"HEAD:./\" + path))\ndef changed(p): return [l for l in sh(\"git\", \"diff\", \"-U0\", \"HEAD\", \"--\", p).splitlines() if l[:1] in \"+-\" and not l.startswith((\"+++\", \"---\"))]\nQ, P, L = \"crates/storage/src/queries.rs\", \"crates/rgr/src/presentation/imports.rs\", \"crates/daemon-runtime/src/livegraph_feed.rs\"\nsame(Q, head_tests(Q), 4)\nsame(P, head_tests(P), 4)\nsame(L, [t for t in head_tests(L) if t != \"imports_auto_body_serves_livegraph_or_falls_back\"], 4)\nd = changed(L)\nassert len(d) == 1 and re.fullmatch(r\"\\+\\s+reason: None,\", d[0]), (\"livegraph_feed.rs gains exactly the new field in its ImportResult test literal\", d)\nassert ['render_imports_shows_header', 'render_imports_shows_count', 'render_imports_singular_count', 'render_imports_shows_entries', 'render_imports_shows_depth', 'render_imports_shows_resolution', 'render_empty_imports', 'compare_render_shows_sqlite_listing_and_summary', 'compare_render_regression_is_loud'] == [t for t in ['render_imports_shows_header', 'render_imports_shows_count', 'render_imports_singular_count', 'render_imports_shows_entries', 'render_imports_shows_depth', 'render_imports_shows_resolution', 'render_empty_imports', 'compare_render_shows_sqlite_listing_and_summary', 'compare_render_regression_is_loud'] if t in head_tests(P)], \"the bound renderer tests exist at HEAD\"\nprint(\"CIB-C08 ok: every existing storage/rgr/livegraph_feed test unchanged by body; the daemon carries the row unchanged\")\nPY",
        "cwd": "rust",
        "environment": "candidate tree",
        "inputs": "D-CIB-REASON-1 = A; F-CIB-REASON-VALIDITY. F-CIB-MISSING-REASON. STORAGE (whole run; 741 at 9755ba47; floor >= 748 = 741 + 7): `find_imports` (storage/src/queries.rs:1726-1771) also selects `e.metadata_json` and the target node's `stable_key` and sets `ImportResult.reason` by ONE validity rule, judged against the row's own edge. OWNERSHIP: a C/C++ `inferred` edge (`e.extractor`, carried as `evidence`, starts `c-core:`/`cpp-core:` - only this slice writes one) must carry a valid reason and is never ABSENT; ABSENT (key serde-skipped; the row serializes exactly as at HEAD) only for a `static` row or a row of another extractor with a NULL carrier, an object without `basis`, or a string `basis` outside the four this slice writes (PYTHON-SUBMODULE-IMPORT-1's `python_submodule`); PRESENT-MISSING for a C/C++ `inferred` edge with a NULL carrier (`{missing: \"no reason carrier\"}`) or an object without `basis` (`{missing: \"reason carrier has no basis\"}`); PRESENT-VALID `{basis, candidates: [<target path>]}` only when `basis` is `unique_basename` on an `inferred` edge or `unique_suffix` on a `static` edge AND `candidates` is an array of exactly one string equal to the edge's own target stable key, whose node is a FILE; otherwise `{unreadable: <what failed>}` - never dropped, never defaulted, never presented as evidence: the carrier is not a JSON object; `basis` is not a string; `candidates` is missing, not an array, holds a non-string, is empty or holds more than one entry under a unique basis; the one candidate is not the edge's target or the target is not a FILE; the basis disagrees with the edge's resolution (`unique_basename` on a static edge, `unique_suffix` on an inferred one); an `ambiguous_basename`/`ambiguous_suffix` basis on a bound edge (an ambiguous include is never an edge); a string `basis` outside the four on a C/C++ `inferred` edge. Seven NEW storage tests: absent (a static C/C++ include row and a non-C/C++ inferred row, both unchanged), another basis (a Python `python_submodule` row, unchanged), valid (both unique bases), malformed structure, `find_imports_reason_that_contradicts_its_edge_is_unreadable_never_evidence` over every contradiction case above, and one per missing carrier state - `find_imports_c_family_inferred_row_with_a_null_carrier_reports_its_reason_missing`, `find_imports_c_family_inferred_row_whose_carrier_lacks_a_basis_reports_its_reason_missing`. RGR (--lib; 1268 at 9755ba47; floor >= 1277 = 1268 + 9): `ImportEntry` decodes `reason`; ONE row formatter serves `ImportsResponse` and `ImportsCompareResponse` (imports.rs:92-99 and :323-333 today duplicate it) and re-checks the reason against its own row, since it renders JSON it did not produce: an `inferred` row whose reason is `unique_basename` with exactly one candidate equal to the row's `file` prints `  <path>  depth=<d>  inferred: unique basename → <candidate>`; an `inferred` row whose reason is `unreadable` prints `  <path>  depth=<d>  inferred: reason unreadable (<what failed>)`; an `inferred` row whose reason disagrees with the row (another candidate, several candidates, `unique_suffix`, no basis, a basis this build does not read) prints `  <path>  depth=<d>  inferred: reason unreadable (<what disagrees>)`; an `inferred` row whose reason is `missing`, or a C/C++ `inferred` row (by `evidence`) with no `reason` key, prints `  <path>  depth=<d>  inferred (reason missing — re-index)`; every `static` row and every other row without a reason prints exactly as at HEAD (a static row's reason stays in JSON). Nine NEW renderer tests incl. `inferred_row_whose_reason_disagrees_with_the_row_is_unreadable_never_evidence` over each disagreement and one per missing state - `inferred_row_whose_reason_is_missing_for_a_null_carrier_says_re_index`, `inferred_row_whose_reason_is_missing_for_a_carrier_without_basis_says_re_index`, `c_family_inferred_row_without_a_reason_key_says_reason_missing_never_bare_inferred` - with `import_entry_decodes_a_reason_its_unreadable_form_its_missing_form_and_its_absence` decoding all four forms; the nine existing renderer tests by name and unchanged body (the `sample_imports` fixture gains only the new field). DAEMON (--lib; 786 at 9755ba47; floor >= 786): no production change - both SQLite paths serialize the storage rows verbatim (dispatch.rs:1759-1777; livegraph_feed.rs:1760-1763, which a C/C++ file always takes: the LiveGraph import view serves TS only); livegraph_feed.rs changes by exactly one added `reason: None,` line in the `imports_auto_body_serves_livegraph_or_falls_back` literal; every other test of the three files unchanged by body."
      },
      "expected": "exit 0: the reason the index records on an inferred include reaches the agent-facing `imports <file>` row in human text and JSON; a reason is shown as evidence only when it agrees with its own row (a unique basis, exactly one candidate that is the row's own target FILE, the basis matching the resolution); a malformed or contradictory carrier is stated as unreadable with what failed, never dropped, never defaulted and never rendered as a reason; a C/C++ inferred row that lost its reason carrier or its basis is stated as reason missing with the re-index remedy, never bare `inferred`; a row for which this slice requires no reason (static, or another producer's) renders and serializes exactly as before - no behavior change for it, the default and compare listings print one row form"
    },
    {
      "checkId": "CIB-C04",
      "obligationIds": [
        "P-CIB-03"
      ],
      "owner": "builder",
      "method": {
        "kind": "command",
        "command": "cargo fmt --check -p repo-graph-indexer -p repo-graph-classification -p repo-graph-repo-index -p repo-graph-trust -p repo-graph-storage -p repo-graph-rgr -p repo-graph-daemon-runtime && cargo clippy -p repo-graph-indexer -p repo-graph-classification -p repo-graph-repo-index -p repo-graph-trust -p repo-graph-storage -p repo-graph-rgr -p repo-graph-daemon-runtime --all-targets -- -D warnings && git -C .. diff HEAD --check",
        "cwd": "rust",
        "environment": "candidate tree",
        "inputs": "formatter and clippy (warnings denied, all targets) over the seven touched crates; whitespace over the whole diff."
      },
      "expected": "exit 0: hygiene clean"
    },
    {
      "checkId": "CIB-C05",
      "obligationIds": [
        "RG-REQ-006-L11",
        "RG-REQ-002-L11",
        "RG-REQ-006-L03",
        "RG-REQ-006-L05",
        "RG-REQ-004-L02",
        "RG-REQ-002-L01",
        "RG-REQ-011-L06",
        "RG-REQ-011-L07",
        "P-CIB-01"
      ],
      "owner": "builder",
      "method": {
        "kind": "command",
        "command": "git cat-file -e HEAD:docs/assurance/TOOLCHAIN-STALENESS-1/implementation-review.json && git show HEAD:docs/assurance/TOOLCHAIN-STALENESS-1/implementation-review.json | python3 -c \"import json,sys; d=json.load(sys.stdin); sys.exit(0 if (d.get('workItemId'), d.get('result')) == ('TOOLCHAIN-STALENESS-1', 'accepted') else 1)\" && git cat-file -e HEAD:docs/assurance/TEST-EDGE-SCOPE-1B/implementation-review.json && git show HEAD:docs/assurance/TEST-EDGE-SCOPE-1B/implementation-review.json | python3 -c \"import json,sys; d=json.load(sys.stdin); sys.exit(0 if (d.get('workItemId'), d.get('result')) == ('TEST-EDGE-SCOPE-1B', 'accepted') else 1)\" && test ! -e /private/tmp/CPP-INCLUDE-BASENAME-1-nginx-headbase && test ! -e /private/tmp/CPP-INCLUDE-BASENAME-1-nginx-after && test ! -e /private/tmp/CPP-INCLUDE-BASENAME-1-leveldb-headbase && test ! -e /private/tmp/CPP-INCLUDE-BASENAME-1-leveldb-after && test ! -e /private/tmp/CPP-INCLUDE-BASENAME-1-poco-headbase && test ! -e /private/tmp/CPP-INCLUDE-BASENAME-1-poco-after && test ! -e /private/tmp/CPP-INCLUDE-BASENAME-1-headbase-wt && test ! -e /private/tmp/CPP-INCLUDE-BASENAME-1-headbase-target && (cd rust && cargo build --release --bin rmap --bin rmapd > /tmp/cib-build.txt 2>&1) && git worktree add --detach /private/tmp/CPP-INCLUDE-BASENAME-1-headbase-wt HEAD && (cd /private/tmp/CPP-INCLUDE-BASENAME-1-headbase-wt/rust && CARGO_TARGET_DIR=/private/tmp/CPP-INCLUDE-BASENAME-1-headbase-target cargo build --release --bin rmap --bin rmapd > /tmp/cib-headbase-build.txt 2>&1) && RB=\"$PWD/rust/target/release\" && BB=/private/tmp/CPP-INCLUDE-BASENAME-1-headbase-target/release && RSD='/Users/apple/Documents/APLICATII BIJUTERIE/agent-manager/scripts/rg-store-diff.py' && mkdir -p /private/tmp/CPP-INCLUDE-BASENAME-1-nginx-headbase /private/tmp/CPP-INCLUDE-BASENAME-1-nginx-after /private/tmp/CPP-INCLUDE-BASENAME-1-leveldb-headbase /private/tmp/CPP-INCLUDE-BASENAME-1-leveldb-after /private/tmp/CPP-INCLUDE-BASENAME-1-poco-headbase /private/tmp/CPP-INCLUDE-BASENAME-1-poco-after && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/nginx' && export RMAP_STATE_ROOT=/private/tmp/CPP-INCLUDE-BASENAME-1-nginx-headbase RMAP_SOCKET_PATH=/private/tmp/CPP-INCLUDE-BASENAME-1-nginx-headbase/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off PATH=\"$BB:$PATH\" && rmap index > /tmp/cib-nginx-headbase-index.txt 2>&1 ) && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/nginx' && export RMAP_STATE_ROOT=/private/tmp/CPP-INCLUDE-BASENAME-1-nginx-after RMAP_SOCKET_PATH=/private/tmp/CPP-INCLUDE-BASENAME-1-nginx-after/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off PATH=\"$RB:$PATH\" && rmap index > /tmp/cib-nginx-after-index.txt 2>&1 ) && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/leveldb' && export RMAP_STATE_ROOT=/private/tmp/CPP-INCLUDE-BASENAME-1-leveldb-headbase RMAP_SOCKET_PATH=/private/tmp/CPP-INCLUDE-BASENAME-1-leveldb-headbase/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off PATH=\"$BB:$PATH\" && rmap index > /tmp/cib-leveldb-headbase-index.txt 2>&1 ) && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/leveldb' && export RMAP_STATE_ROOT=/private/tmp/CPP-INCLUDE-BASENAME-1-leveldb-after RMAP_SOCKET_PATH=/private/tmp/CPP-INCLUDE-BASENAME-1-leveldb-after/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off PATH=\"$RB:$PATH\" && rmap index > /tmp/cib-leveldb-after-index.txt 2>&1 ) && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/poco' && export RMAP_STATE_ROOT=/private/tmp/CPP-INCLUDE-BASENAME-1-poco-headbase RMAP_SOCKET_PATH=/private/tmp/CPP-INCLUDE-BASENAME-1-poco-headbase/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off PATH=\"$BB:$PATH\" && rmap index > /tmp/cib-poco-headbase-index.txt 2>&1 ) && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/poco' && export RMAP_STATE_ROOT=/private/tmp/CPP-INCLUDE-BASENAME-1-poco-after RMAP_SOCKET_PATH=/private/tmp/CPP-INCLUDE-BASENAME-1-poco-after/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off PATH=\"$RB:$PATH\" && rmap index > /tmp/cib-poco-after-index.txt 2>&1 ) && ( set -o pipefail; python3 \"$RSD\" /private/tmp/CPP-INCLUDE-BASENAME-1-nginx-headbase /private/tmp/CPP-INCLUDE-BASENAME-1-nginx-after --expect edges:791 --expect unresolved:804 | tee /tmp/cib-rsd-nginx.txt ) && ( set -o pipefail; python3 \"$RSD\" /private/tmp/CPP-INCLUDE-BASENAME-1-leveldb-headbase /private/tmp/CPP-INCLUDE-BASENAME-1-leveldb-after | tee /tmp/cib-rsd-leveldb.txt ) && ( set -o pipefail; python3 \"$RSD\" /private/tmp/CPP-INCLUDE-BASENAME-1-poco-headbase /private/tmp/CPP-INCLUDE-BASENAME-1-poco-after --expect edges:150 --expect unresolved:134 | tee /tmp/cib-rsd-poco.txt ) && python3 - <<'PY'\nimport sqlite3, glob, json, os, re, collections\nC_FAMILY = (\"c-core:\", \"cpp-core:\")\nKEY = re.compile(r\"^repo_[a-z0-9]+:(.+):FILE$\")\ndef db(root):\n    p = glob.glob(root + \"/databases/*.db\"); assert len(p) == 1, (root, p)\n    w = p[0] + \"-wal\"; assert not (os.path.exists(w) and os.path.getsize(w) > 0), (\"uncheckpointed WAL\", w)\n    return sqlite3.connect(\"file:\" + p[0] + \"?immutable=1\", uri=True)\ndef facts(root):\n    c = db(root); s = c.execute(\"select snapshot_uid from snapshots where status='ready' order by created_at desc limit 1\").fetchone()[0]\n    files = sorted(p for (p,) in c.execute(\"select f.path from files f join file_versions v on v.file_uid=f.file_uid where v.snapshot_uid=?\", (s,)))\n    E, U = {}, {}\n    for sp, tp, tk, ln, co, res, ex, md in c.execute(\"select sf.path, tf.path, tn.stable_key, e.line_start, e.col_start, e.resolution, e.extractor, e.metadata_json from edges e join nodes sn on sn.node_uid=e.source_node_uid join nodes tn on tn.node_uid=e.target_node_uid join files sf on sf.file_uid=sn.file_uid join files tf on tf.file_uid=tn.file_uid where e.snapshot_uid=? and e.type='IMPORTS'\", (s,)):\n        if ex.startswith(C_FAMILY):\n            assert (sp, ln, co) not in E, (\"two C/C++ include edges share a source anchor\", sp, ln, co); E[(sp, ln, co)] = (tp, res, json.loads(md) if md else {})\n    for sp, tk, ln, co, cat, cls, bc, ex, md in c.execute(\"select sf.path, u.target_key, u.line_start, u.col_start, u.category, u.classification, u.basis_code, u.extractor, u.metadata_json from unresolved_edges u join nodes sn on sn.node_uid=u.source_node_uid join files sf on sf.file_uid=sn.file_uid where u.snapshot_uid=? and u.type='IMPORTS'\", (s,)):\n        if ex.startswith(C_FAMILY):\n            assert (sp, ln, co) not in U, (\"two C/C++ unresolved includes share a source anchor\", sp, ln, co); U[(sp, ln, co)] = (tk, cat, cls, bc, json.loads(md) if md else {})\n    bad = c.execute(\"select count(*) from edges e join nodes a on a.node_uid=e.source_node_uid join nodes b on b.node_uid=e.target_node_uid where e.snapshot_uid=? and e.type='IMPORTS' and e.resolution='inferred' and (a.kind<>'FILE' or b.kind<>'FILE')\", (s,)).fetchone()[0]\n    return files, E, U, bad\ndef rule(files, spec):\n    # RG-REQ-006-L11 as ratified, recomputed here from the base root's own file list (independent of the implementation)\n    segs = [x for x in spec.split(\"/\") if x not in (\"\", \".\")]\n    if \":\" in spec or \"..\" in segs or not segs: return None\n    if len(segs) >= 2:\n        suf = \"/\".join(segs); m = sorted(p for p in files if p == suf or p.endswith(\"/\" + suf))\n        return None if not m else ((\"unique_suffix\", \"static\") if len(m) == 1 else (\"ambiguous_suffix\", None), m)\n    m = sorted(p for p in files if p.rsplit(\"/\", 1)[-1] == segs[0])\n    return None if not m else ((\"unique_basename\", \"inferred\") if len(m) == 1 else (\"ambiguous_basename\", None), m)\ndef paths(md):\n    out = []\n    for k in md.get(\"candidates\", []):\n        g = KEY.match(k); assert g, (\"candidate is not a FILE stable key\", k); out.append(g.group(1))\n    return out\ndef check(name, want):\n    fb, Eb, Ub, _ = facts(\"/private/tmp/CPP-INCLUDE-BASENAME-1-%s-headbase\" % name)\n    fa, Ea, Ua, bad = facts(\"/private/tmp/CPP-INCLUDE-BASENAME-1-%s-after\" % name)\n    assert fb == fa, (name, \"file universe differs between the two indexes\")\n    assert bad == 0, (name, \"an inferred IMPORTS edge between non-FILE nodes (the persisted module graph)\")\n    added = set(Ea) - set(Eb); assert set(Eb) <= set(Ea), (name, \"an include edge of the base disappeared\")\n    assert added == set(Ub) - set(Ua), (name, \"every new include edge is exactly a formerly unresolved include\")\n    got = collections.Counter()\n    for k in added:\n        tk, cat, cls, bc, umd = Ub[k]; tp, res, emd = Ea[k]\n        (basis, want_res), cands = rule(fb, tk)\n        assert cat == \"imports_file_not_found\" and want_res == res and emd.get(\"basis\") == basis, (name, \"bound include\", k, tk, res, emd)\n        assert paths(emd) == cands == [tp], (name, \"candidates are the one indexed match and the target\", k, emd, cands, tp)\n        rest = {x: v for x, v in emd.items() if x not in (\"basis\", \"candidates\")}; assert rest == umd, (name, \"carrier keys of the moved row\", k, rest, umd)\n        got[basis] += 1\n    for k in set(Ua) & set(Ub):\n        if Ua[k] == Ub[k]: continue\n        tk, cat, cls, bc, md = Ua[k]; tk0, cat0, cls0, bc0, md0 = Ub[k]\n        (basis, _), cands = rule(fb, tk)\n        assert (tk, cls, bc) == (tk0, cls0, bc0) and cat0 == \"imports_file_not_found\" and cat == \"imports_ambiguous_match\", (name, \"ambiguous row\", k, Ub[k], Ua[k])\n        assert md.get(\"basis\") == basis and sorted(paths(md)) == cands and len(cands) >= 2, (name, \"ambiguous basis/candidates\", k, md, cands)\n        assert {x: v for x, v in md.items() if x not in (\"basis\", \"candidates\")} == md0, (name, \"carrier keys of the ambiguous row\", k)\n        got[basis] += 1\n    for k in set(Ub) - set(Ua) - added: raise AssertionError((name, \"an unresolved include vanished without an edge\", k))\n    assert dict(got) == want, (name, \"moved rows by basis\", dict(got), want)\n    print(name, \"moved rows by basis\", dict(got), \"C/C++ unresolved IMPORTS\", len(Ub), \"->\", len(Ua))\n    return Ea, Ua\ncheck(\"leveldb\", {})\ncheck(\"poco\", {\"unique_suffix\": 69, \"unique_basename\": 65})\nEa, Ua = check(\"nginx\", {\"unique_basename\": 791, \"ambiguous_basename\": 13})\ncfg = {k[1]: v for k, v in Ea.items() if k[0] == \"src/core/ngx_config.h\"}\nassert {ln: (v[0], v[1], v[2][\"basis\"]) for ln, v in cfg.items()} == {22: (\"src/os/unix/ngx_freebsd_config.h\", \"inferred\", \"unique_basename\"), 26: (\"src/os/unix/ngx_linux_config.h\", \"inferred\", \"unique_basename\"), 30: (\"src/os/unix/ngx_solaris_config.h\", \"inferred\", \"unique_basename\"), 34: (\"src/os/unix/ngx_darwin_config.h\", \"inferred\", \"unique_basename\"), 38: (\"src/os/win32/ngx_win32_config.h\", \"inferred\", \"unique_basename\"), 42: (\"src/os/unix/ngx_posix_config.h\", \"inferred\", \"unique_basename\")}, (\"witness src/core/ngx_config.h\", cfg)\nassert [v[:2] for k, v in Ua.items() if k[0] == \"src/core/ngx_config.h\"] == [(\"ngx_auto_headers.h\", \"imports_file_not_found\")], \"ngx_config.h:12 (generated, not indexed) stays unresolved\"\namb = [v for k, v in Ua.items() if k[0] == \"src/core/ngx_core.h\" and k[1] == 52]\nassert len(amb) == 1 and amb[0][:2] == (\"ngx_time.h\", \"imports_ambiguous_match\") and sorted(paths(amb[0][4])) == [\"src/os/unix/ngx_time.h\", \"src/os/win32/ngx_time.h\"], amb\nev = {k[1]: v[:2] for k, v in Ea.items() if k[0] == \"src/event/ngx_event.c\"}\nassert (ev[8], ev[9], ev[10]) == ((\"src/core/ngx_config.h\", \"inferred\"), (\"src/core/ngx_core.h\", \"inferred\"), (\"src/event/ngx_event.h\", \"static\")), ev\nprint(\"CIB-C05 OK\")\nPY",
        "cwd": ".",
        "environment": "fail-closed gate as CIB-C01; a before-binary built once from `git worktree add --detach /private/tmp/CPP-INCLUDE-BASENAME-1-headbase-wt HEAD` (CARGO_TARGET_DIR /private/tmp/CPP-INCLUDE-BASENAME-1-headbase-target) and the candidate release binary each index nginx (b0a4d0f), leveldb (7ee830d) and poco (49af400) into NEW builder-owned isolated roots /private/tmp/CPP-INCLUDE-BASENAME-1-{nginx,leveldb,poco}-{headbase,after} (created only if absent; deleted in CIB-C06), stdio transport, enrichment and retention off; the stores are read with `rg-store-diff.py` and python sqlite3 `immutable=1` only (refused if a -wal holds bytes); the manager's before-stores are not read here",
        "inputs": "F-CIB-ORACLE: the before/after comparison is `agent-manager/scripts/rg-store-diff.py` ALONE over every normalized field of files, nodes, edges and unresolved rows: nginx `--expect edges:791 --expect unresolved:804` (791 new inferred edges; 791 unresolved rows removed + 13 changed to `imports_ambiguous_match`), leveldb identity, poco `--expect edges:150 --expect unresolved:134` (69 static + 65 inferred FILE edges + 16 new MODULE->MODULE pairs; 134 unresolved rows removed; no existing module edge's is_type_only moves - all 492 are 0 and every new contributor is a runtime include). The base is a HEAD build, not the manager's v0.19.0 stores, because the queued predecessors legitimately move other facts of these corpora (CPP-ATTRIBUTE-MACRO-1A's C++ nodes on leveldb/poco, TEST-EDGE-SCOPE-1A's is_test, PYTHON-SUBMODULE-IMPORT-1's Python imports), which a whole-store diff would report as this slice's; the literals were measured on those stores (2026-09-24) and depend only on C/C++ include rows, which no predecessor changes. PLUS ONE QUERY over the moved rows: every new C/C++ include edge is exactly a formerly unresolved include; its basis, resolution and candidates equal RG-REQ-006-L11 recomputed from the base root's own file list; its extractor keys (`isTypeOnly`, `rawPath`) are kept; every changed unresolved row keeps target, classification and basis code and gains basis and every candidate; no inferred IMPORTS edge touches a non-FILE node; per basis nginx unique_basename 791 / ambiguous_basename 13, poco unique_suffix 69 / unique_basename 65, leveldb none. Witnesses: `src/core/ngx_config.h:22,26,30,34` (freebsd, linux, solaris, darwin configs) and `:38`, `:42` -> inferred to their one file each; `:12` `ngx_auto_headers.h` unchanged; `src/core/ngx_core.h:52` `ngx_time.h` ambiguous with both candidates; `src/event/ngx_event.c:8-10` two inferred, one static."
      },
      "expected": "exit 0 and `CIB-C05 OK`: the whole stores differ by exactly the predicted rows and nothing else - every include an earlier stage resolves, every include the rule cannot reach and all of leveldb are preserved unchanged (no behavior change); nginx's module graph appears as inferred, investigable facts with their reason and candidate, never a guessed static edge; poco gains exactly the predicted static and inferred includes - all under full isolation (RG-REQ-011-L06/L07)"
    },
    {
      "checkId": "CIB-C09",
      "obligationIds": [
        "RG-REQ-002-L11",
        "RG-REQ-004-L12",
        "RG-REQ-004-L02",
        "RG-REQ-006-L12",
        "RG-REQ-002-L01",
        "RG-REQ-002-L02",
        "RG-REQ-002-L03",
        "RG-REQ-011-L06",
        "P-CIB-01",
        "P-CIB-04"
      ],
      "owner": "builder",
      "method": {
        "kind": "command",
        "command": "test -d /private/tmp/CPP-INCLUDE-BASENAME-1-nginx-after && RB=\"$PWD/rust/target/release\" && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/nginx' && export RMAP_STATE_ROOT=/private/tmp/CPP-INCLUDE-BASENAME-1-nginx-after RMAP_SOCKET_PATH=/private/tmp/CPP-INCLUDE-BASENAME-1-nginx-after/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off PATH=\"$RB:$PATH\" && rmap modules deps > /tmp/cib-deps.txt 2> /tmp/cib-deps.txt.err ) && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/nginx' && export RMAP_STATE_ROOT=/private/tmp/CPP-INCLUDE-BASENAME-1-nginx-after RMAP_SOCKET_PATH=/private/tmp/CPP-INCLUDE-BASENAME-1-nginx-after/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off PATH=\"$RB:$PATH\" && rmap modules deps --json > /tmp/cib-deps.json 2> /tmp/cib-deps.json.err ) && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/nginx' && export RMAP_STATE_ROOT=/private/tmp/CPP-INCLUDE-BASENAME-1-nginx-after RMAP_SOCKET_PATH=/private/tmp/CPP-INCLUDE-BASENAME-1-nginx-after/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off PATH=\"$RB:$PATH\" && rmap modules deps --include-inferred --json > /tmp/cib-deps-inc.json 2> /tmp/cib-deps-inc.json.err ) && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/nginx' && export RMAP_STATE_ROOT=/private/tmp/CPP-INCLUDE-BASENAME-1-nginx-after RMAP_SOCKET_PATH=/private/tmp/CPP-INCLUDE-BASENAME-1-nginx-after/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off PATH=\"$RB:$PATH\" && rmap imports src/core/ngx_config.h > /tmp/cib-imp-cfg.txt 2> /tmp/cib-imp-cfg.txt.err ) && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/nginx' && export RMAP_STATE_ROOT=/private/tmp/CPP-INCLUDE-BASENAME-1-nginx-after RMAP_SOCKET_PATH=/private/tmp/CPP-INCLUDE-BASENAME-1-nginx-after/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off PATH=\"$RB:$PATH\" && rmap imports src/core/ngx_config.h --json > /tmp/cib-imp-cfg.json 2> /tmp/cib-imp-cfg.json.err ) && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/nginx' && export RMAP_STATE_ROOT=/private/tmp/CPP-INCLUDE-BASENAME-1-nginx-after RMAP_SOCKET_PATH=/private/tmp/CPP-INCLUDE-BASENAME-1-nginx-after/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off PATH=\"$RB:$PATH\" && rmap imports src/event/ngx_event.c > /tmp/cib-imp-ev.txt 2> /tmp/cib-imp-ev.txt.err ) && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/nginx' && export RMAP_STATE_ROOT=/private/tmp/CPP-INCLUDE-BASENAME-1-nginx-after RMAP_SOCKET_PATH=/private/tmp/CPP-INCLUDE-BASENAME-1-nginx-after/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off PATH=\"$RB:$PATH\" && rmap imports src/event/ngx_event.c --json > /tmp/cib-imp-ev.json 2> /tmp/cib-imp-ev.json.err ) && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/nginx' && export RMAP_STATE_ROOT=/private/tmp/CPP-INCLUDE-BASENAME-1-nginx-after RMAP_SOCKET_PATH=/private/tmp/CPP-INCLUDE-BASENAME-1-nginx-after/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off PATH=\"$RB:$PATH\" && rmap trust --json > /tmp/cib-trust.json 2> /tmp/cib-trust.json.err ) && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/nginx' && export RMAP_STATE_ROOT=/private/tmp/CPP-INCLUDE-BASENAME-1-nginx-after RMAP_SOCKET_PATH=/private/tmp/CPP-INCLUDE-BASENAME-1-nginx-after/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off PATH=\"$RB:$PATH\" && rmap modules list --json > /tmp/cib-mlist.json 2> /tmp/cib-mlist.json.err ) && python3 - <<'PY'\nimport json, re\ndef val(p):\n    j = json.load(open(p)); return j.get(\"value\", j) if isinstance(j, dict) else j\ndef find(o, key):\n    if isinstance(o, dict):\n        for k, v in o.items():\n            if k == key: yield v\n            yield from find(v, key)\n    elif isinstance(o, list):\n        for v in o: yield from find(v, key)\nassert val(\"/tmp/cib-deps.json\")[\"results\"] == [], \"the default module view shows no inferred edge\"\nh = open(\"/tmp/cib-deps.txt\").read(); assert \"inferred\" in h and \"--include-inferred\" in h, (\"the default view states the inferred remainder with its flag\", h[-800:])\ngot = sorted((e[\"source\"], e[\"target\"], e[\"import_count\"]) for e in val(\"/tmp/cib-deps-inc.json\")[\"results\"])\nassert got == sorted([(\"src/http\", \"src/core\", 225), (\"src/os\", \"src/core\", 163), (\"src/event\", \"src/core\", 116), (\"src/stream\", \"src/core\", 65), (\"src/mail\", \"src/core\", 38), (\"src/os\", \"src/event\", 22), (\"src/mail\", \"src/event\", 16), (\"src/core\", \"src/event\", 9), (\"src/core\", \"src/os\", 6), (\"src/http\", \"src/event\", 4), (\"src/stream\", \"src/event\", 3)]), (\"nginx module graph under --include-inferred\", got)\nCFG = [\"src/os/unix/ngx_darwin_config.h\", \"src/os/unix/ngx_freebsd_config.h\", \"src/os/unix/ngx_linux_config.h\", \"src/os/unix/ngx_posix_config.h\", \"src/os/unix/ngx_solaris_config.h\", \"src/os/win32/ngx_win32_config.h\"]\ncfg = open(\"/tmp/cib-imp-cfg.txt\").read()\nassert cfg == \"Imports: src/core/ngx_config.h\\n\\n6 imports\\n\\n\" + \"\".join(\"  %s  depth=1  inferred: unique basename → %s\\n\" % (p, p) for p in CFG), (\"imports src/core/ngx_config.h\", cfg)\nrows = val(\"/tmp/cib-imp-cfg.json\")[\"imports\"]\nassert sorted((r[\"symbol\"], r[\"resolution\"], json.dumps(r.get(\"reason\"), sort_keys=True)) for r in rows) == [(p, \"inferred\", json.dumps({\"basis\": \"unique_basename\", \"candidates\": [p]}, sort_keys=True)) for p in CFG], rows\nev = open(\"/tmp/cib-imp-ev.txt\").read()\nfor line in (\"  src/core/ngx_config.h  depth=1  inferred: unique basename → src/core/ngx_config.h\\n\", \"  src/core/ngx_core.h  depth=1  inferred: unique basename → src/core/ngx_core.h\\n\", \"  src/event/ngx_event.h  depth=1  static\\n\"):\n    assert line in ev, (\"imports src/event/ngx_event.c row\", line, ev)\nst = [r for r in val(\"/tmp/cib-imp-ev.json\")[\"imports\"] if r[\"symbol\"] == \"src/event/ngx_event.h\"]\nassert len(st) == 1 and st[0][\"resolution\"] == \"static\" and \"reason\" not in st[0], (\"a row with no recorded reason serializes as before\", st)\nt = set(re.findall(r\"unresolved_imports=(\\d+)\", open(\"/tmp/cib-trust.json\").read()))\nm = set(v for v in find(val(\"/tmp/cib-mlist.json\"), \"unresolved_import_count\") if v is not None)\nassert t == {\"299\"} and m == {299}, (\"trust's unresolved-import figure equals the modules-list headline on one snapshot\", t, m)\nprint(\"CIB-C09 OK\")\nPY",
        "cwd": ".",
        "environment": "the candidate release binary on CIB-C05's nginx after-root /private/tmp/CPP-INCLUDE-BASENAME-1-nginx-after (stdio transport, enrichment and retention off); runs after CIB-C05, before CIB-C06",
        "inputs": "TEST-EDGE-SCOPE-1B's partition and this slice's reader: `modules deps --json` results empty (the default production static view has no inferred edge); `modules deps` names the inferred remainder and `--include-inferred`; `modules deps --include-inferred --json` equals the eleven module pairs of section 1 (667 imports); `imports src/core/ngx_config.h` prints exactly six rows `  <path>  depth=1  inferred: unique basename → <path>` and its JSON carries `reason {basis: unique_basename, candidates: [<path>]}` on each; `imports src/event/ngx_event.c` prints its two inferred rows with the reason and `  src/event/ngx_event.h  depth=1  static`, whose JSON row has no `reason` key; `trust --json`'s `unresolved_imports=` reason and `modules list --json`'s `unresolved_import_count` are both 299."
      },
      "expected": "exit 0 and `CIB-C09 OK`: the agent reads the inferred include as inferred with its reason and candidate, the default module view stays certain-only and states the remainder with its flag, a static row is preserved unchanged, and trust and modules state one unresolved-import count for the snapshot"
    },
    {
      "checkId": "CIB-C06",
      "obligationIds": [
        "RG-REQ-011-L06",
        "P-CIB-03"
      ],
      "owner": "builder",
      "method": {
        "kind": "command",
        "command": "test \"$(grep -E '^registry-before [0-9a-f]{64}$' .agent-manager/slices/CPP-INCLUDE-BASENAME-1/build-progress.md | tail -1 | cut -d' ' -f2)\" = \"$(shasum -a 256 \"$HOME/Library/Application Support/repo-graph/registry.json\" | cut -d' ' -f1)\" && git worktree remove --force /private/tmp/CPP-INCLUDE-BASENAME-1-headbase-wt && rm -rf /private/tmp/CPP-INCLUDE-BASENAME-1-nginx-headbase /private/tmp/CPP-INCLUDE-BASENAME-1-nginx-after /private/tmp/CPP-INCLUDE-BASENAME-1-leveldb-headbase /private/tmp/CPP-INCLUDE-BASENAME-1-leveldb-after /private/tmp/CPP-INCLUDE-BASENAME-1-poco-headbase /private/tmp/CPP-INCLUDE-BASENAME-1-poco-after /private/tmp/CPP-INCLUDE-BASENAME-1-headbase-target && test ! -e /private/tmp/CPP-INCLUDE-BASENAME-1-nginx-headbase && test ! -e /private/tmp/CPP-INCLUDE-BASENAME-1-nginx-after && test ! -e /private/tmp/CPP-INCLUDE-BASENAME-1-leveldb-headbase && test ! -e /private/tmp/CPP-INCLUDE-BASENAME-1-leveldb-after && test ! -e /private/tmp/CPP-INCLUDE-BASENAME-1-poco-headbase && test ! -e /private/tmp/CPP-INCLUDE-BASENAME-1-poco-after && test ! -e /private/tmp/CPP-INCLUDE-BASENAME-1-headbase-wt && test ! -e /private/tmp/CPP-INCLUDE-BASENAME-1-headbase-target && test -d /private/tmp/CPP-INCLUDE-BASENAME-1-nginx-before && test -d /private/tmp/CPP-ATTRIBUTE-MACRO-1-leveldb-before && test -d /private/tmp/DOCS-UNREADABLE-DECODE-1-poco-before && python3 - <<'PY'\nimport subprocess\nst = subprocess.run([\"git\", \"status\", \"--porcelain\", \"--untracked-files=all\"], capture_output=True, text=True, check=True).stdout.splitlines()\ngot = sorted(l[3:] for l in st if not l[3:].startswith(\".agent-manager/\"))\nassert got == ['rust/crates/classification/src/types.rs', 'rust/crates/daemon-runtime/src/livegraph_feed.rs', 'rust/crates/indexer/src/include_resolver.rs', 'rust/crates/indexer/src/orchestrator.rs', 'rust/crates/indexer/src/resolver.rs', 'rust/crates/repo-index/src/compose.rs', 'rust/crates/repo-index/tests/cpp_include_roots.rs', 'rust/crates/rgr/src/presentation/imports.rs', 'rust/crates/storage/src/queries.rs', 'rust/crates/trust/src/rules.rs', 'rust/crates/trust/src/service.rs'], (\"the tree holds exactly the eleven candidate paths\", got)\nwt = subprocess.run([\"git\", \"worktree\", \"list\", \"--porcelain\"], capture_output=True, text=True, check=True).stdout\nassert \"/private/tmp/CPP-INCLUDE-BASENAME-1-headbase-wt\" not in wt, \"the before-binary worktree is removed\"\nprint(\"CIB-C06 OK\")\nPY",
        "cwd": ".",
        "environment": "candidate tree, after every other check",
        "inputs": "the operator's registry digest recorded as `registry-before <sha256>` in build-progress.md at step 0 equals the digest now; the builder's six roots, the before-binary worktree and its target directory are removed; the manager's three before-roots are left in place; the tree holds exactly the eleven candidate paths (untracked files included)."
      },
      "expected": "exit 0: every proof was isolated - the operator's registry is preserved byte-identical - nothing the builder created remains, and the tree holds only the allocated paths"
    }
  ]
}
-->
# CPP-INCLUDE-BASENAME-1 — an include no existing stage resolves binds static only on a unique path suffix, inferred (marked, with its candidate and reason, visible on `imports <file>`) only on a unique basename, and is never guessed

Status: DRAFT (document item CPP-INCLUDE-BASENAME-1-PREP-3, revised by the builder 2026-09-24 under D-CIB-REVIEW-1 — see §9). Baseline: `docs/requirements/baselines/CPP-INCLUDE-BASENAME-1-INPUT-1.json` under RG-BOOTSTRAP-INPUT-9. Round-seven root cause RC-5; RG-REQ-006-L11 as ratified (option A, 2026-09-14; amended 2026-09-23 under RG-REQ-002-L11). Implementation is GATED on TOOLCHAIN-STALENESS-1's and TEST-EDGE-SCOPE-1B's implementation-review records at HEAD with `result == accepted` (CIB-C01 and CIB-C05 verify both, fail-closed): 1B's module surfaces render the inferred remainder this slice produces. At 9755ba47 neither record exists, and neither slice document is at HEAD. Maturity: PROTOTYPE.

## 0. Requirements allocation (the machine-readable block above is binding; this section explains it)

**Implements: RG-REQ-006-L11**, verbatim: "Where no include root resolves a ≥2-segment specifier, a UNIQUE path-suffix match over the indexed file list shall resolve it (the `build_java_suffix_index` shape); a non-unique match stays unresolved; a single-segment specifier is never suffix-matched as `static` — where exactly one indexed file has that basename (nginx `<ngx_core.h>`) it resolves as INFERRED (`unique_basename`), rendered and counted under RG-REQ-002-L11." Its criterion — "a resolver test that a unique 2-segment suffix resolves and a duplicated one stays unresolved; `no_suffix_guessing` stays green for single segments; … nothing that resolves today changes" — is CIB-C01 (tests by name, `no_suffix_guessing` by unchanged body) and CIB-C05 (the whole store differs by exactly the predicted rows on three corpora). Its field numbers ("gstreamer … vcmi +300; duckdb +1,618") name corpora this slice does not index; the field proof here is nginx, leveldb and poco (§2.3).

**Implements: RG-REQ-002-L11** for the include case (D-CIB-REVIEW-1 §4, D-CIB-REASON-1 = A): every candidate is recorded (`metadata_json.candidates`, FILE stable keys, on the inferred edge and on every ambiguous row); the reason is recorded (`metadata_json.basis`: `unique_basename`, `ambiguous_basename`, `ambiguous_suffix`; `unique_suffix` on the static edge); the edge is marked `resolution: inferred`, never fed to the persisted MODULE→MODULE graph and never read by the derived module edges' default view or trust's connectivity (both read `static` only — `storage/src/trust_impl.rs:810-811`); the module surfaces state it as a remainder with its flag (TEST-EDGE-SCOPE-1B, this slice's gate); and the agent-facing row `imports <file>` renders it as something to investigate — `inferred: unique basename → <candidate path>` — with `reason {basis, candidates}` in its JSON (§2.1 item 6). The L's per-slice test list ("the candidate set, the reason, the JSON representation, the default exclusion and the flag inclusion") is CIB-C01/C03 (candidates, reason), CIB-C08 (JSON and human row), CIB-C09 (default exclusion, flag inclusion, the rendered row on nginx).

**Changes: RG-REQ-002-L02** (D-CIB-REVIEW-1 §2, D-CIB-COUNT-1 = A, at the cause). Trust's unresolved-import figure and the `modules list` headline render the same quantity from one snapshot but read two definitions: the headline sums every category of the shared set `MODULES_LIST_UNRESOLVED_IMPORT_CATEGORIES` (defined `classification/src/types.rs:347-352`; read by `daemon-runtime/src/dispatch.rs:9383-9403`; pinned by the storage test whose doc is `storage/src/trust_impl.rs:1361-1368`), while trust's `sum_unresolved_imports` (`trust/src/rules.rs:390-399`) reads the single key `imports_file_not_found` under the doc "Sum unresolved counts for IMPORTS-family categories" — a name that does not match what it does. This slice makes trust read the same set (one definition, two readers), renames its test to what it proves and adds a same-snapshot seam (CIB-C03, CIB-C07, CIB-C09). Trust already depends on `repo-graph-classification` (`trust/Cargo.toml`), so no dependency edge is added. Movement, measured on the before-stores 2026-09-24 (the snapshot's `unresolved_breakdown` equals its row counts on every store): kafka +84 (`imports_ambiguous_suffix`), poco +368 (`imports_ambiguous_match`), grpc-java +8 (5 + 3 `imports_wildcard`), spring-petclinic +2, glamcrm +58; nginx after this slice reads 299 on both surfaces (286 `imports_file_not_found` + 13 `imports_ambiguous_match`) where HEAD's trust would read 286. **Propagation (F-CIB-TRUST-PROPAGATION):** the sum is not a leaf. It feeds `compute_import_graph_reliability` (`trust/src/service.rs:370-374`; `rules.rs:228-253`: any unresolved import makes the axis LOW with reason `unresolved_imports=N`), whose level feeds `compute_change_impact_reliability` (`service.rs:386-390`; `rules.rs:329-364`: LOW with reason `import_graph_reliability_low`) and the two caveats (`service.rs:240-253`); the axes are read by the trust overlay (`trust/src/overlay.rs:179-180` `has_degradation`, :312 the per-result marker `unresolved_import_pressure`), by the daemon's relationship-reliability next action (`daemon-runtime/src/reader_context.rs:213-220`), and by `orient`'s and `stats`' reliability lines (`rgr/src/presentation/orient_reliability.rs:57-62`, `stats.rs:442`). None of those readers' code changes; their output moves exactly when the corrected sum moves the axis. On a snapshot whose ONLY unresolved imports are non-legacy (a wildcard, an ambiguous suffix or match), HEAD reads 0, leaving import-graph and change-impact HIGH; this slice reads the true count and both axes become LOW with their reasons and caveats — the honest value RG-REQ-002-L10 requires, proven by `a_lone_non_legacy_unresolved_import_category_lowers_import_graph_and_change_impact` (CIB-C07). Measured: each of the 16 before-stores under /private/tmp — 11 distinct corpora: leveldb, OpenXcom, nginx, grpc-java, kafka, spring-petclinic, poco, django, FRAKTAG, amodx, glamcrm — has `imports_file_not_found > 0`, so on none of them does a level move — import-graph and change-impact are LOW before and after, and only the number in the reason `unresolved_imports=N` changes. **Trace:** this movement is allocated to RG-REQ-002-L02 (changes: one count, one number, its derived axes) and RG-REQ-002-L10 (preserved: the movement is stated); RG-REQ-009-L04 ("every reliability axis renders with its basis and exact downgrade reasons; a reason shall never outlive its cause") is the catalog's more specific home but lies outside this manifest's four parents and the packet's obligation set, so the allocation parser refuses it here — an operator note, not a blocker (§4).

**Decision records (named by the manifest):** D-CERTAINTY-MARK-1 (the human: mark, never guess, never drop), D-REFRESH-STALE-1 (the human, option B: explicit rebuild, a visible staleness line; each resolver/indexer slice bumps `INDEXER_VERSION`), D-CIB-REVIEW-1 (the operator's resolution of PREP's four decisions and three findings: REFRESH = A, COUNT = A, L06 = B, REASON = A; F-CIB-GATE, F-CIB-ORACLE, the witness).

**Preserves:** RG-REQ-006-L01/L02/L04 (Rust, Java, TS/Python stages — CIB-C01: the new stage is gated to C/C++ extractor edges and runs after every existing stage misses), L03 (no guessing, never-flip — CIB-C01 names and bodies, CIB-C05 whole-store diff), L05 (unresolved preserved, classified and counted — the ambiguous rows keep a category the headline counts, and trust now counts it too), L06–L10/L13 (deps machinery byte-identical — CIB-C02), L12 (`imports <file>` keeps every row it prints today; the listing still omits unresolved rows — L12's recorded gap, not changed here), RG-REQ-002-L01/L03–L10 (no default view asserts an inferred edge; universes and headlines stated; no swallowing read — the reason read renders an unreadable carrier as unreadable, CIB-C08; L10: the ship block states the movements below), RG-REQ-004-L02/L12 (an inferred edge participates only in the `--include-inferred` view; persisted module edges from certain imports only — CIB-C03, CIB-C05, CIB-C09), RG-REQ-011-L06/L07 (isolation — CIB-C05, CIB-C06).

| Id | Obligation | Proof |
|---|---|---|
| P-CIB-01 | every include an existing stage resolves (same directory, configured/derived roots, per-TU map, stable key, extensionless, repo prefix), every root-overlap ambiguity, every include the rule cannot reach and every non-C/C++ import is unchanged; the default module view never shows an inferred edge | CIB-C01 (gate and order tests), CIB-C03, CIB-C05 (whole-store diff equals the predicted rows), CIB-C09 (default `modules deps` empty) |
| P-CIB-02 | every existing include-resolver, import-stage and C++ include integration test is kept by name AND unchanged body; floors hold | CIB-C01, CIB-C02, CIB-C03 |
| P-CIB-03 | hygiene, isolation (registry digest), cleanup of everything the builder created, exactly eleven candidate paths | CIB-C04, CIB-C06 |
| P-CIB-04 | an `imports <file>` row for which this slice requires no basis — every `static` row without a basis this slice writes (every include edge at HEAD) and every row of a non-C/C++ extractor (PYTHON-SUBMODULE-IMPORT-1's `python_submodule` rows, rows without `basis`) — prints the same line and serializes the same JSON as at HEAD; a C/C++ `inferred` row (only this slice writes one; 0 on the three before-stores) is NOT covered by this preservation: a NULL carrier or one without `basis` is reported `reason {missing: …}` / `inferred (reason missing — re-index)`, never printed as bare `inferred`; the LiveGraph fastpath and the compare sidecar are unchanged | CIB-C08 (storage/rgr tests; existing bodies; the two ABSENT tests over other producers' rows; the five missing-reason counterexamples), CIB-C09 (`ngx_event.h` static row, no `reason` key) |
| P-CIB-05 | trust changes only the IMPORTS-family sum and what is derived from it — the import-graph axis (level, `unresolved_imports=N`), the change-impact axis (level, `import_graph_reliability_low`), their two caveats, and the overlay/daemon/rgr readers of those axes, which move only when the axis moves and whose code is unchanged; every field NOT derived from the sum is unchanged: the CALLS sums, the call-graph and dead-code axes, the category, classification and module rows; on a snapshot whose unresolved imports are all `imports_file_not_found` nothing moves | CIB-C07 (the propagation test; every other rules.rs and service.rs test by body; service.rs production code byte-identical), CIB-C02 (every other trust file byte-identical), CIB-C03 (seam) |

**Reported consequences (pre-stated movements, RG-REQ-002-L10):** nginx — 791 of 1,090 C/C++ unresolved includes become inferred edges with their reason and candidate; 13 become `imports_ambiguous_match` (were `imports_file_not_found`) with both candidates; the modules-list headline's "M imports unresolved" and trust's `unresolved_imports=` both read 299; `imports src/core/ngx_config.h` goes from 0 rows to 6 (§1); the six module candidates gain eleven dependency pairs (667 imports) under `--include-inferred` only, among them the candidate cycles `src/core ↔ src/event` and `src/core ↔ src/os` (inferred-only, so absent from the default view and named in 1B's remainder); trust's import-graph and change-impact levels stay LOW (only the reason's number moves: `unresolved_imports=1090` → `=299`). poco — 69 static edges (vendored `dependencies/cpptrace/src/**`), 65 inferred edges, C/C++ unresolved 3,801 → 3,667, persisted MODULE→MODULE 492 → 508 (16 new directory-group pairs, no existing pair's `is_type_only` moves), one new directory-group cycle `dependencies/cpptrace/src/platform ↔ dependencies/cpptrace/src/utils` (a certain fact: each includes the other), no module-candidate pair change; trust's figure 3,442 → 3,676 (the +368 coherence correction, less the 134 bound), axes LOW before and after. leveldb — no change. kafka, grpc-java, spring-petclinic, glamcrm — trust's unresolved-import figure rises to the headline's (+84, +8, +2, +58); their import-graph and change-impact axes are LOW before and after (each has `imports_file_not_found > 0`), so only the reason's number moves; no other change. On a snapshot whose only unresolved imports are non-legacy — none among the measured corpora — both axes move HIGH → LOW with their caveats (§0 Propagation). **The `INDEXER_VERSION` bump (D-CIB-L06-1 = B — a reported consequence, not an allocated change; TOOLCHAIN-STALENESS-1 owns RG-REQ-001-L06 and proves its mechanism):** a store indexed before this slice reports the `indexer` family stale on `orient`/`check` until `rmap repo rebuild <path>`; a REFRESH by the new binary already re-resolves every include of the snapshot (§3), yet the line stays after it because the refresh's `carried_forward` retains the older indexer version whenever a file is copied (RG-REQ-001-L06, conservative by ratification) — it clears on rebuild.

## 1. Problem (ROOT-CAUSED — RC-5, 2026-09-23; re-measured 2026-09-24 on the before-stores)

`indexer/src/include_resolver.rs IncludeResolver::resolve` (:100-187) tries the source file's directory (quoted includes), the configured roots and the derived roots (every `include`/`inc` directory — CPP-INCLUDE-ROOTS-1, `derive_include_roots` :245, pooled in `build_include_resolution_map` :274) and otherwise answers Unresolved; `resolver.rs resolve_target` (:514-577) then falls through to `resolve_import_target`'s stable-key, extensionless, per-TU and repo-prefix stages (:661-724), and Java's suffix stage for Java edges only. No stage matches a path suffix or a basename for C/C++: the ≥2-segment unique-suffix fallback RG-REQ-006-L11 ratified on 2026-09-14 was never built. (`no_suffix_guessing`, include_resolver.rs:739, does NOT pin that absence: it asserts that the extensionless specifier `foo` never completes to `include/foo.h` — a rule the exact-basename stage keeps.)

nginx (b0a4d0f) has no `include/` directory; its headers live beside their sources under `src/core`, `src/event`, `src/http`, `src/os/unix` … and are found by the compiler through `auto/`-generated `-I` flags. A file therefore resolves the headers of its own directory (the stored include rows all carry `metadata_json {"isTypeOnly":false}`, so resolver.rs:520's `is_system = metadata_json.is_none()` is false and angle includes also try the same directory — OBSERVED in the store; unchanged here) and nothing else. Measured on /private/tmp/CPP-INCLUDE-BASENAME-1-nginx-before (v0.19.0; python sqlite3 `immutable=1`; empty WAL): 355 static IMPORTS edges, all FILE→FILE and intra-directory; 1,090 unresolved C/C++ IMPORTS, every one `imports_file_not_found` (1,089 `no_supporting_signal` + 1 `relative_import_target_unresolved` — `src/event/quic/ngx_event_quic_bpf_code.c:6` `"ngx_bpf.h"`); the snapshot's `unresolved_breakdown` is `{"imports_file_not_found": 1090}`; of those, 791 single-segment specifiers with exactly one indexed file of that basename (`ngx_config.h` 296, `ngx_core.h` 295, `ngx_http.h` 83, `ngx_event.h` 70, `nginx.h` 9, `ngx_event_connect.h` 6, …), 13 single-segment specifiers whose basename exists twice — `ngx_alloc.h`, `ngx_atomic.h`, `ngx_dlopen.h`, `ngx_errno.h`, `ngx_files.h`, `ngx_os.h`, `ngx_process.h`, `ngx_process_cycle.h`, `ngx_shmem.h`, `ngx_socket.h`, `ngx_thread.h`, `ngx_time.h`, `ngx_user.h`, each under `src/os/unix/` and `src/os/win32/`, each included once from `src/core/ngx_core.h` — 156 single-segment with no indexed basename (`stddef.h`, `stdlib.h`, `errno.h`, the generated `ngx_auto_config.h` and `ngx_auto_headers.h` absent from the checkout …) and 130 multi-segment system or external headers with no indexed suffix (`sys/types.h`, `netinet/tcp.h`, `arpa/inet.h`, `openssl/rand.h` …); 17 directory MODULE nodes with 0 MODULE→MODULE edges; 6 inferred module candidates (`src/core`, `src/event`, `src/http`, `src/mail`, `src/os`, `src/stream`) with 0 cross-candidate edges. Never worked.

**Code under analysis — the failure witness, before (the before-store) and after (the rule; asserted by CIB-C05 and CIB-C09).** `src/core/ngx_config.h:21-42` (nginx b0a4d0f, quoted from the checkout):
```
    21	#if (NGX_FREEBSD)
    22	#include <ngx_freebsd_config.h>
    25	#elif (NGX_LINUX)
    26	#include <ngx_linux_config.h>
    29	#elif (NGX_SOLARIS)
    30	#include <ngx_solaris_config.h>
    33	#elif (NGX_DARWIN)
    34	#include <ngx_darwin_config.h>
    37	#elif (NGX_WIN32)
    38	#include <ngx_win32_config.h>
    42	#include <ngx_posix_config.h>
```
Before: every one of these rows is an unresolved `imports_file_not_found` (`unknown`, `no_supporting_signal`, `metadata_json {"isTypeOnly":false}`, extractor `c-core:0.1.0`), and the file has no IMPORTS edge at all, so `rmap imports src/core/ngx_config.h` prints `0 imports`. Each basename has exactly one indexed file: `src/os/unix/ngx_freebsd_config.h`, `src/os/unix/ngx_linux_config.h`, `src/os/unix/ngx_solaris_config.h`, `src/os/unix/ngx_darwin_config.h` (the four OS configs of :22-34), `src/os/win32/ngx_win32_config.h` (:38) and `src/os/unix/ngx_posix_config.h` (:42). After: each is ONE IMPORTS edge `resolution: inferred`, `metadata_json {"isTypeOnly":false,"basis":"unique_basename","candidates":["<repo_uid>:<that path>:FILE"]}`, and `rmap imports src/core/ngx_config.h` prints (rows ordered by the target's name, `find_imports`'s `ORDER BY n.name`):
```
Imports: src/core/ngx_config.h

6 imports

  src/os/unix/ngx_darwin_config.h  depth=1  inferred: unique basename → src/os/unix/ngx_darwin_config.h
  src/os/unix/ngx_freebsd_config.h  depth=1  inferred: unique basename → src/os/unix/ngx_freebsd_config.h
  src/os/unix/ngx_linux_config.h  depth=1  inferred: unique basename → src/os/unix/ngx_linux_config.h
  src/os/unix/ngx_posix_config.h  depth=1  inferred: unique basename → src/os/unix/ngx_posix_config.h
  src/os/unix/ngx_solaris_config.h  depth=1  inferred: unique basename → src/os/unix/ngx_solaris_config.h
  src/os/win32/ngx_win32_config.h  depth=1  inferred: unique basename → src/os/win32/ngx_win32_config.h
```
with `"reason": {"basis": "unique_basename", "candidates": ["src/os/unix/ngx_darwin_config.h"]}` (and so on) on each `--json` row. The candidate printed after `→` is the recorded candidate set, read from the carrier — not the edge's target — so the line shows the agent exactly what the index could and could not determine: one file of that name, bound by name alone, to be confirmed against the build's `-I` flags.
- Residual, no indexed file: `src/core/ngx_config.h:12` `#include <ngx_auto_headers.h>` (generated by `auto/configure`, absent from the checkout) → unchanged `imports_file_not_found`; likewise `stddef.h`, `sys/types.h`.
- Residual, ambiguous: `src/core/ngx_core.h:52` `#include <ngx_time.h>` → before `imports_file_not_found`; after unresolved `imports_ambiguous_match`, `basis: ambiguous_basename`, candidates `src/os/unix/ngx_time.h`, `src/os/win32/ngx_time.h` — never a pick (not listed by `imports <file>`, whose unresolved rows are RG-REQ-006-L12's recorded gap).
- Mixed file: `src/event/ngx_event.c:8` `#include <ngx_config.h>`, `:9` `#include <ngx_core.h>` → before unresolved; `:10` `#include <ngx_event.h>` → static `src/event/ngx_event.h` (same directory). After `rmap imports src/event/ngx_event.c`:
  ```
    src/core/ngx_config.h  depth=1  inferred: unique basename → src/core/ngx_config.h
    src/core/ngx_core.h  depth=1  inferred: unique basename → src/core/ngx_core.h
    src/event/ngx_event.h  depth=1  static
  ```
  the static row byte-identical to today's and its JSON row without a `reason` key (P-CIB-04).
- Residual, quoted include whose directory lookup missed: `src/event/quic/ngx_event_quic_bpf_code.c:6` `#include "ngx_bpf.h"` (only `src/core/ngx_bpf.h` exists) → inferred to `src/core/ngx_bpf.h`, `unique_basename` — the same mark as an angle include.
- `src/core/nginx.c:8-9` (`#include <ngx_config.h>` / `#include <ngx_core.h>`) is NOT a witness: it sits beside both headers and resolves static today (same directory) — unchanged.
- Module surfaces: before, `rmap modules deps` has no edges. After, the default view still has none and states the inferred remainder with `--include-inferred` (1B's line); `modules deps --include-inferred` lists `src/http → src/core` (225), `src/os → src/core` (163), `src/event → src/core` (116), `src/stream → src/core` (65), `src/mail → src/core` (38), `src/os → src/event` (22), `src/mail → src/event` (16), `src/core → src/event` (9), `src/core → src/os` (6), `src/http → src/event` (4), `src/stream → src/event` (3).
- `no_suffix_guessing` as found (include_resolver.rs:738-748): files `src/main.c`, `include/foo.h`; `resolver.resolve("src/main.c", "foo", false, …)` → `Unresolved` ("#include "foo" should NOT match "foo.h""). Kept byte-identical.

**Code under analysis — trust's count, before and after (D-CIB-COUNT-1).** `rust/crates/trust/src/rules.rs:390-399` at HEAD:
```rust
/// Sum unresolved counts for IMPORTS-family categories.
///
/// Mirror of `sumUnresolvedImports` from `rules.ts:330`.
pub(crate) fn sum_unresolved_imports(diagnostics: &ExtractionDiagnostics) -> u64 {
    diagnostics
        .unresolved_breakdown
        .get("imports_file_not_found")
        .copied()
        .unwrap_or(0)
}
```
Before: on poco the headline says 3,810 imports unresolved (3,442 + 368) and trust's import-graph reason says `unresolved_imports=3442`; on kafka 44,788 vs 44,704. After: the function sums `unresolved_breakdown` over every category of `MODULES_LIST_UNRESOLVED_IMPORT_CATEGORIES`, keyed by each category's serialized name (the key the indexer writes, `orchestrator.rs:1091-1095`; an absent key is a category with no rows, the same reading `sum_unresolved_calls` applies at :371-387); poco's two surfaces both read 3,676 after this slice, kafka's 44,788, nginx's 299. The doc line stays true; the test `sum_unresolved_imports_picks_imports_family` (:994-1006, which asserted `7` for a breakdown of one `imports_file_not_found` — the narrow behaviour under the family's name) becomes `sum_unresolved_imports_sums_every_category_of_the_shared_imports_set`.

## 2. Contract

### 2.1 The fix, at the cause — one gated trailing stage over one index; certainty carried by the existing axes; the reason carried to the row; one count definition
1. **`include_resolver.rs` — the pure lookup.** `build_include_resolution_map` builds, once, from the same `file_paths` the roots are derived from, a basename-bucketed index (the `JavaSuffixIndex` shape, resolver.rs:271-289: basename → sorted repo-relative paths). A new pure method on `IncludeResolutionMap` answers, for a specifier: (a) **declined** — the specifier contains `:` (a stable-key form) or a `..` component, or is empty after dropping `.`/empty components; (b) **two or more segments**: the indexed paths equal to the joined segments or ending in `/` + them (a directory boundary — `util/foo.h` never matches `myutil/foo.h`); exactly one → unique suffix; more → ambiguous suffix with every candidate; none → no match; (c) **one segment**: the indexed paths whose basename equals the segment EXACTLY (no extension completion — `foo` never matches `foo.h`); one → unique basename (an INFERRED candidate, never a suffix match); more → ambiguous basename with every candidate; none → no match. Candidates are sorted; the answer does not depend on file order. `IncludeResolver::resolve` and every existing test are unchanged.
2. **`resolver.rs` — Stage 6, and only there.** In `resolve_target`'s Imports arm, when `resolve_import_target` returns `None` AND the edge's extractor is C/C++ (`is_c_family_extractor`, :61-63) AND `index.include_resolver` is present, ask the lookup for `edge.target_key` (so it runs only after the include resolver answered Unresolved — an include-root Ambiguous still returns early at :531-534 — and after stages 1-4 missed; Java keeps its own arm; every other language never reaches it). Outcomes, each through `resolve_edges`: unique suffix whose FILE node exists → a `ResolvedEdge` with the edge's own resolution (`static`), pushed to `resolved_import_pairs` like any certain import; unique basename whose FILE node exists → a `ResolvedEdge` with `resolution: Inferred`, NOT pushed to `resolved_import_pairs` (an inferred import never feeds the persisted module graph — the PYTHON-SUBMODULE-IMPORT-1 rule); ambiguous suffix or basename → an unresolved row with category `ImportsAmbiguousMatch`; no match, declined, or a candidate without a FILE node → `Unresolved` exactly as today. `metadata_json` of every bound or ambiguous row is the extractor's object with two keys merged in — `basis` (`unique_suffix` | `unique_basename` | `ambiguous_suffix` | `ambiguous_basename`) and `candidates` (the candidates' FILE stable keys) — every existing key (`isTypeOnly`, `rawPath`) kept; a present `metadata_json` that is not a JSON object declines the stage (the row stays as today — never overwritten, never coerced). Whether the outcomes reuse a variant PYTHON-SUBMODULE-IMPORT-1 added or add their own is the builder's local choice; the stated behavior is not.
3. **Refresh (D-CIB-REFRESH-1 = A — the truth, tested).** A refresh recomputes every binding; only extraction is copied forward. `storage/src/indexer_impl.rs:966-1020` copies an unchanged file's `extraction_edges` (the include's spelled `target_key`, its line, its carrier) with remapped source uids; `indexer/src/orchestrator.rs:1008-1039` then streams EVERY `extraction_edges` row of the refresh snapshot (`query_extraction_edges_batch`, :1011) — copied-forward rows included — through `resolve_edges` (:1039) against an index whose include map is built from the full current file list (`all_file_paths` = copied + extracted + contract files, :1965-1967; `build_include_resolution_map(all_file_paths, …)`, :883). So a refresh by this binary binds a unique basename, and turns it ambiguous when a second file of that basename is added — whether or not the including file changed. No new mechanism: Stage 6 lives inside `resolve_edges`, which the refresh already runs. Test: `refresh_turns_an_inferred_unique_basename_include_ambiguous_when_a_second_header_is_added` (CIB-C03). What the refresh does NOT clear is the staleness line (§0).
4. **`trust/src/rules.rs` — one count definition (D-CIB-COUNT-1 = A).** `sum_unresolved_imports` sums over `repo_graph_classification::types::MODULES_LIST_UNRESOLVED_IMPORT_CATEGORIES` as in §1; its test is renamed and asserts one count in each of the four categories plus a CALLS category sums to four. One test is added to `trust/src/service.rs`'s test module — `a_lone_non_legacy_unresolved_import_category_lowers_import_graph_and_change_impact`: `compute_trust_report` on `minimal_input()` (all axes HIGH, service.rs:1035-1052) with diagnostics whose only unresolved category is `imports_wildcard: 2` yields import_graph LOW with reasons exactly `["unresolved_imports=2"]`, change_impact LOW with reasons exactly `["import_graph_reliability_low"]`, both caveats present, call_graph and dead_code HIGH. service.rs's production code is byte-identical; nothing else in the trust crate changes (CIB-C02, CIB-C07). The shared set's name names only its first reader; it is kept (renaming a public constant read by `dispatch.rs` is a boundary-touching rename outside this allocation — §8) and its doc comment is updated to name both readers.
5. **`classification/src/types.rs` — documentation only.** `ImportsAmbiguousMatch`'s doc comment (:268-270) is widened from "exactly … root overlap" to the three C/C++ ambiguity shapes (root overlap; non-unique suffix; non-unique basename — the shape in `metadata_json.basis`); `MODULES_LIST_UNRESOLVED_IMPORT_CATEGORIES`'s doc comment (:325-346, which says "the sole current production caller is `handle_modules_list`") names trust's `sum_unresolved_imports` as its second reader. No variant, serde name or set entry changes (CIB-C02), so the classifier (which sends `ImportsAmbiguousMatch` to its identifier path, `unresolved_classifier.rs:102-107`, whose `unknown` / `no_supporting_signal` verdict the 13 nginx rows already carry) and trust's `IMPORTS (ambiguous match)` label are untouched.
6. **`imports <file>` — the reason on the agent-facing row (D-CIB-REASON-1 = A).** Verified path: the daemon's `imports` handler serves a C/C++ file from SQLite on both engines — `engine = sqlite` serializes `storage.find_imports(…)`'s rows verbatim (`daemon-runtime/src/dispatch.rs:1759-1777`), and the default `auto` engine falls back to the same serialization (`livegraph_feed.rs:1760-1763`) because the LiveGraph import view serves TS files only (the precondition gate at :1734-1742); `rmap imports --json` prints the daemon value raw (`rgr/src/commands/graph.rs:848-858`) and the human form decodes it into `ImportEntry` (`rgr/src/presentation/imports.rs:28-52`). So the reason needs a storage read and a renderer, no handler change:
   - **Storage (`storage/src/queries.rs find_imports`, :1726-1771).** The query also selects `e.metadata_json` and the target node's `stable_key`; `ImportResult` (:119-133) gains `reason`, serde-skipped when absent (so every row without it serializes exactly as at HEAD). ONE validity rule (F-CIB-REASON-VALIDITY), judged against the row's own edge — a reason is evidence only when it agrees with the fact it explains. **Which rows REQUIRE a reason (F-CIB-MISSING-REASON — the ownership rule):** a row THIS slice produced — an `inferred` IMPORTS edge whose extractor is C/C++ (the edge's `e.extractor`, already selected at :1734 and carried on the row as `evidence`, :131-132/:1752-1763, starting `c-core:` or `cpp-core:` — the same cross-boundary provenance string `resolver.rs:49-63` gates Stage 6 on; storage compares the string and gains no dependency) — must carry a PRESENT-VALID reason; for it, no carrier state is ABSENT. Only this slice writes such an edge: the three before-stores hold 0 inferred C/C++ IMPORTS edges and 0 C/C++ IMPORTS edges with a NULL carrier (nginx 355 static `c-core`; leveldb 498 static C/C++; poco 11,578 static C/C++ — python sqlite3 `immutable=1`, 2026-09-24). ABSENT (no `reason` key; the row serializes exactly as at HEAD) is limited to rows for which this slice requires no basis — every `static` row and every row of another extractor — with a NULL carrier, an object without `basis`, or a string `basis` outside the four this slice writes (PYTHON-SUBMODULE-IMPORT-1's `python_submodule` rows stay as that slice ships them; every include edge at HEAD is static with `{"isTypeOnly":false}` and no `basis`). PRESENT-MISSING — a C/C++ `inferred` edge whose carrier is NULL, or a JSON object without a `basis` key: the mandatory evidence was lost → `"reason": {"missing": "no reason carrier"}` (NULL) or `"reason": {"missing": "reason carrier has no basis"}` (object without `basis`). The shape reuses this surface's one unknown-with-reason form — the tagged `reason` object whose `unreadable` arm this slice already defines — rather than `reason: null` plus a sibling `reason_state` field: one key, one decode path, and `null` stays unused, so absent (no key) and missing (`{missing}`) remain distinguishable by key presence alone. PRESENT-VALID only when every clause holds: `basis` is `unique_basename` and the edge is `inferred`, or `basis` is `unique_suffix` and the edge is `static`; `candidates` is an array of exactly ONE string; that string equals the edge's own target stable key; the target node is a FILE → `"reason": {"basis": "<basis>", "candidates": ["<target's repo-relative path>"]}`. PRESENT-UNREADABLE otherwise, with what failed → `"reason": {"unreadable": "<what failed>"}` — never dropped, never defaulted, never presented as a reason (RG-REQ-002-L04): structural — the carrier is not a JSON object; `basis` is not a string; `candidates` is missing, not an array, holds a non-string, or is empty; contradictory — more than one candidate under a unique basis; a candidate other than the edge's target; a target that is not a FILE; a basis that disagrees with the edge's resolution (`unique_basename` on a static edge, `unique_suffix` on an inferred one); an `ambiguous_basename`/`ambiguous_suffix` basis on a bound edge (an ambiguous include is never an edge); a string `basis` outside the four on a C/C++ `inferred` edge (a basis this slice does not write on an edge only this slice produces). The read keeps its existing responsibility (the per-file import listing) in a 6,310-line file; no new responsibility is appended.
   - **Daemon (`daemon-runtime/src/livegraph_feed.rs`).** No production change. Its test `imports_auto_body_serves_livegraph_or_falls_back` builds an `ImportResult` literal (:4983-4995), which gains exactly `reason: None,`.
   - **Renderer (`rgr/src/presentation/imports.rs`).** `ImportEntry` decodes `reason` (absent → none). ONE row formatter serves the default listing (`ImportsResponse`, :92-99) and the compare listing (`ImportsCompareResponse`, :323-333), which today duplicate the row form — so the reason cannot reach one and not the other. Because it renders JSON it did not produce (a daemon of another build, a hand-edited capture), it re-checks the one agreement it prints: an `inferred` row whose reason is `unique_basename` with exactly one candidate equal to the row's `file` prints `  <path>  depth=<d>  inferred: unique basename → <candidate>`; an `inferred` row whose reason is `unreadable` prints `  <path>  depth=<d>  inferred: reason unreadable (<what failed>)`; an `inferred` row whose reason disagrees with the row — another candidate, several candidates, `unique_suffix`, no `basis`, a basis this build does not read — prints `  <path>  depth=<d>  inferred: reason unreadable (<what disagrees>)`, never the reason; an `inferred` row whose reason is `missing` — or that carries no `reason` key while its `evidence` names a C/C++ extractor (`c-core:`/`cpp-core:`; JSON this build did not produce) — prints `  <path>  depth=<d>  inferred (reason missing — re-index)`, never bare `inferred` (a re-index — refresh or `rmap repo rebuild <path>` — re-resolves the include and rewrites its carrier, §2.1 item 3); every other row — every `static` row (a `unique_suffix` reason stays in JSON; the static fact is the line), every row of another extractor without a reason (and every row whose `evidence` is empty, which names no producer) — prints exactly as at HEAD. The file is 841 lines; the row form is its existing responsibility.
7. **Version (D-REFRESH-STALE-1 = B; D-CIB-L06-1 = B).** `INDEXER_VERSION` (orchestrator.rs:59) moves up exactly one minor from HEAD's value at implementation time, with its declared copy `repo-index/src/compose.rs:1653`; both files differ from HEAD by exactly that line (CIB-C01). The staleness line it triggers is TOOLCHAIN-STALENESS-1's (§0).
8. **Tests.** Nine include_resolver unit tests and six resolver unit tests (CIB-C01); four repo-index end-to-end tests — the two binding shapes, the refresh, the trust↔headline seam (CIB-C03); the renamed trust test and the propagation test (CIB-C07); seven storage tests (absent, another basis, valid, malformed structure, `find_imports_reason_that_contradicts_its_edge_is_unreadable_never_evidence`, and one per missing carrier state: `find_imports_c_family_inferred_row_with_a_null_carrier_reports_its_reason_missing`, `find_imports_c_family_inferred_row_whose_carrier_lacks_a_basis_reports_its_reason_missing`) and nine renderer tests (incl. `inferred_row_whose_reason_disagrees_with_the_row_is_unreadable_never_evidence` and the three missing-reason tests `inferred_row_whose_reason_is_missing_for_a_null_carrier_says_re_index`, `inferred_row_whose_reason_is_missing_for_a_carrier_without_basis_says_re_index`, `c_family_inferred_row_without_a_reason_key_says_reason_missing_never_bare_inferred`) (CIB-C08).
9. **Nothing else:** no extractor, classifier, agent or module-queries change; no production change in daemon-runtime; no reading of `auto/` or `compile_commands.json`; `IncludeResolver::resolve` and the v1.0 stages unchanged.

### 2.2 Evidence taxonomy
| Input | Outcome | Bound test / check |
|---|---|---|
| ≥2 segments, one indexed file ends with the suffix at a directory boundary, no earlier stage resolved it | static edge, `basis: unique_suffix`, in the persisted module graph | `unique_multi_segment_suffix_matches_one_indexed_file`, `include_unique_multi_segment_suffix_resolves_static_with_its_basis`, `unique_multi_segment_suffix_include_resolves_static`; poco 69 |
| ≥2 segments, several files end with the suffix | unresolved `imports_ambiguous_match`, `basis: ambiguous_suffix`, every candidate | `non_unique_multi_segment_suffix_lists_every_candidate`, `include_non_unique_suffix_or_basename_stays_unresolved_ambiguous_with_candidates` |
| ≥2 segments, a file ends with the text but not at a `/` boundary | no match | `multi_segment_suffix_matches_only_at_a_directory_boundary`; `subpath_does_not_match_basename` (unchanged) |
| ≥2 segments, no indexed suffix (`sys/types.h`) | unchanged | `include_suffix_stage_runs_only_after_every_earlier_stage_misses`; nginx 130 |
| 1 segment, exactly one file of that basename | INFERRED edge, `basis: unique_basename`, candidate recorded, not in the persisted module graph | `single_segment_unique_basename_is_an_inferred_candidate_never_a_suffix_match`, `include_unique_basename_resolves_inferred_with_its_candidate_and_basis`, `nginx_style_single_segment_include_resolves_inferred_by_unique_basename`; nginx 791, poco 65 |
| 1 segment, several files of that basename | unresolved `imports_ambiguous_match`, `basis: ambiguous_basename`, every candidate | `single_segment_non_unique_basename_lists_every_candidate`; the same two resolver/integration tests; nginx 13 |
| 1 segment, no file of that basename (`stddef.h`, `ngx_auto_headers.h`) | unchanged | `single_segment_with_no_indexed_basename_matches_nothing`; nginx 156 |
| extensionless `foo` beside `foo.h` | no match | `basename_match_is_exact_never_extension_completion`; `no_suffix_guessing` (unchanged body) |
| a `..` component, a `:` key form, an empty specifier | declined, unchanged | `dot_dot_or_stable_key_specifier_is_declined`; poco `../deps/sljit/sljit_src/sljitLir.c` |
| angle vs quoted | the same rule | the nginx witness rows (angle) and `ngx_bpf.h` (quoted) |
| same directory, configured/derived root, per-TU, stable key, extensionless, repo prefix already resolve | that stage's answer; Stage 6 never runs | `include_suffix_stage_runs_only_after_every_earlier_stage_misses`; the 33 include_resolver and ten import-stage tests by body; CIB-C05 |
| include-root overlap (two roots) | `imports_ambiguous_match` as today, no `basis` | `include_suffix_stage_runs_only_after_every_earlier_stage_misses`; `indexed_cpp_include_ambiguous_across_two_include_roots_is_counted_not_bound` |
| a TS, Python, Rust or Java import with a unique-basename file | unchanged | `non_c_family_import_never_reaches_the_suffix_or_basename_stage` |
| `metadata_json` present but not a JSON object (resolver) | declined, row unchanged | `include_with_malformed_metadata_declines_the_suffix_stage` |
| file order | identical index and candidate order | `suffix_index_is_built_from_the_indexed_file_list_only` |
| refresh: the including file unchanged (copied forward), a second file of the basename added | the include becomes `imports_ambiguous_match`, `ambiguous_basename`, both candidates; no edge | `refresh_turns_an_inferred_unique_basename_include_ambiguous_when_a_second_header_is_added` |
| **row's reason carrier ABSENT** — on a row for which this slice requires no basis (every `static` row; every row of a non-C/C++ extractor): NULL, an object without `basis`, or another `basis` (`python_submodule`) | no `reason` key; the row prints and serializes as at HEAD (byte-identical for other producers' rows) | `find_imports_row_without_a_recorded_basis_carries_no_reason` (a static C/C++ include row `{"isTypeOnly":false}` and an `inferred` non-C/C++ row with a NULL carrier), `find_imports_row_of_another_basis_carries_no_reason` (an `inferred` Python row with `python_submodule`), `row_without_a_rendered_reason_prints_exactly_as_before` (incl. an `inferred` row of another extractor without a reason); CIB-C09 `ngx_event.h` |
| **row's reason carrier PRESENT-MISSING** — a C/C++ (`c-core:`/`cpp-core:`) `inferred` edge — a row only this slice produces — whose carrier is NULL, or an object without `basis` | `reason {missing: "no reason carrier"}` / `reason {missing: "reason carrier has no basis"}`, never absent; the row prints `inferred (reason missing — re-index)`, never bare `inferred`; the renderer prints the same line for a C/C++ inferred row with no `reason` key at all | `find_imports_c_family_inferred_row_with_a_null_carrier_reports_its_reason_missing`, `find_imports_c_family_inferred_row_whose_carrier_lacks_a_basis_reports_its_reason_missing`, `inferred_row_whose_reason_is_missing_for_a_null_carrier_says_re_index`, `inferred_row_whose_reason_is_missing_for_a_carrier_without_basis_says_re_index`, `c_family_inferred_row_without_a_reason_key_says_reason_missing_never_bare_inferred`, `import_entry_decodes_a_reason_its_unreadable_form_its_missing_form_and_its_absence` |
| **row's reason carrier PRESENT-VALID** — `unique_basename` on an inferred edge or `unique_suffix` on a static edge, exactly one candidate, equal to the edge's own target FILE | `reason {basis, candidates: [<target path>]}`; an inferred row prints `inferred: unique basename → <path>`; a static row prints `static` | `find_imports_bound_include_carries_its_basis_and_candidate_paths`, `inferred_unique_basename_row_renders_its_reason_and_candidate`, `import_entry_decodes_a_reason_its_unreadable_form_its_missing_form_and_its_absence`; CIB-C09 `ngx_config.h` |
| **row's reason carrier PRESENT-MALFORMED** — not an object; `basis` not a string; candidates missing, not an array, a non-string entry, or empty | `reason {unreadable: <what failed>}`; an inferred row prints `inferred: reason unreadable (<what failed>)`; never dropped, never defaulted | `find_imports_malformed_reason_carrier_is_unreadable_never_dropped`, `inferred_row_with_an_unreadable_reason_says_so_never_bare_inferred` |
| **row's reason carrier PRESENT-CONTRADICTORY** — several candidates under a unique basis; a candidate other than the edge's target; a non-FILE target; `unique_basename` on a static edge; `unique_suffix` on an inferred edge; an ambiguous basis on a bound edge; a basis outside the four on a C/C++ inferred edge | `reason {unreadable: <what disagrees>}` — never rendered as evidence; an inferred row prints `inferred: reason unreadable (<what disagrees>)`; the renderer applies the same agreement to JSON it did not produce | `find_imports_reason_that_contradicts_its_edge_is_unreadable_never_evidence`, `inferred_row_whose_reason_disagrees_with_the_row_is_unreadable_never_evidence` |
| default and compare listings | one row form | `compare_listing_prints_the_same_rows_as_the_default_listing` |
| a snapshot whose only unresolved imports are non-legacy (`imports_wildcard: 2`) | import-graph and change-impact LOW with their reasons and caveats; call-graph and dead-code unchanged | `a_lone_non_legacy_unresolved_import_category_lowers_import_graph_and_change_impact` |
| trust's unresolved-import figure on one snapshot | equals the modules-list headline over the four IMPORTS categories | `sum_unresolved_imports_sums_every_category_of_the_shared_imports_set`, `trust_unresolved_import_figure_equals_the_modules_list_headline_on_one_snapshot`; CIB-C09 (nginx 299 = 299) |

### 2.3 Outward proof (CIB-C05, CIB-C09)
F-CIB-ORACLE: the before/after store comparison is `agent-manager/scripts/rg-store-diff.py` ALONE — every normalized field of files, nodes, edges and unresolved rows — with exact expectations: nginx `--expect edges:791 --expect unresolved:804`, leveldb identity, poco `--expect edges:150 --expect unresolved:134`. The before side is a HEAD before-binary (`git worktree add --detach /private/tmp/CPP-INCLUDE-BASENAME-1-headbase-wt HEAD`, built once into its own target directory) indexing the same three checkouts as the candidate, the CLAUDE.md baseline form: the manager's v0.19.0 before-stores predate queued slices that legitimately move other facts of these corpora (CPP-ATTRIBUTE-MACRO-1A's C++ nodes on leveldb and poco, TEST-EDGE-SCOPE-1A's `is_test`, PYTHON-SUBMODULE-IMPORT-1's Python imports — poco holds two Python files), which a whole-store diff would report as this slice's. The literals were measured on those before-stores (python sqlite3 `immutable=1`) and depend only on C/C++ include rows and the file list, which no queued predecessor changes. Then ONE query over the moved rows: every new C/C++ include edge is exactly a formerly unresolved include; its basis, resolution and candidates equal RG-REQ-006-L11 recomputed from the base root's own file list; its extractor keys are kept; every changed unresolved row keeps its target, classification and basis code and gains its basis and every candidate; no inferred IMPORTS edge touches a non-FILE node; the per-basis totals and the witness rows of §1. CIB-C09 then reads the nginx after-root through the product: `modules deps` default (no inferred edge; the remainder and its flag named) and `--include-inferred` (the eleven pairs), `imports src/core/ngx_config.h` (the six rows exactly, JSON `reason` on each), `imports src/event/ngx_event.c` (two inferred rows with their reason, the static row unchanged and without `reason`), trust's `unresolved_imports=` equal to `modules list`'s `unresolved_import_count` (299). Indexes: nginx (397 files), leveldb (133) and poco (3,235) twice each — once per binary; poco is the smallest corpus that carries both the static-suffix class (69 rows) and CPP-INCLUDE-ROOTS-1's root-overlap rows (368) this slice must leave unchanged.

## 3. Regression watch
CPP-INCLUDE-ROOTS-1's poco outcome — include-root resolution, root-overlap ambiguity (368 rows, no `basis`), `Net → Foundation` among the module candidates — unchanged (CIB-C05; no candidate pair moves). leveldb unchanged. A static unique-suffix edge is a certain fact and enters the persisted module graph and `cycles`' directory groups (poco's new `cpptrace/src/platform ↔ utils` cycle — reported). The inferred edge reaches readers that ignore `resolution` only where TEST-EDGE-SCOPE-1B has not partitioned them; 1B is this slice's gate for exactly that reason. PYTHON-SUBMODULE-IMPORT-1's `find_file_importers` (explain's Referenced-by) will read these inferred edges: its accepted reader must render an inferred row whose basis is `unique_basename` and which has no `alternateTarget` as inferred, not as an error and not as certain (§4). **Refresh (D-CIB-REFRESH-1 = A):** a refresh re-resolves every include of the snapshot, copied-forward ones included (§2.1 item 3), so a binding appears on the next refresh by this binary — not only on rebuild — and a unique basename becomes ambiguous (or an ambiguous one becomes unique) on the refresh that adds (or removes) the second file; what a refresh keeps is the extraction (spelled specifier, line, carrier). The former sentence "a copied-forward include keeps its binding until rebuild" was false and is withdrawn. Trust: on any snapshot holding a Java wildcard or ambiguous suffix, or a C/C++ ambiguous match, trust's unresolved-import figure rises to the headline's (§0) — a correction, not a regression.

## 4. Stop conditions
- Frozen: `IncludeResolver::resolve` and every existing stage; the extractors; the classifier; the unresolved-category vocabulary and the shared set's entries; every daemon-runtime production path; every trust computation other than `sum_unresolved_imports`.
- TEST-EDGE-SCOPE-1B's accepted reader: if its default `modules deps` does not name the inferred remainder with `--include-inferred`, or if it changed `imports <file>` so inferred rows are not printed inline, STOP and report the actual output — CIB-C09's literals follow RG-REQ-004-L12 and this slice's row form.
- PYTHON-SUBMODULE-IMPORT-1's accepted `find_file_importers` (if at HEAD): if it treats an inferred row with basis `unique_basename` and no alternate as a storage error or as a certain importer, STOP (DECISION_REQUIRED: the reader belongs to that slice). If its accepted `imports <file>` or `find_imports` already renders a reason for inferred rows, STOP and report — one row formatter must serve both slices' bases.
- An rg-store-diff count other than the expectation, a moved row the rule predicts differently, a changed row outside the prediction, or a different file universe: STOP and report the key, both rows and the query.
- A trust figure that moves other than the IMPORTS-family sum and the fields derived from it (P-CIB-05), a change to service.rs outside its test module, or a trust test other than the renamed one whose body changes: STOP.
- Operator note (F-CIB-TRUST-PROPAGATION, non-blocking): the derived import-graph/change-impact movement is traced to RG-REQ-002-L02/L10 because RG-REQ-009 is not among this manifest's parents. Options: A — keep the trace as is (reward: no re-admission; risk: RG-REQ-009-L04's own record does not list this slice); B — add RG-REQ-009 and RG-REQ-009-L04 (changes) to the packet and manifest (reward: the trust H records it; risk: one more manifest revision and review cycle for a movement no measured corpus shows). RECOMMENDED: A — D-CIB-COUNT-1 placed the trust sum under RG-REQ-002-L02.
- Nothing outside the eleven candidate paths. STANDING HONESTY RULES (no `unwrap_or`/`.ok()`/default on a fallible read the stage or the reason read classifies). Every proof under `RMAP_TRANSPORT=stdio`, isolated roots, enrichment and retention off.
- No open decision: D-CIB-REVIEW-1 resolves PREP's four; the record's two numeric expectations are corrected by measurement in §9, not re-decided.

## 5. Validation (ORDERED; `build-progress.md` after EACH step)
0. `git rev-parse HEAD`; `git diff --stat <packet HEAD> HEAD -- rust` reviewed; `registry-before <full sha256>` of `~/Library/Application Support/repo-graph/registry.json`; the two gate records at HEAD with `result == accepted`; the manager's three before-roots exist with empty WALs (read only for re-measurement). 1. Failing tests FIRST (nine include_resolver, six resolver, four repo-index, the renamed trust test and the propagation test, seven storage, nine renderer) — each fails for the stated reason before the code. 2. CIB-C01 → CIB-C02 → CIB-C03 → CIB-C07 → CIB-C08 → CIB-C04. 3. CIB-C05 (the two release builds, six isolated indexes, three rg-store-diff runs, the moved-row query). 4. CIB-C09 (the nine captures on the nginx after-root, the assertions). 5. CIB-C06; hand-off with the evidence object (each check's outcome nested under `outcome`; `supportingEvidence` non-empty: the three rg-store-diff summaries, the moved-row query's printed totals, the `imports` and `modules deps` captures verbatim, the trust/headline pair).

## 6. Definition of done
All nine checks pass; the report quotes as code under analysis `src/core/ngx_config.h:22-42` before (six unresolved rows, `0 imports`) and after (the six `imports` rows verbatim and one JSON `reason`), `src/event/ngx_event.c:8-10` after (two inferred rows with their reason, the static row unchanged), `modules deps` in both views, `src/core/ngx_core.h:52` (`ngx_time.h`, ambiguous with both candidates), one poco static-suffix row (`dependencies/cpptrace/src/binary/elf.hpp:8` `#include "utils/utils.hpp"` or the row the capture shows), `trust/src/rules.rs` `sum_unresolved_imports` before and after with nginx's 299 = 299 and the propagation test's two LOW axes, and one residual per class (no-indexed-file `ngx_config.h:12`, dot-dot). Ship block (operator): the §0 movements, including trust's on kafka (+84), poco (+368), grpc-java (+8), spring-petclinic (+2), glamcrm (+58) and nginx (299), each with its import-graph and change-impact axes LOW before and after (only `unresolved_imports=N` moves), and the stated rule that a snapshot whose only unresolved imports are non-legacy now reads LOW on both axes; existing stores report the `indexer` family stale on `orient`/`check` until `rmap repo rebuild <path>` (TOOLCHAIN-STALENESS-1), even after a refresh that has already re-resolved their includes.

## 7. Corpus roots
Measurement (the manager's; python sqlite3 `immutable=1` only, never served): `/private/tmp/CPP-INCLUDE-BASENAME-1-nginx-before` (/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/nginx b0a4d0f, v0.19.0, 2026-09-23), `/private/tmp/CPP-ATTRIBUTE-MACRO-1-leveldb-before` (leveldb 7ee830d), `/private/tmp/DOCS-UNREADABLE-DECODE-1-poco-before` (poco 49af400) — each basis commit equal to its checkout's HEAD; the trust-movement stores `/private/tmp/DEPS-GRADLE-CATALOG-1-{kafka,grpc-java,petclinic}-before` and `/private/tmp/TS-WORKSPACE-RESOLUTION-1-glamcrm-before`. Proof (builder-owned, created only if absent, deleted in CIB-C06): `/private/tmp/CPP-INCLUDE-BASENAME-1-{nginx,leveldb,poco}-{headbase,after}`, the worktree `/private/tmp/CPP-INCLUDE-BASENAME-1-headbase-wt` and its target directory `/private/tmp/CPP-INCLUDE-BASENAME-1-headbase-target`. The operator registry is read only for its digest; `~/repo-graph-retained/` is never served.

## 8. Follow-ups and adjacent defects (not this slice)
- Reading nginx's own include-path declaration (`auto/` → `-I` flags) or `compile_commands.json` (untracked in the nginx checkout) as configured roots would turn most inferred edges static — evidence sources RG-REQ-006-L03 admits; their own slices.
- The reason for every other inferred import on `imports <file>` (PYTHON-SUBMODULE-IMPORT-1's `python_submodule` basis and its `alternateTarget`, which that slice renders on `explain` only): one follow-up extends the one row formatter to every basis the index records.
- `imports <file>` omits unresolved rows (the inner join, `find_imports`) — RG-REQ-006-L12's recorded gap; the ambiguous rows' candidates are therefore visible in `--json` of no reader yet.
- `MODULES_LIST_UNRESOLVED_IMPORT_CATEGORIES` now has two readers (modules list, trust); its name names one. Proposed correction: `UNRESOLVED_IMPORT_CATEGORIES` (callers: `daemon-runtime/src/dispatch.rs:9397`, `storage/src/trust_impl.rs:1371`/`:1430`, `trust/src/rules.rs`) — a boundary-touching rename for its own change.
- `daemon-runtime/src/dispatch.rs:9392-9393` says the shared set "deliberately excludes the C/C++ `imports_ambiguous_match` basis to keep non-Java repos byte-stable" — false since CPP-INCLUDE-ROOTS-1 added it (types.rs:349); a stale comment outside this allocation.
- Trust reads its unresolved counts from the snapshot's `extraction_diagnostics_json` and renders absent diagnostics as 0 (`trust/src/service.rs:333-342`, `.unwrap_or(0)`) — an RG-REQ-002-L04 swallowing form, pre-existing and unchanged here.
- The include-root overlap arm (resolver.rs:422-433) drops its candidates (`_candidates`); recording them as this slice does for suffix/basename ambiguity would change poco's 368 rows — a separate, stated change.
- `is_system = metadata_json.is_none()` (resolver.rs:520) is false for every stored include row observed, so angle includes also try the same directory; whether that is intended is unexamined here.

## 9. Baseline history and revision notes
- INPUT-1 (2026-09-24): first admission baseline under RG-BOOTSTRAP-INPUT-9 (parents RG-REQ-006, RG-REQ-002, RG-REQ-004, RG-REQ-011; D-CERTAINTY-MARK-1 and D-REFRESH-STALE-1 as governance). nginx measured 2026-09-23 by the manager; nginx, leveldb and poco re-measured 2026-09-24 by the builder.
- PREP (builder's revision of the manager's draft): (1) the new stages moved from inside the include resolver to a Stage 6 after every existing stage — otherwise a non-unique suffix would pre-empt a repo-prefix resolution (a flip RG-REQ-006-L03 forbids) — and gated to C/C++ edges; (2) `no_suffix_guessing` kept as found; (3) the witness moved off `src/core/nginx.c:8-9` (already static); (4) poco is not unchanged (69 static, 65 inferred, +16 MODULE→MODULE pairs); (5) a C/C++-IMPORTS oracle; (6) the non-unique cases reuse `imports_ambiguous_match` with `basis` in `metadata_json` instead of a new `imports_ambiguous_basename` category (a new category would need the classifier branch, the shared set, trust's label and a renamed "all four" test, while RG-REQ-002-L11 asks for the existing axes); (7) RG-REQ-002-L11 demoted to changes; (8) the `imports` assertion without a reason; (9) the 130 multi-segment includes are several families; (10) gates made executable, existing tests bound by body. PREP's review (codex gpt-6-sol) returned decision-required: D-CIB-L06-1, D-CIB-REASON-1, D-CIB-REFRESH-1, D-CIB-COUNT-1; F-CIB-GATE, F-CIB-ORACLE, F-CIB-REFRESH.
- PREP-2 (this revision, 2026-09-24, under D-CIB-REVIEW-1 — the operator's record, pinned by the manifest as governance and in `requiredDecisionIds`): **D-CIB-REFRESH-1 = A** — §3's false sentence withdrawn; §2.1 item 3 states the refresh truth from `orchestrator.rs:1008-1039`/`:883`/`:1965-1967` and `indexer_impl.rs:966-1020`; the refresh fixture test in CIB-C03. **D-CIB-COUNT-1 = A** — §2.1 item 4; `trust/src/rules.rs` joins the allocation (the shared set's module, `classification/src/types.rs`, was already in it — its doc names the second reader); RG-REQ-002-L02 moves from preserves to changes with the measured movement; the seam test (CIB-C03), the trust suite (new check CIB-C07) and the field pair (CIB-C09); P-CIB-05. **D-CIB-L06-1 = B** — the bump is a reported consequence (§0, §6), the four parents stay, and §4.1's decision list is removed. **D-CIB-REASON-1 = A** — RG-REQ-002-L11 back under implements; §2.1 item 6; the storage read, the renderer and the daemon test literal join the allocation (`storage/src/queries.rs`, `rgr/src/presentation/imports.rs`, `daemon-runtime/src/livegraph_feed.rs`); the reason taxonomy rows of §2.2; new check CIB-C08; P-CIB-04. **F-CIB-GATE** — every cargo link is `( set -o pipefail; cargo test … 2>&1 | tee LOG ) && grep …`; CIB-C01 and CIB-C05 verify both records' `result == accepted`. **F-CIB-ORACLE** — the three-field comparator is removed; rg-store-diff alone plus one moved-row query (CIB-C05); the reader-surface assertions move to CIB-C09. **Witness** — `src/core/ngx_config.h:22-34`.
- Where this revision departs from the record's letter, with the evidence: (a) the record gives nginx `--expect … unresolved:791`; rg-store-diff counts a changed row as differing, and 13 nginx rows change category and gain `basis`/`candidates` beside the 791 removed, so the expectation is `unresolved:804` (measured: base-1 791, base-2 13, base-0 156, suf-0 130 on the nginx before-store). (b) The record says "identity on leveldb and poco"; poco is not identity under the ratified rule — 69 static-suffix and 65 unique-basename includes bind (re-measured on the poco before-store at 49af400: 69/65, 16 new MODULE→MODULE pairs, 0 existing pairs touched) — so poco expects `edges:150 unresolved:134`; leveldb is identity. (c) The rg-store-diff base is a HEAD before-binary on the same checkouts, not the manager's v0.19.0 stores (§2.3). (d) The record's "`imports_ambiguous_basename`" (its §1 refresh test, its §2 nginx count) is read as the `ambiguous_basename` basis on an `imports_ambiguous_match` row — PREP's §9 (6), which D-CIB-REVIEW-1 does not revisit; no category is added. (e) The record places the headline's sum at `storage/src/trust_impl.rs:1361-1368`; that is the doc of the storage regression test; the set is defined at `classification/src/types.rs:347-352` and read by `dispatch.rs:9383-9403`. (f) The record asks that the daemon handler join the allocation; it needs no production change (§2.1 item 6), and joins only through its test literal. (g) The witness file has six uniquely named OS headers, not four (`:38` win32, `:42` posix), so its `imports` output is six rows.
- PREP-2 iteration 1 (2026-09-24), after review-0 (codex gpt-6-sol, refinement-required): **F-CIB-TRUST-PROPAGATION** — the sum's derived fields are named and permitted (§0 Propagation, P-CIB-05, §4 stop condition); measured: all 16 before-stores (11 distinct corpora) have `imports_file_not_found > 0`, so no axis level moves on any of them, only `unresolved_imports=N`; `a_lone_non_legacy_unresolved_import_category_lowers_import_graph_and_change_impact` is added to `trust/src/service.rs`'s test module (the eleventh candidate path; production code byte-identical, CIB-C07); the trace is RG-REQ-002-L02/L10 with RG-REQ-009-L04 surfaced as a non-blocking operator note. **F-CIB-REASON-VALIDITY** — class rule: a recorded reason is evidence only when it agrees with the fact it explains; the storage read judges it against its own edge (unique basis ⇔ exactly one candidate, that candidate is the edge's target FILE, `unique_basename` ⇔ inferred, `unique_suffix` ⇔ static; a wrong-typed `basis` and an ambiguous basis on a bound edge are unreadable), and the renderer re-applies the one agreement it prints to JSON it did not produce; §2.1 item 6, the PRESENT-CONTRADICTORY taxonomy row, and one new storage and one new renderer counterexample test over every case (CIB-C08 floors 746 and 1274).
- PREP-3 (2026-09-24), after PREP-2's review (codex gpt-6-sol; F-CIB-TRUST-PROPAGATION and F-CIB-REASON-VALIDITY confirmed closed) and the manager's verification of one remaining finding: **F-CIB-MISSING-REASON** (RG-REQ-002-L11) — revision 2 classified a NULL carrier or an object without `basis` as ABSENT for every row, so a C/C++ inferred include that lost its mandatory evidence would print bare `inferred`. Class rule (ownership): a row this slice produces — an `inferred` IMPORTS edge whose extractor (`e.extractor`, carried on `ImportResult`/`ImportEntry` as `evidence`: `storage/src/queries.rs:131-132`, `:1734`, `:1752-1763`; `rgr/src/presentation/imports.rs:47-48`) starts `c-core:`/`cpp-core:` — must carry a valid reason, and every other state of its carrier is PRESENT-MISSING (NULL; object without `basis`) or PRESENT-UNREADABLE (every malformed or contradictory case, now including a basis this slice does not write), never ABSENT; ABSENT is narrowed to rows for which this slice requires no basis (every static row, every other producer's row), which stay byte-identical. Measured (python sqlite3 `immutable=1` on the three before-stores): 0 inferred C/C++ IMPORTS edges and 0 C/C++ IMPORTS edges with a NULL carrier, so the rule touches no row that exists today. JSON: `reason {missing: "no reason carrier" | "reason carrier has no basis"}` — the tagged `reason` object this surface already uses for `unreadable` (no unknown-with-reason shape exists on `imports` at HEAD), in place of the suggested `reason: null` + `reason_state`, so one key carries every state and absent stays key-absence. Human: `inferred (reason missing — re-index)`, also for a C/C++ inferred row with no `reason` key in JSON this build did not produce. Edited: §2.1 item 6 (ownership rule, PRESENT-MISSING, the renderer line) and item 8 (test counts), the §2.2 ABSENT/MISSING/CONTRADICTORY rows, P-CIB-04, CIB-C08 (two storage and three renderer tests bound by name; the decode test renamed to include the missing form; floors storage ≥ 748 = 741 + 7, rgr --lib ≥ 1277 = 1268 + 9; inputs and expected), §5 step 1's counts and the Status line (both restate what the edited sections say).
