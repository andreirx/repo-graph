<!-- requirements-assurance-implementation-v1
{
  "formatVersion": 1,
  "kind": "implementation-allocation",
  "workItemId": "PYTHON-SUBMODULE-IMPORT-1",
  "baselinePath": "docs/requirements/baselines/PYTHON-SUBMODULE-IMPORT-1-INPUT-1.json",
  "parentRequirementIds": [
    "RG-REQ-006",
    "RG-REQ-002",
    "RG-REQ-011",
    "RG-REQ-001"
  ],
  "implements": [
    "RG-REQ-006-L04"
  ],
  "preserves": [
    "RG-REQ-006-L01",
    "RG-REQ-006-L02",
    "RG-REQ-006-L03",
    "RG-REQ-006-L05",
    "RG-REQ-006-L06",
    "RG-REQ-006-L07",
    "RG-REQ-006-L08",
    "RG-REQ-006-L09",
    "RG-REQ-006-L10",
    "RG-REQ-006-L11",
    "RG-REQ-006-L12",
    "RG-REQ-006-L13",
    "RG-REQ-002-L01",
    "RG-REQ-002-L02",
    "RG-REQ-002-L03",
    "RG-REQ-002-L04",
    "RG-REQ-002-L05",
    "RG-REQ-002-L06",
    "RG-REQ-002-L07",
    "RG-REQ-002-L08",
    "RG-REQ-002-L09",
    "RG-REQ-002-L10",
    "RG-REQ-011-L06"
  ],
  "preservationObligationIds": [
    "P-PSI-01",
    "P-PSI-02",
    "P-PSI-03"
  ],
  "changes": [
    "RG-REQ-002-L11",
    "RG-REQ-001-L06",
    "RG-REQ-001-L09"
  ],
  "acceptanceBoundary": "The resolver's unit tests (the `--lib resolver` filter) and the orchestrator's unit tests (the `--lib orchestrator` filter: the refresh signals load), the storage file-importers tests, the agent explain integration tests, rgr's explain renderer tests, repo-index's Python integration suite with one new fixture, SQL comparison of fresh isolated django and FRAKTAG stores (candidate binary) against the manager's before-stores (v0.19.0 binary, read `immutable=1`), `explain`/`imports` captures on the django after-root, a no-change delta refresh of the django after-root compared snapshot-to-snapshot by SQL (refresh parity), and `orient`/`check`/`check --json` on a builder-owned copy of the v0.19.0 django before-root served by the candidate (the stale-store line) plus `check --json` on the after-root (current); every check is gated on TOOLCHAIN-STALENESS-1's accepted implementation-review record at HEAD (D-REFRESH-STALE-1's queue order).",
  "candidatePaths": [
    "rust/crates/indexer/src/resolver.rs",
    "rust/crates/indexer/src/orchestrator.rs",
    "rust/crates/repo-index/src/compose.rs",
    "rust/crates/repo-index/tests/integration.rs",
    "rust/crates/repo-index/tests/fixtures/python/package-import/pkg/__init__.py",
    "rust/crates/repo-index/tests/fixtures/python/package-import/pkg/base.py",
    "rust/crates/repo-index/tests/fixtures/python/package-import/pkg/models/__init__.py",
    "rust/crates/repo-index/tests/fixtures/python/package-import/consumer/app.py",
    "rust/crates/storage/src/agent_impl.rs",
    "rust/crates/storage/tests/agent_impl.rs",
    "rust/crates/agent/src/storage_port.rs",
    "rust/crates/agent/src/dto/signal.rs",
    "rust/crates/agent/src/explain/mod.rs",
    "rust/crates/agent/tests/explain_symbol.rs",
    "rust/crates/rgr/src/presentation/explain_sections.rs",
    "rust/crates/rgr/src/presentation/explain.rs"
  ],
  "postReviewRecordPaths": [
    "docs/assurance/PYTHON-SUBMODULE-IMPORT-1/verification.json",
    "docs/assurance/PYTHON-SUBMODULE-IMPORT-1/implementation-review.json"
  ],
  "candidateExclusions": [
    {
      "pathPrefix": ".agent-manager/",
      "reason": "local relay state and raw run/progress evidence"
    },
    {
      "pathPrefix": "rust/target/",
      "reason": "reproducible Cargo build output"
    }
  ],
  "checks": [
    {
      "checkId": "PSI-C01",
      "obligationIds": [
        "RG-REQ-006-L04",
        "RG-REQ-002-L11",
        "RG-REQ-001-L06",
        "RG-REQ-001-L09",
        "RG-REQ-006-L01",
        "RG-REQ-006-L02",
        "RG-REQ-006-L03",
        "P-PSI-02"
      ],
      "owner": "builder",
      "method": {
        "kind": "command",
        "command": "git cat-file -e HEAD:docs/assurance/TOOLCHAIN-STALENESS-1/implementation-review.json && git show HEAD:docs/assurance/TOOLCHAIN-STALENESS-1/implementation-review.json | python3 -c \"import json,sys; d=json.load(sys.stdin); sys.exit(0 if (d.get('workItemId'), d.get('result')) == ('TOOLCHAIN-STALENESS-1', 'accepted') else 1)\" && cargo test -p repo-graph-indexer --lib resolver 2>&1 | tee /tmp/psi-c01.txt | grep -E '^test result: ok\\.' && ! grep -E '^test .* FAILED$|^thread .* panicked at|^test result: FAILED' /tmp/psi-c01.txt && for t in python_from_package_import_of_a_sibling_module_resolves_inferred_to_the_module_file_with_the_init_as_alternate python_from_package_import_of_a_subpackage_resolves_inferred_to_its_init_with_the_package_init_as_alternate python_from_package_import_prefers_the_subpackage_over_a_same_named_module_file python_from_package_import_of_a_symbol_keeps_the_package_init_as_a_static_target python_from_package_import_never_retargets_to_a_non_python_file python_from_package_import_keeps_the_init_when_the_init_defines_the_name python_from_package_import_ignores_a_nested_member_named_like_the_submodule python_from_package_import_keeps_the_init_when_the_init_imports_the_name_from_elsewhere python_from_package_import_keeps_the_init_when_the_init_binds_the_name_to_an_attribute_of_the_submodule python_from_package_import_still_retargets_when_the_init_imports_the_submodule_itself python_relative_from_dot_import_of_a_sibling_module_resolves_inferred_to_the_module_file python_inferred_import_never_feeds_the_module_edge_pairs python_from_import_without_an_imported_name_carrier_takes_the_old_ladder non_python_import_with_an_imported_name_never_takes_the_python_stage file_resolution_python_extensionless file_resolution_python_init_shortcut resolve_import_by_stable_key resolve_import_via_extensionless_file_resolution resolve_import_via_per_tu_include resolve_import_via_repo_prefix_fallback java_import_resolves_to_file_node java_import_wildcard_is_named_basis rust_crate_import_leaf_module_file rust_crate_import_bare_crate_hits_lib_rs rust_crate_import_unknown_crate_is_none rust_crate_import_empty_catalog_is_noop aliased_named_import_uses_imported_name_for_lookup namespace_import_member_resolves_to_target_module default_import_member_does_not_resolve_by_bare_method_name; do grep -qE \"^test .*$t .* ok$\" /tmp/psi-c01.txt || { echo \"MISSING $t\"; exit 1; }; done && git diff --quiet HEAD -- crates/python-extractor crates/ts-extractor crates/java-extractor crates/rust-extractor crates/c-extractor crates/cpp-extractor crates/indexer/src/types.rs crates/indexer/src/include_resolver.rs && cargo test -p repo-graph-indexer --lib orchestrator 2>&1 | tee /tmp/psi-c01b.txt | grep -E '^test result: ok\\.' && ! grep -E '^test .* FAILED$|^thread .* panicked at|^test result: FAILED' /tmp/psi-c01b.txt && for t in refresh_loads_copied_forward_import_bindings_for_resolution refresh_creates_refresh_snapshot_with_copy_forward refresh_falls_back_to_full_index_when_no_parent abort_before_snapshot_creation_prevents_db_mutation; do grep -qE \"^test .*$t .* ok$\" /tmp/psi-c01b.txt || { echo \"MISSING $t\"; exit 1; }; done && python3 - <<'PY'\nimport re, subprocess\ndef sh(*a): return subprocess.run(a, capture_output=True, text=True, check=True).stdout\ndef ver(src):\n    m = re.findall(r'^const INDEXER_VERSION: &str = \"(indexer:(\\d+)\\.(\\d+)\\.(\\d+))\";$', src, re.M)\n    assert len(m) == 1, (\"exactly one INDEXER_VERSION constant\", m)\n    return m[0][0], tuple(int(x) for x in m[0][1:])\nORCH, COMP = \"crates/indexer/src/orchestrator.rs\", \"crates/repo-index/src/compose.rs\"\nhead_orch = sh(\"git\", \"show\", \"HEAD:./\" + ORCH)\nH, hv = ver(head_orch)\nC, cv = ver(open(ORCH, encoding=\"utf-8\").read())\nassert cv == (hv[0], hv[1] + 1, 0), (\"INDEXER_VERSION moves up exactly one minor from HEAD's\", H, C)\ndef changed(p): return [l for l in sh(\"git\", \"diff\", \"-U0\", \"HEAD\", \"--\", p).splitlines() if l[:1] in \"+-\" and not l.startswith((\"+++\", \"---\"))]\nassert [l for l in changed(ORCH) if \"INDEXER_VERSION: &str\" in l] == ['-const INDEXER_VERSION: &str = \"%s\";' % H, '+const INDEXER_VERSION: &str = \"%s\";' % C], changed(ORCH)\nassert changed(COMP) == ['-    let extractor = \"%s\"; // Match INDEXER_VERSION in orchestrator.rs' % H, '+    let extractor = \"%s\"; // Match INDEXER_VERSION in orchestrator.rs' % C], (\"compose.rs differs from HEAD by exactly its version line\", changed(COMP))\ndef grep_counts(*rev):\n    out = subprocess.run([\"git\", \"grep\", \"-c\", \"-F\", H, *rev, \"--\", \"*.rs\"], capture_output=True, text=True).stdout\n    return {l.rsplit(\":\", 1)[0][len(rev[0]) + 1 if rev else 0:]: int(l.rsplit(\":\", 1)[1]) for l in out.splitlines()}\nhc, wc = grep_counts(\"HEAD\"), grep_counts()\nwant = {p: n - (1 if p in (ORCH, COMP) else 0) for p, n in hc.items()}\nassert {p: n for p, n in wc.items() if n} == {p: n for p, n in want.items() if n}, (\"the old version literal leaves exactly the two declared sites and is added nowhere\", hc, wc)\ndef prod(src):\n    i = src.find(\"\\n#[cfg(test)]\\nmod tests {\")\n    assert i > 0, \"orchestrator test-module marker\"\n    return src[:i]\ncur, head = prod(open(ORCH, encoding=\"utf-8\").read()), prod(head_orch)\nassert re.search(r\"\\.query_file_signals_batch\\(\", cur), \"refresh does not load the copied-forward signals through the existing port\"\nassert len(re.findall(r\"\\.insert_file_signals\\(\", cur)) == len(re.findall(r\"\\.insert_file_signals\\(\", head)) == 1, \"a new file-signals write site: copied-forward rows must never be re-inserted\"\nassert len(re.findall(r'^const PYTHON_EXTRACTOR_PREFIX: &str = \"python-core:\";$', open(\"crates/indexer/src/resolver.rs\", encoding=\"utf-8\").read(), re.M)) == 1, \"PYTHON_EXTRACTOR_PREFIX\"\nprint(\"PSI-C01 source assertions ok:\", H, \"->\", C, \"| old-literal occurrences at HEAD:\", hc)\nPY",
        "cwd": "rust",
        "environment": "candidate tree",
        "inputs": "GATE first (D-REFRESH-STALE-1: TOOLCHAIN-STALENESS-1 ships before every resolver/indexer slice): HEAD carries TOOLCHAIN-STALENESS-1's committed implementation-review record with `workItemId` TOOLCHAIN-STALENESS-1 and `result` accepted — absent on ec19a165, so this slice cannot be implemented today. Then every resolver-filter test (`--lib resolver` selects 122 tests today: the whole `resolver::tests` module (86), `include_resolver::tests` (33) and three `orchestrator::tests` whose names contain `resolver`; the fifteen OLD names bound here stay green verbatim) plus fourteen NEW `resolver::tests` built with `make_node`/`make_edge` over a `ResolverIndex` whose `nodes_by_stable_key`/`file_resolution` hold the fixture files (`r1:pkg/__init__.py:FILE`, `r1:pkg/base.py:FILE`, …, and the package-dir entry `r1:pkg:FILE → r1:pkg/__init__.py:FILE`), whose `nodes_by_name`/`node_uid_to_file_uid` hold the init's SYMBOL nodes, and with the `import_bindings_by_file` map passed to `resolve_edges` holding the init's own bindings (keyed by the init's file uid). Edge (E): extractor `python-core:0.1.0`, `target_key` `pkg`, `metadata_json` `{\"specifier\":\"pkg\",\"identifier\":\"base\",\"importedName\":\"base\"}`, `resolution: Static`. (1) `pkg/base.py` present, init defines and binds no `base` → RESOLVED to `pkg/base.py` with `resolution: Inferred`, `metadata_json` = the extractor's keys plus `alternateTarget: \"r1:pkg/__init__.py:FILE\"` and `basis: \"python_submodule\"` (today `resolve_edges` copies `edge.resolution` into every `ResolvedEdge`, resolver.rs:409-420); (2) `importedName` `models` with only `pkg/models/__init__.py` → that init, Inferred, alternate `pkg/__init__.py`; (3) both `pkg/base.py` and `pkg/base/__init__.py` → the subpackage init (Python's FileFinder checks the package directory first); (4) `importedName` `Thing`, no `pkg/Thing*` file → `pkg/__init__.py`, `Static`, `metadata_json` byte-identical to the input (today's result); (5) only `pkg/base.json` indexed → the init, Static (the probe is the two exact keys `<dir>/Y/__init__.py`, `<dir>/Y.py` in `nodes_by_stable_key`, never the extensionless `file_resolution` entry); (6) the init has a module-level SYMBOL `base` (`qualified_name == \"base\"`) → the init, Static; (7) the init's only `base` is a METHOD `Thing.base` (`qualified_name \"Thing.base\"`) → retargets, Inferred (a nested member is not a module attribute); (8) the init binds `base` from elsewhere (`from .other import base`: `{identifier base, specifier \".other\", importedName base}`) → the init, Static; (9) the init binds `from .base import base` (`specifier \".base\"`, `importedName base` — the attribute `base` OF the submodule) → the init, Static; (10) the init's only bindings of `base` are the submodule itself — each of `from . import base` (`specifier \".\"`, `importedName base`), `from pkg import base` (`specifier \"pkg\"`, `importedName base`), `import pkg.base as base` (`specifier \"pkg.base\"`, no `importedName`) → retargets, Inferred; (11) the relative form `target_key` `r1:pkg:FILE` + `importedName` `base` → `pkg/base.py`, Inferred; (12) for (1), `ResolutionResult.resolved_import_pairs` does NOT contain the edge (an inferred import never feeds `create_module_edges`, orchestrator.rs:1407-1450, which persists `static` MODULE→MODULE IMPORTS) while the Static edge of (4) is in it; (13) `metadata_json: None` → old ladder; (14) edge (E) with `extractor: \"ts-core:0.2.0\"` → old ladder (gated like the Rust stage on its prefix, resolver.rs:38/:553). The stage runs BEFORE Stage 2 for such edges, reads only inputs the resolver already has at `resolve_edges` (no new `ResolverIndex` field, no I/O). VERSION (D-REFRESH-STALE-1 = B; RG-REQ-001-L06): `INDEXER_VERSION` moves up exactly one minor from HEAD's value — `indexer:1.0.0` → `indexer:1.1.0` on ec19a165; derived from HEAD by the command, never hard-coded, because TOOLCHAIN-STALENESS-1 lands first and may itself move it — at orchestrator.rs:59 and in its declared copy `repo-index/src/compose.rs:1653` (`// Match INDEXER_VERSION in orchestrator.rs`, the Spring-inference provenance stamp; compose.rs's only change, asserted as the exact two-line diff); the old literal leaves exactly those two sites and is added nowhere (on ec19a165 they are its only occurrences under rust/; at step 0 the builder lists HEAD's occurrences and STOPS on any undeclared production copy of the constant). The version reaches `snapshots.toolchain_json.indexer` (`build_toolchain_json`, orchestrator.rs:1457-1464) and the orchestrator-derived OWNS and MODULE→MODULE edges' `extractor` (:1391/:1445). REFRESH DATA PATH (D-PSI-REFRESH-SIGNALS-1 = A; RG-REQ-001-L09): `run_pipeline` (orchestrator.rs:462) builds the resolver's `import_bindings_by_file` (:1007 `let import_bindings_by_file = build_import_bindings_map(&all_signals);`) and the classifier's per-file signals (:1098 `build_file_signals_for_edge`) from `all_signals`, which holds only the freshly extracted files' rows (:521/:702; written at :833 `storage.insert_file_signals(&all_signals)?;`); `refresh_repo` (:1673) passes only `files_to_extract` to it, while `copy_forward_unchanged_files` has already copied the unchanged files' `file_signals` rows into the child snapshot (storage `indexer_impl.rs:1050`, \"3. Copy file_signals.\") and every copied-forward extraction edge is re-resolved. The copied-forward files' signals — the plan's `files_to_copy`, the same file-uid list passed to `copy_forward_unchanged_files` — are loaded through the EXISTING port `query_file_signals_batch(<refresh snapshot uid>, <copied file uids>)` (indexer/src/storage_port.rs:469) and joined to the rows the resolver and the classifier read AFTER `insert_file_signals` (:833) and BEFORE `build_import_bindings_map` (:1007) — never re-inserted (the production code keeps exactly its one `insert_file_signals` call) — so a refresh's resolver and classifier inputs equal a full index's; a full index copies nothing and its inputs remain exactly as before. ONE new orchestrator test `refresh_loads_copied_forward_import_bindings_for_resolution`: a test extractor named `python-core:0.1.0` indexes `pkg/__init__.py` (import binding `{identifier Y, specifier \".other\", importedName Y}` — the init binds `Y` from elsewhere), `pkg/Y.py` and `app.py` (`from pkg import Y`: target key `pkg`, `importedName` `Y`); a refresh changes only `app.py`, so `pkg/__init__.py` is copied forward; the MockStorage records the arguments of every `query_file_signals_batch` call (today it ignores them, orchestrator.rs:2531) and the test asserts exactly one call with the refresh snapshot's uid and exactly the copied-forward file uids, and that the refresh snapshot's resolved `app.py` import lands on `pkg/__init__.py` as `static` with no `alternateTarget` — the target and resolution a full index of the same files gives on a fresh MockStorage (asserted in the same test) — where without the load it would land on `pkg/Y.py` as inferred. The three existing orchestrator tests named stay green."
      },
      "expected": "exit 0 (gate satisfied first): a Python `from X import Y` binds to `X/Y/__init__.py` or `X/Y.py` as INFERRED with the package init recorded as its alternate candidate and `basis: python_submodule` when the init's extracted facts show no module-level symbol `Y` and no binding of `Y` other than the submodule itself, keeps the package init as its static target otherwise, never retargets to a non-Python file, and never feeds the persisted module graph — the amended RG-REQ-006-L04 Python clause, and RG-REQ-002-L11's store part for this import case; every Rust, Java, C/C++ and TypeScript import stage and every existing resolver test remain exactly as before (no behavior change); the extractors are untouched; compose.rs differs from HEAD by exactly its version line, and the orchestrator by its version line, the refresh signals load and its test; `INDEXER_VERSION` is exactly one minor above HEAD's and the old literal is gone from both declared sites (RG-REQ-001-L06's trigger); on a delta refresh the resolver and the classifier read the copied-forward files' signals through the existing port and nothing is re-inserted, so a refresh resolves an unchanged init's bindings as a full index does (RG-REQ-001-L09); a full index's resolver inputs remain exactly as before (no behavior change)"
    },
    {
      "checkId": "PSI-C02",
      "obligationIds": [
        "RG-REQ-002-L11",
        "RG-REQ-006-L04",
        "RG-REQ-006-L12",
        "P-PSI-02",
        "RG-REQ-006-L06",
        "RG-REQ-006-L07",
        "RG-REQ-006-L08",
        "RG-REQ-006-L09",
        "RG-REQ-006-L10",
        "RG-REQ-006-L11",
        "RG-REQ-006-L13",
        "RG-REQ-002-L01",
        "RG-REQ-002-L02",
        "RG-REQ-002-L03",
        "RG-REQ-002-L04",
        "RG-REQ-002-L05",
        "RG-REQ-002-L06",
        "RG-REQ-002-L07",
        "RG-REQ-002-L08",
        "RG-REQ-002-L09",
        "RG-REQ-002-L10"
      ],
      "owner": "builder",
      "method": {
        "kind": "command",
        "command": "cargo test -p repo-graph-storage --test agent_impl find_file_importers 2>&1 | tee /tmp/psi-c02a.txt | grep -E '^test result: ok\\.' && ! grep -E '^test .* FAILED$|^thread .* panicked at|^test result: FAILED' /tmp/psi-c02a.txt && for t in find_file_importers_lists_importing_files_with_owning_module find_file_importers_is_empty_for_a_file_nobody_imports find_file_importers_carries_each_edge_resolution_basis_and_alternate_path find_file_importers_fails_loudly_on_an_inferred_edge_with_an_unreadable_carrier; do grep -qE \"^test .*$t .* ok$\" /tmp/psi-c02a.txt || { echo \"MISSING $t\"; exit 1; }; done && cargo test -p repo-graph-agent --test explain_symbol 2>&1 | tee /tmp/psi-c02b.txt | grep -E '^test result: ok\\.' && ! grep -E '^test .* FAILED$|^thread .* panicked at|^test result: FAILED' /tmp/psi-c02b.txt && for t in explain_type_focus_emits_members_and_referenced_by explain_referenced_by_partitions_inferred_importers_with_their_alternate explain_referenced_by_counts_a_file_with_any_certain_edge_as_certain_only; do grep -qE \"^test .*$t .* ok$\" /tmp/psi-c02b.txt || { echo \"MISSING $t\"; exit 1; }; done && cargo test -p repo-graph-rgr --lib presentation::explain 2>&1 | tee /tmp/psi-c02c.txt | grep -E '^test result: ok\\.' && ! grep -E '^test .* FAILED$|^thread .* panicked at|^test result: FAILED' /tmp/psi-c02c.txt && for t in referenced_by_heading_states_the_inferred_remainder_and_rows_name_the_alternate referenced_by_with_no_inferred_rows_renders_exactly_as_before referenced_by_with_malformed_inferred_rows_renders_the_unreadable_line; do grep -qE \"^test .*$t .* ok$\" /tmp/psi-c02c.txt || { echo \"MISSING $t\"; exit 1; }; done && cargo test -p repo-graph-repo-index --test integration python 2>&1 | tee /tmp/psi-c02d.txt | grep -E '^test result: ok\\.' && ! grep -E '^test .* FAILED$|^thread .* panicked at|^test result: FAILED' /tmp/psi-c02d.txt && for t in index_python_from_package_import_resolves_inferred_to_the_submodule_file index_python_imports_resolve index_python_extracts_symbols; do grep -qE \"^test .*$t .* ok$\" /tmp/psi-c02d.txt || { echo \"MISSING $t\"; exit 1; }; done && test -f crates/repo-index/tests/fixtures/python/package-import/pkg/__init__.py && test -f crates/repo-index/tests/fixtures/python/package-import/pkg/base.py && test -f crates/repo-index/tests/fixtures/python/package-import/pkg/models/__init__.py && test -f crates/repo-index/tests/fixtures/python/package-import/consumer/app.py && git diff --quiet HEAD -- crates/classification crates/daemon-runtime crates/module-queries crates/trust crates/repo-graph-livegraph crates/rgr/src/commands crates/rgr/src/presentation/imports.rs crates/storage/src/queries.rs crates/storage/src/trust_impl.rs crates/storage/src/crud crates/repo-index/src ':(exclude)crates/repo-index/src/compose.rs' crates/repo-index/tests/fixtures/python/simple-app",
        "cwd": "rust",
        "environment": "candidate tree",
        "inputs": "The vertical from the store to the reader. Storage `find_file_importers` (agent_impl.rs:1601-1639) additionally selects `e.resolution`, `json_extract(e.metadata_json,'$.basis')` and the alternate's PATH (a join of `json_extract(e.metadata_json,'$.alternateTarget')` to `nodes.stable_key` → `files.path` in the same snapshot) into `AgentFileImporter {file, module_path, resolution, basis, alternate_path}`; an `inferred` row whose `basis` is not a string, or whose `alternateTarget` is present but is not a string or names no FILE node of the snapshot, or whose `basis` is `python_submodule` without an alternate, is a named storage error at the read — never a coerced row (the `forward_decl` precedent; RG-REQ-002-L04). `agent/src/explain/mod.rs` (:588-606) partitions the importers by file: a file with at least one `static`/`dynamic` edge to the target is a certain importer (in `ExplainReferencedByEvidence.items`, `count` and `top_modules`, exactly as today); a file whose every edge to the target is `inferred` goes to NEW `inferred_items` ({file, module, basis, alternate}) and `inferred_count` (serde-skipped when empty/zero, so a type with no inferred importer serializes byte-identically). rgr renders the heading `Referenced by (N files, +M inferred — investigate)` when M > 0 (unchanged `Referenced by (N files)` otherwise), the certain rows as today, then each inferred row `  - <path>  (inferred: python submodule — alternate <init path>)` (a basis other than `python_submodule` renders `  - <path>  (inferred — alternate <path>)`, never the raw code — RG-REQ-002-L08); a malformed `inferred_items`/`inferred_count` → the existing single `referenced-by unreadable on this snapshot` line. `imports <file>` (queries `find_imports`, rgr presentation/imports.rs:92-99) prints the edge's `resolution` verbatim already (`  <target>  depth=1  inferred`) — no code change, pinned by the live capture in PSI-C04 (RG-REQ-006-L12). ONE new end-to-end test over the NEW fixture `rust/crates/repo-index/tests/fixtures/python/package-import/` (four files: `pkg/__init__.py` (a docstring + `Thing = 1`), `pkg/base.py` (`class BaseHandler: pass`), `pkg/models/__init__.py` (a docstring), `consumer/app.py` with `from pkg import base`, `from pkg import models`, `from pkg import Thing`) asserting the three targets, each edge's `resolution` (inferred/inferred/static), `basis` and `alternateTarget`, and that the persisted MODULE→MODULE IMPORTS edges hold `consumer → pkg` (fed by the static `Thing` import) and NOT `consumer → pkg/models` (fed only by an inferred import); `index_python_imports_resolve` and `index_python_extracts_symbols` stay green; the `simple-app` fixture untouched. Byte-identity (git diff --quiet) of classification (the unresolved classifier — RG-REQ-006-L05), daemon-runtime (payloads are serde of the DTOs), module-queries, trust, LiveGraph, storage's `queries.rs`/`trust_impl.rs`/`crud` (module edges, cycles, `path`, `map`, stats, deps reads), rgr's command layer and imports renderer, and repo-index's src except compose.rs's version line. Every RG-REQ-002 L01–L10 honesty obligation and RG-REQ-006-L06..L11/L13 are PRESERVED by leaving their machinery untouched; the new Referenced-by field follows the unreadable-line pattern (L04); a type with no inferred importer serializes byte-identically (L02)."
      },
      "expected": "exit 0: an inferred import reaches the agent on `explain`'s Referenced-by as an inferred remainder with its basis and alternate, a file with any certain edge stays a certain importer, a type with no inferred importer renders exactly as before, a malformed carrier fails loudly at the read or renders the unreadable line and is never coerced; on the fixture the inferred imports feed no module edge; the unresolved contract, classification, module-edge, cycle, `path`, `map`, stats and deps machinery remain exactly as before (no behavior change in their code); every RG-REQ-002 L01–L10 rule and every deps/imports surface remain exactly as before (no behavior change outside the inferred rows)"
    },
    {
      "checkId": "PSI-C03",
      "obligationIds": [
        "P-PSI-03"
      ],
      "owner": "builder",
      "method": {
        "kind": "command",
        "command": "cargo fmt --check -p repo-graph-indexer -p repo-graph-repo-index -p repo-graph-storage -p repo-graph-agent -p repo-graph-rgr && { cargo clippy -p repo-graph-indexer -p repo-graph-repo-index -p repo-graph-storage -p repo-graph-agent -p repo-graph-rgr --all-targets -- -D warnings > /tmp/psi-c03.txt 2>&1; rc=$?; tail -3 /tmp/psi-c03.txt; test $rc -eq 0; }",
        "cwd": "rust",
        "environment": "candidate tree",
        "inputs": "rustfmt and clippy (deny warnings, all targets) over the five touched crates"
      },
      "expected": "exit 0: no formatting or lint debt enters with the candidate"
    },
    {
      "checkId": "PSI-C04",
      "obligationIds": [
        "RG-REQ-006-L04",
        "RG-REQ-002-L11",
        "RG-REQ-006-L05",
        "RG-REQ-006-L12",
        "P-PSI-01"
      ],
      "owner": "builder",
      "method": {
        "kind": "command",
        "command": "(cd rust && cargo build --release --bin rmap --bin rmapd > /tmp/psi-build.txt 2>&1; rc=$?; tail -1 /tmp/psi-build.txt; exit $rc) && test ! -e /private/tmp/PYTHON-SUBMODULE-IMPORT-1-django-after && mkdir -p /private/tmp/PYTHON-SUBMODULE-IMPORT-1-django-after && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/django' && export RMAP_STATE_ROOT=/private/tmp/PYTHON-SUBMODULE-IMPORT-1-django-after RMAP_SOCKET_PATH=/private/tmp/PYTHON-SUBMODULE-IMPORT-1-django-after/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release:$PATH\" \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" index > /tmp/psi-dj-index.txt 2> /tmp/psi-dj-index.txt.err; echo $? > /tmp/psi-dj-index.txt.rc ) && test \"$(cat /tmp/psi-dj-index.txt.rc)\" = 0 && test ! -e /private/tmp/PYTHON-SUBMODULE-IMPORT-1-fraktag-after && mkdir -p /private/tmp/PYTHON-SUBMODULE-IMPORT-1-fraktag-after && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/FRAKTAG' && export RMAP_STATE_ROOT=/private/tmp/PYTHON-SUBMODULE-IMPORT-1-fraktag-after RMAP_SOCKET_PATH=/private/tmp/PYTHON-SUBMODULE-IMPORT-1-fraktag-after/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release:$PATH\" \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" index > /tmp/psi-fr-index.txt 2> /tmp/psi-fr-index.txt.err; echo $? > /tmp/psi-fr-index.txt.rc ) && test \"$(cat /tmp/psi-fr-index.txt.rc)\" = 0 && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/django' && export RMAP_STATE_ROOT=/private/tmp/PYTHON-SUBMODULE-IMPORT-1-django-after RMAP_SOCKET_PATH=/private/tmp/PYTHON-SUBMODULE-IMPORT-1-django-after/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release:$PATH\" \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" explain 'BaseHandler' > /tmp/psi-dj-explain.txt 2> /tmp/psi-dj-explain.txt.err; echo $? > /tmp/psi-dj-explain.txt.rc ) && test \"$(cat /tmp/psi-dj-explain.txt.rc)\" = 0 && grep -qE '^Referenced by \\(1 files, \\+2 inferred — investigate\\)$' /tmp/psi-dj-explain.txt && grep -qE '^  - django/test/client\\.py$' /tmp/psi-dj-explain.txt && grep -qE '^  - django/core/handlers/wsgi\\.py  \\(inferred: python submodule — alternate django/core/handlers/__init__\\.py\\)$' /tmp/psi-dj-explain.txt && grep -qE '^  - django/core/handlers/asgi\\.py  \\(inferred: python submodule — alternate django/core/handlers/__init__\\.py\\)$' /tmp/psi-dj-explain.txt && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/django' && export RMAP_STATE_ROOT=/private/tmp/PYTHON-SUBMODULE-IMPORT-1-django-after RMAP_SOCKET_PATH=/private/tmp/PYTHON-SUBMODULE-IMPORT-1-django-after/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release:$PATH\" \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" imports django/core/handlers/wsgi.py > /tmp/psi-dj-imports.txt 2> /tmp/psi-dj-imports.txt.err; echo $? > /tmp/psi-dj-imports.txt.rc ) && test \"$(cat /tmp/psi-dj-imports.txt.rc)\" = 0 && grep -qE '^  django/core/handlers/base\\.py  depth=1  inferred$' /tmp/psi-dj-imports.txt && ! grep -q 'django/core/handlers/__init__.py' /tmp/psi-dj-imports.txt && python3 - <<'PY'\nimport sqlite3, json, os, re, collections\ndef db(root, frozen):\n    p = json.load(open(root + \"/registry.json\"))[\"repos\"][0][\"db_path\"]\n    if frozen:\n        w = p + \"-wal\"\n        assert not os.path.exists(w) or os.path.getsize(w) == 0, (\"manager root WAL not empty\", w)\n        return sqlite3.connect(\"file:\" + p + \"?immutable=1\", uri=True)\n    return sqlite3.connect(\"file:\" + p + \"?mode=ro\", uri=True)\ndef one(c, q, *a): return c.execute(q, a).fetchone()[0]\ndef normalizer(c):\n    [ru] = [r[0] for r in c.execute(\"select distinct repo_uid from snapshots\")]\n    def n(v):\n        if isinstance(v, str):\n            v = v.replace(ru, \"<repo>\")\n            if re.fullmatch(r\"indexer:[0-9]+\\.[0-9]+\\.[0-9]+\", v): v = \"<indexer>\"\n        return v\n    return lambda row: tuple(n(v) for v in row)\ndef same(label, x, y):\n    if x != y:\n        sx, sy = set(x), set(y)\n        raise AssertionError((label, len(x), len(y), \"before-only\", [r for r in x if r not in sy][:3], \"after-only\", [r for r in y if r not in sx][:3]))\n# Schema audit (RULE: every persisted column of a compared table is either compared or named here as excluded; a\n# reference column is compared THROUGH the referenced row's stable key or path). A schema-change TRIPWIRE: it fails when the\n# store's schema differs from this declaration; it does not by itself prove that each partitioned query exercises every\n# declared column — the queries below are written to (NODE_ROW, EDGE_ROW/OTHER, UNRES_ROW, and Q for the from-import edges).\nSCHEMA = {\n    \"nodes\": ({\"stable_key\", \"kind\", \"subtype\", \"name\", \"qualified_name\", \"file_uid\", \"parent_node_uid\", \"line_start\", \"col_start\", \"line_end\", \"col_end\", \"signature\", \"visibility\", \"doc_comment\", \"metadata_json\"}, {\"node_uid\", \"snapshot_uid\", \"repo_uid\"}),\n    \"edges\": ({\"source_node_uid\", \"target_node_uid\", \"type\", \"resolution\", \"extractor\", \"line_start\", \"col_start\", \"line_end\", \"col_end\", \"metadata_json\", \"is_type_only\"}, {\"edge_uid\", \"snapshot_uid\", \"repo_uid\"}),\n    \"unresolved_edges\": ({\"source_node_uid\", \"target_key\", \"type\", \"resolution\", \"extractor\", \"category\", \"classification\", \"classifier_version\", \"basis_code\", \"line_start\", \"col_start\", \"line_end\", \"col_end\", \"metadata_json\"}, {\"edge_uid\", \"snapshot_uid\", \"repo_uid\", \"observed_at\"}),\n}\ndef audit(c, schema):\n    for tname, (cmp, excl) in schema.items():\n        cols = {r[1] for r in c.execute(\"pragma table_info(%s)\" % tname)}\n        assert cols == cmp | excl and not cmp & excl, (\"schema audit: a column is neither compared nor declared excluded\", tname, sorted(cols ^ (cmp | excl)))\nNODE_ROW = \"select n.stable_key, f.path, n.kind, n.subtype, n.name, n.qualified_name, p.stable_key, n.parent_node_uid is not null, n.line_start, n.col_start, n.line_end, n.col_end, n.signature, n.visibility, n.doc_comment, n.metadata_json from nodes n left join files f on f.file_uid=n.file_uid left join nodes p on p.node_uid=n.parent_node_uid\"\nEDGE_ROW = \"select sn.stable_key, tn.stable_key, e.type, e.resolution, e.extractor, e.line_start, e.col_start, e.line_end, e.col_end, e.metadata_json, e.is_type_only from edges e join nodes sn on sn.node_uid=e.source_node_uid join nodes tn on tn.node_uid=e.target_node_uid\"\nUNRES_ROW = \"select sn.stable_key, u.target_key, u.type, u.resolution, u.extractor, u.category, u.classification, u.classifier_version, u.basis_code, u.line_start, u.col_start, u.line_end, u.col_end, u.metadata_json from unresolved_edges u join nodes sn on sn.node_uid=u.source_node_uid\"\ndef rows(c, q): n = normalizer(c); return sorted((n(r) for r in c.execute(q)), key=repr)\nB = db(\"/private/tmp/PYTHON-SUBMODULE-IMPORT-1-django-before\", True)\nA = db(\"/private/tmp/PYTHON-SUBMODULE-IMPORT-1-django-after\", False)\nQ = \"\"\"select sf.path, e.line_start, e.col_start, json_extract(e.metadata_json,'$.specifier'), json_extract(e.metadata_json,'$.importedName'), json_extract(e.metadata_json,'$.identifier'), sn.stable_key, tf.path, tn.stable_key, e.resolution, json_extract(e.metadata_json,'$.basis'), json_extract(e.metadata_json,'$.alternateTarget'), e.metadata_json, e.extractor, e.line_end, e.col_end, e.is_type_only from edges e join nodes sn on sn.node_uid=e.source_node_uid join files sf on sf.file_uid=sn.file_uid join nodes tn on tn.node_uid=e.target_node_uid join files tf on tf.file_uid=tn.file_uid where e.type='IMPORTS' and json_extract(e.metadata_json,'$.importedName') is not null\"\"\"\ndef edges(c):\n    n = normalizer(c); out, meta, fixed = {}, {}, {}\n    for sp, ln, col, spec, name, ident, ssk, tp, tsk, res, basis, alt, mj, ext, le, ce, ito in c.execute(Q):\n        k = (sp, ln, col, spec, name, ident, n((ssk,))[0]); assert k not in out, (\"edge identity not unique\", k); out[k] = (tp, n((tsk,))[0], res, basis, alt); meta[k] = json.loads(mj); fixed[k] = n((ext, le, ce, ito))\n    return out, meta, fixed\n# the amended RG-REQ-006-L04 rule, recomputed on the BEFORE store (SLICE_DOC 2.1 step 1)\npaths = set(r[0] for r in B.execute(\"select path from files\"))\ndefs = collections.defaultdict(set)\nfor name, path in B.execute(\"select n.name, f.path from nodes n join files f on f.file_uid=n.file_uid where n.kind='SYMBOL' and n.qualified_name=n.name\"): defs[path].add(name)\nbinds = {p: json.loads(j) for p, j in B.execute(\"select f.path, s.import_bindings_json from file_signals s join files f on f.file_uid=s.file_uid where s.import_bindings_json is not null\")}\ndef submodule_itself(b, d, y):\n    spec, imp = b.get(\"specifier\", \"\"), b.get(\"importedName\")\n    if imp is not None: return imp == y and spec in (\".\", d.replace(\"/\", \".\"))\n    return spec == d.replace(\"/\", \".\") + \".\" + y\ndef rule(tp, y):\n    if not tp.endswith(\"/__init__.py\"): return None\n    d = tp[:-len(\"/__init__.py\")]\n    sub = next((p for p in (d + \"/\" + y + \"/__init__.py\", d + \"/\" + y + \".py\") if p in paths), None)\n    if sub is None: return (tp, \"symbol\")\n    if y in defs.get(tp, ()): return (tp, \"init-defines\")\n    ys = [b for b in binds.get(tp, []) if b.get(\"identifier\") == y]\n    if any(not submodule_itself(b, d, y) for b in ys): return (tp, \"init-binds-other\")\n    return (sub, \"submodule-itself\" if ys else \"submodule\")\nassert one(B, \"select count(*) from snapshots\") == 1 and one(A, \"select count(*) from snapshots\") == 1, \"one snapshot per django store (PSI-C04 runs before PSI-C06's refresh)\"\naudit(B, SCHEMA); audit(A, SCHEMA)\n(EB, MB, XB), (EA, MA, XA) = edges(B), edges(A)\nassert set(EB) == set(EA), (\"from-import edge identities differ\", len(set(EB) ^ set(EA)))\nkinds = collections.Counter(); movers = {}\nfor k, (tp, tsk, res, basis, alt) in EB.items():\n    r = rule(tp, k[4])\n    if r is None: continue\n    kinds[r[1]] += 1\n    if r[0] != tp: movers[k] = (r[0], tp)\nprint(\"rule on the before-store:\", dict(kinds), \"| movers:\", len(movers), \"| from-import edges:\", len(EB))\nassert len(movers) == 1440, (\"before-root sanity: movers\", len(movers))\nsk = dict(A.execute(\"select f.path, n.stable_key from nodes n join files f on f.file_uid=n.file_uid where n.kind='FILE'\"))\nnA = normalizer(A); nsk = {p: nA((v,))[0] for p, v in sk.items()}\nbad = []\nfor k, got in EA.items():\n    if k in movers:\n        want = (movers[k][0], nsk[movers[k][0]], \"inferred\", \"python_submodule\", sk[movers[k][1]])\n    else:\n        want = (EB[k][0], EB[k][1], EB[k][2], None, None)\n    if got != want: bad.append((k, got, want))\nassert not bad, (\"edges not where the rule puts them\", len(bad), bad[:5])\nbad_fixed = [k for k in EB if XA[k] != XB[k]]\nassert not bad_fixed, (\"a from-import edge's extractor, line_end, col_end or is_type_only changed (only target, resolution and the two metadata keys may move)\", len(bad_fixed), [(k, XB[k], XA[k]) for k in bad_fixed[:3]])\nbad_meta = [k for k in EB if {x: y for x, y in MA[k].items() if x not in (\"alternateTarget\", \"basis\")} != MB[k] or (k in movers) != (\"alternateTarget\" in MA[k] and \"basis\" in MA[k]) or \"basis\" in MB[k] or \"alternateTarget\" in MB[k]]\nassert not bad_meta, (\"a from-import edge's metadata changed beyond the two added keys\", len(bad_meta), [(k, MB[k], MA[k]) for k in bad_meta[:3]])\nfor sp, ln in ((\"django/core/handlers/wsgi.py\", 5), (\"django/shortcuts.py\", 13)):\n    for k in EB:\n        if k[0] == sp and k[1] == ln: print(\"code-under-analysis\", sp + \":\" + str(ln), \"before\", EB[k], \"after\", EA[k])\ndef by_type(c): return sorted(c.execute(\"select e.type, e.resolution, count(*) from edges e join nodes a on a.node_uid=e.source_node_uid where not (e.type='IMPORTS' and a.kind='MODULE') group by 1,2\").fetchall())\ntb, ta = by_type(B), by_type(A)\nprint(\"edges by type/resolution (MODULE->MODULE excluded) before\", tb, \"after\", ta)\nwant_a = sorted([(t, r, n - (1440 if (t, r) == (\"IMPORTS\", \"static\") else 0)) for t, r, n in tb] + [(\"IMPORTS\", \"inferred\", 1440)])\nassert ta == want_a, (\"only the 1440 movers change certainty; no other edge moves\", ta, want_a)\nsame(\"an unresolved row changed (RG-REQ-006-L05)\", rows(B, UNRES_ROW), rows(A, UNRES_ROW))\nsame(\"a node changed (P-PSI-01)\", rows(B, NODE_ROW), rows(A, NODE_ROW))\nOTHER = EDGE_ROW + \" where not (e.type='IMPORTS' and (sn.kind='MODULE' or json_extract(e.metadata_json,'$.importedName') is not null))\"\nsame(\"an edge outside the from-imports and the persisted module graph changed (P-PSI-01)\", rows(B, OTHER), rows(A, OTHER))\ndef own(c): return dict(c.execute(\"select f.path, mn.qualified_name from edges e join nodes mn on mn.node_uid=e.source_node_uid join nodes fn on fn.node_uid=e.target_node_uid join files f on f.file_uid=fn.file_uid where e.type='OWNS' and mn.kind='MODULE'\"))\ndef derived(c):\n    o = own(c); s = set()\n    for sp, tp, res in c.execute(\"select sf.path, tf.path, e.resolution from edges e join nodes a on a.node_uid=e.source_node_uid join files sf on sf.file_uid=a.file_uid join nodes b on b.node_uid=e.target_node_uid join files tf on tf.file_uid=b.file_uid where e.type='IMPORTS' and a.kind='FILE' and b.kind='FILE'\"):\n        if res == \"inferred\": continue\n        m1, m2 = o.get(sp), o.get(tp)\n        if m1 and m2 and m1 != m2: s.add((m1, m2))\n    return s\ndef stored(c): return c.execute(\"select a.qualified_name, b.qualified_name from edges e join nodes a on a.node_uid=e.source_node_uid join nodes b on b.node_uid=e.target_node_uid where e.type='IMPORTS' and a.kind='MODULE' and b.kind='MODULE'\").fetchall()\nsb, sa = stored(B), stored(A)\nassert len(sb) == len(set(sb)) == 2451 and set(sb) == derived(B), \"before-root sanity: persisted module edges\"\nassert len(sa) == len(set(sa)) and set(sa) == derived(A), \"an inferred import fed the persisted module graph (RG-REQ-002-L11)\"\nprint(\"persisted MODULE->MODULE IMPORTS before\", len(sb), \"after\", len(sa), \"| lost\", len(set(sb) - set(sa)), \"gained\", len(set(sa) - set(sb)))\nassert set(sa) <= set(sb), \"a module pair appeared that no certain import supports\"\ndef importers(c, p): return sorted(set(c.execute(\"select sf.path, e.resolution from edges e join nodes tn on tn.node_uid=e.target_node_uid join files tf on tf.file_uid=tn.file_uid join nodes sn on sn.node_uid=e.source_node_uid join files sf on sf.file_uid=sn.file_uid where e.type='IMPORTS' and tf.path=?\", (p,))))\nprint(\"base.py importers before\", importers(B, \"django/core/handlers/base.py\"), \"after\", importers(A, \"django/core/handlers/base.py\"))\nassert importers(A, \"django/core/handlers/base.py\") == [(\"django/core/handlers/asgi.py\", \"inferred\"), (\"django/core/handlers/wsgi.py\", \"inferred\"), (\"django/test/client.py\", \"static\")]\nfor label, c in ((\"django before\", B), (\"django after\", A)):\n    print(label, \"nodes\", one(c, \"select count(*) from nodes\"), \"edges\", one(c, \"select count(*) from edges\"), \"unresolved\", one(c, \"select count(*) from unresolved_edges\"), \"toolchain\", one(c, \"select toolchain_json from snapshots\"))\ndef dump(c): return rows(c, NODE_ROW), rows(c, EDGE_ROW), rows(c, UNRES_ROW)\nFB, FA = db(\"/private/tmp/PYTHON-SUBMODULE-IMPORT-1-fraktag-before\", True), db(\"/private/tmp/PYTHON-SUBMODULE-IMPORT-1-fraktag-after\", False)\nassert one(FB, \"select count(*) from snapshots\") == 1 and one(FA, \"select count(*) from snapshots\") == 1, \"one snapshot per FRAKTAG store\"\naudit(FB, SCHEMA); audit(FA, SCHEMA)\nnb_, eb_, ub_ = dump(FB); na_, ea_, ua_ = dump(FA)\nprint(\"fraktag before nodes/edges/unresolved\", len(nb_), len(eb_), len(ub_), \"after\", len(na_), len(ea_), len(ua_))\nsame(\"FRAKTAG nodes differ - the Python stage leaked\", nb_, na_)\nsame(\"FRAKTAG edges differ - the Python stage leaked\", eb_, ea_)\nsame(\"FRAKTAG unresolved rows differ - the Python stage leaked\", ub_, ua_)\nprint(\"PSI-C04 store oracles ok\")\nPY",
        "cwd": ".",
        "environment": "the candidate binary indexes /Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/django (4d455ae) and /Users/apple/Documents/APLICATII BIJUTERIE/FRAKTAG into NEW isolated `-after` roots, each created only when absent (never an unconditional delete), under RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off (builder-owned, deleted in PSI-C05); the manager's `-before` roots /private/tmp/PYTHON-SUBMODULE-IMPORT-1-django-before and /private/tmp/PYTHON-SUBMODULE-IMPORT-1-fraktag-before (indexed 2026-09-23 by the v0.19.0 binary = this HEAD's rust tree) are READ with sqlite3 only, `immutable=1` after asserting their WAL empty, located through their own registry.json, never served, never reindexed; the oracle recomputes the amended L04 rule on the BEFORE store and checks every from-import edge landed where the rule puts it",
        "inputs": "BEFORE (measured 2026-09-23 on /private/tmp/PYTHON-SUBMODULE-IMPORT-1-django-before, `immutable=1`): 17,614 IMPORTS edges (15,163 FILE→FILE + 2,451 persisted MODULE→MODULE), no `inferred` edge of any type, 3,056 unresolved IMPORTS; 15,068 from-import edges (unique by source path, line, column, specifier, importedName, identifier), 6,770 of them landing on an `__init__.py`; the rule computed on the before-store: 5,330 name a symbol (no `X/Y/__init__.py` or `X/Y.py`), 0 have an init that defines `Y`, 0 an init that binds `Y` to anything but the submodule, 1,424 an init with no binding of `Y`, 16 an init whose only bindings of `Y` are the submodule itself (`django/db/models/__init__.py:2` `from django.db.models import signals` ×10 importers; `django/conf/__init__.py` `global_settings` ×2; `django/template/__init__.py` `autoreload` ×2; `django/tasks/__init__.py` `checks`, `signals` ×1 each) — so exactly 1,440 movers, 985 of them onto a subpackage init (`django/db/__init__.py` loses 709, mostly `from django.db import models`); `django/core/handlers/wsgi.py:5` `from django.core.handlers import base` → `django/core/handlers/__init__.py` static; `django/shortcuts.py:13` `from django.template import loader` → `django/template/__init__.py` static; `django/core/handlers/base.py` is imported by ONE file (`django/test/client.py`, static); `explain BaseHandler` `Referenced by (1 files)` and `imports django/core/handlers/wsgi.py` listing `django/core/handlers/__init__.py  depth=1  static` (the manager's captures on the before-root). AFTER (predicted; the product is the truth — a differing literal is a STOP-and-report naming the import line, its init's facts and the probed keys): every from-import edge where the rule puts it — the 1,440 movers on `X/Y/__init__.py` / `X/Y.py` with `resolution: inferred`, `basis: python_submodule`, `alternateTarget` = the init's stable key; every other from-import edge with its before target and resolution and neither key; exactly 1,440 `inferred` edges in the store, all IMPORTS; every other edge type/resolution count unchanged; ROW-LEVEL multisets unchanged (the repository uid normalized to `<repo>` and an `indexer:x.y.z` value to `<indexer>`, because the two roots carry different repo uids and the orchestrator-derived edges carry the bumped indexer version by design; only row identity columns — `node_uid`, `edge_uid`, `snapshot_uid`, `file_uid` — and the freshness field `observed_at` are excluded): every unresolved row (source node's stable key, target key, type, resolution, extractor, category, classification, classifier version, basis, span, metadata), every node (stable key, file path, kind, subtype, name, qualified name, parent node's stable key and whether a parent is recorded, span, signature, visibility, doc comment, metadata), every edge that is neither a from-import nor a persisted MODULE→MODULE import (source and target nodes' stable keys, type, resolution, extractor, span, metadata, `is_type_only`), and every from-import edge — identified by its source file, span, specifier, imported name, identifier AND source node's stable key — with its target file and target node's stable key, its extractor, `line_end`, `col_end` and `is_type_only` unchanged, and its metadata except the two added keys (`alternateTarget`, `basis` present exactly on the movers); a schema-change tripwire asserts, on all four stores, that every persisted column of `nodes`, `edges` and `unresolved_edges` is declared either compared (a reference column through the referenced row's stable key or path) or excluded (`node_uid`, `edge_uid`, `snapshot_uid`, `repo_uid`, `observed_at`), so a store with an undeclared column stops the check — it checks the declaration, not that each query exercises every declared column; the queries are written to (NODE_ROW, EDGE_ROW/OTHER, UNRES_ROW, and Q for the from-import edges); the persisted MODULE→MODULE IMPORTS edges equal the pairs derived from the store's own non-inferred FILE→FILE imports and ownership (RG-REQ-002-L11: an inferred import is never a certain module edge) — REPORTED count, predicted 2,451 → 2,084 (367 module pairs were fed only by movers; the stored edge total 84,985 → 84,618 accordingly) and no new pair; `base.py`'s importers exactly {asgi.py inferred, wsgi.py inferred, client.py static}; `explain BaseHandler` `Referenced by (1 files, +2 inferred — investigate)` with `django/test/client.py` and then the two inferred rows naming the alternate `django/core/handlers/__init__.py`; `imports django/core/handlers/wsgi.py` lists `django/core/handlers/base.py  depth=1  inferred` and no longer the init. FRAKTAG (TypeScript/JavaScript plus four Python files, none in a package — no `__init__.py`, so the stage has no input): node, edge and unresolved row multisets identical under the same row-level rule and normalization (1,402 / 801 / 1,970 rows on the before-store)."
      },
      "expected": "exit 0: on the corpus that motivated RC-7 every from-package import of a module file lands on that file as an inferred, investigable fact with the package init named as its alternate, every init that binds the name keeps its static target, no inferred import enters the persisted module graph (the module-pair count falls — stated, not hidden), `explain`'s Referenced-by and `imports <file>` show the certainty, every unresolved row, every node and every edge outside the from-imports and the derived module graph remain exactly as before row by row (no behavior change), and a corpus with no Python package is identical row by row (no behavior change)"
    },
    {
      "checkId": "PSI-C06",
      "obligationIds": [
        "RG-REQ-001-L09",
        "RG-REQ-006-L04",
        "RG-REQ-002-L11"
      ],
      "owner": "builder",
      "method": {
        "kind": "command",
        "command": "test -x rust/target/release/rmap && test -d /private/tmp/PYTHON-SUBMODULE-IMPORT-1-django-after && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/django' && export RMAP_STATE_ROOT=/private/tmp/PYTHON-SUBMODULE-IMPORT-1-django-after RMAP_SOCKET_PATH=/private/tmp/PYTHON-SUBMODULE-IMPORT-1-django-after/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release:$PATH\" \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" refresh > /tmp/psi-dj-refresh.txt 2> /tmp/psi-dj-refresh.txt.err; echo $? > /tmp/psi-dj-refresh.txt.rc ) && test \"$(cat /tmp/psi-dj-refresh.txt.rc)\" = 0 && python3 - <<'PY'\nimport sqlite3, json\np = json.load(open(\"/private/tmp/PYTHON-SUBMODULE-IMPORT-1-django-after/registry.json\"))[\"repos\"][0][\"db_path\"]\nassert p.startswith(\"/private/tmp/PYTHON-SUBMODULE-IMPORT-1-django-after/\"), (\"after-root registry points outside the after-root\", p)\nc = sqlite3.connect(\"file:\" + p + \"?mode=ro\", uri=True)\nsnaps = c.execute(\"select snapshot_uid, kind, parent_snapshot_uid, status from snapshots order by created_at\").fetchall()\nassert len(snaps) == 2 and snaps[0][1] == \"full\" and snaps[1][1] == \"refresh\" and snaps[1][2] == snaps[0][0] and all(s[3] == \"ready\" for s in snaps), (\"one full index, then one delta refresh of it\", snaps)\nF, R = snaps[0][0], snaps[1][0]\nSCHEMA = {\n    \"file_versions\": ({\"file_uid\", \"content_hash\", \"ast_hash\", \"extractor\", \"parse_status\", \"size_bytes\", \"line_count\"}, {\"snapshot_uid\", \"indexed_at\"}),\n    \"file_signals\": ({\"file_uid\", \"import_bindings_json\", \"package_dependencies_json\", \"tsconfig_aliases_json\"}, {\"snapshot_uid\"}),\n    \"edges\": ({\"source_node_uid\", \"target_node_uid\", \"type\", \"resolution\", \"extractor\", \"line_start\", \"col_start\", \"line_end\", \"col_end\", \"metadata_json\", \"is_type_only\"}, {\"edge_uid\", \"snapshot_uid\", \"repo_uid\"}),\n    \"unresolved_edges\": ({\"source_node_uid\", \"target_key\", \"type\", \"resolution\", \"extractor\", \"category\", \"classification\", \"classifier_version\", \"basis_code\", \"line_start\", \"col_start\", \"line_end\", \"col_end\", \"metadata_json\"}, {\"edge_uid\", \"snapshot_uid\", \"repo_uid\", \"observed_at\"}),\n    \"nodes\": ({\"stable_key\", \"kind\", \"subtype\", \"name\", \"qualified_name\", \"file_uid\", \"parent_node_uid\", \"line_start\", \"col_start\", \"line_end\", \"col_end\", \"signature\", \"visibility\", \"doc_comment\", \"metadata_json\"}, {\"node_uid\", \"snapshot_uid\", \"repo_uid\"}),\n}\nfor tname, (cmp, excl) in SCHEMA.items():\n    cols = {r[1] for r in c.execute(\"pragma table_info(%s)\" % tname)}\n    assert cols == cmp | excl and not cmp & excl, (\"schema audit: a column is neither compared nor declared excluded\", tname, sorted(cols ^ (cmp | excl)))\ndef rows(q, u): return sorted(c.execute(q, (u,)).fetchall(), key=repr)\ndef same(label, q):\n    x, y = rows(q, F), rows(q, R)\n    if x != y:\n        sx, sy = set(x), set(y)\n        raise AssertionError((label, len(x), len(y), \"full-only\", [r for r in x if r not in sy][:3], \"refresh-only\", [r for r in y if r not in sx][:3]))\n    return len(x)\nn_files = same(\"file versions differ: the tree changed or copy-forward altered a file (not a no-change refresh)\", \"select v.file_uid, v.content_hash, v.ast_hash, v.extractor, v.parse_status, v.size_bytes, v.line_count from file_versions v where v.snapshot_uid = ?\")\nn_sig = same(\"file_signals differ: the resolver and classifier inputs are not the full index's\", \"select s.file_uid, s.import_bindings_json, s.package_dependencies_json, s.tsconfig_aliases_json from file_signals s where s.snapshot_uid = ?\")\nn_edges = same(\"IMPORTS/CALLS edges differ between the full index and the refresh\", \"select s.stable_key, t.stable_key, e.type, e.resolution, e.extractor, e.line_start, e.col_start, e.line_end, e.col_end, e.metadata_json, e.is_type_only from edges e join nodes s on s.node_uid = e.source_node_uid join nodes t on t.node_uid = e.target_node_uid where e.snapshot_uid = ? and e.type in ('IMPORTS', 'CALLS')\")\nn_unres = same(\"unresolved rows differ between the full index and the refresh\", \"select s.stable_key, u.target_key, u.type, u.resolution, u.extractor, u.category, u.classification, u.classifier_version, u.basis_code, u.line_start, u.col_start, u.line_end, u.col_end, u.metadata_json from unresolved_edges u join nodes s on s.node_uid = u.source_node_uid where u.snapshot_uid = ?\")\ninf = c.execute(\"select count(*) from edges where snapshot_uid = ? and resolution = 'inferred'\", (R,)).fetchone()[0]\ndef rep_rows(q): return rows(q, F) == rows(q, R)\nprint(\"REPORTED (not asserted; outside D-PSI-REFRESH-SIGNALS-1's IMPORTS/CALLS scope): nodes equal\", rep_rows(\"select n.stable_key, n.file_uid, n.kind, n.subtype, n.name, n.qualified_name, p.stable_key, n.parent_node_uid is not null, n.line_start, n.col_start, n.line_end, n.col_end, n.signature, n.visibility, n.doc_comment, n.metadata_json from nodes n left join nodes p on p.node_uid = n.parent_node_uid where n.snapshot_uid = ?\"), \"| other edge types equal\", rep_rows(\"select s.stable_key, t.stable_key, e.type, e.resolution, e.extractor, e.line_start, e.col_start, e.line_end, e.col_end, e.metadata_json, e.is_type_only from edges e join nodes s on s.node_uid = e.source_node_uid join nodes t on t.node_uid = e.target_node_uid where e.snapshot_uid = ? and e.type not in ('IMPORTS', 'CALLS')\"))\nprint(\"PSI-C06 OK refresh parity:\", n_files, \"file versions,\", n_sig, \"file_signals rows,\", n_edges, \"IMPORTS+CALLS edges (\", inf, \"inferred ),\", n_unres, \"unresolved rows\")\nPY",
        "cwd": ".",
        "environment": "the django after-root /private/tmp/PYTHON-SUBMODULE-IMPORT-1-django-after created by PSI-C04 (candidate binary, stdio, enrichment and retention off), refreshed once with no working-tree change; its store is located through its own registry.json and read `mode=ro` after the stdio daemon exits (never `immutable=1`: a just-served store may hold WAL pages)",
        "inputs": "REFRESH PARITY (D-PSI-REFRESH-SIGNALS-1 = A; RG-REQ-001-L09): after PSI-C04's full index of django, `rmap refresh` with no change takes the delta path (a `refresh` snapshot whose parent is the full one; every file copied forward — orchestrator.rs falls back to a full index only when nothing is copied, :1758) and re-resolves every copied-forward extraction edge. Compared between the two snapshots, each as a row-level multiset (only row identity — `node_uid`, `edge_uid`, `snapshot_uid` — and the freshness fields `indexed_at`/`observed_at` excluded; same root, so no normalization): the INPUTS — every file version (file uid, content hash, AST hash, extractor, parse status, size, line count: proves the tree did not change and nothing was re-extracted differently) and every `file_signals` row with all its payload columns (file uid, import bindings, package dependencies, tsconfig aliases: proves the copied signals are the full index's, neither substituted nor duplicated); the OUTPUTS — every IMPORTS and CALLS edge (source and target stable keys, type, resolution, extractor, span, metadata incl. `basis`/`alternateTarget`, `is_type_only`) and every unresolved row (source stable key, target key, type, resolution, extractor, category, classification, classifier version, basis, span, metadata incl. any candidate evidence). An executable schema audit asserts that every persisted column of `file_versions`, `file_signals`, `edges`, `unresolved_edges` and `nodes` is compared (a reference column through the referenced row's stable key) or one of the declared exclusions (`snapshot_uid`, `repo_uid`, `node_uid`, `edge_uid`, `indexed_at`, `observed_at`). Node parity (stable key, file, parent node's stable key, span, signature, metadata …) and other-edge-type parity are REPORTED, not asserted (outside the record's IMPORTS/CALLS scope; a difference there is stated in the report, not hidden). The oracle discriminates: without the load a no-change refresh resolves every file with an EMPTY `import_bindings_by_file` and classifies with empty per-file signals (orchestrator.rs:1007/:1098; the bindings drive the namespace- and named-import CALLS stages, resolver.rs:935/:1127) — INFERRED from the source, not run on HEAD (no rmap on a HEAD binary in this item). On the Python rule itself django has no init that binds a submodule's name to something else (0 on the before-store, PSI-C04), so the rule's refresh discrimination is the orchestrator test of PSI-C01; this check proves the real SQLite copy-forward and read path end to end."
      },
      "expected": "exit 0 and `PSI-C06 OK`: a no-change delta refresh of django starts from the same inputs as its full index (identical file versions and identical `file_signals` rows, payloads included) and yields the same IMPORTS and CALLS edges (endpoints, type, resolution, extractor, span, metadata including every inferred edge's `basis` and `alternateTarget`) and the same unresolved rows (category, classification, basis, span and metadata) row by row — for these facts a refresh yields what a full index yields (RG-REQ-001-L09), and the inferred submodule imports keep their certainty and alternate across a refresh (RG-REQ-002-L11)"
    },
    {
      "checkId": "PSI-C07",
      "obligationIds": [
        "RG-REQ-001-L06",
        "RG-REQ-011-L06",
        "P-PSI-03"
      ],
      "owner": "builder",
      "method": {
        "kind": "command",
        "command": "git cat-file -e HEAD:docs/assurance/TOOLCHAIN-STALENESS-1/implementation-review.json && git show HEAD:docs/assurance/TOOLCHAIN-STALENESS-1/implementation-review.json | python3 -c \"import json,sys; d=json.load(sys.stdin); sys.exit(0 if (d.get('workItemId'), d.get('result')) == ('TOOLCHAIN-STALENESS-1', 'accepted') else 1)\" && test -x rust/target/release/rmap && test -d /private/tmp/PYTHON-SUBMODULE-IMPORT-1-django-after && test ! -e /private/tmp/PYTHON-SUBMODULE-IMPORT-1-django-stale && M0=\"$(shasum -a 256 /private/tmp/PYTHON-SUBMODULE-IMPORT-1-django-before/registry.json /private/tmp/PYTHON-SUBMODULE-IMPORT-1-django-before/databases/*)\" && cp -R /private/tmp/PYTHON-SUBMODULE-IMPORT-1-django-before /private/tmp/PYTHON-SUBMODULE-IMPORT-1-django-stale && sed -i '' 's#/private/tmp/PYTHON-SUBMODULE-IMPORT-1-django-before/#/private/tmp/PYTHON-SUBMODULE-IMPORT-1-django-stale/#g' /private/tmp/PYTHON-SUBMODULE-IMPORT-1-django-stale/registry.json && ! grep -q '/private/tmp/PYTHON-SUBMODULE-IMPORT-1-django-before' /private/tmp/PYTHON-SUBMODULE-IMPORT-1-django-stale/registry.json && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/django' && export RMAP_STATE_ROOT=/private/tmp/PYTHON-SUBMODULE-IMPORT-1-django-stale RMAP_SOCKET_PATH=/private/tmp/PYTHON-SUBMODULE-IMPORT-1-django-stale/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release:$PATH\" \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" orient > /tmp/psi-stale-orient.txt 2> /tmp/psi-stale-orient.txt.err; echo $? > /tmp/psi-stale-orient.txt.rc ) && test \"$(cat /tmp/psi-stale-orient.txt.rc)\" = 0 && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/django' && export RMAP_STATE_ROOT=/private/tmp/PYTHON-SUBMODULE-IMPORT-1-django-stale RMAP_SOCKET_PATH=/private/tmp/PYTHON-SUBMODULE-IMPORT-1-django-stale/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release:$PATH\" \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" check > /tmp/psi-stale-check.txt 2> /tmp/psi-stale-check.txt.err; echo $? > /tmp/psi-stale-check.txt.rc ) && test \"$(cat /tmp/psi-stale-check.txt.rc)\" = 2 && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/django' && export RMAP_STATE_ROOT=/private/tmp/PYTHON-SUBMODULE-IMPORT-1-django-stale RMAP_SOCKET_PATH=/private/tmp/PYTHON-SUBMODULE-IMPORT-1-django-stale/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release:$PATH\" \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" check --json > /tmp/psi-stale-check.json 2> /tmp/psi-stale-check.json.err; echo $? > /tmp/psi-stale-check.json.rc ) && test \"$(cat /tmp/psi-stale-check.json.rc)\" = 2 && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/django' && export RMAP_STATE_ROOT=/private/tmp/PYTHON-SUBMODULE-IMPORT-1-django-after RMAP_SOCKET_PATH=/private/tmp/PYTHON-SUBMODULE-IMPORT-1-django-after/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release:$PATH\" \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" check --json > /tmp/psi-after-check.json 2> /tmp/psi-after-check.json.err; echo $? > /tmp/psi-after-check.json.rc ) && echo \"REPORTED after-root check exit $(cat /tmp/psi-after-check.json.rc)\" && test \"$M0\" = \"$(shasum -a 256 /private/tmp/PYTHON-SUBMODULE-IMPORT-1-django-before/registry.json /private/tmp/PYTHON-SUBMODULE-IMPORT-1-django-before/databases/*)\" && rm -rf /private/tmp/PYTHON-SUBMODULE-IMPORT-1-django-stale && test ! -e /private/tmp/PYTHON-SUBMODULE-IMPORT-1-django-stale && python3 - <<'PY'\nimport json, re\nsrc = open(\"rust/crates/indexer/src/orchestrator.rs\", encoding=\"utf-8\").read()\n[C] = re.findall(r'^const INDEXER_VERSION: &str = \"(indexer:[0-9]+\\.[0-9]+\\.[0-9]+)\";$', src, re.M)\nassert C != \"indexer:1.0.0\", (\"the candidate's INDEXER_VERSION must differ from the v0.19.0 stamp\", C)\nline = re.compile(r\"^.*indexer facts from indexer:1\\.0\\.0 \\(current \" + re.escape(C) + r\"\\) — run rmap repo rebuild .+ to refresh$\", re.M)\nfor f in (\"/tmp/psi-stale-orient.txt\", \"/tmp/psi-stale-check.txt\"):\n    text = open(f, encoding=\"utf-8\").read()\n    assert line.search(text), (\"the RG-REQ-001-L06 indexer line is missing\", f, text[-800:])\ndef walk(v):\n    if isinstance(v, dict):\n        yield v\n        for x in v.values(): yield from walk(x)\n    elif isinstance(v, list):\n        for x in v: yield from walk(x)\nenv = json.load(open(\"/tmp/psi-stale-check.json\"))\nts = env[\"value\"][\"toolchain_staleness\"]\nassert ts.get(\"state\") == \"stale\", (\"the copied v0.19.0 store must read stale under the candidate\", ts)\nfams = ts[\"families\"]\nassert [f for f in fams if f.get(\"family\") == \"indexer\"] == [{\"family\": \"indexer\", \"snapshot_version\": \"indexer:1.0.0\", \"current_version\": C}], (\"the indexer family entry\", fams)\nothers = [f for f in fams if f.get(\"family\") != \"indexer\"]\nprint(\"REPORTED extractor families also stale on the v0.19.0 django store (an earlier slice's Python/JavaScript extractor bump, if any):\", others)\nconds = [x for x in walk(env) if x.get(\"code\") == \"TOOLCHAIN_STALENESS\"]\nassert conds and all(x.get(\"status\") == \"incomplete\" for x in conds), (\"TOOLCHAIN_STALENESS must be incomplete\", conds)\nfresh = json.load(open(\"/tmp/psi-after-check.json\"))[\"value\"][\"toolchain_staleness\"]\nassert fresh == {\"state\": \"current\"}, (\"the candidate's own full index and refresh must read current\", fresh)\nprint(\"PSI-C07 OK stale:\", fams, \"| after-root:\", fresh)\nPY",
        "cwd": ".",
        "environment": "a builder-owned `cp -R` copy /private/tmp/PYTHON-SUBMODULE-IMPORT-1-django-stale of the manager's v0.19.0 django before-root /private/tmp/PYTHON-SUBMODULE-IMPORT-1-django-before (registry `db_path` rewritten to the copy — the CAM-A04 shape; the manager's root's registry and database digests asserted unchanged afterwards; the copy deleted by this check and again by PSI-C05 if this check stopped early), served by the candidate under stdio with enrichment and retention off; plus `check --json` on the builder's django after-root (after PSI-C06's refresh); gated on TOOLCHAIN-STALENESS-1's accepted implementation-review record at HEAD",
        "inputs": "STALE-STORE LINE (RG-REQ-001-L06 as amended by RG-BOOTSTRAP-INPUT-9; D-REFRESH-STALE-1 = B): the before-store's `toolchain_json` is `{\"extractors\":[\"ts-core:0.2.0\",\"c-core:0.1.0\",\"cpp-core:0.1.0\",\"java-core:0.1.0\",\"python-core:0.1.0\",\"rust-core:0.2.0\"],\"indexer\":\"indexer:1.0.0\"}` (one `full` snapshot; OBSERVED 2026-09-24, `immutable=1`). Served by the candidate (INDEXER_VERSION C = one minor above HEAD's; `indexer:1.1.0` when TOOLCHAIN-STALENESS-1 leaves it at 1.0.0), `orient` (exit 0) and `check` render the line `indexer facts from indexer:1.0.0 (current C) — run rmap repo rebuild <path> to refresh`; `check` exits 2 and `check --json` exits 2 (at v0.19.0 every condition of `check` on django passes — `smoke-runs/2026-09-22T22-39-58Z/django-check---full.txt`, `Verdict: PASS@Fresh`, same commit 4d455ae — so INCOMPLETE is the staleness); `check --json`'s `value.toolchain_staleness` is `stale` with exactly one `indexer` family `{\"family\":\"indexer\",\"snapshot_version\":\"indexer:1.0.0\",\"current_version\":C}` (an extractor family appears only if an earlier slice bumped a Python or JavaScript extractor — reported; django has no C/C++ file, so C++ is never evaluated); every `TOOLCHAIN_STALENESS` condition has status `incomplete`; the builder's own after-root (full index then refresh by the candidate) reads `{\"state\":\"current\"}` — a refresh by the same binary is never falsely stale; its `check` exit is REPORTED."
      },
      "expected": "exit 0 and `PSI-C07 OK`: an existing store written by the previous indexer keeps its old bindings until an explicit `rmap repo rebuild <path>` and the running binary says so on `orient` and `check` and as the exact `indexer` family in `check --json`, with `check` INCOMPLETE (exit 2) — RG-REQ-001-L06's consequence of this slice's version bump; a store written by the candidate reads current; the manager's before-root is byte-identical afterwards and the copy is gone (RG-REQ-011-L06)"
    },
    {
      "checkId": "PSI-C05",
      "obligationIds": [
        "RG-REQ-011-L06",
        "P-PSI-03"
      ],
      "owner": "builder",
      "method": {
        "kind": "command",
        "command": "test \"$(grep -E '^registry-before [0-9a-f]{64}$' .agent-manager/slices/PYTHON-SUBMODULE-IMPORT-1/build-progress.md | tail -1 | cut -d' ' -f2)\" = \"$(shasum -a 256 \"$HOME/Library/Application Support/repo-graph/registry.json\" | cut -d' ' -f1)\" && { PIDS=$(pgrep -x rmapd | paste -sd, -); [ -z \"$PIDS\" ] || ! ps -o command= -p \"$PIDS\" | grep -v '/\\.local/bin/rmapd' | grep -q .; } && rm -rf /private/tmp/PYTHON-SUBMODULE-IMPORT-1-django-after /private/tmp/PYTHON-SUBMODULE-IMPORT-1-fraktag-after /private/tmp/PYTHON-SUBMODULE-IMPORT-1-django-stale && ! test -e /private/tmp/PYTHON-SUBMODULE-IMPORT-1-django-after && ! test -e /private/tmp/PYTHON-SUBMODULE-IMPORT-1-fraktag-after && ! test -e /private/tmp/PYTHON-SUBMODULE-IMPORT-1-django-stale && ls -d /private/tmp/PYTHON-SUBMODULE-IMPORT-1-django-before /private/tmp/PYTHON-SUBMODULE-IMPORT-1-fraktag-before >/dev/null && ! git status --short | grep -vE '^[ MA?][ MA?] (rust/crates/indexer/src/resolver\\.rs|rust/crates/indexer/src/orchestrator\\.rs|rust/crates/repo\\-index/src/compose\\.rs|rust/crates/repo\\-index/tests/integration\\.rs|rust/crates/repo\\-index/tests/fixtures/python/package\\-import/pkg/__init__\\.py|rust/crates/repo\\-index/tests/fixtures/python/package\\-import/pkg/base\\.py|rust/crates/repo\\-index/tests/fixtures/python/package\\-import/pkg/models/__init__\\.py|rust/crates/repo\\-index/tests/fixtures/python/package\\-import/consumer/app\\.py|rust/crates/storage/src/agent_impl\\.rs|rust/crates/storage/tests/agent_impl\\.rs|rust/crates/agent/src/storage_port\\.rs|rust/crates/agent/src/dto/signal\\.rs|rust/crates/agent/src/explain/mod\\.rs|rust/crates/agent/tests/explain_symbol\\.rs|rust/crates/rgr/src/presentation/explain_sections\\.rs|rust/crates/rgr/src/presentation/explain\\.rs)$' | grep -q . && git diff --check",
        "cwd": ".",
        "environment": "candidate tree, after every other check",
        "inputs": "the full sha256 of the operator registry recorded as `registry-before <digest>` in build-progress.md at step 0 equals the current one; no stray isolated daemon; the builder-owned `-after` roots and the `-stale` copy removed; the two manager-owned `-before` roots kept; `git status --short` names only the sixteen candidate paths (the four fixture files `git add`ed, never `git add -N`); no trailing whitespace; every step is fail-closed: the registry-digest equality is the first link of one `&&` chain, so a mismatch exits non-zero before the daemon probe and before any deletion (the rule for every check: no `;` outside a compound command — `( … )`, `{ …; }` or `for … done` — so each step is one link of the `&&` chain)"
      },
      "expected": "exit 0: every proof was isolated (the operator's registry byte-identical — no behavior change outside the isolated roots), the tree holds only the allocated paths"
    }
  ]
}
-->

# PYTHON-SUBMODULE-IMPORT-1 — `from package import module` lands on the module file as an inferred, investigable fact

Status: SPECIFIED and GROUNDED 2026-09-23 on HEAD b372172e under RG-BOOTSTRAP-INPUT-8; re-verified in the document item PYTHON-SUBMODULE-IMPORT-1-PREP-2 on HEAD ec19a165 under RG-BOOTSTRAP-INPUT-9 (`git diff --stat b372172e HEAD -- rust` is empty; line numbers below are ec19a165's). RC-7 of audit round seven; the RE-CUT after the first cut's two document items — PSI-R01 (an init that binds the name wins) and PSI-D01/D02 (a twelve-path expansion refused; the certainty question) — which the human's ruling of 2026-09-23 resolved as RG-REQ-002-L11 and the amended RG-REQ-006-L04 Python clause. Track: audit round seven fix queue (per-language import resolution). CODE slice, index-time (a reindex moves Python stores): one resolver stage, its carrier to `explain`, the refresh signals load, the version bump. The three decisions of PREP-2 are answered (§4.1). IMPLEMENTATION IS BLOCKED until TOOLCHAIN-STALENESS-1's implementation review is accepted and committed (the gate of PSI-C01 and PSI-C07). NO RELEASE is cut with this slice until TEST-EDGE-SCOPE-1B is accepted (D-PSI-RELEASE-GATE-1). Builder: claude / claude-opus-5-5; reviewer: codex / gpt-5.6-terra.

## 0. Requirements allocation (the machine-readable block above is binding; this section explains it)

**Allocation rule (the rule the accepted CPP-ATTRIBUTE-MACRO-1A allocation set):** `implements` lists only an obligation this slice completes; `changes` lists an authorized obligation whose observable behaviour this slice moves without completing it; `preserves` lists only obligations whose behaviour stays exactly as before, each covered by a no-behavior-change oracle.

**Implements:** RG-REQ-006-L04's Python clause as amended — "`from X import Y` where `X/Y.py` or `X/Y/__init__.py` is indexed and the init's extracted facts show no binding of `Y` (no symbol `Y`, no import of a `Y` other than the submodule itself) resolves to that file as INFERRED with the init as the alternate candidate (RG-REQ-002-L11) — the extractor's blind spots (multi-target assignment, `__getattr__`, star imports) are the stated reason it is not `static`; an init that shows a binding of `Y` keeps the init target" — with every case its verification criterion names: the sibling-module, subpackage, symbol-import, init-binds-`Y` and relative-form resolver tests, each asserting `resolution` and the alternate on the edge (PSI-C01), and the end-to-end fixture with `explain <Class>`'s Referenced-by rendering the inferred importers under the inferred remainder (PSI-C02, PSI-C04). L04's TypeScript stages are already met and untouched. Today all 1,440 such django imports land on `__init__.py` as `static`.

**Changes:** RG-REQ-002-L11 — PARTIAL, stated not hidden. Delivered for the import case: every candidate recorded (the edge's target plus `alternateTarget`), the reason recorded (`basis: python_submodule`), `resolution: inferred`, rendered on `explain`'s Referenced-by as something to investigate and on `imports <file>` as `inferred`; never counted certain where this slice decides it — the inferred edge never feeds the persisted MODULE→MODULE graph (cycles, stats, trust's mutual pairs read it) and the derived module edges already read `static` only (`storage/src/trust_impl.rs:810-811`, `storage/src/crud/module_edges_support.rs:55-56`). NOT delivered, so L11 is not implemented by this slice and no evidence of it may report L11 as passed: (a) the stated remainder and `--include-inferred` on the module surfaces (`modules list/deps`, `cycles`, `orient`, trust connectivity) — RG-REQ-004-L12's four-partition slice TEST-EDGE-SCOPE-1, later in the ratified queue; until then those surfaces show the certain edges only and the inferred remainder is stated in the ship block, not on the surface; (b) the alternate on `imports <file>` (the row says `inferred`; `explain` names the alternate); (c) the FILE-level IMPORTS readers that ignore `resolution` — `path` (`storage/src/queries.rs:1821` `find_shortest_path`), the `map` dependency sketch (`:2849` `map_resolved_dep_edges_in_path`), boundary-violation evidence (`:3011` `find_imports_between_paths`, consumed by `explain`, `orient` and the boundary aggregators), the LiveGraph imports-readiness bulk read (`:1779` `all_imports`), `cycles` over FILE nodes (`:1254` `find_cycles`) and storage's `agent_impl.rs` boundary/file-import reads (`:1469`, `:1623`) — which will carry the inferred edges unmarked in a binary built from `main` until TEST-EDGE-SCOPE-1B partitions every IMPORTS reader; no release is cut before that (D-PSI-RELEASE-GATE-1, §4.1). REPORTED consequence of the partition this slice does make: on django 367 persisted module pairs fed only by the retargeted imports leave the module graph (2,451 → 2,084 predicted, PSI-C04); an honest fall under RG-REQ-002-L10, stated in the ship block.

**Changes:** RG-REQ-001-L06 (D-PSI-05, §4.1; RG-REQ-001 is the fourth parent) — the `INDEXER_VERSION` bump (one minor above HEAD's; `indexer:1.0.0` → `indexer:1.1.0` when TOOLCHAIN-STALENESS-1 leaves it at 1.0.0) makes every store written by the previous indexer render TOOLCHAIN-STALENESS-1's `indexer facts from indexer:1.0.0 (current indexer:1.1.0) — run rmap repo rebuild <path> to refresh` on `orient` and `check`, `TOOLCHAIN_STALENESS` INCOMPLETE and `check` exit 2 until `rmap repo rebuild <path>` — an observable move of L06 this slice causes but does not complete (TOOLCHAIN-STALENESS-1 implements the mechanism), as CPP-ATTRIBUTE-MACRO-1A allocated it. Proof: PSI-C01 (the bump, derived from HEAD) and PSI-C07 (the line, the exact `indexer` family and exit 2 on a copy of the v0.19.0 django store; `current` on the candidate's own store).

**Changes:** RG-REQ-001-L09 (D-PSI-REFRESH-SIGNALS-1, option A) — a delta refresh today resolves its copied-forward extraction edges with only the freshly extracted files' import bindings and classifies them with only those files' signals (§1), so the same source can yield a different graph on a refresh than on a full index; the load makes the refresh's resolver and classifier inputs equal a full index's. That MOVES observable behaviour on refreshed stores of every language (CALLS bound through namespace/named imports, unresolved classification — toward the full-index answer), so under the allocation rule L09 is a `change`, not a `preserve` (the selection packet listed it under `preserves`; this document applies the rule above and records the deviation in §9). It does not complete L09: the reindex-twice equality check and the write-side `toolchain_json` test L09 names remain unbuilt. A full index's inputs are unchanged (nothing is copied). Proof: PSI-C01 (the orchestrator test: the load's arguments and a refresh equal to the full index on an init that binds the name from elsewhere) and PSI-C06 (full index vs no-change refresh of django, snapshot to snapshot).

**Decision records (named by the manifest):** D-CERTAINTY-MARK-1 (the human, 2026-09-23: mark, never guess, never drop); D-REFRESH-STALE-1 (the human, option B: no version-keyed invalidation — this slice changes what the resolver produces for unchanged files, so it bumps `INDEXER_VERSION` and ships only after TOOLCHAIN-STALENESS-1); D-PSI-REFRESH-SIGNALS-1 (the operator, option A, 2026-09-24: the copied-forward files' import bindings are loaded for the refresh resolution — refresh equals full index; the human may override); D-PSI-RELEASE-GATE-1 (the operator, option B, 2026-09-24: no release until TEST-EDGE-SCOPE-1B partitions every IMPORTS reader; the human may override). The first cut's PSI-D01 (scope) is answered by this cut's shape — no new `ResolverIndex` field, and the resolver stage itself needs no orchestrator change (the orchestrator changes only by the version line and D-PSI-REFRESH-SIGNALS-1's signals load, which is a data-path fix for every rule that reads another file's signals, not part of the stage): the init's module-level symbols (`nodes_by_name` + `node_uid_to_file_uid`) and its import bindings (the `import_bindings_by_file` map `resolve_edges` already receives, resolver.rs:388-398) are inputs the resolver already has; PSI-D02 (certainty) by L11 — the edge is `inferred`, the blind spots are the stated reason.

**Preserves:** RG-REQ-006-L01/L02/L03 (Rust, Java, C/C++ stages — PSI-C01), L05 (unresolved contract — every unresolved row of a full index unchanged, PSI-C04; classification code byte-identical, PSI-C02), L06..L11/L13 (deps/imports machinery byte-identical — PSI-C02), L12 (`imports <file>` renders the row with its resolution, code unchanged — PSI-C02/C04), RG-REQ-002-L01–L10 (honesty machinery untouched; the new field follows the unreadable-line pattern — PSI-C02), RG-REQ-011-L06 (isolation; the manager's before-root byte-identical after its copy is served — PSI-C05, PSI-C07).

| Id | Obligation | Proof |
|---|---|---|
| P-PSI-01 | a corpus with no Python package is identical row by row (nodes, edges incl. resolution, extractor and metadata, unresolved rows incl. classification, span and metadata); on django no import changes resolution STATE (resolved/unresolved) — only target and certainty — and every node (by stable key, with its file and parent's stable key), every unresolved row and every edge outside the from-imports and the persisted module graph (endpoints by stable key) is unchanged row by row (repository uid and indexer version normalized; only row identities and freshness fields excluded, the exclusions audited against the stores' schema) | PSI-C04 |
| P-PSI-02 | every existing resolver stage and test; every extractor byte-identical; compose.rs differs only by the version line and the orchestrator only by the version line, the refresh signals load and its test (one `insert_file_signals` site, as today); a full index's resolver inputs unchanged; the old fixture; a type with no inferred importer renders as before | PSI-C01; PSI-C02 |
| P-PSI-03 | isolation, cleanup, hygiene (the `-after` roots and the `-stale` copy removed; the manager's `-before` roots never served and byte-identical) | PSI-C03; PSI-C05; PSI-C07 |

## 1. Problem (ROOT-CAUSED — RC-7 + seam read + the first cut's two reviews, re-read 2026-09-23 on ec19a165)

`python-extractor/src/extractor.rs:797-839 emit_from_import_binding`: for `from X import Y` the IMPORTS edge's `target_key` is the MODULE part `X` (:806, via `python_specifier_to_target_key` :512-560 — `X` with `/` for an absolute specifier, `<repo>:<dir>:FILE` for a relative one); `Y` is stamped on `metadata_json.importedName` (:834) and never on the key; the same statement's ImportBinding carries `{identifier, specifier, importedName}` (camelCase in `file_signals.import_bindings_json`), and a whole-module `import a.b as c` carries no `importedName` (:590-652). `indexer/src/resolver.rs:661-724 resolve_import_target` probes ONE candidate for an absolute key (Stage 4, :711-717: `<repo>:X:FILE` through `file_resolution`), bound by `build_file_resolution_map` (:1828-1835) to `X/__init__.py`; the relative form hits Stage 2 on the same entry; `edge.metadata_json` is not passed into the ladder (:554-562); the only language-gated stages are Rust (:741-791, `RUST_EXTRACTOR_PREFIX` :38/:553) and Java (:811/:570); `resolve_edges` copies the extractor's `resolution` (`Static`) into every `ResolvedEdge` (:409-420) and pushes every resolved IMPORTS edge into `resolved_import_pairs` (:402-408), from which the orchestrator persists `static` MODULE→MODULE IMPORTS edges (`create_module_edges`, orchestrator.rs:1366-1450). Python's rule: `from X import Y` runs `X/__init__.py`, then takes the attribute `Y` of `X`; only when there is none does it load the submodule — the package directory `X/Y/` before the module file `X/Y.py`. The first review (PSI-R01) caught the first cut retargeting unconditionally. What the index sees of the init: its module-level SYMBOL nodes (`qualified_name == name`; simple module-level assignments only, extractor.rs:265-281) and its import bindings (a star import leaves no binding, :765-789) — evidence the index HAS; its blind spots are why the target is INFERRED, never `static`. `explain BaseHandler` lists one importer (`django/test/client.py`) because `find_file_importers` (storage/src/agent_impl.rs:1601-1639) reads IMPORTS edges by target file and carries no `resolution`; `imports <file>` (`find_imports`, queries.rs:1726; rgr presentation/imports.rs:92-99) prints the edge's resolution verbatim.

Code under analysis (django 4d455ae): `django/core/handlers/wsgi.py:5` `from django.core.handlers import base` and `:113` `class WSGIHandler(base.BaseHandler):` (likewise `asgi.py:14` and `:150`); `django/core/handlers/__init__.py` is empty; the before-store binds both imports to `django/core/handlers/__init__.py`, static.

Refresh (§4.1, D-PSI-REFRESH-SIGNALS-1): `refresh_repo` (orchestrator.rs:1673) copies the unchanged files forward — their nodes, extraction edges and `file_signals` rows (storage `indexer_impl.rs:1050`, "3. Copy file_signals.") — and calls `run_pipeline` (:462) with the files to extract only. `run_pipeline` re-resolves every extraction edge of the snapshot, copied-forward included (the batch loop over `query_extraction_edges_batch`, :1009-1039), against `resolver_nodes` read from the whole snapshot (:868), but its signals are the freshly extracted files' alone: `let mut all_signals: Vec<FileSignalRow> = Vec::new();` (:521), filled per extracted file (:702), written at :833 `storage.insert_file_signals(&all_signals)?;`, and turned into the resolver's map at :1007 `let import_bindings_by_file = build_import_bindings_map(&all_signals);` and into the classifier's per-file signals at :1098. The copied rows are never read back although the port exists (`query_file_signals_batch`, indexer/src/storage_port.rs:469). So an unchanged init's bindings are invisible to a refresh — the new stage would retarget `from pkg import Y` in a changed file whose unchanged init imports `Y` from elsewhere, where a full index keeps the init — and the same absence already moves call resolution through namespace/named import bindings (resolver.rs:935, :1127) and unresolved classification for every language on refreshed stores: a no-change refresh copies every file and resolves with an EMPTY map. Closed in this slice (§2.1 step 5).

## 2. Contract

### 2.1 The fix, at the cause — one gated stage that infers and says so; the certainty carried to the reader
1. `indexer/src/resolver.rs`: `const PYTHON_EXTRACTOR_PREFIX: &str = "python-core:";`. In the Imports arm, for an edge whose extractor starts with it and whose `metadata_json.importedName` is a plain identifier `Y`, BEFORE Stage 2: let `D` be the package directory the ladder would bind (`X` for an absolute key without `:`, `<dir>` for a relative key `<repo>:<dir>:FILE`) and `I` = `<repo>:D/__init__.py:FILE`; the child is the FIRST of `<repo>:D/Y/__init__.py:FILE`, `<repo>:D/Y.py:FILE` present in `nodes_by_stable_key` (exact keys — never the extensionless `file_resolution` entry, so a non-Python file is never a target; package directory first, as Python's FileFinder). If there is no child, or `I` is not an indexed file → fall through unchanged. Otherwise the init BINDS `Y` when (a) a SYMBOL node of `I`'s file has `qualified_name == Y` (a module-level definition; a nested member such as `Thing.Y` is not a module attribute), or (b) a binding in `import_bindings_by_file[I's file uid]` has `identifier == Y` and is NOT the submodule itself. A binding IS the submodule itself only in the three forms `from . import Y` (`specifier "."`, `importedName Y`), `from <D dotted> import Y` (`specifier` = `D` with `/`→`.`, `importedName Y`) and `import <D dotted>.Y as Y` (`specifier "<D dotted>.Y"`, no `importedName`); `from .Y import Y` binds the attribute `Y` OF the submodule and keeps the init, as does every aliased or foreign form. When the init binds `Y` → fall through unchanged (the init stays the `static` target). Otherwise the outcome is a new `TargetResolution` variant (e.g. `InferredImport { target, alternate, basis }`) that makes `resolve_edges` write `resolution: Inferred`, merge `alternateTarget` (the init's stable key) and `basis: "python_submodule"` into the edge's `metadata_json` (keys camelCase like `importedName`, `isTypeOnly`, `mroCandidates`; `basis` is D-CERTAINTY-MARK-1's and RG-REQ-006-L11's word), and NOT push the edge into `resolved_import_pairs` — an inferred import is never a certain module edge (L11). A miss anywhere → today's ladder, byte-identical. No new `ResolverIndex` field, no I/O.
2. Storage `find_file_importers` selects `e.resolution`, the `basis` and the alternate's path (the `alternateTarget` stable key joined to its FILE node's `files.path` in the same snapshot); `AgentFileImporter` gains `resolution: String`, `basis: Option<String>`, `alternate_path: Option<String>`; an `inferred` row with an unreadable carrier (non-string `basis`, an `alternateTarget` that is not a string or names no FILE node, a `python_submodule` basis without an alternate) is a named storage error, never a coerced row. Agent `explain` partitions by file: any `static`/`dynamic` edge makes a certain importer (items, `count`, `top_modules` as today); a file whose every edge to the target is inferred goes to `ExplainReferencedByEvidence.inferred_items` / `inferred_count` (serde-skipped when empty). rgr renders `Referenced by (N files, +M inferred — investigate)` and inferred rows `  - <path>  (inferred: python submodule — alternate <init path>)` after the certain rows; unchanged when M = 0; a malformed inferred field → the existing unreadable line.
3. `repo-index/tests/integration.rs` + the NEW fixture `rust/crates/repo-index/tests/fixtures/python/package-import/` (`pkg/__init__.py`, `pkg/base.py`, `pkg/models/__init__.py`, `consumer/app.py` — the consumer sits in a directory so it owns a module): one end-to-end test asserting the three targets, each edge's resolution/basis/alternate, and the module edges (`consumer → pkg` kept by the static import; no `consumer → pkg/models`).
4. Version (D-REFRESH-STALE-1 = B; RG-REQ-001-L06): `INDEXER_VERSION` moves up exactly one minor from HEAD's value — `indexer:1.0.0` → `indexer:1.1.0` on ec19a165 (orchestrator.rs:59); if TOOLCHAIN-STALENESS-1 moves it first, the next minor — and the same literal in its declared copy `repo-index/src/compose.rs:1653` (`// Match INDEXER_VERSION in orchestrator.rs`; leaving it would stamp Spring inferences with a version the running indexer is not). The version reaches `snapshots.toolchain_json.indexer` (`build_toolchain_json`, orchestrator.rs:1457-1464) and the orchestrator-derived OWNS and MODULE→MODULE edges' `extractor` (:1391/:1445). Toolchain versions are not an invalidation input (D-REFRESH-STALE-1), so the bump is what makes TOOLCHAIN-STALENESS-1's line fire on every existing store until `rmap repo rebuild <path>` (PSI-C07). compose.rs's only change is this line (PSI-C01 asserts the exact two-line diff, derived from HEAD's value).
5. Refresh signals (D-PSI-REFRESH-SIGNALS-1 = A; RG-REQ-001-L09): on a delta refresh the copied-forward files' `file_signals` rows — the plan's `files_to_copy`, the same file-uid list `copy_forward_unchanged_files` received — are read through the existing port `query_file_signals_batch(<refresh snapshot uid>, <copied file uids>)` and joined to the freshly extracted rows AFTER `insert_file_signals` (:833, which keeps writing only the freshly extracted rows — copy-forward already copied the others) and BEFORE `build_import_bindings_map` (:1007) and the classifier's `build_file_signals_for_edge` (:1098), so a refresh's resolver and classifier see every file's signals, exactly as a full index does. A full index copies nothing: its inputs are unchanged. Where the join lives (in `refresh_repo`, passed into `run_pipeline`, or inside `run_pipeline` keyed on the copied set) is the builder's local choice; the orchestrator's production code keeps exactly one `insert_file_signals` call and gains a `query_file_signals_batch` call (PSI-C01). One orchestrator test (`refresh_loads_copied_forward_import_bindings_for_resolution`, PSI-C01) and the django parity proof (PSI-C06).
6. Nothing else: no extractor, classification, module-queries, trust, LiveGraph, daemon, storage query (`queries.rs`, `trust_impl.rs`, `crud`, the file-signals port) or imports-renderer change; the orchestrator changes only by steps 4 and 5 and their test.

### 2.2 Evidence taxonomy
| Input | Outcome | Bound test / check |
|---|---|---|
| `from X import Y`, `X/Y.py` indexed, init binds no `Y` | `X/Y.py`, inferred, alternate = init, basis `python_submodule` | PSI-C01 (1); PSI-C02; PSI-C04 |
| `from X import Y`, `X/Y/__init__.py` indexed (and when `X/Y.py` also is) | the subpackage init, inferred, alternate = package init | PSI-C01 (2), (3); PSI-C02 |
| `from X import Y`, no `X/Y/__init__.py`/`X/Y.py` (a symbol; also `X/Y.json` only) | `X/__init__.py`, static, metadata unchanged | PSI-C01 (4), (5); PSI-C02 |
| init defines `Y` at module level / only a nested `C.Y` | the init, static / retargets, inferred | PSI-C01 (6), (7) |
| init imports `Y` from elsewhere; `from .Y import Y` | the init, static (Python binds the attribute) | PSI-C01 (8), (9) |
| init's only `Y` bindings are the submodule itself (three forms) | retargets, inferred | PSI-C01 (10) |
| `from . import Y` (relative key) | `<dir>/Y.py`, inferred | PSI-C01 (11) |
| an inferred import and the module graph | never in `resolved_import_pairs`; no persisted module edge | PSI-C01 (12); PSI-C02; PSI-C04 |
| no `importedName` carrier / a non-Python edge | today's ladder | PSI-C01 (13), (14) |
| Referenced-by with / without inferred importers; a file with both kinds; malformed carrier | remainder heading + alternate rows / byte-identical / certain once / storage error or unreadable line | PSI-C02 |
| django (1,440 movers) / FRAKTAG | §2.3 / identical | PSI-C04 |
| delta refresh: a changed importer, its unchanged init binds `Y` from elsewhere | the init, static — as a full index (the copied init's bindings are loaded) | PSI-C01 (orchestrator test) |
| no-change delta refresh of the django after-root | the full index's IMPORTS/CALLS edges and unresolved rows, snapshot to snapshot | PSI-C06 |
| a v0.19.0 store (indexer:1.0.0) served by the candidate / the candidate's own store | the indexer staleness line, `TOOLCHAIN_STALENESS` incomplete, exit 2 / `current` | PSI-C07 |

### 2.3 Outward proof (django, fresh isolated index by the candidate vs the manager's before-store)
Every from-import edge where the amended rule puts it (rule recomputed on the before-store: 5,330 symbol imports keep the init; 1,424 + 16 = 1,440 retarget; 0 inits define or foreign-bind the name); exactly 1,440 inferred edges; IMPORTS FILE→FILE 15,163 and every unresolved row unchanged; persisted MODULE→MODULE edges = the pairs of the store's own certain imports (REPORTED, predicted 2,451 → 2,084); `base.py` importers {client.py static} → {asgi.py inferred, wsgi.py inferred, client.py static}; `explain BaseHandler` `Referenced by (1 files)` → `Referenced by (1 files, +2 inferred — investigate)` with the two alternate-naming rows; `imports django/core/handlers/wsgi.py` → `django/core/handlers/base.py  depth=1  inferred`; `django/shortcuts.py:13` `from django.template import loader` → `django/template/loader.py`, inferred, alternate `django/template/__init__.py`. FRAKTAG identical. A no-change refresh of the django after-root yields the same IMPORTS and CALLS edges and unresolved rows as its full index (PSI-C06). A copy of the v0.19.0 django store served by the candidate renders `indexer facts from indexer:1.0.0 (current indexer:1.1.0) — run rmap repo rebuild <path> to refresh` (the current version is the candidate's constant) on `orient` and `check`, `check` exits 2, and the candidate's own store reads `current` (PSI-C07).

## 3. Regression watch
| Preserved | What would regress | Proof |
|---|---|---|
| RG-REQ-006-L01/L02/L03 | any Rust/Java/C++ import stage | PSI-C01 (resolver filter; fifteen named tests) |
| RG-REQ-006-L05 | an import changing resolution STATE; an unresolved row lost, exchanged or reclassified | PSI-C04 (the row-level multiset of every unresolved row: source, target key, type, classification, basis, span, metadata); PSI-C02 (classification byte-identical) |
| RG-REQ-006-L06..L11/L13 | any deps/imports machinery | PSI-C02 (module-queries, trust, daemon-runtime, storage queries, rgr commands byte-identical) |
| RG-REQ-006-L12 | `imports <file>` not showing the row's resolution | PSI-C02 (renderer unchanged); PSI-C04 (capture) |
| RG-REQ-002-L01–L10 | a coerced default, a zero for unknown, a Referenced-by change for a type without inferred importers | PSI-C02 (unreadable-line and byte-identity tests; untouched crates) |
| P-PSI-01 | a corpus without a Python package moving; a resolution-state change | PSI-C04 |
| P-PSI-02 | an extractor change; a compose change beyond the version line; an orchestrator change beyond the version line and the signals load (a second signals write site); the old fixture | PSI-C01 (diff and source assertions); PSI-C02 |
| RG-REQ-001-L09 (changed) | a refresh re-inserting copied signals or reading the wrong snapshot/file set; a refresh graph differing from the full index | PSI-C01 (orchestrator test records the call's arguments); PSI-C06 |
| RG-REQ-001-L06 (changed) | an existing store read as current after the bump; the candidate's own store read as stale | PSI-C07 |
| RG-REQ-011-L06, P-PSI-03 | isolation, hygiene; the manager's before-root mutated by serving its copy | PSI-C03; PSI-C05; PSI-C07 (digests before/after) |

## 4. Stop conditions
Frozen: every extractor; compose.rs except the version line; the orchestrator except the version line, the refresh signals load (§2.1 step 5) and its test; the file-signals port and its storage implementation; storage schema (the keys are additive `metadata_json`); classification, module-queries, trust, LiveGraph, the daemon, storage's `queries.rs`/`trust_impl.rs`/`crud`, the imports renderer; the Rust/Java/C++ stages and the Stage 1–4 order for non-Python edges; `build_file_resolution_map`. No filesystem I/O in the resolver; no new `ResolverIndex` field; no star-import prepass (a star-importing init is a stated blind spot). If the TOOLCHAIN-STALENESS-1 gate (PSI-C01, PSI-C07) fails, STOP before any code. If HEAD carries an occurrence of its `INDEXER_VERSION` literal under `rust/` beyond the two declared sites that is a production copy of the constant, STOP and report it. If a §2.3 literal differs, STOP and report the import line, the init's facts and the probed keys. If PSI-C06 finds a refresh/full-index difference, STOP and report the first differing rows and their files — never narrow the compared columns. If PSI-C07's stale copy cannot be served without writing to the manager's root, STOP. Nothing outside the sixteen candidate paths. STANDING HONESTY RULES. Unmet DoD → STOP. Do NOT commit. Run every check to completion in the FOREGROUND; end your turn only with the evidence object.

### 4.1 Decisions (resolved by records the manifest names — D-PSI-03/-04/-05 of PREP-2)
- **D-PSI-03 → D-PSI-REFRESH-SIGNALS-1 (operator, option A; the human may override):** on a refresh the orchestrator loads the copied-forward files' signals through the existing `query_file_signals_batch` port and joins them to what the resolver's `import_bindings_by_file` and the classifier read, without re-inserting them (§2.1 step 5; PSI-C01's orchestrator test and source assertions; PSI-C06's django parity proof). RG-REQ-001-L09 is allocated under `changes` because refreshed stores of every language move toward the full-index answer (§0). No residual for this slice's rule.
- **D-PSI-04 → D-PSI-RELEASE-GATE-1 (operator, option B; the human may override):** this slice's inferred IMPORTS edges are excluded by the module-edge derivation today (`resolution = 'static'`), but `cycles` over FILE nodes, `path`, the `map` dependency reads, the boundary and file-import reads (§0 (c)) and trust's mutual MODULE-pair reads (`trust_impl.rs:710/:716`, over the persisted MODULE→MODULE edges this slice keeps static-only) carry no resolution predicate; TEST-EDGE-SCOPE-1B (scope: every IMPORTS reader the record lists) ships before any release — agent-manager's `scripts/repo-graph-release-cut.sh` step 1 now refuses to cut unless `docs/assurance/TEST-EDGE-SCOPE-1B/implementation-review.json` at HEAD has `result` accepted (OBSERVED 2026-09-24, lines 4-5 and 12-17). (The record's `trust_impl.rs:856` is an `unresolved_edges` read, not an IMPORTS-edge reader — a harmless over-inclusion in 1B's scope list.) Until then a binary built from `main` may show an inferred import as certain on those surfaces — stated here and in the ship block; the operator's installed release never carries it.
- **D-PSI-05 (the PREP-2 reviewer's recommended option A, applied by the manager in this manifest revision — no separate record):** RG-REQ-001 is the fourth parent; RG-REQ-001-L06 is allocated under `changes` (the `INDEXER_VERSION` bump makes existing stores stale — the visible line and `check`'s exit 2 are this slice's consequence, proven by PSI-C07); the sixteen candidate paths are the authority.

## 5. Validation (ORDERED; `build-progress.md` after EACH step)
0. On the CLEAN tree: record `git rev-parse HEAD`; `registry-before <full sha256>` in build-progress.md; verify TOOLCHAIN-STALENESS-1's accepted record at HEAD (the gate of PSI-C01 and PSI-C07 — absent → STOP), record HEAD's `INDEXER_VERSION` and every occurrence of its literal under `rust/` (§4), and verify the two `-before` roots (§7). 1. Failing tests FIRST (the new resolver tests and `refresh_loads_copied_forward_import_bindings_for_resolution` fail on HEAD for the intended reason). 2. PSI-C01 → PSI-C02 → PSI-C03 — NEVER `cargo test --workspace`. 3. PSI-C04 (build, index the two after-roots, captures, store oracles). 4. PSI-C06 (no-change refresh of the django after-root, parity). 5. PSI-C07 (the stale copy of the django before-root; the after-root's `current`). 6. PSI-C05 (deletes the after-roots and the stale copy), hand-off with the evidence object (each check's outcome NESTED under `outcome`; `supportingEvidence` non-empty).

## 6. Definition of done
All seven checks pass; §2.3 holds; the §4.1 decisions are answered and applied; the ship block (docs/ROADMAP.md, operator at closeout) states that existing stores keep serving the old `__init__.py` bindings until `rmap repo rebuild <path>` and that the running binary says so on `orient`/`check` (TOOLCHAIN-STALENESS-1's `indexer facts from indexer:1.0.0 (current indexer:1.1.0) — run rmap repo rebuild <path> to refresh`), states the module-graph movement with its cause (RG-REQ-002-L10: django persisted module pairs before/after as PSI-C04 printed them) and states RG-REQ-002-L11 as PARTIAL with the residuals of §0 (never as implemented), states that no release is cut until TEST-EDGE-SCOPE-1B is accepted (D-PSI-RELEASE-GATE-1) and that a binary built from `main` meanwhile shows the inferred imports unmarked on `cycles`/`path`/`map`/boundary reads, and states that refreshed stores of every language now resolve and classify as a full index does (RG-REQ-001-L09 changed, PSI-C06 counts); the report quotes, as code-under-analysis, orchestrator.rs:833 `storage.insert_file_signals(&all_signals)?;` and :1007 `let import_bindings_by_file = build_import_bindings_map(&all_signals);` as found and the load that now feeds :1007 on a refresh, PSI-C06's parity line, PSI-C07's rendered stale line verbatim, and, from django: `django/core/handlers/wsgi.py:5` BEFORE target (`__init__.py`, static) and AFTER (`base.py`, inferred, alternate) with `:113` `class WSGIHandler(base.BaseHandler):` as the use it serves, `explain BaseHandler`'s Referenced-by before/after verbatim, `imports django/core/handlers/wsgi.py`'s row before/after, `django/shortcuts.py:13 from django.template import loader` before/after, the rule's kind counts on the before-store, the django totals, the FRAKTAG identity line, and one residual example (`django/db/models/__init__.py:2` `from django.db.models import signals` — an init binding that IS the submodule, so its importers retarget).

## 7. Corpus roots (pre-provisioned by the manager on 2026-09-23 with the installed v0.19.0 binary)
- `/private/tmp/PYTHON-SUBMODULE-IMPORT-1-django-before` (`/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/django` 4d455ae), `/private/tmp/PYTHON-SUBMODULE-IMPORT-1-fraktag-before` (`/Users/apple/Documents/APLICATII BIJUTERIE/FRAKTAG`); READ with sqlite3 only, `immutable=1` after asserting their WAL empty (both WAL files are 0 bytes on 2026-09-23). The builder creates `/private/tmp/PYTHON-SUBMODULE-IMPORT-1-django-after` and `/private/tmp/PYTHON-SUBMODULE-IMPORT-1-fraktag-after` with the candidate only when absent (same recipe) and deletes them in PSI-C05; PSI-C06 refreshes the django after-root once. PSI-C07 creates `/private/tmp/PYTHON-SUBMODULE-IMPORT-1-django-stale` with `cp -R` from the django before-root only when absent, rewrites its registry `db_path` to the copy, serves the COPY (never the original; the original's registry and database digests are asserted unchanged) and deletes it; PSI-C05 deletes it again if PSI-C07 stopped early. The retained roots under `~/repo-graph-retained/` are never served; the operator registry is read only for its digest.

## 8. Follow-ups and stated residuals (not this slice)
RG-REQ-004-L12's `--include-inferred` view and remainder line on the module surfaces (TEST-EDGE-SCOPE-1). Certainty partition for `cycles`, `path`, the `map` dependency sketch, boundary-violation evidence, trust's import reads and the LiveGraph imports-readiness read — TEST-EDGE-SCOPE-1B, a RELEASE GATE for this slice (D-PSI-RELEASE-GATE-1, option B). The alternate on `imports <file>`. The refresh import-bindings gap is CLOSED in this slice (D-PSI-REFRESH-SIGNALS-1, option A — PSI-C01/PSI-C06). Star-importing inits are a stated blind spot: their imports retarget as inferred like the rest — the alternate names the init the agent should check. Python IMPLEMENTS edges (`class WSGIHandler(base.BaseHandler)` is node metadata only). `docs/TECH-DEBT.md:487-488` (`__init__.py` package resolution) is refined by this rule — the operator amends that entry at closeout. A `src/`-layout package whose init imports itself by its import name (not its repo path) keeps the static init target — conservative, stated.

## 9. Baseline history
- INPUT-1 (2026-09-23): first admission baseline under RG-BOOTSTRAP-INPUT-8 (parents RG-REQ-006, RG-REQ-002, RG-REQ-011; D-CERTAINTY-MARK-1 as governance).
- INPUT-1 revised for the document item PYTHON-SUBMODULE-IMPORT-1-PREP-2 under RG-BOOTSTRAP-INPUT-9 (2026-09-23), manager draft: re-pinned (README changed); D-REFRESH-STALE-1 (human, option B) added as governance and to `requiredDecisionIds`; the `INDEXER_VERSION` bump; `rust/crates/indexer/src/orchestrator.rs` added as a candidate path; ship block: existing stores need `rmap repo rebuild <path>`.
- INPUT-1 corrected in PREP-2 by the requirements author after verifying every claim on ec19a165 and the before-stores: (1) allocation rule applied — RG-REQ-002-L11 moved from `implements` to `changes` (PARTIAL; residuals named in §0); (2) PSI-C01 no longer asserts the orchestrator byte-identical (it contradicted the bump) — it asserts the exact one-line diffs and gates on TOOLCHAIN-STALENESS-1's accepted record; the version literal's declared copy `repo-index/src/compose.rs:1653` added as the sixteenth candidate path; PSI-C05's path list completed (it omitted orchestrator.rs); (3) the rule made exact: package directory first, exact-key probes (no non-Python target), module-level definitions only, the three submodule-itself binding forms (`from .Y import Y` keeps the init — the draft counted `.Y` as the submodule), camelCase carrier keys `alternateTarget`/`basis` (D-CERTAINTY-MARK-1's word, replacing `alternate_target`/`inference`); (4) an inferred import never enters `resolved_import_pairs`, so the persisted module graph moves (2,451 → 2,084 predicted) — the draft's "IMPORTS 17,614 / edge totals unchanged" was false; (5) the PSI-C04 oracle keys edges by (source, line, column, specifier, importedName, identifier) — the draft's (source, importedName) collided on 19 keys — compares every from-import edge, reads the manager's roots `immutable=1` through their registry, and recomputes 1,440 movers with 16 submodule-itself bindings (the draft said 12); (6) FRAKTAG described truthfully (four Python files, no package); (7) `--lib resolver` described truthfully (122 = 86 + 33 + 3); `import_bindings_by_file` named as the `resolve_edges` parameter it is; (8) the refresh-bindings gap, the unmarked FILE-level readers and the RG-REQ-001-L06 allocation raised as D-PSI-03/04/05 (§4.1); (9) the fixture's consumer moved to `consumer/app.py` so the module-edge exclusion is observable.
- INPUT-1 revised for the document item PYTHON-SUBMODULE-IMPORT-1-PREP-3 (2026-09-24, manager): PREP-2's three decisions answered by records — D-PSI-REFRESH-SIGNALS-1 (operator A: the copied-forward signals load, PSI-C01 + the new PSI-C06 parity proof), D-PSI-RELEASE-GATE-1 (operator B: TEST-EDGE-SCOPE-1B gates the release; §4.1, §8), D-PSI-05 (reviewer A: RG-REQ-001 a parent, RG-REQ-001-L06 under `changes`, PSI-C07 the stale-store line gated on TOOLCHAIN-STALENESS-1); §4.1 rewritten as resolved; checks reordered C01–C04, C06, C07, C05.
- INPUT-1 refined in PREP-3 by the requirements author (claude-opus-5-5) after verifying the manager's draft on ec19a165 and the before-stores (sqlite3 `immutable=1`), with `-- --list` for the test names and the allocation validator: (1) PSI-C01 asserted the orchestrator's diff to be the version line alone — contradicting the signals load it also required; it now asserts the version line exactly, one `insert_file_signals` call in production code (as at HEAD) and a `query_file_signals_batch` call, and the orchestrator test must record the port call's arguments (the MockStorage ignores them today, orchestrator.rs:2531); (2) the version literals are derived from HEAD (one minor above HEAD's value) instead of hard-coded `1.0.0 → 1.1.0`, because TOOLCHAIN-STALENESS-1 lands first and may move the constant itself (CAM-A04 anticipates it); the "no `indexer:1.0.0` anywhere" grep became "the old literal leaves exactly the two declared sites and is added nowhere" (TOOLCHAIN-STALENESS-1's tests may carry it as data); (3) PSI-C07's heredoc terminator shared its line with `&& test …`, so the heredoc never closed and the shell tail ran as Python — rebuilt on the accepted CAM-A04 shape (orient exit 0, `check` and `check --json` exit 2, the stale line on both human surfaces, the exact `indexer` family, `TOOLCHAIN_STALENESS` incomplete, the candidate's own after-root `current`, digests of the manager's root before/after, the copy removed; PSI-C05 removes it again if PSI-C07 stopped early); (4) PSI-C06 read a just-served store `immutable=1` (it can skip WAL pages) found by glob and did not prove the refresh took the delta path — it now reads `mode=ro` through the root's registry, asserts one `full` then one `refresh` snapshot with the parent link, equal FILE sets and `file_signals` counts, and compares unresolved rows with their source and classification; (5) RG-REQ-001-L09 moved from `preserves` (the packet's listing) to `changes`: the load moves refreshed-store CALLS and classification for every language, which the allocation rule of §0 does not allow under `preserves`; (6) PSI-C04 asserts one snapshot per compared store (its unfiltered SQL depends on running before PSI-C06); (7) §0, §1, §2.1 (new step 5), §2.2, §2.3, §3, §4, §4.1, §5, §6, §7 aligned with the two records and the new checks.
- INPUT-1 refined in PREP-3 revision 1 by the requirements author after review-0 (codex, refinement-required, three findings), each closed by a rule over its class: (F-PSI-UNRESOLVED-ORACLE) RULE — every claim that rows are unchanged is checked as a row-level multiset of the rows' semantic columns, excluding only row identities (`node_uid`, `edge_uid`, `snapshot_uid`, `file_uid`) and freshness fields (`indexed_at`, `observed_at`), with the repository uid normalized to `<repo>` and an `indexer:x.y.z` value to `<indexer>` when two roots are compared: PSI-C04 now compares every unresolved row (was grouped counts), every node (was a count), every edge outside the from-imports and the persisted module graph (was type/resolution counts), every from-import edge's metadata except the two added keys, and FRAKTAG's rows under the same rule (its earlier dump compared raw `target_key`, which carries the repo uid — `<repo>:packages/ui/src/index.css:FILE` — and so could not match between two roots with different uids); (F-PSI-REFRESH-PARITY) PSI-C06 now compares the inputs row by row — every file version with its content hash and every `file_signals` row with all payload columns — and every unresolved row with its metadata, resolution, extractor, classifier version and span, keeping the parent-snapshot and edge assertions; node and other-edge-type parity is REPORTED; (F-PSI-CLEANUP-GUARD) RULE — no `;` outside a compound command in any check, so every step is one link of the `&&` chain: PSI-C05's daemon probe moved inside a `{ …; }` group after the registry-digest test (fault-injected: with a wrong recorded digest the reviewed command reached cleanup, the revised one exits 1 before it), and PSI-C03's clippy/`rc` steps grouped the same way. Also: §4.1 records that the release-cut precondition is now present in agent-manager's script.
- INPUT-1 refined in PREP-3 revision 2 after review-1 (codex, refinement-required, F-PSI-UNRESOLVED-ORACLE re-opened: the row oracles identified nodes and endpoints by path and qualified name, omitted `nodes.stable_key` and the parent relationship, and the from-import key omitted the source's stable key). RULE, now executable: every persisted column of a compared table is either compared — a reference column (`file_uid`, `parent_node_uid`, `source_node_uid`, `target_node_uid`) through the referenced row's stable key or path — or one of the declared exclusions (row identities `node_uid`/`edge_uid`/`snapshot_uid`/`repo_uid`, freshness `indexed_at`/`observed_at`); a schema audit in PSI-C04 (four stores: `nodes`, `edges`, `unresolved_edges`) and PSI-C06 (`file_versions`, `file_signals`, `edges`, `unresolved_edges`, `nodes`) asserts the store's `pragma table_info` equals compared ∪ excluded, so the stated rule and the oracle cannot drift. PSI-C04: nodes by normalized stable key with file path and parent stable key (plus whether a parent is recorded, so a dangling parent is not read as none); edges and unresolved rows by source/target stable keys; from-import edges keyed additionally by the source node's stable key and compared with the target node's stable key (movers: the stable key of the rule's target file). PSI-C06's reported node and other-edge parity gained the parent stable key, file and full span.
- INPUT-1 refined in PREP-3 revision 3 (cycle-4 directive; review-2, F-PSI-UNRESOLVED-ORACLE): PSI-C04's from-import query `Q` now selects `e.extractor`, `e.line_end`, `e.col_end` and `e.is_type_only` and asserts them unchanged on every from-import edge (extractor normalized as elsewhere); only target, resolution and the `alternateTarget`/`basis` metadata keys may move. Correction to the revision-2 entry above: the PRAGMA schema audit is a schema-change tripwire over the declaration — it does not prove that each partitioned query exercises every declared column (the revision-2 wording "cannot drift" overstated it); PSI-C04's inputs and the audit's comment now say so. Nothing else changed.
