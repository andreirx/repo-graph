<!-- requirements-assurance-implementation-v1
{
  "formatVersion": 1,
  "kind": "implementation-allocation",
  "workItemId": "DOCS-UNREADABLE-DECODE-1",
  "baselinePath": "docs/requirements/baselines/DOCS-UNREADABLE-DECODE-1-INPUT-1.json",
  "parentRequirementIds": [
    "RG-REQ-008",
    "RG-REQ-011"
  ],
  "implements": [
    "RG-REQ-008-L05",
    "RG-REQ-008-L08"
  ],
  "preserves": [
    "RG-REQ-008-L01",
    "RG-REQ-008-L03",
    "RG-REQ-008-L04",
    "RG-REQ-008-L06",
    "RG-REQ-011-L06"
  ],
  "preservationObligationIds": [
    "P-DU-01",
    "P-DU-02",
    "P-DU-03"
  ],
  "changes": [],
  "acceptanceBoundary": "The doc-facts unit suite (byte hash, undecodable handling, `content_hash` always serialized — a string when the bytes were read, `null` when they were not), the rgr lib tests (the `DocEntry` decode of a string, of `null`, the refusal of a missing key, and the filtered `--json` view re-emitting the key in both cases) with the docs_command CLI tests and the daemon docs-overlay tests, fmt/clippy over doc-facts, daemon-runtime and rgr, and the field proof at the CLI boundary: `rmap docs list` human and `--json` by the observed v0.19.0 comparator (the installed `~/.local/bin/rmap`, identified only by its self-reported `rmap 0.19.0`; the source that built it is not attested) versus the candidate on the poco and leveldb before-roots, plus the candidate on a builder-made corpus holding one document whose bytes cannot be read (`docs/NOTES.md`, mode 000), as named per check.",
  "candidatePaths": [
    "rust/crates/doc-facts/src/lib.rs",
    "rust/crates/doc-facts/src/lib_tests.rs",
    "rust/crates/rgr/src/presentation/docs.rs",
    "rust/crates/rgr/src/presentation/docs_tests.rs"
  ],
  "postReviewRecordPaths": [
    "docs/assurance/DOCS-UNREADABLE-DECODE-1/verification.json",
    "docs/assurance/DOCS-UNREADABLE-DECODE-1/implementation-review.json"
  ],
  "candidateExclusions": [
    {
      "pathPrefix": ".agent-manager/",
      "reason": "local relay state and raw run/progress evidence"
    },
    {
      "pathPrefix": "rust/target/",
      "reason": "reproducible Cargo build output"
    }
  ],
  "checks": [
    {
      "checkId": "DU-C01",
      "obligationIds": [
        "P-DU-01",
        "P-DU-02",
        "RG-REQ-008-L01",
        "RG-REQ-008-L03",
        "RG-REQ-008-L05",
        "RG-REQ-008-L06",
        "RG-REQ-008-L08"
      ],
      "owner": "builder",
      "method": {
        "kind": "command",
        "command": "cargo test -p repo-graph-doc-facts 2>&1 | tee /tmp/du-c01.txt | grep -E '^test result: ok\\.' && ! grep -E '^test .* FAILED$|^thread .* panicked at|^test result: FAILED' /tmp/du-c01.txt && for t in entry_with_unread_bytes_serializes_content_hash_null_with_the_key_present entry_with_undecodable_bytes_serializes_its_byte_hash undecodable_utf8_document_keeps_a_byte_hash_and_counts_unreadable readable_document_hash_is_unchanged_by_the_byte_read extract_warns_on_an_undecodable_document inventory_unreadable_sidecar_is_admitted_and_counted_never_asserted_generated inventory_unreadable_license_named_doc_is_license_by_name_and_still_counted inventory_license_is_name_only_content_body_is_not_upgraded extract_handles_unreadable_files extract_from_repo_with_readme inventory_flags_generated_only_with_marker env_files_excluded_from_inventory inventory_reports_unscanned_markup_outside_docs_tree stem_and_extension_match_case_insensitively admits_txt_and_rst_under_docs_tree unscanned_counts_markup_outside_a_docs_tree_never_txt marker_confirms_generated markerless_user_map_is_not_self_generated; do grep -qE \"^test ([a-z_]+::)*$t \\.\\.\\. ok$\" /tmp/du-c01.txt || { echo \"MISSING $t\"; exit 1; }; done && ! grep -qE 'let content = fs::read_to_string\\(&doc\\.path\\)\\?;' crates/doc-facts/src/lib.rs && git diff --quiet HEAD -- crates/doc-facts/src/discovery.rs crates/doc-facts/src/classification.rs crates/doc-facts/src/self_generated.rs crates/doc-facts/src/release_notes.rs crates/doc-facts/src/types.rs crates/doc-facts/src/extractors crates/doc-facts/Cargo.toml && test \"$(grep -cE '^[[:space:]]*pub content_hash: Option<String>,$' crates/doc-facts/src/lib.rs)\" = 1 && test \"$(awk '/pub content_hash: Option<String>,/{print prev} {prev=$0}' crates/doc-facts/src/lib.rs | grep -c skip_serializing_if)\" = 0 && awk '/pub release_family: Option<String>,/{print prev} {prev=$0}' crates/doc-facts/src/lib.rs | grep -q 'skip_serializing_if = \"Option::is_none\"'",
        "cwd": "rust",
        "environment": "candidate tree",
        "inputs": "PRODUCER (doc-facts). `read_and_hash` (rust/crates/doc-facts/src/lib.rs:484-496) today calls `fs::read_to_string` (:485), so bytes that are not UTF-8 fail the read and the document is admitted with NO hash. After: `fs::read` the BYTES, hash the bytes, set `doc.content_hash` whenever the bytes were read, then `String::from_utf8`: `Ok` → `doc.content = Some`; `Err` → `doc.content = None` and the function reports the undecodable outcome to its callers (e.g. `Ok(ReadOutcome::{Text, Undecodable})`, IO errors unchanged in `Err`; the shape is the builder's, the observable facts below are the contract). For valid UTF-8 the decoded string's bytes ARE the file's bytes, so every readable hash is unchanged. `discover_doc_inventory`'s hashing pass (:177-189) counts an undecodable document in `unreadable_count` exactly as it counts a non-`NotFound` IO error, INCLUDING the `!self_generated::has_map_sidecar_name` guard (:183): a sidecar-named undecodable file is counted once, by the sidecar pass (`read_doc_content` :472-480 finds `doc.content == None`, re-reads with `read_to_string`, gets `InvalidData` → `MarkerRead::Unreadable` → counted at :213-216), never twice. The `license` kind is NAME-only (AUDIT5-MINORS-1 F1), so an undecodable `LICENSE*` stays `license`; the only content-reading kind rule, the release-notes manifest check (:246-253), sees `doc.content == None` exactly as today. `extract_semantic_facts` (:344-356) pushes, for an undecodable document, a warning byte-identical to today's (`format!(\"failed to read: {}\", e)` over the `InvalidData` error `fs::read_to_string` returns — the test compares against `fs::read_to_string(&path).unwrap_err()`, not a hard-coded std string) and extracts nothing (`extractors/mod.rs:20-29` returns empty without `content`). SERIALIZATION (RG-REQ-008-L08 as amended, D-DU-CC10): `DocInventoryEntry.content_hash` (lib.rs:71-72) loses `#[serde(skip_serializing_if = \"Option::is_none\")]`, so the key is always serialized — a string when the bytes were read, `null` when they could not be; `release_family` (lib.rs:80-81) keeps its skip (the wire of every non-release entry unchanged). NEW tests (lib_tests.rs): `undecodable_utf8_document_keeps_a_byte_hash_and_counts_unreadable` (`discover_doc_inventory(dir, true)` over `README.md` UTF-8, `packaging/README.txt` = the bytes `Apple\\x92s Cocoa\\n`, and a sidecar-named `src/gen/MAP.md` holding byte 0x92: all three admitted; `packaging/README.txt`'s `content_hash` = `00873e70bc71b05cc510c80ff317e1f2ed5a82eee47b0405b83a3c2e21a90109` (sha256 of those 15 bytes, lowercase 64-hex, the encoding `read_and_hash` already emits); `unreadable_count == 2`; `serde_json::to_value` of every entry carries a STRING `content_hash`); `readable_document_hash_is_unchanged_by_the_byte_read` (a fixture of the UTF-8 text `# Résumé — ✓\\n` hashes to the literal `ae9c3d6d18a8667af95c59681aa94026b17feafb9d7af579331995b26dbd1155`, computed independently of the implementation); `extract_warns_on_an_undecodable_document`; `entry_with_unread_bytes_serializes_content_hash_null_with_the_key_present` (`discover_doc_inventory(dir, true)` over `README.md` and a `docs/NOTES.md` set to mode 0o000 with `fs::set_permissions` and restored before assertions, the precedent at lib_tests.rs:478-493: the entry is admitted, `unreadable_count == 1`, and `serde_json::to_string` of its entry contains the literal `\"content_hash\":null`); `entry_with_undecodable_bytes_serializes_its_byte_hash` (the undecodable entry's `serde_json::to_value(..)[\"content_hash\"]` is the JSON string of the sha256 of its bytes). The existing inventory, extraction, discovery (L01, L06) and generated-marker (L03) tests are bound by name. The greps pin: the `read_to_string` read in `read_and_hash` is gone; exactly one `pub content_hash: Option<String>,` in lib.rs with no `skip_serializing_if` on the line above it; `release_family` keeps its skip; discovery.rs, classification.rs, self_generated.rs, release_notes.rs, types.rs, extractors/ and Cargo.toml unchanged. Other consumer of the changed read (no code change, named for impact): `storage/src/agent_orient_reads.rs:90` (`discover_doc_inventory(&resolved, true)` for `orient`'s orientation docs) projects `path/kind/generated` only — an undecodable file has no `content` before or after, so the orient line does not move. Guardrail call (CLAUDE.md, files over 500 lines — lib.rs 515, lib_tests.rs 564): the change modifies an existing responsibility (the content read) and tests it; no new responsibility is appended."
      },
      "expected": "exit 0: a document whose bytes are not UTF-8 is admitted with the sha256 of its bytes and counted once as unreadable (RG-REQ-008-L05); a document whose bytes cannot be read is admitted, counted and serialized with the key `content_hash` present and `null` (RG-REQ-008-L08 as amended by D-DU-CC10); the extraction warning for an undecodable document reads exactly as before and nothing is extracted from it; no behavior change for readable documents (every readable hash unchanged — pinned by an independent literal) nor for discovery, the generated-marker decision, the vendored/unscanned inputs or `release_family` serialization (their modules unchanged, their tests green)"
    },
    {
      "checkId": "DU-C02",
      "obligationIds": [
        "P-DU-02",
        "RG-REQ-008-L03",
        "RG-REQ-008-L04",
        "RG-REQ-008-L05",
        "RG-REQ-008-L06",
        "RG-REQ-008-L08"
      ],
      "owner": "builder",
      "method": {
        "kind": "command",
        "command": "cargo test -p repo-graph-rgr --lib 2>&1 | tee /tmp/du-c02.txt | grep -E '^test result: ok\\.' && ! grep -E '^test .* FAILED$|^thread .* panicked at|^test result: FAILED' /tmp/du-c02.txt && for t in doc_entry_decodes_a_string_content_hash doc_entry_decodes_a_null_content_hash_as_none doc_entry_rejects_a_missing_content_hash_key json_filtered_view_re_emits_content_hash_as_string_and_null json_filtered_view_preserves_entry_fields_and_recomputes_counts_by_kind json_filtered_view_surfaces_unreadable_when_present json_filtered_view_surfaces_unscanned_and_rule_when_present list_default_excludes_generated_maps_and_states_the_count json_default_excludes_generated_and_reports_excluded_count vendored_demoted_from_headline_and_listing_but_stated list_render_surfaces_unreadable_count entry_list_budgeted_with_remainder family_lines_and_entries_share_one_budget; do grep -qE \"^test presentation::docs::tests::$t \\.\\.\\. ok$\" /tmp/du-c02.txt || { echo \"MISSING $t\"; exit 1; }; done && test \"$(grep -cE '^[[:space:]]*pub content_hash: Option<String>,$' crates/rgr/src/presentation/docs.rs)\" = 1 && test \"$(awk '/pub content_hash: Option<String>,/{print prev} {prev=$0}' crates/rgr/src/presentation/docs.rs | grep -cE 'default|skip_serializing_if')\" = 0 && cargo test -p repo-graph-rgr --test docs_command 2>&1 | tee /tmp/du-c02b.txt | grep -E '^test result: ok\\.' && ! grep -E '^test .* FAILED$|^thread .* panicked at|^test result: FAILED' /tmp/du-c02b.txt && for t in docs_list_daemon_required docs_list_usage_error_unexpected_args; do grep -qE \"^test $t \\.\\.\\. ok$\" /tmp/du-c02b.txt || { echo \"MISSING $t\"; exit 1; }; done && cargo test -p repo-graph-daemon-runtime --lib docs_list_overlay 2>&1 | tee /tmp/du-c02c.txt | grep -E '^test result: ok\\.' && ! grep -E '^test .* FAILED$|^thread .* panicked at|^test result: FAILED' /tmp/du-c02c.txt && for t in vendored_override_clears_release_family non_vendored_release_note_keeps_family vendored_non_release_doc_demoted_family_already_none; do grep -qE \"^test docs_list_overlay::tests::$t \\.\\.\\. ok$\" /tmp/du-c02c.txt || { echo \"MISSING $t\"; exit 1; }; done && git diff --quiet HEAD -- crates/daemon-runtime crates/storage crates/agent && test \"$(git diff HEAD --name-only --relative -- crates/rgr | sort | tr '\\n' ' ')\" = 'crates/rgr/src/presentation/docs.rs crates/rgr/src/presentation/docs_tests.rs '",
        "cwd": "rust",
        "environment": "candidate tree",
        "inputs": "CONSUMER (rgr). `DocEntry.content_hash` (rust/crates/rgr/src/presentation/docs.rs:192, today `pub content_hash: String,`) becomes `pub content_hash: Option<String>,` decoded so that a JSON string → `Some`, JSON `null` → `None`, and a MISSING key is still the decode error `missing field `content_hash`` (L08: the key is always present). serde's derive decodes a MISSING `Option` field as `None` unless the field carries `deserialize_with` (INFERRED from serde_derive's missing-field handling; `doc_entry_rejects_a_missing_content_hash_key` is the oracle), so the field carries `#[serde(deserialize_with = \"<a required-nullable decoder>\")]` and NEITHER `default` NOR `skip_serializing_if` — the same file's `present_value` (docs.rs:108-113) is the precedent for a presence-aware decoder. Serialization keeps the key in both cases, so the filtered `--json` view (`filtered_json_view`, docs.rs:242-288, which re-serializes `DocEntry` when generated maps are excluded) re-emits `\"content_hash\": \"<hash>\"` or `\"content_hash\": null`; the raw passthrough (nothing filtered) prints the daemon value unchanged. The human render does not print the hash (unchanged). Every literal `DocEntry` construction in docs_tests.rs (11 — 13 lines match `DocEntry {`, two of them the return types of `fn entry`/`fn release_entry`; docs.rs:188 is the struct definition, not a construction; grep-verified 2026-09-23) gains `Some(..)`. NEW tests (docs_tests.rs): `doc_entry_decodes_a_string_content_hash`, `doc_entry_decodes_a_null_content_hash_as_none`, `doc_entry_rejects_a_missing_content_hash_key` (the error text contains `missing field `content_hash``), `json_filtered_view_re_emits_content_hash_as_string_and_null` (a response with a generated `MAP.md` — which forces the filtered view — `README.md` with `Some(\"h1\")` and `docs/NOTES.md` with `None`: the view's `README.md` entry has `\"content_hash\": \"h1\"`, its `docs/NOTES.md` entry has the KEY `content_hash` with value `null`). Existing tests bound by name: the filtered/raw JSON view (`json_filtered_view_preserves_entry_fields_and_recomputes_counts_by_kind`), generated exclusion (L03), vendored demotion (L04), the unreadable line and JSON count (L05), the unscanned count and rule (L06), the budget. `docs_command` CLI tests and the daemon's `docs_list_overlay` tests (L04) stay green. The greps pin exactly one `pub content_hash: Option<String>,` in docs.rs with no `default`/`skip_serializing_if` on the attribute line above it; daemon-runtime, storage and agent are unchanged (`handle_docs_list` and the payload path are untouched — the producer change is doc-facts'); rgr's tracked diff is exactly docs.rs and docs_tests.rs (`--relative`: `git diff --name-only` is repository-relative by default). Guardrail call (docs.rs 575 lines, docs_tests.rs 914): the decode of an existing DTO field and its tests — no new responsibility."
      },
      "expected": "exit 0: the CLI decodes both ratified shapes (a string and `null`), still refuses an entry without the key, and re-emits the key in both cases on the filtered `--json` view; every existing rgr docs test (generated, vendored, unreadable, unscanned, budget), the docs_command CLI tests and the overlay tests stay green; the daemon, storage and agent crates are unchanged (no behavior change outside the decode)"
    },
    {
      "checkId": "DU-C03",
      "obligationIds": [
        "P-DU-03"
      ],
      "owner": "builder",
      "method": {
        "kind": "command",
        "command": "cargo fmt --check -p repo-graph-doc-facts -p repo-graph-rgr && cargo clippy -p repo-graph-doc-facts -p repo-graph-daemon-runtime -p repo-graph-rgr --all-targets -- -D warnings > /tmp/du-c03.txt 2>&1; rc=$?; tail -3 /tmp/du-c03.txt; test $rc -eq 0 && git -C .. diff HEAD --check",
        "cwd": "rust",
        "environment": "candidate tree",
        "inputs": "formatter over doc-facts and rgr; clippy over doc-facts, daemon-runtime (the consumer of `DocInventoryEntry`) and rgr, all targets, warnings denied; whitespace hygiene of the tracked diff."
      },
      "expected": "exit 0: no formatting, lint or whitespace debt enters with the candidate"
    },
    {
      "checkId": "DU-C04",
      "obligationIds": [
        "P-DU-01",
        "RG-REQ-008-L01",
        "RG-REQ-008-L04",
        "RG-REQ-008-L05",
        "RG-REQ-008-L06",
        "RG-REQ-008-L08"
      ],
      "owner": "builder",
      "method": {
        "kind": "command",
        "command": "R=/private/tmp/DOCS-UNREADABLE-DECODE-1; C=$R-unreadable-after; LC='/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases'; BEFORE=\"$HOME/.local/bin/rmap\"; CAND='/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap'; run() { ( cd \"$1\" && env RMAP_STATE_ROOT=\"$2\" RMAP_SOCKET_PATH=\"$2/d.sock\" RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off \"$3\" $4 > \"$5\" 2> \"$5.err\"; echo $? > \"$5.rc\" ); }; rm -f /tmp/du-poco-* /tmp/du-ldb-* /tmp/du-unr*; test \"$(id -u)\" != 0 && test \"$(\"$BEFORE\" --version)\" = 'rmap 0.19.0' && test -x \"$HOME/.local/bin/rmapd\" && test -x \"$CAND\" && test -x \"${CAND}d\" && test -d \"$R-poco-before\" && test -d \"$R-leveldb-before\" && test ! -e \"$C\" && trap 'chmod -R u+rwX \"$C\" 2>/dev/null; rm -rf \"$C\"' EXIT && run \"$LC/poco\" \"$R-poco-before\" \"$BEFORE\" 'docs list' /tmp/du-poco-before.txt && run \"$LC/poco\" \"$R-poco-before\" \"$BEFORE\" 'docs list --json' /tmp/du-poco-before.json && run \"$LC/leveldb\" \"$R-leveldb-before\" \"$BEFORE\" 'docs list' /tmp/du-ldb-before.txt && run \"$LC/leveldb\" \"$R-leveldb-before\" \"$BEFORE\" 'docs list --json' /tmp/du-ldb-before.json && run \"$LC/poco\" \"$R-poco-before\" \"$CAND\" 'docs list' /tmp/du-poco-after.txt && run \"$LC/poco\" \"$R-poco-before\" \"$CAND\" 'docs list --json' /tmp/du-poco-after.json && run \"$LC/leveldb\" \"$R-leveldb-before\" \"$CAND\" 'docs list' /tmp/du-ldb-after.txt && run \"$LC/leveldb\" \"$R-leveldb-before\" \"$CAND\" 'docs list --json' /tmp/du-ldb-after.json && mkdir -p \"$C/state\" \"$C/corpus/docs\" && printf '# Readable\\n' > \"$C/corpus/README.md\" && printf 'private notes\\n' > \"$C/corpus/docs/NOTES.md\" && printf 'fn main() {}\\n' > \"$C/corpus/main.rs\" && git -C \"$C/corpus\" init -q && git -C \"$C/corpus\" add -A && git -C \"$C/corpus\" -c user.email=du@example.invalid -c user.name=du commit -qm c1 && run \"$C/corpus\" \"$C/state\" \"$CAND\" index /tmp/du-unr-index.txt && chmod 000 \"$C/corpus/docs/NOTES.md\" && run \"$C/corpus\" \"$C/state\" \"$CAND\" 'docs list' /tmp/du-unr.txt && run \"$C/corpus\" \"$C/state\" \"$CAND\" 'docs list --json' /tmp/du-unr.json && chmod 644 \"$C/corpus/docs/NOTES.md\" && python3 - <<'PY'\nimport json, hashlib, os\nLC = '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases'\nMISSING = 'error: failed to parse response: missing field `content_hash`'\ndef rd(p): return open(p, encoding='utf-8').read()\ndef rc(p): return int(open(p + '.rc').read().strip())\ndef sha(p): return hashlib.sha256(open(p, 'rb').read()).hexdigest()\nfor p in ('/tmp/du-poco-before.txt', '/tmp/du-poco-before.json'):\n    assert rc(p) == 2, ('before exit', p, rc(p))\n    assert MISSING in rd(p + '.err'), ('before stderr', p, rd(p + '.err'))\nfor p in ('/tmp/du-poco-after.txt', '/tmp/du-poco-after.json', '/tmp/du-ldb-before.txt', '/tmp/du-ldb-before.json', '/tmp/du-ldb-after.txt', '/tmp/du-ldb-after.json', '/tmp/du-unr-index.txt', '/tmp/du-unr.txt', '/tmp/du-unr.json'):\n    assert rc(p) == 0, ('exit', p, rc(p), rd(p + '.err'))\nlines = rd('/tmp/du-poco-after.txt').splitlines()\nfor want in ('15 documents', '+1 vendored doc (excluded; third-party/dependency directories — see --json for the full set)', '+1 unreadable, counted (content unreadable — kind refinement unverifiable)', '+5 markdown/prose files outside a docs tree not scanned (--json for the rule)', '  readme  11', '  doc  4', '  packaging/README.txt  readme'):\n    assert want in lines, ('poco human line missing', want)\nv = json.load(open('/tmp/du-poco-after.json'))\nents = {e['path']: e for e in v['entries']}\nassert v['count'] == 16 and len(ents) == 16, ('poco count', v['count'], len(ents))\nassert v.get('unreadable') == 1, ('poco unreadable', v.get('unreadable'))\nassert all(isinstance(e.get('content_hash'), str) for e in v['entries']), ('poco entry without a string hash', [e['path'] for e in v['entries'] if not isinstance(e.get('content_hash'), str)])\nu = ents['packaging/README.txt']\nassert u['kind'] == 'readme', u\nassert u['content_hash'] == sha(LC + '/poco/packaging/README.txt') == 'dae0857da12cb0f9652108e2607ea6d1d1a2b2ae2b8d9b07188cee1fddf86993', u\nassert ents['README.md']['content_hash'] == sha(LC + '/poco/README.md'), ents['README.md']\nassert ents['dependencies/README.md']['kind'] == 'vendored', ents['dependencies/README.md']\nassert rd('/tmp/du-ldb-before.txt') == rd('/tmp/du-ldb-after.txt'), 'leveldb human output moved'\nassert '9 documents' in rd('/tmp/du-ldb-after.txt').splitlines(), 'leveldb headline'\nlb, la = json.load(open('/tmp/du-ldb-before.json')), json.load(open('/tmp/du-ldb-after.json'))\nassert lb == la, 'leveldb JSON moved (compared parsed: counts_by_kind key order follows a HashMap in any build of this source)'\nfor e in la['entries']:\n    assert e['content_hash'] == sha(LC + '/leveldb/' + e['path']), ('leveldb hash', e)\nx = json.load(open('/tmp/du-unr.json'))\nxe = {e['path']: e for e in x['entries']}\nassert set(xe) == {'README.md', 'docs/NOTES.md'}, sorted(xe)\nn = xe['docs/NOTES.md']\nassert 'content_hash' in n and n['content_hash'] is None, ('bytes-unreadable entry', n)\nassert n['kind'] == 'doc' and n['generated'] is False, n\nassert xe['README.md']['content_hash'] == hashlib.sha256(b'# Readable\\n').hexdigest(), xe['README.md']\nassert x.get('unreadable') == 1, x.get('unreadable')\nul = rd('/tmp/du-unr.txt').splitlines()\nassert '2 documents' in ul and '+1 unreadable, counted (content unreadable — kind refinement unverifiable)' in ul, ul\nprint('DU-C04 ok: poco 2->0 both modes, 16 entries each with a byte hash (packaging/README.txt dae0857d...f86993), 1 unreadable, 1 vendored; leveldb identical (human bytes, JSON parsed); docs/NOTES.md (mode 000) listed with content_hash null')\nPY",
        "cwd": ".",
        "environment": "the candidate's release build (`rust/target/release/rmap` + `rmapd`, built at §5 step 3) and the observed v0.19.0 comparator `~/.local/bin/rmap` (its sibling `~/.local/bin/rmapd` serves stdio — `find_rmapd` prefers the executable's sibling, rgr/src/daemon_client/stdio_transport.rs:217-226). PROVENANCE: the comparator is identified only by its self-reported `rmap 0.19.0`; which source built it is NOT attested. `git diff --stat v0.19.0 HEAD -- rust` being empty at HEAD 8b80253f says the two SOURCE trees are equal, not that the installed executables were built from either. The comparator's before assertions are BEHAVIOURAL (what an installed v0.19.0 does on poco); P-DU-01's independent oracles are the per-file sha256 values and the unit-test literal, not the comparator. If leveldb's comparator and candidate outputs differ, STOP and report the difference as UNATTRIBUTED (the slice, or a local build difference in the comparator); attributing it needs an attested baseline — a before-binary built from `git worktree add /private/tmp/DOCS-UNREADABLE-DECODE-1-before HEAD` (CLAUDE.md) — which is the operator's call, not the builder's. Pre-provisioned manager-owned roots /private/tmp/DOCS-UNREADABLE-DECODE-1-poco-before (poco, indexed 2026-09-22T23:40Z by the manager with a v0.19.0-labelled binary — per the manager's record, not attested) and /private/tmp/DOCS-UNREADABLE-DECODE-1-leveldb-before (leveldb); `docs list` is a query-time walk of the repo's root path, so the SAME roots are served by both binaries — never reindexed; the roots STAY. The builder-made root /private/tmp/DOCS-UNREADABLE-DECODE-1-unreadable-after (state/ + corpus/) is created and removed by the command itself (EXIT trap). RMAP_TRANSPORT=stdio, RMAP_AUTO_ENRICH=off, RMAP_AUTO_RETENTION=off on every invocation; not run as root (a mode-000 file must be unreadable).",
        "inputs": "poco (C++, HEAD 49af400): `packaging/README.txt` is Windows-1252 — `file`: `Non-ISO extended-ASCII text`; byte 0x92 at offset 232 (also 0x91 at 324, 0x92 at 337); admitted by the ratified stem rule. BEFORE (the observed v0.19.0 comparator): `docs list` AND `docs list --json` exit 2 with stderr `error: failed to parse response: missing field `content_hash`` (both modes asserted). AFTER (the manager's and the PREP-4 author's python emulation of discovery.rs over the checkout, 2026-09-23: 16 candidates, 5 refused markup files, one non-UTF-8 file; the product is the truth — a differing literal is a STOP-and-report): the human lines `15 documents`, `+1 vendored doc (excluded; third-party/dependency directories — see --json for the full set)`, `+1 unreadable, counted (content unreadable — kind refinement unverifiable)`, `+5 markdown/prose files outside a docs tree not scanned (--json for the rule)`, `  readme  11`, `  doc  4`, `  packaging/README.txt  readme` (readme: `README.md`, `Benchmark/README.md`, `DNSSD/README.md`, `Data/SQLParser/README.md`, `Data/SQLParser/benchmark/README.md`, `MongoDB/samples/ReplicaSet/README.md`, `NetSSL_OpenSSL/samples/download/doc/readme.txt`, `NetSSL_Win/doc/README.txt`, `cmake/README`, `packaging/README.md`, `packaging/README.txt`; doc: `CHANGELOG`, `CONTRIBUTING.md`, `NetSSL_OpenSSL/doc/howtobuild.txt`, `Zip/doc/ZIP Spec.txt`; vendored: `dependencies/README.md` — `dependencies` is in `classification/src/vendored_path.rs` VENDORED_SEGMENTS); `--json` (the raw daemon payload — no generated maps, so no filtered view): `count` 16, `unreadable` 1, EVERY entry a string `content_hash`, `packaging/README.txt`'s = `dae0857da12cb0f9652108e2607ea6d1d1a2b2ae2b8d9b07188cee1fddf86993` (sha256 of its bytes, computed read-only from the checkout 2026-09-23), `README.md`'s = the sha256 of its bytes (`92512c74…ec1b6a`; unchanged by the byte read). leveldb (HEAD 7ee830d; 9 documents, all UTF-8): human output byte-identical between the two binaries; `--json` identical AFTER parsing (in any build of this source serde_json's `preserve_order` feature is unified on — classification and indexer enable it — and the daemon builds `counts_by_kind` from a `HashMap`, dispatch.rs:5700-5713, so its key order is per-process; comparing bytes would test HashMap iteration, not the slice — INFERRED from source for the candidate, and assumed, not attested, for the comparator); every leveldb hash equals the sha256 of the file's bytes. BYTES-UNREADABLE corpus (RG-REQ-008-L05/L08 as amended; D-DU-CC10): a git repo with `README.md` (`# Readable\\n`), `docs/NOTES.md` (inside a doc tree so the ratified rule admits it — a root `NOTES.md` is NOT a document: `notes` is not a stem) and `main.rs`; committed and indexed by the candidate while readable, THEN `docs/NOTES.md` set to mode 000 (the daemon runs as this user, so `fs::read` fails with EACCES); `docs list --json` exits 0 and lists exactly `README.md` (a string hash = sha256 of `# Readable\\n`) and `docs/NOTES.md` as `{\"path\":\"docs/NOTES.md\",\"kind\":\"doc\",\"generated\":false,\"content_hash\":null}` (the key present), `unreadable` 1; the human output reads `2 documents` and `+1 unreadable, counted (content unreadable — kind refinement unverifiable)`; the mode is restored and the root removed. All assertions are in the trailing python (each failure names its file and value)."
      },
      "expected": "exit 0: the first repository with an undecodable README gets its documentation surface back — the file listed with the sha256 of its bytes and counted out loud as RG-REQ-008-L05 requires, every JSON entry carrying `content_hash` as L08 requires; a document whose bytes cannot be read at all is admitted, counted and listed with the key `content_hash` present and `null` (an unknown marked, never omitted, never fabricated); discovery (L01), vendored (L04) and unscanned (L06) lines hold on poco; a repository with nothing unreadable (leveldb) shows no behavior change — human output byte-identical, JSON identical after parsing, every hash equal to its file's sha256"
    },
    {
      "checkId": "DU-C05",
      "obligationIds": [
        "RG-REQ-011-L06",
        "P-DU-03"
      ],
      "owner": "builder",
      "method": {
        "kind": "command",
        "command": "test \"$(grep -E '^registry-before [0-9a-f]{64}$' .agent-manager/slices/DOCS-UNREADABLE-DECODE-1/build-progress.md | tail -1 | cut -d' ' -f2)\" = \"$(shasum -a 256 \"$HOME/Library/Application Support/repo-graph/registry.json\" | cut -d' ' -f1)\" && PIDS=$(pgrep -x rmapd | paste -sd, -); { [ -z \"$PIDS\" ] || ! ps -o command= -p \"$PIDS\" | grep -v '/\\.local/bin/rmapd' | grep -q .; } && ! test -e /private/tmp/DOCS-UNREADABLE-DECODE-1-unreadable-after && ls -d /private/tmp/DOCS-UNREADABLE-DECODE-1-poco-before /private/tmp/DOCS-UNREADABLE-DECODE-1-leveldb-before >/dev/null && ! git status --short | grep -vE '^[ MA?][ MA?] rust/crates/(doc-facts/src/lib\\.rs|doc-facts/src/lib_tests\\.rs|rgr/src/presentation/docs\\.rs|rgr/src/presentation/docs_tests\\.rs)$' | grep -q . && git diff --check",
        "cwd": ".",
        "environment": "candidate tree, after every other check",
        "inputs": "the full sha256 of the operator registry recorded as `registry-before <digest>` in .agent-manager/slices/DOCS-UNREADABLE-DECODE-1/build-progress.md at §5 step 0 equals the current one; no rmapd process other than the operator's installed `~/.local/bin/rmapd`; the builder-made root /private/tmp/DOCS-UNREADABLE-DECODE-1-unreadable-after is gone; the two manager-owned `-before` roots are kept; `git status --short` names only the four candidate paths; no whitespace errors."
      },
      "expected": "exit 0: every proof was isolated (the operator's registry unchanged — no behavior change outside the isolated roots), the builder's root is removed, and the tree holds only the allocated paths"
    }
  ]
}
-->

# DOCS-UNREADABLE-DECODE-1 — an unreadable document is listed and counted, never a crash

Status: SPECIFIED and GROUNDED 2026-09-23 on HEAD 3f812e3e (RC-1 of audit round seven, verified from source and reproduced on a fresh isolated poco index); REVISED the same day for the fourth document item (PREP-4) under RG-BOOTSTRAP-INPUT-9 after the human answered CC-10 (D-DU-CC10, option A), re-grounded on HEAD 8b80253f (`git diff --stat v0.19.0 HEAD -- rust` empty — a statement about source trees only). Track: audit round seven fix queue (a crash). CODE slice: doc-facts (the byte read and the always-present `content_hash`) and rgr (the `DocEntry` decode). Query-time surface: no reindex, no toolchain version change. Builder: claude / claude-opus-5-5 (human trial directive 2026-09-22); reviewer: codex.

## 0. Requirements allocation (the machine-readable block above is binding; this section explains it)

**Implements:**
- **RG-REQ-008-L05** — an unreadable document is admitted, counted (`+N unreadable, counted (content unreadable — kind refinement unverifiable)`) and never asserted; "a document whose bytes could not be read at all carries `content_hash: null` in JSON (L08); a document whose bytes were read but do not decode as UTF-8 carries the hash of its bytes." Today the CLI fails the whole `docs list` on the first admitted document whose bytes are not UTF-8 (poco). Proof: DU-C01 (byte hash, counted once, extraction warning), DU-C04 (poco before/after; the mode-000 corpus).
- **RG-REQ-008-L08 as amended (INPUT-8/INPUT-9, D-DU-CC10, the human 2026-09-23)** — "`content_hash` is always present and is `null` when the document's bytes could not be read (an unknown is marked, never omitted, never fabricated — RG-REQ-002-L11; amended 2026-09-23, CC-10, D-DU-CC10)". Its verification criterion names this slice: "a daemon-side serialization test that an entry whose bytes could not be read serializes `"content_hash": null` (the key present) and a non-UTF-8 readable entry serializes its byte hash; a CLI decode test that `DocEntry` accepts both a string and `null` and that the filtered `--json` view re-emits the key in both cases." The three checks that carry L08: DU-C01 (the producer — `entry_with_unread_bytes_serializes_content_hash_null_with_the_key_present` over a real mode-000 file through `discover_doc_inventory`, the function the daemon's `docs list` handler calls, and `entry_with_undecodable_bytes_serializes_its_byte_hash`); DU-C02 (the consumer — `doc_entry_decodes_a_string_content_hash`, `doc_entry_decodes_a_null_content_hash_as_none`, `doc_entry_rejects_a_missing_content_hash_key`, `json_filtered_view_re_emits_content_hash_as_string_and_null`); DU-C04 (the daemon payload end to end at the CLI: poco's 16 string hashes and the corpus's `null`).

**Decision D-DU-01 (first document review; operator, no requirement change):** `read_and_hash` hashes the file's BYTES — byte-identical to today's hash of the decoded string for every valid-UTF-8 file — so a document whose bytes are not UTF-8 keeps a real hash, gets no `content`, and is counted unreadable. Its original consequence "this slice touches NOTHING in the CLI" is SUPERSEDED by D-DU-CC10: the bytes-unreadable corner (permission/IO), where no truthful hash exists, is now specified by L08 (`null`, key present) and implemented here on both sides of the wire.

**Changes:** none allocated. REPORTED consequences: (1) poco's `docs list` goes from exit 2 to `15 documents` with the unreadable line, and every poco entry carries a string `content_hash`; (2) an entry whose bytes could not be read now serializes `"content_hash": null` where the daemon omitted the key — an additive wire change the human ratified (D-DU-CC10); no rmap output ever showed the omitted form, because the CLI failed the whole command on it.

**Preserves:** RG-REQ-008-L01 (discovery), L03 (generated), L04 (vendored), L06 (unscanned) — their modules unchanged, their tests bound by name, their lines asserted on poco (DU-C01/C02/C04); RG-REQ-011-L06 (isolation — DU-C05).

**Preservation obligations:**

| Id | Obligation | Proof |
|---|---|---|
| P-DU-01 | every readable document's hash is unchanged; a repository with nothing unreadable shows no change — human output byte-identical, JSON identical after parsing (key order of `counts_by_kind` is per-process in any build of this source, §2.3); the independent oracles are the sha256 values, the comparator is a behavioural cross-check with unattested build provenance | DU-C01 (`readable_document_hash_is_unchanged_by_the_byte_read` against an independent literal); DU-C04 (leveldb human bytes and parsed JSON, every hash = sha256 of the file; poco `README.md`) |
| P-DU-02 | daemon-runtime, storage and agent unchanged; in doc-facts discovery/classification/self_generated/release_notes/types/extractors and Cargo.toml unchanged; `release_family` keeps its `skip_serializing_if`; in rgr only `DocEntry.content_hash`'s type and decoder and the tests change (docs.rs, docs_tests.rs); the human render unchanged (it does not print the hash) | DU-C01 (diff --quiet, greps); DU-C02 (diff --quiet over the three crates, rgr diff = the two files, the existing render/view/budget tests) |
| P-DU-03 | isolation, cleanup, hygiene | DU-C03; DU-C05 |

## 1. Problem (ROOT-CAUSED — RC-1, 2026-09-23)

`legacy-codebases/poco/packaging/README.txt` (poco HEAD 49af400) is Windows-1252: `file` says `Non-ISO extended-ASCII text`; its first line is ` POCO C++ Libraries`; the first offending byte is 0x92 at offset 232, the apostrophe in `- A collection of C++ class libraries, conceptually similar to the Java Class Library, the .NET Framework or Apple\x92s Cocoa.` (0x91/0x92 recur at offsets 324 and 337; `open(p,'rb').read().decode('utf-8')` → `'utf-8' codec can't decode byte 0x92 in position 232: invalid start byte`). It is the only one of poco's 16 inventory entries that is not valid UTF-8 (python emulation of `discovery.rs` over the checkout, 2026-09-23). DOCS-DISCOVERY-1's stem rule admits it. The code as found:

- `rust/crates/doc-facts/src/lib.rs:485` (in `read_and_hash`, :484-496): `    let content = fs::read_to_string(&doc.path)?;` — fails with `InvalidData`; the entry is admitted with `content_hash: None` and counted in `unreadable_count` (:177-189).
- `rust/crates/doc-facts/src/lib.rs:71-72`: `    #[serde(skip_serializing_if = "Option::is_none")]` / `    pub content_hash: Option<String>,` — the key is omitted on the wire.
- `rust/crates/rgr/src/presentation/docs.rs:192`: `    pub content_hash: String,` — required (no `#[serde(default)]`, unlike `release_family` below it), so `serde_json::from_value::<DocsListResponse>` (rgr/src/commands/docs.rs:163 — in BOTH modes, before the `--json` passthrough) fails: `error: failed to parse response: missing field `content_hash``, exit 2 (captured by the manager on the poco before-root: `/private/tmp/DOCS-UNREADABLE-DECODE-1-poco-before/docs-before.txt`).

Never worked (the required field predates every slice; no smoke corpus had an admitted undecodable document before the widened rule). A document whose bytes cannot be read at all (permission/IO) takes the same path today — the CLI dies on it too; no corpus has one, so DU-C04 builds one.

## 2. Contract

### 2.1 The fix, at the cause
1. **doc-facts `read_and_hash`** (DU-C01): `fs::read` the bytes → hash the bytes → `String::from_utf8`: `Ok` → content; `Err` → no content, reported as undecodable; the inventory counts it `unreadable` like a non-`NotFound` IO error (sidecar guard kept — counted once); `extract_semantic_facts` warns with the byte-identical text and extracts nothing.
2. **doc-facts serialization** (DU-C01): `DocInventoryEntry.content_hash` is always serialized — a string when the bytes were read, `null` when they were not. `release_family` unchanged.
3. **rgr decode** (DU-C02): `DocEntry.content_hash: Option<String>` with a required-nullable decoder (string → `Some`, `null` → `None`, missing key → `missing field `content_hash``), serialized without a skip so the filtered `--json` view re-emits the key in both cases. The human render is unchanged.
4. Nothing else moves: no daemon-runtime, storage, agent or schema change. The changed read reaches exactly three product paths (`git grep -n 'read_and_hash\|discover_doc_inventory(\|extract_semantic_facts(' -- rust/crates`, 2026-09-23): `docs list` (`dispatch.rs:5678`, `true`), `docs extract` (`dispatch.rs:5808`), and `orient`'s orientation-docs read (`storage/src/agent_orient_reads.rs:90`, `true`, projecting `path/kind/generated` only — unchanged). `agent/src/storage_port.rs:1027` only documents a `false` call. `DocInventoryEntry` is serialized only by the `docs list` handler (`dispatch.rs:5711`) and constructed by the overlay's tests (`docs_list_overlay.rs:40`, field unchanged).

Local calls recorded (CLAUDE.md "decide and record"): the outcome type of `read_and_hash` and the name of the rgr decoder are the builder's; the four touched files exceed 500 lines (lib.rs 515, lib_tests.rs 564, docs.rs 575, docs_tests.rs 914) — the slice changes existing responsibilities (the content read; one DTO field's decode) and adds their tests, it appends no new responsibility.

### 2.2 Evidence taxonomy
| Input | Outcome | Bound test / check |
|---|---|---|
| bytes not UTF-8 (0x92) beside a UTF-8 doc and an undecodable sidecar-named `MAP.md` | all admitted; each with the sha256 of its bytes (`00873e70…a90109` for `Apple\x92s Cocoa\n`); `unreadable_count == 2` (each once) | DU-C01 `undecodable_utf8_document_keeps_a_byte_hash_and_counts_unreadable` |
| valid UTF-8 `# Résumé — ✓\n` | `ae9c3d6d…dbd1155` (independent literal) | DU-C01 `readable_document_hash_is_unchanged_by_the_byte_read` |
| `docs extract` over an undecodable document | the unchanged `failed to read: …` warning; nothing extracted | DU-C01 `extract_warns_on_an_undecodable_document` |
| a mode-000 `docs/NOTES.md` through `discover_doc_inventory` | admitted, counted, serialized `"content_hash":null` | DU-C01 `entry_with_unread_bytes_serializes_content_hash_null_with_the_key_present` |
| an undecodable entry serialized | `"content_hash": "<sha256 of bytes>"` | DU-C01 `entry_with_undecodable_bytes_serializes_its_byte_hash` |
| `DocEntry` JSON with a string / `null` / no key | `Some` / `None` / `missing field `content_hash`` | DU-C02 `doc_entry_decodes_a_string_content_hash`, `doc_entry_decodes_a_null_content_hash_as_none`, `doc_entry_rejects_a_missing_content_hash_key` |
| filtered `--json` view with a generated map, a hashed and an unhashed entry | the key re-emitted as `"h1"` and as `null` | DU-C02 `json_filtered_view_re_emits_content_hash_as_string_and_null` |
| poco / leveldb / the mode-000 corpus at the CLI | §2.3 | DU-C04 |

### 2.3 Outward proof
poco `docs list` (both modes): exit 2 (`error: failed to parse response: missing field `content_hash``) → `15 documents`, `+1 vendored doc (excluded; …)`, `+1 unreadable, counted (content unreadable — kind refinement unverifiable)`, `+5 markdown/prose files outside a docs tree not scanned (--json for the rule)`, `readme 11 / doc 4`, the row `packaging/README.txt  readme`; `--json` 16 entries, `unreadable: 1`, EVERY entry a string `content_hash` (the undecodable one = `dae0857da12cb0f9652108e2607ea6d1d1a2b2ae2b8d9b07188cee1fddf86993`, the sha256 of its bytes). leveldb: human bytes identical, JSON identical after parsing — the raw `--json` passthrough prints `counts_by_kind` in the daemon's `HashMap` order (dispatch.rs:5700-5713) and serde_json's `preserve_order` is on in any build of this source (unified from classification/indexer), so byte order is per-process with or without this slice (INFERRED from source — for the installed comparator an assumption, since its build is not attested; the check compares parsed values instead of pretending otherwise). The mode-000 corpus: the expected AFTER entry `{"path":"docs/NOTES.md","kind":"doc","generated":false,"content_hash":null}` — the packet's `NOTES.md` example placed under `docs/`, because a root `NOTES.md` is not a document under the ratified L01 rule (`notes` is not a stem and the root is not a doc tree), so it would never be listed.

## 3. Regression watch
| Preserved | What would regress | Proof |
|---|---|---|
| RG-REQ-008-L08 | a readable entry without a string hash; a missing key accepted; the key dropped from the filtered view | DU-C01 (serialization tests); DU-C02 (decode + view tests); DU-C04 (`all string` on poco, `null` on the corpus) |
| RG-REQ-008-L05 | an undecodable/unreadable document dropped, double-counted or crashing the command | DU-C01 (`unreadable_count == 2` with the sidecar); DU-C04 (poco and corpus `+1 unreadable, counted`) |
| RG-REQ-008-L01/L03/L04/L06 | discovery, generated, vendored, unscanned handling | DU-C01 (modules unchanged; discovery/self_generated tests by name); DU-C02 (rgr generated/vendored/unscanned tests; overlay tests; daemon-runtime unchanged); DU-C04 (poco vendored and unscanned lines, 16 entries) |
| P-DU-01 | a readable entry's hash; any output change on a repo with nothing unreadable | DU-C01; DU-C04 (leveldb) |
| P-DU-02 | daemon-runtime, storage, agent; the human render; `release_family` wire | DU-C01; DU-C02 |
| RG-REQ-011-L06, P-DU-03 | isolation, cleanup | DU-C03; DU-C05 |

## 4. Stop conditions
Frozen: daemon-runtime (`handle_docs_list`, `docs_list_overlay`), storage, agent; the inventory's admission/kind/generated/vendored/unscanned logic; `release_family` serialization; the human render. This slice changes only HOW `read_and_hash` reads and reports, whether `content_hash` is serialized when `None`, and how the CLI decodes it. The hash of a readable document must not move (a moved hash is a STOP). A missing `content_hash` key must stay a decode error (do not add `#[serde(default)]`). Nothing outside the four candidate paths. STANDING HONESTY RULES. If a poco literal differs from §2.3, STOP and report the file-level reason (which file moved which count) — never bend an oracle. Unmet DoD → STOP. Do NOT commit. Run every check to completion in the FOREGROUND; end the turn only with the evidence object.

## 5. Validation (ORDERED; `.agent-manager/slices/DOCS-UNREADABLE-DECODE-1/build-progress.md` after EACH step)
0. On the clean tree: record `git rev-parse HEAD` and that `git diff --stat v0.19.0 HEAD -- rust` is empty; `~/.local/bin/rmap --version` = `rmap 0.19.0` (the observed v0.19.0 comparator — its self-reported version is its only identity; the source that built it is not attested; not copied, not rebuilt); `registry-before <full sha256 of ~/Library/Application Support/repo-graph/registry.json>`; verify the two `-before` roots exist (§7).
1. Failing tests FIRST (the §2.2 names). 2. DU-C01 → DU-C02 → DU-C03 — NEVER `cargo test --workspace`. 3. `(cd rust && cargo build --release --bin rmap --bin rmapd)` (the candidate) → DU-C04 (same roots for both binaries; the builder root is created and removed by the check). 4. DU-C05; hand-off with the evidence object (each check's outcome NESTED under `outcome`; `supportingEvidence` non-empty).

## 6. Definition of done
All five checks pass; §2.3 holds; the report quotes `poco/packaging/README.txt`'s first line, the `file` verdict and the byte at offset 232; the BEFORE stderr line (both modes) and the AFTER header lines; the undecodable entry's `content_hash` beside the sha256 of its bytes and `README.md`'s hash beside its bytes' sha256; leveldb's identical `9 documents` header; the corpus's `docs/NOTES.md` JSON entry with `"content_hash": null`.

## 7. Corpus roots
- Manager-owned, pre-provisioned 2026-09-22T23:40Z by the manager with a v0.19.0-labelled binary (per the manager's record; the building source is not attested — `docs list` is a query-time walk, so the index's provenance does not enter the docs output): `/private/tmp/DOCS-UNREADABLE-DECODE-1-poco-before` (`/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/poco`), `/private/tmp/DOCS-UNREADABLE-DECODE-1-leveldb-before` (leveldb). Stdio transport, auto passes off; served by both binaries (query-time walks), never reindexed; they STAY.
- Builder-made, created and removed inside DU-C04: `/private/tmp/DOCS-UNREADABLE-DECODE-1-unreadable-after` (`state/`, `corpus/`).
- The retained roots under `~/repo-graph-retained/` are never served; the operator registry is read only for its digest.

## 8. Follow-ups (not this slice)
RC-9 / DOC-RELEVANCE-STEMS-1: the same poco walk classifies `NetSSL_OpenSSL/samples/download/doc/readme.txt` as `readme` (a sample's doc) — the rule as ratified. (DOCS-BYTES-UNREADABLE-1, filed by the PREP-2/PREP-3 cut, is ABSORBED: D-DU-CC10 specified the bytes-unreadable form and this cut implements it.)

## 9. Baseline history
- INPUT-1 (2026-09-23; revised before approval): first admission baseline under RG-BOOTSTRAP-INPUT-7. The first document review (codex gpt-5.6-terra) raised D-DU-01 — the draft called L08 implemented while omitting `content_hash` on purpose — and F-DU-01 (the before-binary `--json` invocation was not asserted). Resolved without a requirement change: the byte hash in doc-facts, both before-mode assertions added. The second document review (PREP-2) held that even a CLI tolerance for an absent hash contradicts L08 (CC-10, decision-required) — the CLI change was dropped (DOCS-BYTES-UNREADABLE-1 pending CC-10), leaving doc-facts only.
- PREP-3 (2026-09-23, still INPUT-1, before approval): corrected the hash form (full 64-hex sha256), the license claim (NAME-only), stated the sidecar-name guard (counted once), pinned the `docs extract` warning text, named the `orient` consumer, completed the L01/L03/L06 and P-DU mappings, quoted the code-under-analysis lines. The review blocked decision-required on CC-10; the manager parked the slice for the human.
- PREP-4 (2026-09-23, INPUT-1 revised under RG-BOOTSTRAP-INPUT-9): CC-10 answered by D-DU-CC10 (human, option A) → RG-REQ-008-L08 moves from preserves to implements; the producer serialization (doc-facts) and the CLI decode (rgr `DocEntry.content_hash: Option<String>`, 11 literal constructions in docs_tests.rs) join the byte-read cut; D-DU-CC10 is governance and the one `requiredDecisionIds`; the audit root-cause record is no longer pinned (evidence records are not pinned — operator rule 2026-09-23); documentation facts are computed at query time, so no toolchain version changes. Corrections the PREP-4 author verified against source and applied: the code citations (`content_hash` skip at lib.rs:71-72, not :17-18; `release_family` skip :80-81, not :26; `read_and_hash` :484-496); the literal count (docs.rs has no `DocEntry` construction — :188 is the struct definition; docs_tests.rs has 11 constructions on 13 matching lines); a MISSING `Option` field decodes as `None` under serde's derive, so the missing-key refusal needs a `deserialize_with` decoder and the grep forbids only `default`/`skip_serializing_if`; the filtered `--json` view re-emission test L08 names was missing and is added; DU-C02's rgr-diff literal used cwd-relative paths while `git diff --name-only` prints repository-relative ones (now `--relative`); DU-C01's vacuous `! grep … | awk | grep` clause removed and a positive field-presence grep added; DU-C04 rebuilt — the before side is the installed v0.19.0 executable (its sibling `rmapd` serves stdio; no copied before-bin), the mode-000 document moved to `docs/NOTES.md` (a root `NOTES.md` is not discovered), `chmod 000` moved after `git add`/commit/index (git cannot add an unreadable file), the python heredoc (whose terminator line `PY && …` never terminated) replaced by one trailing python assertion block, leveldb `--json` compared parsed (per-process `HashMap` key order), every exit code and stderr asserted, the builder root removed by an EXIT trap; DU-C05 allows the four candidate paths and asserts the builder root is gone instead of a before-bin that no longer exists; §0/§2/§3/§4/§8 and P-DU-02 rewritten for the two-sided cut.
- PREP-4 iteration 1 (2026-09-24, INPUT-1 unapproved): review finding F-DU-BASELINE-PROVENANCE-1 (traceability) — the draft inferred that the installed binary IS the v0.19.0 source baseline from `rmap --version` and an empty `git diff v0.19.0 HEAD -- rust`. Class: a claim about the SOURCE that built an executable, drawn from a version label or a source-tree comparison. Rule applied to every instance (the acceptance boundary, DU-C04 environment/inputs/python message, P-DU-01, §2.3, §5 step 0, §7, §9, and the manager's before-roots): the installed executable is the OBSERVED v0.19.0 COMPARATOR, identified only by its self-reported version; the source that built it (and so its Cargo feature set) is not attested; a source-tree diff speaks only of source trees. The before assertions stay behavioural. P-DU-01 rests on independent oracles (per-file sha256, the unit-test literal), and a comparator/candidate mismatch on leveldb is a STOP reported as UNATTRIBUTED; attributing it needs an attested worktree baseline, which is the operator's call. No check command changed.
