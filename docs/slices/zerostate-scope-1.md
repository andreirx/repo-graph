# ZEROSTATE-SCOPE-1 — no repo wears another repo's gap sentence

Status: SPECIFIED (2026-09-02) · Track: v0.13.0 queue #3 (honesty-class per the second
opinion: false contextualization). CODE slice, presentation. Maturity: MATURE.

## 1. Problem (measured — audit 2026-09-01T09-06-40Z + diverse run)

1. The surfaces detector-roster zero-state is a STATIC blob: leveldb — a pure C++ repo —
   renders "no HTTP detector for Django URLconf routes." The roster line was built for
   django and pasted everywhere.
2. boundaries-list/summary zero-states still say "No recognized boundary patterns found in
   this codebase" — the blames-the-codebase form every other surface has shed.
3. cycles' TS import-type caveat gates on DOMINANT language: repo-graph's only production
   cycle is TypeScript (tools/rgistr) yet gets no caveat. The fact is any-TS-in-cycle.

## 2. Contract

1. **The gap clause is per-repo computed**: the roster sentence keeps the build's detector
   families (the dispatch-anchored const, MODULES-IDENTITY-2), but the "no detector for X"
   clause names ONLY materially-present frameworks/languages of THIS repo (the existing
   materiality gate + language facts). leveldb says its C/C++ truth; django keeps URLconf;
   no repo wears another's sentence. Zero materially-present uncovered candidates → the
   clause is omitted (the roster + "other surface kinds may exist without detectors"
   scoping stays).
2. **boundaries zero-states adopt the coverage form**: "no boundary patterns detected —
   detection on this build covers <the real detector families>; <per-repo gap clause>" —
   never "in this codebase" blame. Same source as surfaces (no second roster).
3. **cycles TS caveat gates on any-TS-membership**: the caveat renders iff ≥1 member file
   of any rendered cycle is TypeScript/JavaScript (the stored language fact of member
   files — reachable on the SQLite route; on routes where member languages are not
   carried, keep the current repo-level materiality gate and state nothing new). repo-graph
   gets its caveat.
4. JSON additive; exit codes unchanged.

## 3. Stop conditions

Frozen: detection itself, storage schema, exit codes, trust, cycle computation. STANDING
HONESTY RULES. New public APIs beyond additive DTO fields → DECISION_REQUIRED (read-only
accessor precedent citable). Unmet DoD → STOP + DECISION_REQUIRED. Never touch the
operator's real state root. Do NOT commit.

## 4. Validation (SYNCHRONOUS; INCREMENTAL REPORT — binding)

- Unit: per-repo gap clause across language mixes (incl. none-uncovered → omitted);
  boundaries coverage form; any-TS cycle caveat (incl. the dominant-Rust +
  one-TS-cycle repo-graph shape).
- Live proof (isolated state root, registry sha unchanged): leveldb surfaces/boundaries
  zero-states carry C/C++-scoped truth with NO Django sentence; django keeps URLconf;
  repo-graph cycles carries the TS caveat. Captures.
- Chunked cargo gates; consolidation witness; dogfood-isolated green.

## 5. Definition of done

Every gap sentence is about the repo it renders on; boundaries stops blaming codebases;
the TS caveat follows cycle membership; one roster source; gates green.
