<!-- requirements-assurance-implementation-v1
{
  "formatVersion": 1,
  "kind": "implementation-allocation",
  "workItemId": "DOCS-DISCOVERY-1",
  "baselinePath": "docs/requirements/baselines/DOCS-DISCOVERY-1-INPUT-3.json",
  "parentRequirementIds": [
    "RG-REQ-003",
    "RG-REQ-008",
    "RG-REQ-011"
  ],
  "implements": [
    "RG-REQ-008-L01",
    "RG-REQ-008-L02",
    "RG-REQ-008-L06",
    "RG-REQ-008-L07",
    "RG-REQ-003-L09"
  ],
  "preserves": [
    "RG-REQ-008-L03",
    "RG-REQ-008-L04",
    "RG-REQ-008-L05",
    "RG-REQ-008-L08",
    "RG-REQ-008-L09",
    "RG-REQ-011-L06",
    "RG-REQ-011-L07"
  ],
  "preservationObligationIds": [
    "P-DD-01",
    "P-DD-02",
    "P-DD-03",
    "P-DD-04"
  ],
  "changes": [],
  "acceptanceBoundary": "The `docs list` (human and --json), `modules list` and `orient` outputs of the candidate rmap against the before binary on eight pre-provisioned isolated `-before` roots (hadoop, django, buildroot, kafka, leveldb, grpc-java, FRAKTAG, repo-graph itself — the docs surfaces are query-time walks, so the same root serves both binaries), plus cargo test on repo-graph-doc-facts (whole), repo-graph-daemon-runtime (--lib modules_method; the whole --lib, which carries docs_list_overlay) and repo-graph-rgr (--lib whole; docs_command), as named per check.",
  "candidatePaths": [
    "rust/crates/doc-facts/src/discovery.rs",
    "rust/crates/doc-facts/src/classification.rs",
    "rust/crates/doc-facts/src/lib.rs",
    "rust/crates/doc-facts/src/lib_tests.rs",
    "rust/crates/daemon-runtime/src/modules_method.rs",
    "rust/crates/daemon-runtime/src/orient_additive_fields.rs",
    "rust/crates/daemon-runtime/src/dispatch.rs",
    "rust/crates/rgr/src/presentation/docs.rs",
    "rust/crates/rgr/src/presentation/docs_tests.rs",
    "rust/crates/rgr/src/presentation/orient_tests.rs"
  ],
  "postReviewRecordPaths": [
    "docs/assurance/DOCS-DISCOVERY-1/verification.json",
    "docs/assurance/DOCS-DISCOVERY-1/implementation-review.json"
  ],
  "candidateExclusions": [
    {
      "pathPrefix": ".agent-manager/",
      "reason": "local relay state and raw run/progress evidence"
    },
    {
      "pathPrefix": "rust/target/",
      "reason": "reproducible Cargo build output"
    }
  ],
  "checks": [
    {
      "checkId": "DD-C01",
      "obligationIds": [
        "RG-REQ-008-L01",
        "RG-REQ-011-L07"
      ],
      "owner": "builder",
      "method": {
        "kind": "command",
        "command": "cargo test -p repo-graph-doc-facts --lib discovery 2>&1 | tee /tmp/dd-c01.txt | grep -E '^test result: ok\\.' && ! grep -E '^test .* FAILED$|^thread .* panicked at|^test result: FAILED' /tmp/dd-c01.txt && for t in stem_rule_admits_documentation_stems_with_or_without_a_doc_extension stem_with_a_non_doc_extension_is_refused stem_and_extension_match_case_insensitively adoc_and_markdown_admitted_inside_a_docs_tree src_site_convention_opens_a_docs_tree unscanned_counts_markup_outside_a_docs_tree_never_txt unscanned_count_skips_the_rmap_tool_state_dir orientation_stems_are_doc_name_stems compose_files_still_admitted_by_exact_name discovers_readme_at_root discovers_docker_compose env_files_still_candidates_for_extraction admits_txt_and_rst_under_docs_tree discovers_map_md_in_subdirectory skips_node_modules discovers_docs_directory_markdown; do grep -qE \"^test .*$t .* ok$\" /tmp/dd-c01.txt || { echo \"MISSING $t\"; exit 1; }; done && grep -qE '^pub const DOC_NAME_STEMS' crates/doc-facts/src/discovery.rs && grep -qE '^pub const ORIENTATION_STEMS' crates/doc-facts/src/discovery.rs && grep -qE '^pub const DOC_EXTENSIONS' crates/doc-facts/src/discovery.rs && ! grep -qE '\"ARCHITECTURE\\.md\",|\"CONTRIBUTING\\.md\",|\"CHANGELOG\\.md\",' crates/doc-facts/src/discovery.rs",
        "cwd": "rust",
        "environment": "candidate tree",
        "inputs": "rust/crates/doc-facts/src/discovery.rs: ONE shared stem constant `pub const DOC_NAME_STEMS: &[&str] = &[\"readme\", \"contributing\", \"changelog\", \"architecture\", \"design\", \"overview\", \"install\", \"building\", \"authors\", \"news\"]` (the ratified set, RG-REQ-008-L01), `pub const ORIENTATION_STEMS: &[&str] = &[\"architecture\", \"design\", \"overview\", \"contributing\"]` (the L07 subset; the test `orientation_stems_are_doc_name_stems` asserts every orientation stem is a doc-name stem), `DOC_EXTENSIONS` made `pub` and extended to `[\".md\", \".markdown\", \".txt\", \".rst\", \".adoc\"]` (:102 today `[\".md\", \".txt\", \".rst\"]`); `pub fn doc_name_stem(file_name: &str) -> Option<&'static str>`: lowercase the name, strip ONE doc extension if the lowercased name ends with it (D-DD1-003: case-insensitive on both — `README.TXT`, `README.MD`, `Readme.md` are documents), return the `DOC_NAME_STEMS` entry the remainder equals (so `README`, `readme.txt`, `README.TXT`, `INSTALL`, `BUILDING.txt`, `ChangeLog` match; `README.html`, `install.sh`, `readme.txt.bak` do not — their remainder is the whole lowercased name); the docs-tree branch recognises `DOC_EXTENSIONS` case-insensitively the same way; `in_docs_tree` (:113-117) additionally opens on the `src/site/**` convention — a component `site` whose IMMEDIATE parent component is `src`, ancestors only (`x/src/site/markdown/a.md` yes; `site/a.md`, `src/a.md`, `srcx/site/a.md` no); `is_doc_candidate` (:126-162) becomes: `.env*` (unchanged) → the four compose names (`docker-compose.yml`, `docker-compose.yaml`, `compose.yml`, `compose.yaml` — the five exact doc names `README.md`, `README`, `ARCHITECTURE.md`, `CONTRIBUTING.md`, `CHANGELOG.md` are DELETED from the inline `known_docs` array (:134-144), subsumed by the stem rule — the negated grep) → MAP sidecar (unchanged) → `doc_name_stem(file_name).is_some()` → a `DOC_EXTENSIONS` name inside a docs tree. The walk (`discover_recursive` :43-97) additionally COUNTS every file it visits whose lowercased name ends with a MARKUP extension `.md`, `.markdown`, `.rst`, `.adoc` and that `is_doc_candidate` refused — EXCEPT a file under the repository-root `.rgr/` tool-state directory, decided by the existing `crate::self_generated::is_tool_state_path` (the ONE exhaust predicate, never a second definition — RG-REQ-011-L07: rmap's own state is never reported as the authors' refused prose; no corpus carries a `.rgr/` today, so no field number moves) (`.txt` is deliberately NOT counted — D-DD1-002: a `.txt` outside a docs tree is not known to be prose: `CMakeLists.txt`, `requirements.txt`, `robots.txt`); `discover_doc_files` returns `pub struct DocDiscovery { pub files: Vec<DocFile>, pub unscanned_markup_outside_docs_tree: usize }` (its two callers lib.rs:151 and :331 read `.files`). SKIP_DIRS, MAX_DEPTH, symlink handling, hidden-directory handling, `.gitignore` (not honored — pre-existing, unchanged) untouched. Nine NEW tests (names in the command; `unscanned_count_skips_the_rmap_tool_state_dir`: root `notes.md` and `.rgr/notes.md` → count 1) beside the SEVEN existing ones (all kept verbatim; their `discover_doc_files(..)` calls read `.files` — compiler-forced)"
      },
      "expected": "exit 0: the discovery rule is the ratified one — one stem list, one extension list, one doc-tree rule with the `src/site` convention — expressed once, with its refusals counted; the compose, MAP-sidecar, `.env` and skip behaviours remain exactly as before (no behavior change to RG-REQ-011-L07's exhaust exclusion)"
    },
    {
      "checkId": "DD-C02",
      "obligationIds": [
        "RG-REQ-008-L02"
      ],
      "owner": "builder",
      "method": {
        "kind": "command",
        "command": "cargo test -p repo-graph-doc-facts --lib classification 2>&1 | tee /tmp/dd-c02.txt | grep -E '^test result: ok\\.' && ! grep -E '^test .* FAILED$|^thread .* panicked at|^test result: FAILED' /tmp/dd-c02.txt && for t in readme_kind_by_stem_for_rst_txt_and_bare bare_stem_documents_classify_doc_not_config markdown_and_adoc_in_a_docs_tree_classify_doc config_extension_beats_docs_directory plain_prose_outside_docs_tree_is_neutral_doc_not_architecture is_license_document_by_name_only classify_readme classify_architecture architecture_dir_rule_matches_path_components_not_substrings classify_txt_and_rst_as_docs; do grep -qE \"^test .*$t .* ok$\" /tmp/dd-c02.txt || { echo \"MISSING $t\"; exit 1; }; done && ! grep -qE 'file_name == \"readme\\.md\" \\|\\| file_name == \"readme\"' crates/doc-facts/src/classification.rs",
        "cwd": "rust",
        "environment": "candidate tree",
        "inputs": "rust/crates/doc-facts/src/classification.rs `classify_doc_kind` (:36-135): the `readme` rule (:49-52, `file_name == \"readme.md\" || file_name == \"readme\"` — the negated grep proves it is gone) becomes `doc_name_stem(<the ORIGINAL basename, before the lowercasing at :37>) == Some(\"readme\")` so `README.rst`, `README.txt`, `readme.txt`, `README` classify `readme`; the neutral-doc rule (:129, `ends_with(\".md\") || ends_with(\".rst\") || ends_with(\".txt\")` — the third independent extension spelling) reads the shared `DOC_EXTENSIONS` so `.markdown`/`.adoc` prose classifies `doc`; a stem-admitted file with NO extension (`INSTALL`, `AUTHORS`, `NEWS`, `CHANGELOG`, `ChangeLog`) classifies `doc`, not the `Config` default (:133-134) — inserted after the neutral-doc rule, before the default; the architecture name rule (:65-70), the config rules (:86-92), the architecture directory rule (:112-122), `is_license_document` (:24-33) and the release-notes path (lib.rs:234-254) are UNTOUCHED. Three NEW tests beside the existing ones named in the command (kept verbatim)"
      },
      "expected": "exit 0: kinds are decided by the ratified name and directory rules — readme by stem, bare documentation stems as `doc` — with the config-precedence, architecture, license and release-notes bases exactly as before (no behavior change to them)"
    },
    {
      "checkId": "DD-C03",
      "obligationIds": [
        "RG-REQ-008-L06",
        "RG-REQ-008-L03",
        "RG-REQ-008-L04",
        "RG-REQ-008-L05",
        "RG-REQ-008-L09",
        "RG-REQ-011-L07",
        "P-DD-01"
      ],
      "owner": "builder",
      "method": {
        "kind": "command",
        "command": "cargo test -p repo-graph-doc-facts  2>&1 | tee /tmp/dd-c03.txt | grep -E '^test result: ok\\.' && ! grep -E '^test .* FAILED$|^thread .* panicked at|^test result: FAILED' /tmp/dd-c03.txt && for t in inventory_reports_unscanned_markup_outside_docs_tree env_files_excluded_from_inventory inventory_flags_generated_only_with_marker inventory_unreadable_sidecar_is_admitted_and_counted_never_asserted_generated inventory_license_is_name_only_content_body_is_not_upgraded inventory_release_notes_need_an_inspected_toctree_manifest_not_just_the_dir_name extract_from_repo_with_readme extract_handles_unreadable_files marker_confirms_generated markerless_user_map_is_not_self_generated; do grep -qE \"^test .*$t .* ok$\" /tmp/dd-c03.txt || { echo \"MISSING $t\"; exit 1; }; done && git diff --quiet HEAD -- crates/doc-facts/src/self_generated.rs crates/doc-facts/src/release_notes.rs crates/doc-facts/src/types.rs && grep -qE '^pub struct DiscoveryRule' crates/doc-facts/src/discovery.rs && grep -qE '^pub fn discovery_rule\\(' crates/doc-facts/src/discovery.rs && ! grep -qE '^pub struct DiscoveryRule|^pub fn discovery_rule\\(' crates/doc-facts/src/lib.rs",
        "cwd": "rust",
        "environment": "candidate tree",
        "inputs": "the WHOLE repo-graph-doc-facts crate (discovery, classification, lib_tests, self_generated, release_notes). rust/crates/doc-facts/src/lib.rs: `discover_doc_inventory` (:137) reads `discovery::discover_doc_files(repo_path)` as `DocDiscovery` (`.files` at :151, the `.env` filter unchanged) and `DocInventoryResult` (:82-107) gains `pub unscanned_markup_outside_docs_tree: usize` (the `unreadable_count` precedent at :106; the ONE literal at :305-310 gains the field; `extract_semantic_facts` (:331) reads `.files`). rust/crates/doc-facts/src/discovery.rs (NOT lib.rs: lib.rs is 504 lines on HEAD and CLAUDE.md's structural guardrail forbids appending a new responsibility to a file over 500 lines — lib.rs gains only the field, the two `.files` reads and one `pub use discovery::{…}` re-export of the constants, `doc_name_stem`, `DiscoveryRule` and `discovery_rule`; the positive and negated greps): `pub struct DiscoveryRule` (serde::Serialize; fields `stems`, `extensions`, `doc_tree_dirs`, `doc_tree_conventions` = `[\"src/site\"]`, `unscanned_extensions` = `[\".md\", \".markdown\", \".rst\", \".adoc\"]` — all `&'static [&'static str]`, and every field IS the constant the walk applies (`DOC_NAME_STEMS`, `DOC_EXTENSIONS`, `DOC_TREE_DIRS`, a `DOC_TREE_CONVENTIONS` that `in_docs_tree` reads, the unscanned-markup extension constant that the count reads — the first admission's candidate names them `DOC_TREE_CONVENTIONS` and `UNSCANNED_MARKUP_EXTENSIONS`, already in this shape) — the rule the JSON states is the rule that ran, never a re-typed copy) and `pub fn discovery_rule() -> DiscoveryRule` state the rule the JSON carries (RG-REQ-008-L06 `(--json for the rule)`). NEW test `inventory_reports_unscanned_markup_outside_docs_tree` in lib_tests.rs: a temp repo with `README.md`, `notes.md` (root, refused), `CMakeLists.txt` (root, not counted), `docs/a.md` → count 1 and `entries` = README.md + docs/a.md. Byte-identity of self_generated.rs, release_notes.rs, types.rs (the diff --quiet)"
      },
      "expected": "exit 0: the inventory carries how many markup files the rule refused and the rule itself, while generated/vendored/unreadable handling, the `.env` exclusion, the exhaust predicate, the license and release-notes bases and every existing doc-facts test remain exactly as before (no behavior change outside the discovery rule and the new count)"
    },
    {
      "checkId": "DD-C04",
      "obligationIds": [
        "RG-REQ-008-L07",
        "RG-REQ-003-L09"
      ],
      "owner": "builder",
      "method": {
        "kind": "command",
        "command": "cargo test -p repo-graph-daemon-runtime --lib modules_method 2>&1 | tee /tmp/dd-c04.txt | grep -E '^test result: ok\\.' && ! grep -E '^test .* FAILED$|^thread .* panicked at|^test result: FAILED' /tmp/dd-c04.txt && for t in orientation_named_files_match_by_stem_with_any_doc_extension orientation_named_files_root_and_docs_level orientation_root_readme_included orientation_nested_readme_excluded orientation_multiple_docs_capped_at_four orientation_root_first_ranking orientation_never_recommends_arbitrary_docs_files orientation_singular_doc_dir_not_a_target orientation_vendored_under_docs_excluded orientation_recommendation_lines_differ_across_repos; do grep -qE \"^test .*$t .* ok$\" /tmp/dd-c04.txt || { echo \"MISSING $t\"; exit 1; }; done && ! grep -qE '\"architecture\" \\| \"design\" \\| \"overview\" \\| \"contributing\"' crates/daemon-runtime/src/modules_method.rs && grep -q 'ORIENTATION_STEMS' crates/daemon-runtime/src/modules_method.rs && ! grep -q 'discover_doc_inventory(..., false)' crates/daemon-runtime/src/orient_additive_fields.rs",
        "cwd": "rust",
        "environment": "candidate tree",
        "inputs": "rust/crates/daemon-runtime/src/modules_method.rs `is_named_orientation_file` (:439-456): the re-spelled inline stem list `\"architecture\" | \"design\" | \"overview\" | \"contributing\"` (:445-448 — the negated grep proves it is gone) becomes `repo_graph_doc_facts::doc_name_stem(<basename>).is_some_and(|s| repo_graph_doc_facts::ORIENTATION_STEMS.contains(&s))` (daemon-runtime already depends on repo-graph-doc-facts, Cargo.toml:47 — no new edge); the depth rule (:450-455: root, or exactly one level under `docs/`/`design/`) and `select_orientation_docs` (:381-430: root README by kind, root-first then path-ASC ranking, cap 4, vendored/generated excluded, `docs/`/`design/` directory targets) are UNTOUCHED. NEW test `orientation_named_files_match_by_stem_with_any_doc_extension`: root `CONTRIBUTING.rst`, `docs/ARCHITECTURE.adoc`, `design/overview.txt` are named targets; root `INSTALL` and `AUTHORS` (doc-name stems, not orientation stems) are not; `docs/deep/ARCHITECTURE.md` is not (depth). The stale comment at orient_additive_fields.rs:165 (`get_doc_inventory` uses `discover_doc_inventory(..., false)` — the real call storage/src/agent_orient_reads.rs:90 passes `true`) is corrected to `true` (comment-only; the negated grep). Existing orientation tests named in the command kept verbatim"
      },
      "expected": "exit 0: the recommendation's named-file rule reads the same stem constant as discovery and classification (one spelling), so a root `README.rst`/`CONTRIBUTING.rst` or a `docs/ARCHITECTURE.adoc` is recommended; ranking, cap and directory targets remain exactly as before (no behavior change to them)"
    },
    {
      "checkId": "DD-C05",
      "obligationIds": [
        "RG-REQ-008-L06",
        "RG-REQ-008-L08",
        "RG-REQ-008-L07",
        "RG-REQ-003-L09",
        "P-DD-02"
      ],
      "owner": "builder",
      "method": {
        "kind": "command",
        "command": "cargo test -p repo-graph-rgr --lib presentation::docs 2>&1 | tee /tmp/dd-c05.txt | grep -E '^test result: ok\\.' && ! grep -E '^test .* FAILED$|^thread .* panicked at|^test result: FAILED' /tmp/dd-c05.txt && for t in list_render_states_unscanned_markup_outside_docs_tree list_render_omits_the_unscanned_line_when_nothing_was_refused json_filtered_view_surfaces_unscanned_and_rule_when_present malformed_unscanned_payload_is_rejected_never_rendered raw_json_path_prints_only_payloads_whose_count_carries_the_rule list_default_excludes_generated_maps_and_states_the_count vendored_demoted_from_headline_and_listing_but_stated list_render_surfaces_unreadable_count entry_list_budgeted_with_remainder family_lines_and_entries_share_one_budget json_no_generated_maps_is_raw_passthrough json_filtered_view_surfaces_unreadable_when_present; do grep -qE \"^test .*$t .* ok$\" /tmp/dd-c05.txt || { echo \"MISSING $t\"; exit 1; }; done && cargo test -p repo-graph-rgr --lib presentation::orient::tests 2>&1 | tee /tmp/dd-c05b.txt | grep -E '^test result: ok\\.' && ! grep -E '^test .* FAILED$|^thread .* panicked at|^test result: FAILED' /tmp/dd-c05b.txt && for t in orient_renders_the_docs_recommendation_line orient_method_not_recorded_renders_canonical_sentence; do grep -qE \"^test .*$t .* ok$\" /tmp/dd-c05b.txt || { echo \"MISSING $t\"; exit 1; }; done && cargo test -p repo-graph-rgr --lib presentation::modules_list 2>&1 | tee /tmp/dd-c05c.txt | grep -E '^test result: ok\\.' && ! grep -E '^test .* FAILED$|^thread .* panicked at|^test result: FAILED' /tmp/dd-c05c.txt && for t in orientation_recommendation_renders orientation_no_docs_recommendation_renders unavailable_orientation_docs_renders_reason inequality_across_fixtures; do grep -qE \"^test .*$t .* ok$\" /tmp/dd-c05c.txt || { echo \"MISSING $t\"; exit 1; }; done && awk '/fn handle_docs_list\\(/,/^    }$/' crates/daemon-runtime/src/dispatch.rs > /tmp/dd-c05-handler.txt && grep -q 'payload\\[\"discovery_rule\"\\]' /tmp/dd-c05-handler.txt && grep -q 'payload\\[\"unscanned_markup_outside_docs_tree\"\\]' /tmp/dd-c05-handler.txt && ! grep -qE 'unwrap_or\\(|unwrap_or_default|\\.ok\\(\\)' /tmp/dd-c05-handler.txt",
        "cwd": "rust",
        "environment": "candidate tree",
        "inputs": "rust/crates/rgr/src/presentation/docs.rs: `DocsListResponse` (:39-55) gains `pub unscanned_markup_outside_docs_tree: usize` and `pub discovery_rule: Option<DiscoveryRuleView>` — a TYPED view (Serialize + Deserialize; the five fields `stems`, `extensions`, `doc_tree_dirs`, `doc_tree_conventions`, `unscanned_extensions` as `Vec<String>`), never an untyped `serde_json::Value` (the seven test literals in docs_tests.rs :12/:51/:146/:166/:190/:277/:335 gain them — compiler-forced). THE COUNT AND THE RULE ARE ONE FACT, validated at the ONE decode both modes pass through (review of this document, F-RG-REQ-008-L06-RAW-JSON-RULE): `rgr/src/commands/docs.rs:163` runs `serde_json::from_value::<DocsListResponse>` BEFORE branching into `--json` or human mode, and on `Err` prints `error: failed to parse response: …` and exits `EXIT_RUNTIME_ERROR` (:184-186) — so the invariant lives in the decode itself (e.g. `#[serde(try_from = …)]` from a private wire struct whose count is `Option<usize>` and rule `Option<DiscoveryRuleView>`): a payload decodes only when BOTH keys are absent (count 0, rule `None` — every pre-slice and nothing-refused payload) or BOTH are present with the count > 0 and the rule a well-formed object; a positive count with the rule missing, `null` or ill-shaped, a rule without a count, or an explicit count of 0 fails with a named malformed-payload error. Neither the human line nor the raw `--json` passthrough (`filtered_json_view` → `None` → `unwrap_or(result)`, :166-168) can then ever show a count without its rule; commands/docs.rs stays byte-identical (DD-C06) — it already fails closed on a decode error; `render_human` (:172) prints, AFTER the unreadable line (:230-235) and BEFORE the empty-headline early return (:237), the ratified line `+N markdown/prose files outside a docs tree not scanned (--json for the rule)` when the count is > 0 (`file`/`files` by count, the vendored line's convention; absent when 0); `filtered_json_view` (:116-152) re-emits `unscanned_markup_outside_docs_tree` and `discovery_rule` when the count is > 0 (the `unreadable` precedent at :148-150) so the filtered `--json` never drops them — the decoded `discovery_rule` re-serialized, present whenever the count is (the decode guarantees it; there is no `null` case). hadoop's 6,813 generated MAP sidecars (the docs list line at the second admission; OC-2 corrected the figure) make its `--json` the FILTERED view, so DD-C08 exercises this path on a real corpus. rust/crates/daemon-runtime/src/dispatch.rs `handle_docs_list` payload (:5707-5723): when `inventory.unscanned_markup_outside_docs_tree > 0`, `payload[\"unscanned_markup_outside_docs_tree\"]` and `payload[\"discovery_rule\"]` (= `serde_json::to_value(repo_graph_doc_facts::discovery_rule())`) are added — gated together like `unreadable` (:5721-5723) so a repo with nothing refused keeps its byte-identical payload (D-DD1-002); the rule value is built INFALLIBLY (F-DD1-001, first admission: `serde_json::to_value(discovery_rule()).unwrap_or(Value::Null)` silently emitted `null` on a serialization error — a `DiscoveryRule` of static string slices cannot fail to serialize, so the construction is either `serde_json::json!({...})` over the constants or `to_value(..).expect(\"DiscoveryRule serializes: static string slices only\")` — NEVER a silent fallback (STANDING HONESTY RULE). The check closes the CLASS, not the reported line: it extracts `handle_docs_list` (:5626 to its closing brace :5725, awk) and requires both payload keys present and NO silent-fallback form (`unwrap_or(`, `unwrap_or_default`, `.ok()`) anywhere in the handler — HEAD's handler has none (its one `unwrap_or_else` is the pre-existing root-path join); a draft `grep -A3` after the assignment passed vacuously when the key was built elsewhere or the fallback sat on an earlier line. The three pre-existing `unwrap_or(serde_json::Value::Null)` at dispatch.rs:4164/:5599/:9066 lie outside the handler and are out of scope). rgr/src/presentation/orient_tests.rs is mounted as `presentation::orient::tests` (orient.rs:569-570 `#[path]`), hence the cargo filter (OC-1 — the first admission's filter `presentation::orient_tests` selected zero tests). NEW tests: `list_render_states_unscanned_markup_outside_docs_tree` (count 3 → the literal plural line; count 1 → `+1 markdown/prose file outside a docs tree not scanned (--json for the rule)`), `list_render_omits_the_unscanned_line_when_nothing_was_refused`, `json_filtered_view_surfaces_unscanned_and_rule_when_present`; `malformed_unscanned_payload_is_rejected_never_rendered` (`serde_json::from_value::<DocsListResponse>` is `Err` for: count 3 with no rule, count 3 with `discovery_rule: null`, count 3 with a rule missing `stems`, a rule with no count, an explicit count 0 — and `Ok` with count 0/rule `None` when both keys are absent); `raw_json_path_prints_only_payloads_whose_count_carries_the_rule` (a payload with count 2, a well-formed rule and NO generated maps decodes, `filtered_json_view` returns `None` — the raw passthrough the command prints — and that payload carries the rule object beside the count; the same payload with the rule removed does not decode, so the command prints the error, never the payload); rust/crates/rgr/src/presentation/orient_tests.rs NEW `orient_renders_the_docs_recommendation_line` (`nginx_like()` with `orientation_docs = Some(json!({\"paths\": [\"README.md\", \"docs/\"], \"recommendation\": \"For module boundaries as the authors describe them, read: README.md, docs/ — or the tree.\"}))` → `render_human(OrientDepth::Full)` contains the line — the orient-side assertion RG-REQ-003-L09's criterion names as missing). Existing tests named in the command kept verbatim; the modules_list orientation tests untouched"
      },
      "expected": "exit 0: `docs list` states what the rule refused and `--json` carries the count and the rule — on the filtered AND the raw-passthrough path, because a payload whose count and rule are not present together does not decode and is never rendered in either mode; `orient` is proven to render the recommendation; budget, JSON completeness, generated/vendored/unreadable lines and the modules-list rendering remain exactly as before (no behavior change to them)"
    },
    {
      "checkId": "DD-C06",
      "obligationIds": [
        "P-DD-01",
        "P-DD-02",
        "P-DD-03",
        "RG-REQ-008-L08",
        "RG-REQ-008-L09"
      ],
      "owner": "builder",
      "method": {
        "kind": "command",
        "command": "cargo test -p repo-graph-rgr --lib 2>&1 | tee /tmp/dd-c06.txt | grep -E '^test result: ok\\.' && ! grep -E '^test .* FAILED$|^thread .* panicked at|^test result: FAILED' /tmp/dd-c06.txt && cargo test -p repo-graph-daemon-runtime --lib 2>&1 | tee /tmp/dd-c06b.txt | grep -E '^test result: ok\\.' && ! grep -E '^test .* FAILED$|^thread .* panicked at|^test result: FAILED' /tmp/dd-c06b.txt && for t in vendored_override_clears_release_family; do grep -qE \"^test .*$t .* ok$\" /tmp/dd-c06b.txt || { echo \"MISSING $t\"; exit 1; }; done && cargo test -p repo-graph-rgr --test docs_command 2>&1 | tee /tmp/dd-c06d.txt | grep -E '^test result: ok\\.' && ! grep -E '^test .* FAILED$|^thread .* panicked at|^test result: FAILED' /tmp/dd-c06d.txt && for t in docs_list_daemon_required; do grep -qE \"^test .*$t .* ok$\" /tmp/dd-c06d.txt || { echo \"MISSING $t\"; exit 1; }; done && git diff --quiet HEAD -- crates/agent crates/storage crates/classification crates/indexer crates/daemon-runtime/src/docs_list_overlay.rs crates/daemon-runtime/src/handlers crates/rgr/src/commands crates/rgr/src/presentation/modules_list.rs crates/rgr/src/presentation/orient_sections.rs",
        "cwd": "rust",
        "environment": "candidate tree",
        "inputs": "the acceptance boundary: the whole rgr lib; daemon-runtime's WHOLE `--lib` suite (CLAUDE.md relay gate — the whole `--lib` unit suite of every daemon crate the change touches: modules_method.rs, orient_additive_fields.rs and dispatch.rs are daemon-runtime; the first admission ran it outside the packet, `785 passed; 1 ignored`), which carries `vendored_override_clears_release_family`; rgr's `docs_command` CLI tests (`rgr/tests/cli_out_5_inventory.rs`'s `docs_list_*`/`docs_extract_*` are `#[ignore]` — daemon-backed; verified by the manager on HEAD: `0 passed; 10 ignored` — they are the OPERATOR's dogfood gate, not a builder check); and the CODE byte-identity of every path the slice must NOT touch (byte-identity of code, not of output: each of these surfaces reads the widened inventory — see expected) — the agent crate (`agent/src/doc_relevance.rs:273-279` keeps its own root-doc name list: a FIFTH spelling, out of scope — DOC-RELEVANCE-STEMS-1, §8 — because agent has no doc-facts dependency and a new crate edge is not this slice's), storage (`agent_orient_reads.rs` — it calls `discover_doc_inventory` unchanged), classification, indexer, the vendored overlay, the quality handlers, rgr commands, the modules_list and orient_sections renderers"
      },
      "expected": "exit 0: every existing test in the touched crates stays green, and the code of every inheriting surface (`docs extract`, the vendored overlay, storage's inventory read, orient's doc relevance, the modules-list and orient renderers) remains byte-identical with its rendering contract unchanged (no behavior change to their code or rendering form) — their OUTPUTS move only through the widened inventory they read (`docs extract` scans the newly admitted files on its next explicit run; orient's Docs ranking on every focus sees the larger set), the intended consequence of RG-REQ-008-L01 reported in §0, not a regression"
    },
    {
      "checkId": "DD-C07",
      "obligationIds": [
        "P-DD-04"
      ],
      "owner": "builder",
      "method": {
        "kind": "command",
        "command": "cargo fmt --check -p repo-graph-doc-facts -p repo-graph-daemon-runtime -p repo-graph-rgr && cargo clippy -p repo-graph-doc-facts -p repo-graph-daemon-runtime -p repo-graph-rgr --all-targets -- -D warnings > /tmp/dd-c07.txt 2>&1; rc=$?; tail -3 /tmp/dd-c07.txt; test $rc -eq 0",
        "cwd": "rust",
        "environment": "candidate tree",
        "inputs": "rustfmt and clippy (deny warnings, all targets) over the three touched crates"
      },
      "expected": "exit 0: no formatting or lint debt enters with the candidate"
    },
    {
      "checkId": "DD-C08",
      "obligationIds": [
        "RG-REQ-008-L01",
        "RG-REQ-008-L02",
        "RG-REQ-008-L06",
        "RG-REQ-008-L07",
        "RG-REQ-008-L03",
        "RG-REQ-003-L09"
      ],
      "owner": "builder",
      "method": {
        "kind": "command",
        "command": "( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/hadoop' && export RMAP_STATE_ROOT=/private/tmp/DOCS-DISCOVERY-1-hadoop-before RMAP_SOCKET_PATH=/private/tmp/DOCS-DISCOVERY-1-hadoop-before/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/private/tmp/DOCS-DISCOVERY-1-before-bin:$PATH\" \"/private/tmp/DOCS-DISCOVERY-1-before-bin/rmap\" docs list > /tmp/dd-hadoop-docs-before.txt 2> /tmp/dd-hadoop-docs-before.txt.err ) || { echo \"RUN-FAILED /tmp/dd-hadoop-docs-before.txt\"; exit 1; } && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/hadoop' && export RMAP_STATE_ROOT=/private/tmp/DOCS-DISCOVERY-1-hadoop-before RMAP_SOCKET_PATH=/private/tmp/DOCS-DISCOVERY-1-hadoop-before/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release:$PATH\" \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" docs list > /tmp/dd-hadoop-docs-after.txt 2> /tmp/dd-hadoop-docs-after.txt.err ) || { echo \"RUN-FAILED /tmp/dd-hadoop-docs-after.txt\"; exit 1; } && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/hadoop' && export RMAP_STATE_ROOT=/private/tmp/DOCS-DISCOVERY-1-hadoop-before RMAP_SOCKET_PATH=/private/tmp/DOCS-DISCOVERY-1-hadoop-before/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release:$PATH\" \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" docs list --json > /tmp/dd-hadoop-docs-after.json 2> /tmp/dd-hadoop-docs-after.json.err ) || { echo \"RUN-FAILED /tmp/dd-hadoop-docs-after.json\"; exit 1; } && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/hadoop' && export RMAP_STATE_ROOT=/private/tmp/DOCS-DISCOVERY-1-hadoop-before RMAP_SOCKET_PATH=/private/tmp/DOCS-DISCOVERY-1-hadoop-before/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/private/tmp/DOCS-DISCOVERY-1-before-bin:$PATH\" \"/private/tmp/DOCS-DISCOVERY-1-before-bin/rmap\" modules list > /tmp/dd-hadoop-modules-before.txt 2> /tmp/dd-hadoop-modules-before.txt.err ) || { echo \"RUN-FAILED /tmp/dd-hadoop-modules-before.txt\"; exit 1; } && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/hadoop' && export RMAP_STATE_ROOT=/private/tmp/DOCS-DISCOVERY-1-hadoop-before RMAP_SOCKET_PATH=/private/tmp/DOCS-DISCOVERY-1-hadoop-before/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release:$PATH\" \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" modules list > /tmp/dd-hadoop-modules-after.txt 2> /tmp/dd-hadoop-modules-after.txt.err ) || { echo \"RUN-FAILED /tmp/dd-hadoop-modules-after.txt\"; exit 1; } && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/hadoop' && export RMAP_STATE_ROOT=/private/tmp/DOCS-DISCOVERY-1-hadoop-before RMAP_SOCKET_PATH=/private/tmp/DOCS-DISCOVERY-1-hadoop-before/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release:$PATH\" \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" orient --budget medium > /tmp/dd-hadoop-orient-after.txt 2> /tmp/dd-hadoop-orient-after.txt.err ) || { echo \"RUN-FAILED /tmp/dd-hadoop-orient-after.txt\"; exit 1; } && grep -qE '^22 documents$' /tmp/dd-hadoop-docs-before.txt && grep -E '^[0-9]+ generated maps? excluded \\(tool-generated map summaries; use --include-generated to show\\)$' /tmp/dd-hadoop-docs-before.txt > /tmp/dd-hadoop-gen-before.txt && grep -E '^[0-9]+ generated maps? excluded \\(tool-generated map summaries; use --include-generated to show\\)$' /tmp/dd-hadoop-docs-after.txt > /tmp/dd-hadoop-gen-after.txt && diff /tmp/dd-hadoop-gen-before.txt /tmp/dd-hadoop-gen-after.txt && grep -qE '^556 documents$' /tmp/dd-hadoop-docs-after.txt && grep -qE '^\\+3 vendored docs \\(excluded; third-party/dependency directories — see --json for the full set\\)$' /tmp/dd-hadoop-docs-after.txt && grep -qE '^\\+3 markdown/prose files outside a docs tree not scanned \\(--json for the rule\\)$' /tmp/dd-hadoop-docs-after.txt && ! grep -qE 'markdown/prose files outside a docs tree not scanned \\(--json for the rule\\)' /tmp/dd-hadoop-docs-before.txt && grep -qE '^  doc  527$' /tmp/dd-hadoop-docs-after.txt && grep -qE '^  readme  26$' /tmp/dd-hadoop-docs-after.txt && grep -qE '^  architecture  2$' /tmp/dd-hadoop-docs-after.txt && grep -qE '^  config  1$' /tmp/dd-hadoop-docs-after.txt && grep -qE '^  readme  17$' /tmp/dd-hadoop-docs-before.txt && grep -qE 'read: README\\.txt — or the tree\\.$' /tmp/dd-hadoop-modules-after.txt && grep -qE '^No README or architecture doc found — the tree is the best orientation\\.$' /tmp/dd-hadoop-modules-before.txt && ! grep -qE 'No README or architecture doc found' /tmp/dd-hadoop-modules-after.txt && grep -qE 'read: README\\.txt — or the tree\\.$' /tmp/dd-hadoop-orient-after.txt && python3 - /tmp/dd-hadoop-docs-after.json <<'PY'\nimport json, sys\nd = json.load(open(sys.argv[1])); v = d.get('value', d)\nassert v['count'] == 559, v['count']\nassert v['unscanned_markup_outside_docs_tree'] == 3, v.get('unscanned_markup_outside_docs_tree')\nr = v['discovery_rule']\nassert r['stems'] == ['readme', 'contributing', 'changelog', 'architecture', 'design', 'overview', 'install', 'building', 'authors', 'news'], r\nassert r['extensions'] == ['.md', '.markdown', '.txt', '.rst', '.adoc'] and r['doc_tree_dirs'] == ['docs', 'doc', 'design'] and r['doc_tree_conventions'] == ['src/site'] and r['unscanned_extensions'] == ['.md', '.markdown', '.rst', '.adoc'], r\npaths = {e['path']: e['kind'] for e in v['entries']}\nassert paths['README.txt'] == 'readme' and paths['BUILDING.txt'] == 'doc', (paths.get('README.txt'), paths.get('BUILDING.txt'))\nassert paths['hadoop-common-project/hadoop-common/src/main/native/ChangeLog'] == 'doc'\nassert paths['hadoop-hdfs-project/hadoop-hdfs-native-client/src/main/native/libhdfspp/third_party/uriparser2/uriparser2/uriparser/AUTHORS'] == 'vendored'\nsite = [p for p in paths if '/src/site/' in p]\nassert len(site) == 520 and sum(1 for p in site if paths[p] == 'doc') == 519 and paths['hadoop-yarn-project/hadoop-yarn/hadoop-yarn-site/src/site/markdown/yarn-service/Overview.md'] == 'architecture', (len(site), sum(1 for p in site if paths[p] == 'doc'))\nassert paths['hadoop-common-project/hadoop-auth/src/site/markdown/Configuration.md'] == 'doc'\nprint('hadoop seam ok', v['count'], len(site))\nPY",
        "cwd": ".",
        "environment": "pre-provisioned isolated root /private/tmp/DOCS-DISCOVERY-1-hadoop-before (fresh index of /Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/hadoop by the base-revision binary on 2026-09-21, 185 s; docs surfaces are query-time filesystem walks that read the store only for the repo's root path — dispatch.rs:5641/:5678 — so the SAME root is served by the before binary /private/tmp/DOCS-DISCOVERY-1-before-bin/rmap (copied from rust/target/release on the CLEAN tree at step 0) and by the candidate; under RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off; the root STAYS)",
        "inputs": "hadoop (Java; 24 entries today: 17 readme, 3 doc, 1 architecture, 1 config, 2 vendored; header `22 documents`). AFTER (measured 2026-09-21/22 by the manager's walk of the checkout under the ratified rule incl. D-DD1-003, CC-8): `556 documents`, `+3 vendored docs` (the `third_party/uriparser2/.../AUTHORS` joins the two), `+3 markdown/prose files outside a docs tree not scanned (--json for the rule)` (`.github/pull_request_template.md`, `.github/workflow-security.md`, `hadoop-yarn-project/.../src/main/resources/definition/YARN-Services-Examples.md`), By kind `doc 527` (519 `src/site/markdown` guides as `doc` — 520 entries under `src/site` incl. `hadoop-yarn-site/src/site/markdown/yarn-service/Overview.md` as `architecture` — such as `hadoop-common-project/hadoop-auth/src/site/markdown/Configuration.md`, plus the root `BUILDING.txt` and `hadoop-common-project/hadoop-auth/BUILDING.txt` and `hadoop-common-project/hadoop-common/src/main/native/{AUTHORS,ChangeLog,NEWS}`, beside the 3 docs listed today: 3 + 519 + 2 + 3 = 527), `readme 26` (17 today + the root `README.txt` + 8 module `README.txt` — `hadoop-auth`, `hadoop-nfs`, `hadoop-hdfs-httpfs`, `hadoop-hdfs-nfs`, `hadoop-project-dist`, the pipes examples and the two `proto2-generated/.../protobuf/README.txt`; decomposition re-derived by this document item's walk of the checkout, which reproduces 527/26/2/1 and +3 vendored), `architecture 2` (+ an `overview.md` under `src/site`), `config 1`; `modules list` and `orient --budget medium` both print `For module boundaries as the authors describe them, read: README.txt — or the tree.` (D-DD1-004: the default small orient budget omits the modules section — frozen orient budgeting, not this slice) (before: the root `README.txt` was invisible and the line read `No README or architecture doc found — the tree is the best orientation.` — a false statement about a repository whose README is at its root); JSON `count` 559, the count and the rule object present; the `N generated maps excluded (…)` line (17,114 `rmap map` sidecars carry the marker in this checkout) is byte-identical before and after — the field proof that the generated-marker exclusion is untouched"
      },
      "expected": "exit 0: a reader of hadoop's docs surfaces now sees the authors' 519 site guides, the root README and what the rule still refuses; the recommendation names the real README; the generated-map exclusion line remains identical (no behavior change to RG-REQ-008-L03)"
    },
    {
      "checkId": "DD-C09",
      "obligationIds": [
        "RG-REQ-008-L01",
        "RG-REQ-008-L02",
        "RG-REQ-008-L07",
        "RG-REQ-003-L09",
        "RG-REQ-008-L06"
      ],
      "owner": "builder",
      "method": {
        "kind": "command",
        "command": "( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/django' && export RMAP_STATE_ROOT=/private/tmp/DOCS-DISCOVERY-1-django-before RMAP_SOCKET_PATH=/private/tmp/DOCS-DISCOVERY-1-django-before/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/private/tmp/DOCS-DISCOVERY-1-before-bin:$PATH\" \"/private/tmp/DOCS-DISCOVERY-1-before-bin/rmap\" docs list > /tmp/dd-django-docs-before.txt 2> /tmp/dd-django-docs-before.txt.err ) || { echo \"RUN-FAILED /tmp/dd-django-docs-before.txt\"; exit 1; } && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/django' && export RMAP_STATE_ROOT=/private/tmp/DOCS-DISCOVERY-1-django-before RMAP_SOCKET_PATH=/private/tmp/DOCS-DISCOVERY-1-django-before/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release:$PATH\" \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" docs list > /tmp/dd-django-docs-after.txt 2> /tmp/dd-django-docs-after.txt.err ) || { echo \"RUN-FAILED /tmp/dd-django-docs-after.txt\"; exit 1; } && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/django' && export RMAP_STATE_ROOT=/private/tmp/DOCS-DISCOVERY-1-django-before RMAP_SOCKET_PATH=/private/tmp/DOCS-DISCOVERY-1-django-before/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release:$PATH\" \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" docs list --json > /tmp/dd-django-docs-after.json 2> /tmp/dd-django-docs-after.json.err ) || { echo \"RUN-FAILED /tmp/dd-django-docs-after.json\"; exit 1; } && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/django' && export RMAP_STATE_ROOT=/private/tmp/DOCS-DISCOVERY-1-django-before RMAP_SOCKET_PATH=/private/tmp/DOCS-DISCOVERY-1-django-before/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/private/tmp/DOCS-DISCOVERY-1-before-bin:$PATH\" \"/private/tmp/DOCS-DISCOVERY-1-before-bin/rmap\" modules list > /tmp/dd-django-modules-before.txt 2> /tmp/dd-django-modules-before.txt.err ) || { echo \"RUN-FAILED /tmp/dd-django-modules-before.txt\"; exit 1; } && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/django' && export RMAP_STATE_ROOT=/private/tmp/DOCS-DISCOVERY-1-django-before RMAP_SOCKET_PATH=/private/tmp/DOCS-DISCOVERY-1-django-before/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release:$PATH\" \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" modules list > /tmp/dd-django-modules-after.txt 2> /tmp/dd-django-modules-after.txt.err ) || { echo \"RUN-FAILED /tmp/dd-django-modules-after.txt\"; exit 1; } && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/django' && export RMAP_STATE_ROOT=/private/tmp/DOCS-DISCOVERY-1-django-before RMAP_SOCKET_PATH=/private/tmp/DOCS-DISCOVERY-1-django-before/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release:$PATH\" \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" orient --budget medium > /tmp/dd-django-orient-after.txt 2> /tmp/dd-django-orient-after.txt.err ) || { echo \"RUN-FAILED /tmp/dd-django-orient-after.txt\"; exit 1; } && grep -qE '^616 documents$' /tmp/dd-django-docs-before.txt && grep -qE '^622 documents$' /tmp/dd-django-docs-after.txt && grep -qE '^\\+54 vendored docs' /tmp/dd-django-docs-after.txt && grep -qE '^\\+4 markdown/prose files outside a docs tree not scanned \\(--json for the rule\\)$' /tmp/dd-django-docs-after.txt && grep -qE '^  release-notes  386$' /tmp/dd-django-docs-after.txt && grep -qE '^  doc  225$' /tmp/dd-django-docs-after.txt && grep -qE '^  readme  7$' /tmp/dd-django-docs-after.txt && grep -qE '^  architecture  3$' /tmp/dd-django-docs-after.txt && grep -qE '^  license  1$' /tmp/dd-django-docs-after.txt && grep -qE 'read: docs/ — or the tree\\.$' /tmp/dd-django-modules-before.txt && grep -qE 'read: CONTRIBUTING\\.rst, README\\.rst, docs/ — or the tree\\.$' /tmp/dd-django-modules-after.txt && grep -qE 'read: CONTRIBUTING\\.rst, README\\.rst, docs/ — or the tree\\.$' /tmp/dd-django-orient-after.txt && python3 - /tmp/dd-django-docs-after.json <<'PY'\nimport json, sys\nd = json.load(open(sys.argv[1])); v = d.get('value', d)\nassert v['count'] == 676, v['count']\npaths = {e['path']: e['kind'] for e in v['entries']}\nfor p, k in [('README.rst', 'readme'), ('tests/README.rst', 'readme'), ('extras/README.TXT', 'readme'), ('docs/README.rst', 'readme'), ('CONTRIBUTING.rst', 'doc'), ('AUTHORS', 'doc'), ('INSTALL', 'doc')]:\n    assert paths.get(p) == k, (p, paths.get(p))\nassert v['unscanned_markup_outside_docs_tree'] == 4\nprint('django seam ok')\nPY",
        "cwd": ".",
        "environment": "pre-provisioned /private/tmp/DOCS-DISCOVERY-1-django-before (fresh index by the base binary, 18 s); served by both binaries; under RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off; STAYS",
        "inputs": "django (Python; 670 entries today, 54 vendored; header `616 documents`; `modules list` reads `read: docs/ — or the tree.` because `README.rst` and `CONTRIBUTING.rst` were invisible). AFTER: `622 documents` (+ `README.rst`, `tests/README.rst`, `extras/README.TXT` → readme, and the already-listed `docs/README.rst` reclassifies from `doc` to `readme` by the stem rule (RG-REQ-008-L02) → readme 3 → 7; `CONTRIBUTING.rst`, `AUTHORS`, `INSTALL` → doc 223 → 225 net of the reclassification — OC-1: the first admission predicted 6/226 by classifying only the ADDED files), `+54 vendored docs` unchanged, `+4 markdown/prose files outside a docs tree not scanned (--json for the rule)` (`.github/CODE_OF_CONDUCT.md`, `.github/SECURITY.md`, `.github/copilot-instructions.md`, `.github/pull_request_template.md`), release-notes 386 / architecture 3 / license 1 unchanged; `extras/README.TXT` is admitted by the case-insensitive stem rule (D-DD1-003); the recommendation on `modules list` AND `orient --budget medium` becomes `read: CONTRIBUTING.rst, README.rst, docs/ — or the tree.` — the frozen ranking is root-first then path-ASC (MODULES-METHOD-1 `orientation_root_first_ranking`), so `CONTRIBUTING.rst` precedes `README.rst`; RG-REQ-008-L07's field example writes them in the other order — CC-9 (text-only, next bootstrap revision); JSON `count` 676"
      },
      "expected": "exit 0: django's README and contribution guide are found and recommended where the authors put them (reStructuredText, root), and the rule's refusals are stated"
    },
    {
      "checkId": "DD-C10",
      "obligationIds": [
        "RG-REQ-008-L01",
        "RG-REQ-008-L02",
        "RG-REQ-008-L06"
      ],
      "owner": "builder",
      "method": {
        "kind": "command",
        "command": "( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/buildroot' && export RMAP_STATE_ROOT=/private/tmp/DOCS-DISCOVERY-1-buildroot-before RMAP_SOCKET_PATH=/private/tmp/DOCS-DISCOVERY-1-buildroot-before/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/private/tmp/DOCS-DISCOVERY-1-before-bin:$PATH\" \"/private/tmp/DOCS-DISCOVERY-1-before-bin/rmap\" docs list > /tmp/dd-buildroot-docs-before.txt 2> /tmp/dd-buildroot-docs-before.txt.err ) || { echo \"RUN-FAILED /tmp/dd-buildroot-docs-before.txt\"; exit 1; } && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/buildroot' && export RMAP_STATE_ROOT=/private/tmp/DOCS-DISCOVERY-1-buildroot-before RMAP_SOCKET_PATH=/private/tmp/DOCS-DISCOVERY-1-buildroot-before/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release:$PATH\" \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" docs list > /tmp/dd-buildroot-docs-after.txt 2> /tmp/dd-buildroot-docs-after.txt.err ) || { echo \"RUN-FAILED /tmp/dd-buildroot-docs-after.txt\"; exit 1; } && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/buildroot' && export RMAP_STATE_ROOT=/private/tmp/DOCS-DISCOVERY-1-buildroot-before RMAP_SOCKET_PATH=/private/tmp/DOCS-DISCOVERY-1-buildroot-before/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release:$PATH\" \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" docs list --json > /tmp/dd-buildroot-docs-after.json 2> /tmp/dd-buildroot-docs-after.json.err ) || { echo \"RUN-FAILED /tmp/dd-buildroot-docs-after.json\"; exit 1; } && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/buildroot' && export RMAP_STATE_ROOT=/private/tmp/DOCS-DISCOVERY-1-buildroot-before RMAP_SOCKET_PATH=/private/tmp/DOCS-DISCOVERY-1-buildroot-before/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/private/tmp/DOCS-DISCOVERY-1-before-bin:$PATH\" \"/private/tmp/DOCS-DISCOVERY-1-before-bin/rmap\" modules list > /tmp/dd-buildroot-modules-before.txt 2> /tmp/dd-buildroot-modules-before.txt.err ) || { echo \"RUN-FAILED /tmp/dd-buildroot-modules-before.txt\"; exit 1; } && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/buildroot' && export RMAP_STATE_ROOT=/private/tmp/DOCS-DISCOVERY-1-buildroot-before RMAP_SOCKET_PATH=/private/tmp/DOCS-DISCOVERY-1-buildroot-before/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release:$PATH\" \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" modules list > /tmp/dd-buildroot-modules-after.txt 2> /tmp/dd-buildroot-modules-after.txt.err ) || { echo \"RUN-FAILED /tmp/dd-buildroot-modules-after.txt\"; exit 1; } && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/buildroot' && export RMAP_STATE_ROOT=/private/tmp/DOCS-DISCOVERY-1-buildroot-before RMAP_SOCKET_PATH=/private/tmp/DOCS-DISCOVERY-1-buildroot-before/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release:$PATH\" \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" orient --budget medium > /tmp/dd-buildroot-orient-after.txt 2> /tmp/dd-buildroot-orient-after.txt.err ) || { echo \"RUN-FAILED /tmp/dd-buildroot-orient-after.txt\"; exit 1; } && grep -qE '^4 documents$' /tmp/dd-buildroot-docs-before.txt && grep -qE '^310 documents$' /tmp/dd-buildroot-docs-after.txt && grep -qE '^\\+3 markdown/prose files outside a docs tree not scanned \\(--json for the rule\\)$' /tmp/dd-buildroot-docs-after.txt && grep -qE '^  readme  234$' /tmp/dd-buildroot-docs-after.txt && grep -qE '^  doc  75$' /tmp/dd-buildroot-docs-after.txt && grep -qE '^  config  1$' /tmp/dd-buildroot-docs-after.txt && grep -qE 'read: README, docs/ — or the tree\\.$' /tmp/dd-buildroot-modules-after.txt && grep -qE 'read: README, docs/ — or the tree\\.$' /tmp/dd-buildroot-modules-before.txt && python3 - /tmp/dd-buildroot-docs-after.json <<'PY'\nimport json, sys\nd = json.load(open(sys.argv[1])); v = d.get('value', d)\nassert v['count'] == 310, v['count']\npaths = {e['path']: e['kind'] for e in v['entries']}\nadoc = [p for p in paths if p.endswith('.adoc')]\nassert len(adoc) == 73 and all(p.startswith('docs/') for p in adoc) and all(paths[p] == 'doc' for p in adoc), (len(adoc), adoc[:3])\nboard = [p for p in paths if p.startswith('board/') and p.lower().endswith('/readme.txt')]\nassert len(board) == 225 and all(paths[p] == 'readme' for p in board), len(board)\nassert sum(1 for p in paths if paths[p] == 'readme') == 234\nassert paths['docs/manual/manual.adoc'] == 'doc'\nassert v['unscanned_markup_outside_docs_tree'] == 3\nprint('buildroot seam ok', len(adoc), len(board))\nPY",
        "cwd": ".",
        "environment": "pre-provisioned /private/tmp/DOCS-DISCOVERY-1-buildroot-before (2 s); served by both binaries; under RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off; STAYS",
        "inputs": "buildroot (4 entries today: `README`, `docs/website/copyright.txt`, `docs/website/robots.txt`, one config). AFTER: `310 documents` — the 73 AsciiDoc manual files under `docs/manual/` (`docs/manual/manual.adoc`, `docs/manual/adding-packages.adoc` …) as `doc` (73 + 2 = 75) and the readmes by stem: 225 under `board/**` (223 `readme.txt` + 2 `README.txt`, admitted case-insensitively under D-DD1-003 — `board/arm/foundation-v8/readme.txt` …), 8 elsewhere, plus the root `README` = 234 (OC-1: the first admission wrote 233 `board/**/readme.txt` case-sensitively) — the stem rule admits a readme anywhere, exactly as `README.md` was admitted anywhere before (RC-8 estimated ~77 because it counted only the `.adoc` files; CC-8); `+3 markdown/prose files outside a docs tree not scanned (--json for the rule)` (`.github/pull_request_template.md`, `.gitlab/issue_templates/default.md`, `SECURITY.md`); `config 1`; the recommendation `read: README, docs/ — or the tree.` unchanged (the root README was already found by exact name)"
      },
      "expected": "exit 0: buildroot's manual and board documentation are found; a reader is told three markdown files at the top level were not scanned"
    },
    {
      "checkId": "DD-C11",
      "obligationIds": [
        "RG-REQ-008-L01",
        "RG-REQ-008-L06",
        "RG-REQ-008-L07",
        "P-DD-03"
      ],
      "owner": "builder",
      "method": {
        "kind": "command",
        "command": "( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/kafka' && export RMAP_STATE_ROOT=/private/tmp/DOCS-DISCOVERY-1-kafka-before RMAP_SOCKET_PATH=/private/tmp/DOCS-DISCOVERY-1-kafka-before/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/private/tmp/DOCS-DISCOVERY-1-before-bin:$PATH\" \"/private/tmp/DOCS-DISCOVERY-1-before-bin/rmap\" docs list > /tmp/dd-kafka-docs-before.txt 2> /tmp/dd-kafka-docs-before.txt.err ) || { echo \"RUN-FAILED /tmp/dd-kafka-docs-before.txt\"; exit 1; } && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/kafka' && export RMAP_STATE_ROOT=/private/tmp/DOCS-DISCOVERY-1-kafka-before RMAP_SOCKET_PATH=/private/tmp/DOCS-DISCOVERY-1-kafka-before/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release:$PATH\" \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" docs list > /tmp/dd-kafka-docs-after.txt 2> /tmp/dd-kafka-docs-after.txt.err ) || { echo \"RUN-FAILED /tmp/dd-kafka-docs-after.txt\"; exit 1; } && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/kafka' && export RMAP_STATE_ROOT=/private/tmp/DOCS-DISCOVERY-1-kafka-before RMAP_SOCKET_PATH=/private/tmp/DOCS-DISCOVERY-1-kafka-before/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release:$PATH\" \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" docs list --json > /tmp/dd-kafka-docs-after.json 2> /tmp/dd-kafka-docs-after.json.err ) || { echo \"RUN-FAILED /tmp/dd-kafka-docs-after.json\"; exit 1; } && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/kafka' && export RMAP_STATE_ROOT=/private/tmp/DOCS-DISCOVERY-1-kafka-before RMAP_SOCKET_PATH=/private/tmp/DOCS-DISCOVERY-1-kafka-before/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/private/tmp/DOCS-DISCOVERY-1-before-bin:$PATH\" \"/private/tmp/DOCS-DISCOVERY-1-before-bin/rmap\" modules list > /tmp/dd-kafka-modules-before.txt 2> /tmp/dd-kafka-modules-before.txt.err ) || { echo \"RUN-FAILED /tmp/dd-kafka-modules-before.txt\"; exit 1; } && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/kafka' && export RMAP_STATE_ROOT=/private/tmp/DOCS-DISCOVERY-1-kafka-before RMAP_SOCKET_PATH=/private/tmp/DOCS-DISCOVERY-1-kafka-before/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release:$PATH\" \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" modules list > /tmp/dd-kafka-modules-after.txt 2> /tmp/dd-kafka-modules-after.txt.err ) || { echo \"RUN-FAILED /tmp/dd-kafka-modules-after.txt\"; exit 1; } && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/kafka' && export RMAP_STATE_ROOT=/private/tmp/DOCS-DISCOVERY-1-kafka-before RMAP_SOCKET_PATH=/private/tmp/DOCS-DISCOVERY-1-kafka-before/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release:$PATH\" \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" orient --budget medium > /tmp/dd-kafka-orient-after.txt 2> /tmp/dd-kafka-orient-after.txt.err ) || { echo \"RUN-FAILED /tmp/dd-kafka-orient-after.txt\"; exit 1; } && grep -qE '^133 documents$' /tmp/dd-kafka-docs-before.txt && grep -qE '^133 documents$' /tmp/dd-kafka-docs-after.txt && grep -qE '^\\+1 markdown/prose file outside a docs tree not scanned \\(--json for the rule\\)$' /tmp/dd-kafka-docs-after.txt && grep -v '^+1 markdown/prose file ' /tmp/dd-kafka-docs-after.txt > /tmp/dd-kafka-docs-after-stripped.txt && diff /tmp/dd-kafka-docs-before.txt /tmp/dd-kafka-docs-after-stripped.txt && diff /tmp/dd-kafka-modules-before.txt /tmp/dd-kafka-modules-after.txt && grep -qE 'read: CONTRIBUTING\\.md, README\\.md, docs/ — or the tree\\.$' /tmp/dd-kafka-modules-after.txt && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/leveldb' && export RMAP_STATE_ROOT=/private/tmp/DOCS-DISCOVERY-1-leveldb-before RMAP_SOCKET_PATH=/private/tmp/DOCS-DISCOVERY-1-leveldb-before/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/private/tmp/DOCS-DISCOVERY-1-before-bin:$PATH\" \"/private/tmp/DOCS-DISCOVERY-1-before-bin/rmap\" docs list > /tmp/dd-leveldb-docs-before.txt 2> /tmp/dd-leveldb-docs-before.txt.err ) || { echo \"RUN-FAILED /tmp/dd-leveldb-docs-before.txt\"; exit 1; } && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/leveldb' && export RMAP_STATE_ROOT=/private/tmp/DOCS-DISCOVERY-1-leveldb-before RMAP_SOCKET_PATH=/private/tmp/DOCS-DISCOVERY-1-leveldb-before/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release:$PATH\" \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" docs list > /tmp/dd-leveldb-docs-after.txt 2> /tmp/dd-leveldb-docs-after.txt.err ) || { echo \"RUN-FAILED /tmp/dd-leveldb-docs-after.txt\"; exit 1; } && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/leveldb' && export RMAP_STATE_ROOT=/private/tmp/DOCS-DISCOVERY-1-leveldb-before RMAP_SOCKET_PATH=/private/tmp/DOCS-DISCOVERY-1-leveldb-before/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release:$PATH\" \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" docs list --json > /tmp/dd-leveldb-docs-after.json 2> /tmp/dd-leveldb-docs-after.json.err ) || { echo \"RUN-FAILED /tmp/dd-leveldb-docs-after.json\"; exit 1; } && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/leveldb' && export RMAP_STATE_ROOT=/private/tmp/DOCS-DISCOVERY-1-leveldb-before RMAP_SOCKET_PATH=/private/tmp/DOCS-DISCOVERY-1-leveldb-before/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/private/tmp/DOCS-DISCOVERY-1-before-bin:$PATH\" \"/private/tmp/DOCS-DISCOVERY-1-before-bin/rmap\" modules list > /tmp/dd-leveldb-modules-before.txt 2> /tmp/dd-leveldb-modules-before.txt.err ) || { echo \"RUN-FAILED /tmp/dd-leveldb-modules-before.txt\"; exit 1; } && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/leveldb' && export RMAP_STATE_ROOT=/private/tmp/DOCS-DISCOVERY-1-leveldb-before RMAP_SOCKET_PATH=/private/tmp/DOCS-DISCOVERY-1-leveldb-before/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release:$PATH\" \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" modules list > /tmp/dd-leveldb-modules-after.txt 2> /tmp/dd-leveldb-modules-after.txt.err ) || { echo \"RUN-FAILED /tmp/dd-leveldb-modules-after.txt\"; exit 1; } && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/leveldb' && export RMAP_STATE_ROOT=/private/tmp/DOCS-DISCOVERY-1-leveldb-before RMAP_SOCKET_PATH=/private/tmp/DOCS-DISCOVERY-1-leveldb-before/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release:$PATH\" \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" orient --budget medium > /tmp/dd-leveldb-orient-after.txt 2> /tmp/dd-leveldb-orient-after.txt.err ) || { echo \"RUN-FAILED /tmp/dd-leveldb-orient-after.txt\"; exit 1; } && grep -qE '^7 documents$' /tmp/dd-leveldb-docs-before.txt && grep -qE '^9 documents$' /tmp/dd-leveldb-docs-after.txt && ! grep -qE 'markdown/prose file' /tmp/dd-leveldb-docs-after.txt && grep -qE '^  AUTHORS  doc$' /tmp/dd-leveldb-docs-after.txt && grep -qE '^  NEWS  doc$' /tmp/dd-leveldb-docs-after.txt && diff /tmp/dd-leveldb-modules-before.txt /tmp/dd-leveldb-modules-after.txt && python3 - /tmp/dd-leveldb-docs-after.json <<'PY'\nimport json, sys\nd = json.load(open(sys.argv[1])); v = d.get('value', d)\nassert v['count'] == 9 and 'unscanned_markup_outside_docs_tree' not in v and 'discovery_rule' not in v, sorted(v)\nprint('leveldb seam ok: gated keys absent when nothing was refused')\nPY",
        "cwd": ".",
        "environment": "pre-provisioned /private/tmp/DOCS-DISCOVERY-1-{kafka,leveldb}-before (fresh indexes by the base binary: kafka 99 s, leveldb 1 s); served by both binaries; under RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off; the roots STAY",
        "inputs": "the control corpora, measured 2026-09-21 (CC-8) and reproduced by the first admission: kafka — `133 documents` before and after, every entry and the `By kind` block identical, the header gains exactly `+1 markdown/prose file outside a docs tree not scanned (--json for the rule)` (`.github/pull_request_template.md`; singular), `modules list` byte-identical (`read: CONTRIBUTING.md, README.md, docs/ — or the tree.`); leveldb — `7 documents` → `9 documents` (root `AUTHORS` and `NEWS` as `doc`), NO unscanned line and NO `unscanned_markup_outside_docs_tree`/`discovery_rule` keys in `--json` (nothing refused: `CMakeLists.txt` is `.txt`, not counted — D-DD1-002), `modules list` byte-identical. The heredoc is the LAST element of the chain (OC-1: the first admission's single DD-C11 had a heredoc terminator glued to `&&`, so the command could not execute)"
      },
      "expected": "exit 0: where the ratified rule admits nothing new, the surfaces remain byte-identical apart from the new honesty line, and where it admits a root AUTHORS/NEWS the reader sees them (no behavior change to any count the rule does not touch)"
    },
    {
      "checkId": "DD-C12",
      "obligationIds": [
        "RG-REQ-008-L01",
        "RG-REQ-008-L04",
        "RG-REQ-008-L06",
        "P-DD-03"
      ],
      "owner": "builder",
      "method": {
        "kind": "command",
        "command": "( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/grpc-java' && export RMAP_STATE_ROOT=/private/tmp/DOCS-DISCOVERY-1-grpc-java-before RMAP_SOCKET_PATH=/private/tmp/DOCS-DISCOVERY-1-grpc-java-before/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/private/tmp/DOCS-DISCOVERY-1-before-bin:$PATH\" \"/private/tmp/DOCS-DISCOVERY-1-before-bin/rmap\" docs list > /tmp/dd-grpc-java-docs-before.txt 2> /tmp/dd-grpc-java-docs-before.txt.err ) || { echo \"RUN-FAILED /tmp/dd-grpc-java-docs-before.txt\"; exit 1; } && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/grpc-java' && export RMAP_STATE_ROOT=/private/tmp/DOCS-DISCOVERY-1-grpc-java-before RMAP_SOCKET_PATH=/private/tmp/DOCS-DISCOVERY-1-grpc-java-before/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release:$PATH\" \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" docs list > /tmp/dd-grpc-java-docs-after.txt 2> /tmp/dd-grpc-java-docs-after.txt.err ) || { echo \"RUN-FAILED /tmp/dd-grpc-java-docs-after.txt\"; exit 1; } && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/grpc-java' && export RMAP_STATE_ROOT=/private/tmp/DOCS-DISCOVERY-1-grpc-java-before RMAP_SOCKET_PATH=/private/tmp/DOCS-DISCOVERY-1-grpc-java-before/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release:$PATH\" \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" docs list --json > /tmp/dd-grpc-java-docs-after.json 2> /tmp/dd-grpc-java-docs-after.json.err ) || { echo \"RUN-FAILED /tmp/dd-grpc-java-docs-after.json\"; exit 1; } && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/grpc-java' && export RMAP_STATE_ROOT=/private/tmp/DOCS-DISCOVERY-1-grpc-java-before RMAP_SOCKET_PATH=/private/tmp/DOCS-DISCOVERY-1-grpc-java-before/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/private/tmp/DOCS-DISCOVERY-1-before-bin:$PATH\" \"/private/tmp/DOCS-DISCOVERY-1-before-bin/rmap\" modules list > /tmp/dd-grpc-java-modules-before.txt 2> /tmp/dd-grpc-java-modules-before.txt.err ) || { echo \"RUN-FAILED /tmp/dd-grpc-java-modules-before.txt\"; exit 1; } && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/grpc-java' && export RMAP_STATE_ROOT=/private/tmp/DOCS-DISCOVERY-1-grpc-java-before RMAP_SOCKET_PATH=/private/tmp/DOCS-DISCOVERY-1-grpc-java-before/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release:$PATH\" \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" modules list > /tmp/dd-grpc-java-modules-after.txt 2> /tmp/dd-grpc-java-modules-after.txt.err ) || { echo \"RUN-FAILED /tmp/dd-grpc-java-modules-after.txt\"; exit 1; } && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/grpc-java' && export RMAP_STATE_ROOT=/private/tmp/DOCS-DISCOVERY-1-grpc-java-before RMAP_SOCKET_PATH=/private/tmp/DOCS-DISCOVERY-1-grpc-java-before/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release:$PATH\" \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" orient --budget medium > /tmp/dd-grpc-java-orient-after.txt 2> /tmp/dd-grpc-java-orient-after.txt.err ) || { echo \"RUN-FAILED /tmp/dd-grpc-java-orient-after.txt\"; exit 1; } && grep -qE '^49 documents$' /tmp/dd-grpc-java-docs-before.txt && grep -qE '^50 documents$' /tmp/dd-grpc-java-docs-after.txt && grep -qE '^\\+12 markdown/prose files outside a docs tree not scanned \\(--json for the rule\\)$' /tmp/dd-grpc-java-docs-after.txt && grep -qE '^  AUTHORS  doc$' /tmp/dd-grpc-java-docs-after.txt && diff /tmp/dd-grpc-java-modules-before.txt /tmp/dd-grpc-java-modules-after.txt && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/FRAKTAG' && export RMAP_STATE_ROOT=/private/tmp/DOCS-DISCOVERY-1-fraktag-before RMAP_SOCKET_PATH=/private/tmp/DOCS-DISCOVERY-1-fraktag-before/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/private/tmp/DOCS-DISCOVERY-1-before-bin:$PATH\" \"/private/tmp/DOCS-DISCOVERY-1-before-bin/rmap\" docs list > /tmp/dd-fraktag-docs-before.txt 2> /tmp/dd-fraktag-docs-before.txt.err ) || { echo \"RUN-FAILED /tmp/dd-fraktag-docs-before.txt\"; exit 1; } && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/FRAKTAG' && export RMAP_STATE_ROOT=/private/tmp/DOCS-DISCOVERY-1-fraktag-before RMAP_SOCKET_PATH=/private/tmp/DOCS-DISCOVERY-1-fraktag-before/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release:$PATH\" \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" docs list > /tmp/dd-fraktag-docs-after.txt 2> /tmp/dd-fraktag-docs-after.txt.err ) || { echo \"RUN-FAILED /tmp/dd-fraktag-docs-after.txt\"; exit 1; } && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/FRAKTAG' && export RMAP_STATE_ROOT=/private/tmp/DOCS-DISCOVERY-1-fraktag-before RMAP_SOCKET_PATH=/private/tmp/DOCS-DISCOVERY-1-fraktag-before/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release:$PATH\" \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" docs list --json > /tmp/dd-fraktag-docs-after.json 2> /tmp/dd-fraktag-docs-after.json.err ) || { echo \"RUN-FAILED /tmp/dd-fraktag-docs-after.json\"; exit 1; } && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/FRAKTAG' && export RMAP_STATE_ROOT=/private/tmp/DOCS-DISCOVERY-1-fraktag-before RMAP_SOCKET_PATH=/private/tmp/DOCS-DISCOVERY-1-fraktag-before/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/private/tmp/DOCS-DISCOVERY-1-before-bin:$PATH\" \"/private/tmp/DOCS-DISCOVERY-1-before-bin/rmap\" modules list > /tmp/dd-fraktag-modules-before.txt 2> /tmp/dd-fraktag-modules-before.txt.err ) || { echo \"RUN-FAILED /tmp/dd-fraktag-modules-before.txt\"; exit 1; } && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/FRAKTAG' && export RMAP_STATE_ROOT=/private/tmp/DOCS-DISCOVERY-1-fraktag-before RMAP_SOCKET_PATH=/private/tmp/DOCS-DISCOVERY-1-fraktag-before/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release:$PATH\" \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" modules list > /tmp/dd-fraktag-modules-after.txt 2> /tmp/dd-fraktag-modules-after.txt.err ) || { echo \"RUN-FAILED /tmp/dd-fraktag-modules-after.txt\"; exit 1; } && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/FRAKTAG' && export RMAP_STATE_ROOT=/private/tmp/DOCS-DISCOVERY-1-fraktag-before RMAP_SOCKET_PATH=/private/tmp/DOCS-DISCOVERY-1-fraktag-before/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release:$PATH\" \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" orient --budget medium > /tmp/dd-fraktag-orient-after.txt 2> /tmp/dd-fraktag-orient-after.txt.err ) || { echo \"RUN-FAILED /tmp/dd-fraktag-orient-after.txt\"; exit 1; } && grep -qE '^14 documents$' /tmp/dd-fraktag-docs-before.txt && grep -qE '^14 documents$' /tmp/dd-fraktag-docs-after.txt && grep -qE '^\\+5 vendored docs' /tmp/dd-fraktag-docs-before.txt && grep -qE '^\\+11 vendored docs' /tmp/dd-fraktag-docs-after.txt && grep -qE '^\\+[0-9]+ markdown/prose files outside a docs tree not scanned \\(--json for the rule\\)$' /tmp/dd-fraktag-docs-after.txt && diff /tmp/dd-fraktag-modules-before.txt /tmp/dd-fraktag-modules-after.txt",
        "cwd": ".",
        "environment": "pre-provisioned /private/tmp/DOCS-DISCOVERY-1-{grpc-java,fraktag}-before (18 s, 1 s); served by both binaries; under RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off; STAY",
        "inputs": "grpc-java — `49` → `50 documents` (root `AUTHORS` as `doc`), `+12 markdown/prose files …` (`CODE-OF-CONDUCT.md`, `COMPILING.md`, `GOVERNANCE.md`, `SECURITY.md`, `.github/ISSUE_TEMPLATE/*.md` …), `modules list` byte-identical; FRAKTAG — headline `14 documents` unchanged, `+5` → `+11 vendored docs` (six `AUTHORS`/`readme.*` files under `packages/engine/fraktag-env/lib/python3.11/site-packages/...` — RG-REQ-008-L04's one vendored predicate), an unscanned line (165 at measurement — `AGENTS.md`, `CLAUDE.md`, the knowledge-base content under `packages/engine/data/new-kb/**.md`; asserted by pattern because the checkout is live), `modules list` byte-identical"
      },
      "expected": "exit 0: a root AUTHORS is found, vendored documentation stays excluded and counted, and the refused markup is stated (no behavior change to the vendored or recommendation surfaces)"
    },
    {
      "checkId": "DD-C13",
      "obligationIds": [
        "RG-REQ-008-L01",
        "RG-REQ-008-L02",
        "RG-REQ-008-L03",
        "RG-REQ-008-L06"
      ],
      "owner": "builder",
      "method": {
        "kind": "command",
        "command": "( cd '/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph' && export RMAP_STATE_ROOT=/private/tmp/DOCS-DISCOVERY-1-repo-graph-before RMAP_SOCKET_PATH=/private/tmp/DOCS-DISCOVERY-1-repo-graph-before/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/private/tmp/DOCS-DISCOVERY-1-before-bin:$PATH\" \"/private/tmp/DOCS-DISCOVERY-1-before-bin/rmap\" docs list > /tmp/dd-repo-graph-docs-before.txt 2> /tmp/dd-repo-graph-docs-before.txt.err ) || { echo \"RUN-FAILED /tmp/dd-repo-graph-docs-before.txt\"; exit 1; } && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph' && export RMAP_STATE_ROOT=/private/tmp/DOCS-DISCOVERY-1-repo-graph-before RMAP_SOCKET_PATH=/private/tmp/DOCS-DISCOVERY-1-repo-graph-before/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release:$PATH\" \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" docs list > /tmp/dd-repo-graph-docs-after.txt 2> /tmp/dd-repo-graph-docs-after.txt.err ) || { echo \"RUN-FAILED /tmp/dd-repo-graph-docs-after.txt\"; exit 1; } && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph' && export RMAP_STATE_ROOT=/private/tmp/DOCS-DISCOVERY-1-repo-graph-before RMAP_SOCKET_PATH=/private/tmp/DOCS-DISCOVERY-1-repo-graph-before/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release:$PATH\" \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" docs list --json > /tmp/dd-repo-graph-docs-after.json 2> /tmp/dd-repo-graph-docs-after.json.err ) || { echo \"RUN-FAILED /tmp/dd-repo-graph-docs-after.json\"; exit 1; } && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph' && export RMAP_STATE_ROOT=/private/tmp/DOCS-DISCOVERY-1-repo-graph-before RMAP_SOCKET_PATH=/private/tmp/DOCS-DISCOVERY-1-repo-graph-before/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/private/tmp/DOCS-DISCOVERY-1-before-bin:$PATH\" \"/private/tmp/DOCS-DISCOVERY-1-before-bin/rmap\" modules list > /tmp/dd-repo-graph-modules-before.txt 2> /tmp/dd-repo-graph-modules-before.txt.err ) || { echo \"RUN-FAILED /tmp/dd-repo-graph-modules-before.txt\"; exit 1; } && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph' && export RMAP_STATE_ROOT=/private/tmp/DOCS-DISCOVERY-1-repo-graph-before RMAP_SOCKET_PATH=/private/tmp/DOCS-DISCOVERY-1-repo-graph-before/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release:$PATH\" \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" modules list > /tmp/dd-repo-graph-modules-after.txt 2> /tmp/dd-repo-graph-modules-after.txt.err ) || { echo \"RUN-FAILED /tmp/dd-repo-graph-modules-after.txt\"; exit 1; } && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph' && export RMAP_STATE_ROOT=/private/tmp/DOCS-DISCOVERY-1-repo-graph-before RMAP_SOCKET_PATH=/private/tmp/DOCS-DISCOVERY-1-repo-graph-before/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release:$PATH\" \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" orient --budget medium > /tmp/dd-repo-graph-orient-after.txt 2> /tmp/dd-repo-graph-orient-after.txt.err ) || { echo \"RUN-FAILED /tmp/dd-repo-graph-orient-after.txt\"; exit 1; } && grep -qE '^\\+[0-9]+ markdown/prose files outside a docs tree not scanned \\(--json for the rule\\)$' /tmp/dd-repo-graph-docs-after.txt && grep -E '^[0-9]+ generated maps? excluded \\(tool-generated map summaries; use --include-generated to show\\)$' /tmp/dd-repo-graph-docs-before.txt > /tmp/dd-repo-graph-gen-before.txt && grep -E '^[0-9]+ generated maps? excluded \\(tool-generated map summaries; use --include-generated to show\\)$' /tmp/dd-repo-graph-docs-after.txt > /tmp/dd-repo-graph-gen-after.txt && diff /tmp/dd-repo-graph-gen-before.txt /tmp/dd-repo-graph-gen-after.txt && python3 - /tmp/dd-repo-graph-docs-before.txt /tmp/dd-repo-graph-docs-after.txt /tmp/dd-repo-graph-docs-after.json <<'PY'\nimport json, sys, re\ndef header(p):\n    t = open(p).read(); total = int(re.search(r'^(\\d+) documents$', t, re.M).group(1))\n    kinds = {m.group(1): int(m.group(2)) for m in re.finditer(r'^  ([a-z-]+)  (\\d+)$', t, re.M)}\n    return total, kinds\nbt, bk = header(sys.argv[1]); at, ak = header(sys.argv[2])\nprint('before', bt, bk, '| after', at, ak)\nassert at == bt + 1, (bt, at)\nassert ak['architecture'] == bk['architecture'] + 1 and ak['readme'] == bk['readme'] + 1 and ak['doc'] == bk['doc'] - 1, (bk, ak)\nd = json.load(open(sys.argv[3])); v = d.get('value', d)\npaths = {e['path']: e['kind'] for e in v['entries']}\nassert paths.get('agent_docs/architecture.md') == 'architecture', paths.get('agent_docs/architecture.md')\nassert v['unscanned_markup_outside_docs_tree'] >= 1000, v['unscanned_markup_outside_docs_tree']\nprint('repo-graph seam ok: +1 architecture (agent_docs/architecture.md), one doc-tree README.* reclassified readme, unscanned', v['unscanned_markup_outside_docs_tree'])\nPY",
        "cwd": ".",
        "environment": "pre-provisioned /private/tmp/DOCS-DISCOVERY-1-repo-graph-before (9 s); served by both binaries against the LIVE checkout of this repository; under RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off; STAYS",
        "inputs": "repo-graph itself — asserted RELATIVELY because the checkout moves with every commit (OC-1: the first admission read 657/658 from the 100f2ae measurement; two decision records committed since made it 659/660): after headline = before + 1 (`agent_docs/architecture.md`, architecture + 1), one README.rst/.txt already inside a docs tree (`docs/skills/README.txt`) reclassifies from `doc` to `readme` by the stem rule (readme + 1, doc − 1), the unscanned line ≥ 1000 (dominated by `.agent-manager/**.md`, local relay state the walk does not gitignore — pre-existing, stated); the before values are read from the before capture of THIS run; the `N generated maps excluded (…)` line (the checkout's gitignored `rmap map` sidecars, e.g. the root `MAP.md`) identical before and after. This document item's walk of the checkout under the ratified rule finds exactly ONE newly admitted file (`agent_docs/architecture.md`)"
      },
      "expected": "exit 0: the repository's own inventory gains exactly the architecture note the rule now admits and reclassifies its doc-tree readme, stated against a live baseline; the generated-map exclusion line remains identical (no behavior change to RG-REQ-008-L03)"
    },
    {
      "checkId": "DD-C14",
      "obligationIds": [
        "RG-REQ-011-L06",
        "P-DD-04"
      ],
      "owner": "builder",
      "method": {
        "kind": "command",
        "command": "test \"$(grep -E '^registry-before [0-9a-f]{64}$' .agent-manager/slices/DOCS-DISCOVERY-1/build-progress.md | tail -1 | cut -d' ' -f2)\" = \"$(shasum -a 256 \"$HOME/Library/Application Support/repo-graph/registry.json\" | cut -d' ' -f1)\" && PIDS=$(pgrep -x rmapd | paste -sd, -); { [ -z \"$PIDS\" ] || ! ps -o command= -p \"$PIDS\" | grep -v '/\\.local/bin/rmapd' | grep -q .; } && ! test -d /private/tmp/DOCS-DISCOVERY-1-before-bin && ls -d /private/tmp/DOCS-DISCOVERY-1-hadoop-before /private/tmp/DOCS-DISCOVERY-1-django-before /private/tmp/DOCS-DISCOVERY-1-buildroot-before /private/tmp/DOCS-DISCOVERY-1-kafka-before /private/tmp/DOCS-DISCOVERY-1-leveldb-before /private/tmp/DOCS-DISCOVERY-1-grpc-java-before /private/tmp/DOCS-DISCOVERY-1-fraktag-before /private/tmp/DOCS-DISCOVERY-1-repo-graph-before >/dev/null && ! git status --short | grep -vE '^[ MA?][ MA?] (rust/crates/doc\\-facts/src/discovery\\.rs|rust/crates/doc\\-facts/src/classification\\.rs|rust/crates/doc\\-facts/src/lib\\.rs|rust/crates/doc\\-facts/src/lib_tests\\.rs|rust/crates/daemon\\-runtime/src/modules_method\\.rs|rust/crates/daemon\\-runtime/src/orient_additive_fields\\.rs|rust/crates/daemon\\-runtime/src/dispatch\\.rs|rust/crates/rgr/src/presentation/docs\\.rs|rust/crates/rgr/src/presentation/docs_tests\\.rs|rust/crates/rgr/src/presentation/orient_tests\\.rs)$' | grep -q . && git diff --check",
        "cwd": ".",
        "environment": "candidate tree, after every other check",
        "inputs": "PRECONDITION: the document item (this SLICE_DOC, the INPUT-2 manifest and docs/assurance/DOCS-DISCOVERY-1/oracle-corrections.md) is committed before the admission — as INPUT-1's was (b44a7c4) — otherwise the `git status` clause fails on files this slice does not own; the relay writes the post-review records after this check. the full sha256 of the operator registry recorded as `registry-before <digest>` in build-progress.md at step 0 equals the current one; no stray isolated daemon; the before-binary directory removed; the eight `-before` roots kept for the manager's gate; `git status --short` names only the ten candidate paths (no new files in this slice); no trailing whitespace"
      },
      "expected": "exit 0: every proof was isolated (the operator's registry byte-identical — no behavior change outside the isolated roots), the tree holds only the allocated paths"
    }
  ]
}
-->

# DOCS-DISCOVERY-1 — documentation is found where the authors put it

Status: SPECIFIED and GROUNDED 2026-09-22 on HEAD b44a7c4 (every seam read from the sources by a read-only investigation; every number measured by walking the 17 local checkouts with the ratified rule — CC-8 — and every BEFORE literal read from the product on eight fresh isolated roots) · Track: audit round six, Q8 (RC-8; never worked). Ratified order Q1 → Q3 → Q4 → Q5 → Q6 → Q7 → Q8 → Q9 → ALIAS-SUSPICION-1 → SEED-DOCUMENT-1; Q8 was skipped on 2026-09-20 by a false "awaits ratification" claim (RG-REQ-008-L01 was ratified 2026-09-14) and is taken now. CODE slice: doc-facts (discovery, classification, inventory), daemon-runtime (recommendation stems, payload), rgr (one header line, two JSON fields, one orient test). Query-time only: NO reindex, no schema change — the before-roots serve both binaries. Builder: claude / claude-opus-4-8; reviewer: codex / gpt-5.6-terra. Supersedes the 2026-09-12 draft of this document (which named a Codex builder and estimated numbers).

## 0. Requirements allocation (the machine-readable block above is binding; this section explains it)

**Implements:** RG-REQ-008-L01 — the discovery scope is ONE stem list × ONE extension list × ONE doc-tree rule with the `src/site/**` convention (DD-C01/C08/C09/C10/C11/C12/C13). RG-REQ-008-L02 — `readme` by stem; bare documentation stems are `doc`, never `config` (DD-C02/C08/C09). RG-REQ-008-L06 — the header states the markup files the rule refused, `--json` carries the count and the rule (DD-C03/C05/C08–C13). RG-REQ-008-L07 — the recommendation's named-file rule reads the same stem constant (DD-C04/C08/C09). RG-REQ-003-L09 — orient renders the recommendation, now asserted on the orient side (DD-C05/C08/C09).

**Changes:** none allocated. REPORTED consequences (measured, CC-8): hadoop `22 → 556 documents`, django `616 → 622`, buildroot `4 → 310`, leveldb `7 → 9`, grpc-java `49 → 50`, repo-graph + 1; FRAKTAG headline unchanged with `+5 → +11 vendored docs`; kafka byte-identical but for the new honesty line; hadoop's recommendation line (`No README or architecture doc found — the tree is the best orientation.` today — false: `README.txt` sits at the root) and django's (`read: docs/`) gain their real root READMEs. INHERITED consequences (code untouched, input widened — stated so no reader mistakes them for regressions): `docs extract` scans the newly admitted files on its next explicit run (its `files scanned` / facts counts grow; its rendering form is unchanged); orient's Docs ranking (`agent/src/doc_relevance.rs`, every focus: repo/file/path/symbol, fed by storage's `get_doc_inventory` → `discover_doc_inventory(.., true)`) ranks over the larger inventory, so which documents it names can change — the first admission observed hadoop's small `orient` byte-identical; other corpora are UNVERIFIED on that surface.

**Preserves:** RG-REQ-008-L03/L04/L05 (generated, vendored, unreadable handling — DD-C03/C05/C06/C08/C11/C12/C13), RG-REQ-008-L08 (budget, JSON completeness, `docs extract` — DD-C05/C06), RG-REQ-008-L09 (read-only query-time inventory; `.env*` — DD-C03/C06), RG-REQ-011-L06 (isolation — DD-C14), RG-REQ-011-L07 (the exhaust predicate — DD-C01/C03).

**Decision D-DD1-001 (operator; the human may override at closeout) — the shape of "one constant".** `DOC_NAME_STEMS` (10) and `ORIENTATION_STEMS` (4, tested to be a subset) live in doc-facts; classification and modules_method call `doc_name_stem`. Rejected: one list with a per-stem "orientation" flag (a struct for two booleans), and a doc-facts `is_orientation_file` that would move the depth rule out of the recommendation module it belongs to.

**Decision D-DD1-002 (operator; the human may override) — what "unscanned prose" counts and when the JSON carries the rule.** The count covers files whose lowercased name ends with `.md`, `.markdown`, `.rst` or `.adoc` and that the rule refused; `.txt` is not counted because a `.txt` outside a docs tree is not known to be prose (leveldb `CMakeLists.txt`, kafka `requirements.txt`, buildroot `uEnv.txt` would otherwise be reported as "markdown/prose files"). `unscanned_markup_outside_docs_tree` and `discovery_rule` are emitted in `--json` only when the count is > 0 — gated together like `unreadable`, so a repo with nothing refused keeps a byte-identical payload. Rejected: counting every doc-extension file (false "prose" claims on the three corpora above); emitting the rule always (+1 key on every payload for no reader who is not following the header line).

**Decision D-DD1-004 (operator; the human may override; raised by the first admission's reviewer as D-DD1-001 'orient depth', recommendation followed) — the orient proof runs at `--budget medium`.** The default (small) `rmap orient` omits the modules section entirely (orient budgeting is frozen; `orient_sections.rs` is outside this slice), so the recommendation line is asserted on `orient --budget medium` where the section renders; small output is untouched. The alternative — expanding the small budget to carry the recommendation — changes ratified orient budgeting and is a separate slice; RG-REQ-003-L07 names `docs` in the small headline set, so whether the recommendation belongs there is a catalog question for the human (noted, not decided here).

**Catalog corrections filed by this grounding (text-only, next bootstrap revision):** CC-9 — RG-REQ-008-L07's field example `django read: README.rst, CONTRIBUTING.rst, docs/` lists the two root files in the opposite order of the frozen root-first-then-path-ASC ranking (MODULES-METHOD-1 `orientation_root_first_ranking`); the product prints `CONTRIBUTING.rst, README.rst, docs/`. Ruled by the human 2026-09-22: extension matching case-insensitive (D-DD1-003, RG-BOOTSTRAP-INPUT-7); no `license` stem (hadoop's root `LICENSE.txt`/`NOTICE.txt` stay outside the inventory — closed). The `debian/install` residual (a bare `install` stem admits Debian packaging's path list — OpenXcom `install/debian/install`; 1 file in 17 corpora) — RULED by the human 2026-09-22: keep as is (not core to code understanding; an agent can tell a path list from prose); stated, never special-cased.

**Preservation obligations:**

| Id | Obligation | Proof |
|---|---|---|
| P-DD-01 | generated-marker exclusion, the vendored overlay, unreadable counting, the release-notes and license bases, the `.env` exclusion and the exhaust predicate are untouched (self_generated.rs, release_notes.rs, types.rs, docs_list_overlay.rs, the classification crate byte-identical) | DD-C03; DD-C06 |
| P-DD-02 | the human budget, the raw-passthrough `--json` parity, `docs extract`, the modules-list renderer and orient's doc relevance are untouched in CODE and rendering form (their outputs move only through the widened inventory — the inherited consequences above) | DD-C05; DD-C06 |
| P-DD-03 | every existing test in the touched crates stays green; a corpus the rule admits nothing new on (kafka) is byte-identical but for the honesty line | DD-C06; DD-C11; DD-C12 |
| P-DD-04 | isolation, cleanup, hygiene | DD-C07; DD-C14 |

## 1. Problem (ROOT-CAUSED — RC-8, docs/audits/2026-09-08-root-causes-v0.18.0.md; re-verified on HEAD b44a7c4)

`rust/crates/doc-facts/src/discovery.rs:126-162 is_doc_candidate` admits a file only by (A) nine exact, case-sensitive names in an inline array (:134-144: `README.md`, `README`, `ARCHITECTURE.md`, `CONTRIBUTING.md`, `CHANGELOG.md` and four compose names), (B) a MAP sidecar name, or (C) an extension in `[".md", ".txt", ".rst"]` (:102) under an ancestor literally `docs`/`doc`/`design` (:108, :113-117). `classification.rs:49-52` decides `readme` by exact `readme.md`/`readme`, so `README.rst` is a neutral `doc`; `classification.rs:129` spells the extension list a third time; `modules_method.rs:445-448` re-spells the orientation stems and :453 the doc directories; `agent/src/doc_relevance.rs:273-279` spells the root-doc names a fifth time. Nothing counts what the rule refuses. MEASURED on fresh isolated indexes (2026-09-21, HEAD binary): hadoop's 519 guides under `hadoop-*/src/site/markdown/` (`hadoop-common-project/hadoop-auth/src/site/markdown/Configuration.md`), its root `README.txt` and `BUILDING.txt`, django's `README.rst`/`CONTRIBUTING.rst`/`AUTHORS`/`INSTALL`, buildroot's 73 `docs/manual/*.adoc` and 233 non-root `readme.txt`/`README.txt` (225 under `board/**`, 8 elsewhere — OC-1 #5 corrected the earlier "233 `board/**/readme.txt`" label), leveldb's `AUTHORS`/`NEWS` all fall outside; hadoop's recommendation names no README and django's reads `read: docs/` only. Never worked (AUDIT5-MINORS-1 F1 changed kinds only).

## 2. Contract

### 2.1 The fix, at the cause — one stem constant, one extension list, one tree rule, one counted refusal, two consumers, one header line

1. **discovery.rs** (DD-C01): `pub const DOC_NAME_STEMS`, `pub const ORIENTATION_STEMS`, `pub const DOC_EXTENSIONS` (+ `.markdown`, `.adoc`), `pub fn doc_name_stem`, `in_docs_tree` + `src/site` (component `site` whose immediate parent is `src`), `is_doc_candidate` = `.env` | compose | MAP sidecar | stem | extension-in-tree; the walk counts refused markup files (never `.txt`; never a file under the root `.rgr/` tool-state directory — `self_generated::is_tool_state_path`, RG-REQ-011-L07); `discover_doc_files` returns `DocDiscovery { files, unscanned_markup_outside_docs_tree }`. Matching: stem AND extension case-insensitive (D-DD1-003, ratified 2026-09-22; the stem is the name minus a recognised documentation extension, or the whole name).
2. **classification.rs** (DD-C02): `readme` by `doc_name_stem == Some("readme")`; the neutral-doc extension rule reads `DOC_EXTENSIONS`; bare stems → `doc`.
3. **lib.rs / lib_tests.rs** (DD-C03): `DocInventoryResult.unscanned_markup_outside_docs_tree`; both discovery callers read `.files`; one `pub use discovery::{…}` re-export. `DiscoveryRule` + `discovery_rule()` are DEFINED in discovery.rs beside the constants they state (every field is the constant the walk applies) — lib.rs is 504 lines on HEAD and CLAUDE.md forbids appending a new responsibility to a file over 500 lines.
4. **modules_method.rs** (DD-C04): `is_named_orientation_file` reads `doc_name_stem` + `ORIENTATION_STEMS`; depth rule, ranking, cap, directory targets unchanged. orient_additive_fields.rs:165 comment corrected (`true`).
5. **dispatch.rs** (DD-C05): `handle_docs_list` payload gains the count and the rule when the count > 0; the rule value is built INFALLIBLY (F-DD1-001: `json!` over the constants or `to_value(..).expect(..)` — never `unwrap_or(Value::Null)`), and the check forbids every silent-fallback form (`unwrap_or(`, `unwrap_or_default`, `.ok()`) anywhere in the handler — the class rule, not the one reported line.
6. **rgr docs.rs / docs_tests.rs / orient_tests.rs** (DD-C05): two fields (the count; the rule as a typed `DiscoveryRuleView`), decoded as ONE fact — a payload decodes only when both keys are absent or both are present with the count > 0 and a well-formed rule; any other pairing is a named malformed-payload error at the single decode (`commands/docs.rs:163`, unchanged) that BOTH modes pass through, so neither the human line nor the raw `--json` passthrough can show a count without its rule; the header line `+N markdown/prose files outside a docs tree not scanned (--json for the rule)` (singular `file` for 1) after the unreadable line; `filtered_json_view` re-emits both; the orient-side recommendation test.
7. **Nothing else moves:** no schema, no CLI, no walk bound (MAX_DEPTH 10, SKIP_DIRS, symlinks, hidden dirs, `.gitignore` not honored — all pre-existing and untouched), no vendored/generated/unreadable/license/release-notes change, no agent-crate change.

### 2.2 Evidence taxonomy (one row = one bound test)

| Input | Outcome | Bound test / check |
|---|---|---|
| `README.txt`, `README.rst`, `CONTRIBUTING.rst`, `INSTALL`, `AUTHORS`, `NEWS`, `BUILDING.txt`, `ChangeLog` anywhere | admitted (stem, with or without a doc extension) | DD-C01 `stem_rule_admits_documentation_stems_with_or_without_a_doc_extension` |
| `README.html`, `install.sh`, `readme.txt.bak` | refused (extension not a doc extension) | DD-C01 `stem_with_a_non_doc_extension_is_refused` |
| `Readme.md`, `README.TXT`, `README.MD`, `docs/NOTES.MD` | all admitted (D-DD1-003) | DD-C01 `stem_and_extension_match_case_insensitively` |
| `docs/a.adoc`, `doc/b.markdown` | admitted | DD-C01 `adoc_and_markdown_admitted_inside_a_docs_tree` |
| `x/src/site/markdown/a.md` / `site/a.md`, `src/a.md`, `srcx/site/a.md` | admitted / refused | DD-C01 `src_site_convention_opens_a_docs_tree` |
| root `notes.md` (refused), root `CMakeLists.txt`, `docs/a.md` | count 1 (`.txt` never counted; admitted never counted) | DD-C01 `unscanned_counts_markup_outside_a_docs_tree_never_txt`; DD-C03 `inventory_reports_unscanned_markup_outside_docs_tree` |
| root `notes.md`, `.rgr/notes.md` (rmap's own tool-state dir) | count 1 (the exhaust predicate decides; never a second definition) | DD-C01 `unscanned_count_skips_the_rmap_tool_state_dir` |
| every orientation stem | is a doc-name stem | DD-C01 `orientation_stems_are_doc_name_stems` |
| the four compose names | still admitted by exact name | DD-C01 `compose_files_still_admitted_by_exact_name` |
| `README.rst`, `README.txt`, `README` | kind `readme` | DD-C02 `readme_kind_by_stem_for_rst_txt_and_bare` |
| `INSTALL`, `AUTHORS`, `NEWS`, `CHANGELOG` | kind `doc` (not `config`) | DD-C02 `bare_stem_documents_classify_doc_not_config` |
| `docs/a.markdown`, `docs/b.adoc` | kind `doc` | DD-C02 `markdown_and_adoc_in_a_docs_tree_classify_doc` |
| root `CONTRIBUTING.rst`, `docs/ARCHITECTURE.adoc`, `design/overview.txt` / root `INSTALL`, `docs/deep/ARCHITECTURE.md` | named orientation targets / not | DD-C04 `orientation_named_files_match_by_stem_with_any_doc_extension` |
| count 3 / count 1 / count 0 | the plural line / the singular line / no line | DD-C05 `list_render_states_unscanned_markup_outside_docs_tree`, `list_render_omits_the_unscanned_line_when_nothing_was_refused` |
| filtered `--json` with a count | count and rule re-emitted | DD-C05 `json_filtered_view_surfaces_unscanned_and_rule_when_present` |
| payload: count 3 + no rule / `null` rule / rule missing `stems`; rule without count; explicit count 0 — vs both keys absent | `Err` (named malformed payload, never rendered) — vs `Ok` (count 0, no line) | DD-C05 `malformed_unscanned_payload_is_rejected_never_rendered` |
| raw `--json` (no generated maps) with count 2 + rule / the same without the rule | decodes and passes through carrying the rule / does not decode — the command prints the error, not the payload | DD-C05 `raw_json_path_prints_only_payloads_whose_count_carries_the_rule` |
| orient with an orientation block | the recommendation line renders | DD-C05 `orient_renders_the_docs_recommendation_line` |
| corpora: hadoop, django, buildroot (moving); kafka, leveldb, grpc-java, FRAKTAG, repo-graph (controls) | §2.3 | DD-C08/C09/C10/C11/C12/C13 |

### 2.3 Outward proof (what a user of the product gains)

hadoop `docs list`: `22 documents` → `556 documents`, `doc 527 / readme 26 / architecture 2 / config 1`, `+3 vendored docs`, `+3 markdown/prose files outside a docs tree not scanned (--json for the rule)`; `modules list`/`orient --budget medium`: `No README or architecture doc found …` → `read: README.txt — or the tree.` django: `616 → 622`, `readme 7`, `doc 225`, `+4 markdown/prose files …`; `read: docs/` → `read: CONTRIBUTING.rst, README.rst, docs/ — or the tree.` buildroot: `4 → 310` (`readme 234`, `doc 75`), `+3 markdown/prose files …`. kafka: `133 documents` both, `+1 markdown/prose file …` added, everything else byte-identical. leveldb: `7 → 9` (`AUTHORS`, `NEWS`), no honesty line (nothing refused). grpc-java `49 → 50`, `+12 …`. FRAKTAG `14` both, `+11 vendored docs`. repo-graph before + 1 (`agent_docs/architecture.md`; 659 → 660 at the first admission). All reproduced by the first admission's candidate on 2026-09-22.

## 3. Regression watch

| Preserved | What would regress | Proof |
|---|---|---|
| RG-REQ-008-L03, P-DD-01 | generated-marker exclusion | DD-C03 (`inventory_flags_generated_only_with_marker`, self_generated.rs byte-identical); DD-C05 (`list_default_excludes_generated_maps_and_states_the_count`); DD-C08 and DD-C13 (the `N generated maps excluded (…)` line byte-identical before/after on hadoop — 17,114 marked sidecars — and on repo-graph) |
| RG-REQ-008-L04 | the vendored overlay | DD-C06 (`vendored_override_clears_release_family` inside daemon-runtime's whole `--lib`, overlay byte-identical); DD-C08/C09/C12 (`+N vendored docs` lines) |
| RG-REQ-008-L05 | unreadable counting | DD-C03; DD-C05 (`list_render_surfaces_unreadable_count`) |
| RG-REQ-008-L08, P-DD-02 | budget, JSON completeness, `docs extract` | DD-C05 (`entry_list_budgeted_with_remainder`, `family_lines_and_entries_share_one_budget`, `extract_render_shows_*` in the rgr lib); DD-C06 (rgr lib whole; `docs_command`; the daemon-backed `docs_extract_*` are `#[ignore]` — operator dogfood gate) |
| RG-REQ-008-L09 | a store read or a write into the target | DD-C03 (`env_files_excluded_from_inventory`); DD-C06 (no storage/agent change); DD-C08–C13 (the same root serves both binaries — no reindex) |
| RG-REQ-011-L07 | the exhaust predicate | DD-C01 (MAP sidecar rule, `.env` rule tests; `unscanned_count_skips_the_rmap_tool_state_dir` — the new count never reports rmap's own `.rgr/` state); DD-C03 (self_generated.rs byte-identical) |
| P-DD-03 | any existing test; kafka's surfaces | DD-C06; DD-C11; DD-C12 |
| RG-REQ-011-L06, P-DD-04 | isolation | DD-C14 |

## 4. Stop conditions

Frozen: the stem set and extension set exactly as RG-REQ-008-L01 states them (no eleventh stem, no `.mdx`, no exclusion heuristic such as a `debian/` rule — the residual is stated, not fixed); case-insensitive matching exactly as D-DD1-003 states it; the ranking, cap and depth rule of the recommendation; MAX_DEPTH, SKIP_DIRS, symlink and hidden-dir behaviour, `.gitignore` not honored; the generated, vendored, unreadable, license and release-notes bases; schema, CLI, wire protocol. Nothing outside the ten candidate paths (a literal site or inheriting surface the compiler or a test names outside them is a FINDING; the agent crate's fifth spelling is a named follow-up, never touched here). STANDING HONESTY RULES. If a measured literal in DD-C08–C13 differs from the product, STOP and report the file-level reason (the checkouts are live; the manager's walk is the prediction, the product is the truth). Unmet DoD → STOP. Do NOT commit. Run every check to completion in the FOREGROUND; end your turn only with the evidence object.

## 5. Validation (ORDERED; `build-progress.md` after EACH step)

0. PRECONDITION (the manager's, before admission): the document item — this SLICE_DOC, the INPUT-2 manifest, the OC-1 ledger — is committed (DD-C14's `git status` clause names only the ten candidate paths). On the CLEAN tree: record `git rev-parse HEAD`; `(cd rust && cargo build --release --bin rmap --bin rmapd)` (warm cache) → `mkdir -p /private/tmp/DOCS-DISCOVERY-1-before-bin && cp rust/target/release/rmap rust/target/release/rmapd /private/tmp/DOCS-DISCOVERY-1-before-bin/`; `registry-before <full sha256>` in build-progress.md; verify the eight `-before` roots exist (§7).
1. Failing tests FIRST (§2.2 names): discovery → classification → inventory → modules_method → rgr render/JSON → orient test.
2. Chunked gates DD-C01 → C02 → C03 → C04 → C05, then DD-C06 (whole suites incl. daemon-runtime's whole `--lib`, foreground) and DD-C07 — NEVER `cargo test --workspace`.
3. `(cd rust && cargo build --release --bin rmap --bin rmapd)` (candidate) → DD-C08 (hadoop) → C09 (django) → C10 (buildroot) → C11 (kafka, leveldb) → C12 (grpc-java, FRAKTAG) → C13 (repo-graph); every `/tmp/dd-*` capture from THIS cycle's run; no `-after` root is created (query-time walks).
4. DD-C14 (remove the before-bin dir; the eight `-before` roots STAY), hand-off with the evidence object (each check's outcome NESTED under `outcome`; `supportingEvidence` non-empty).

## 6. Definition of done

All fourteen checks pass; §2.3 holds on all eight corpora; the report carries the code-under-analysis examples: hadoop's `hadoop-common-project/hadoop-auth/src/site/markdown/Configuration.md` and root `README.txt` (BEFORE absent, AFTER `readme`), django's `README.rst`/`CONTRIBUTING.rst` with the BEFORE/AFTER recommendation lines, buildroot's `docs/manual/manual.adoc` and one `board/**/readme.txt`, one refused markup file per moving corpus (e.g. grpc-java `CODE-OF-CONDUCT.md`) with the rendered honesty line, and leveldb's `CMakeLists.txt` as the `.txt` that is deliberately not counted.

## 7. Corpus roots (pre-provisioned by the manager on 2026-09-21 with the base-revision binary at 100f2ae; rebuild recipe)

- `/private/tmp/DOCS-DISCOVERY-1-{hadoop,django,buildroot,kafka,leveldb,grpc-java,fraktag,repo-graph}-before`: fresh `rmap index` of `/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/{hadoop (185 s), django (18 s), buildroot (2 s), kafka (99 s), leveldb (1 s), grpc-java (18 s)}`, `/Users/apple/Documents/APLICATII BIJUTERIE/FRAKTAG` (1 s) and this repository (9 s); stdio transport, auto passes off.
- The docs surfaces read the store only for the repo's root path, so the builder serves the SAME roots with both binaries; the retained roots under `~/repo-graph-retained/` are never served; the operator registry is read only for its digest.

## 8. Follow-ups (not this slice)

DOC-RELEVANCE-STEMS-1: `agent/src/doc_relevance.rs:273-279 is_repo_root_doc` keeps a fifth root-doc name list (`README.md`, `README`, `ARCHITECTURE.md`, `CONTRIBUTING.md`) used by orient's doc ranking — blind to `README.rst`; sharing the constant needs an agent → doc-facts crate edge (a dependency-edge decision). DOCS-GITIGNORE-1: the walk does not honor `.gitignore` (repo-graph's `.agent-manager/**` is counted as unscanned prose; `packages/engine/fraktag-env` is walked). The `agent/src/storage_port.rs:1285` doc comment lists four kinds of seven (stale comment).

## 9. Baseline history

- INPUT-1 (2026-09-22): first admission baseline, grounded on the CC-8 measurement (RG-BOOTSTRAP-INPUT-7). Its first implementation admission (builder claude-opus-5-5, reviewer codex gpt-5.6-terra, cycle 1): the candidate reproduced every measured headline on all eight corpora and the builder STOPPED correctly on two drifted predictions instead of bending an oracle; the review found ONE code defect (F-DD1-001: the `discovery_rule` value fell back to a silent `null` on a serialization error) and SIX manager oracle defects (F-DD1-002: DD-C05's cargo filter selected zero tests; DD-C08/C09 grepped the small-budget `orient`, which has no modules section; DD-C08 counted 520 `src/site` entries against 519; DD-C09 predicted django's kinds from the added files only — `docs/README.rst` reclassifies `readme`; DD-C10's board predicate was case-sensitive; DD-C11 had a heredoc terminator mid-chain and literal repo-graph counts on a live checkout) and raised the orient-depth decision. Candidate preserved as `candidate-1.patch` in the superseded record.
- INPUT-2 (2026-09-22): OC-1 (the six oracle corrections; DD-C11 split into DD-C11/C12/C13 with every heredoc last; DD-C13 relative), the F-DD1-001 fix direction in DD-C05/§2.1.5, D-DD1-004 recorded; ledger docs/assurance/DOCS-DISCOVERY-1/oracle-corrections.md (pinned). Document item DOCS-DISCOVERY-1-PREP-2 (requirements author, 2026-09-22; read-only verification of every citation, test name, filter, literal site and checkout file on HEAD b44a7c4 plus the first admission's evidence) corrected, in these two files only: (1) DD-C05's F-DD1-001 grep closed as a CLASS — the whole `handle_docs_list` body carries no `unwrap_or(`/`unwrap_or_default`/`.ok()` and both keys are present (the `-A3` draft passed vacuously when the key was built elsewhere); the unreadable-gate citation :5721-5723; `discovery_rule` re-emitted as received by the filtered view; (2) `DiscoveryRule`/`discovery_rule()` placed in discovery.rs, not lib.rs (504 lines — CLAUDE.md's 500-line guardrail), every field the constant the walk applies; (3) the unscanned count excludes the root `.rgr/` tool-state directory by the existing exhaust predicate (RG-REQ-011-L07) — one new test, no field number moves; (4) DD-C06 runs daemon-runtime's WHOLE `--lib` (CLAUDE.md relay gate) and its expected no longer claims the inheriting surfaces' OUTPUTS are unchanged — their code and form are; `docs extract` and orient's Docs ranking read the widened inventory (§0 inherited consequences); (5) DD-C08/DD-C13 assert the `N generated maps excluded` line byte-identical before/after — the RG-REQ-008-L03 field proof §3 had claimed for DD-C13 without an assertion; (6) DD-C08's kind decomposition re-derived (readme = 17 + root + 8 module `README.txt`; doc = 3 + 519 + two `BUILDING.txt` + native AUTHORS/ChangeLog/NEWS) by a walk that reproduces 556 = 527/26/2/1; (7) §1's "233 `board/**/readme.txt`" relabelled (OC-1 #5); (8) DD-C14's precondition (the document item committed first) stated. Review 0 of this document item (codex, refinement-required; F-RG-REQ-008-L06-RAW-JSON-RULE, correctness): the raw `--json` passthrough (`filtered_json_view` → `None` → the daemon payload printed as received) could show a positive count with a missing or `null` rule under a human line that points at it — and item (1)'s "re-emitted as received (`null` if a version-skewed daemon omitted it)" permitted exactly that. Closed as a CLASS by one rule: the count and the rule are ONE fact, validated at the single decode both modes pass through (`commands/docs.rs:163`, unchanged; it already exits `EXIT_RUNTIME_ERROR` on a decode error) — a payload decodes only when both keys are absent or both are present with the count > 0 and a well-formed typed `DiscoveryRuleView`; two new DD-C05 tests (`malformed_unscanned_payload_is_rejected_never_rendered`, `raw_json_path_prints_only_payloads_whose_count_carries_the_rule`); item (1)'s `null` allowance is withdrawn.
- INPUT-3 (2026-09-22): OC-2 after the second implementation admission (builder claude-opus-5-5, cycle 1: 13/14 checks passed, the product complete): DD-C06's `! grep -E 'FAILED|panicked'` matched two PASSING pre-existing daemon-runtime test NAMES (`panicked_livegraph_records_unknown_edges_not_zero`, `invalid_regex_is_reported_not_panicked`) — every cargo check now matches Rust's failure MARKERS (`^test .* FAILED$|^thread .* panicked at|^test result: FAILED`), never words; the hadoop generated-maps figure corrected to the observed 6,813; the report's hadoop witness must cite `Configuration.md:15` (F-DD1-WITNESS-LINE). Candidate preserved as `candidate-2.patch` in the superseded INPUT-2 record; no code change expected at the third admission.
