<!-- requirements-assurance-implementation-v1
{
  "formatVersion": 1,
  "kind": "implementation-allocation",
  "workItemId": "CPP-ATTRIBUTE-MACRO-1A",
  "baselinePath": "docs/requirements/baselines/CPP-ATTRIBUTE-MACRO-1A-INPUT-1.json",
  "parentRequirementIds": [
    "RG-REQ-001",
    "RG-REQ-002",
    "RG-REQ-011"
  ],
  "implements": [
    "RG-REQ-001-L01"
  ],
  "preserves": [
    "RG-REQ-001-L02",
    "RG-REQ-001-L03",
    "RG-REQ-001-L04",
    "RG-REQ-001-L05",
    "RG-REQ-001-L07",
    "RG-REQ-001-L08",
    "RG-REQ-001-L09",
    "RG-REQ-001-L10",
    "RG-REQ-002-L01",
    "RG-REQ-002-L02",
    "RG-REQ-002-L03",
    "RG-REQ-002-L04",
    "RG-REQ-002-L05",
    "RG-REQ-002-L06",
    "RG-REQ-002-L07",
    "RG-REQ-002-L08",
    "RG-REQ-002-L09",
    "RG-REQ-002-L10",
    "RG-REQ-011-L06"
  ],
  "preservationObligationIds": [
    "P-CAM-01",
    "P-CAM-02",
    "P-CAM-03"
  ],
  "changes": [
    "RG-REQ-002-L11",
    "RG-REQ-001-L06"
  ],
  "acceptanceBoundary": "The cpp-extractor test module (whole), the storage members read, the agent explain integration tests, rgr's explain renderer tests, and SQL comparison of fresh isolated leveldb and OpenXcom stores (candidate binary) against the manager's before-stores (v0.19.0 binary), `explain` captures on the leveldb after-root, and `orient`/`check` captures on a builder-owned COPY of the leveldb before-root served by the candidate (the toolchain-staleness consequence), all in CAM-A04, as named per check.",
  "candidatePaths": [
    "rust/crates/cpp-extractor/src/extractor.rs",
    "rust/crates/storage/src/agent_impl.rs",
    "rust/crates/agent/src/storage_port.rs",
    "rust/crates/agent/src/dto/signal.rs",
    "rust/crates/agent/src/explain/mod.rs",
    "rust/crates/agent/tests/explain_symbol.rs",
    "rust/crates/rgr/src/presentation/explain_sections.rs",
    "rust/crates/rgr/src/presentation/explain.rs"
  ],
  "postReviewRecordPaths": [
    "docs/assurance/CPP-ATTRIBUTE-MACRO-1A/verification.json",
    "docs/assurance/CPP-ATTRIBUTE-MACRO-1A/implementation-review.json"
  ],
  "candidateExclusions": [
    {
      "pathPrefix": ".agent-manager/",
      "reason": "local relay state and raw run/progress evidence"
    },
    {
      "pathPrefix": "rust/target/",
      "reason": "reproducible Cargo build output"
    }
  ],
  "checks": [
    {
      "checkId": "CAM-A01",
      "obligationIds": [
        "RG-REQ-001-L01",
        "RG-REQ-002-L11",
        "P-CAM-02"
      ],
      "owner": "builder",
      "method": {
        "kind": "command",
        "command": "cargo test -p repo-graph-cpp-extractor  2>&1 | tee /tmp/cama-01.txt | grep -E '^test result: ok\\.' && ! grep -E '^test .* FAILED$|^thread .* panicked at|^test result: FAILED' /tmp/cama-01.txt && for t in field_with_trailing_attribute_macro_naming_a_member_is_a_typed_data_member_not_a_node pointer_field_with_trailing_attribute_macro_naming_a_member_records_the_member_type definition_with_trailing_attribute_macro_is_stored_once_marked_with_both_identity_candidates split_prototype_and_typeless_macro_definition_are_both_kept_and_the_definition_is_marked atomic_counter_class_yields_six_nodes_three_marked_and_no_field_phantom constructor_and_destructor_shapes_are_marked_with_both_candidates ambiguous_shape_without_member_evidence_is_kept_and_marked_never_silently_renamed two_line_trailing_macro_on_a_prototype_stays_one_unmarked_declaration terminated_prototype_and_a_following_typeless_definition_are_not_paired q_object_body_macro_emits_nothing every_marked_node_carries_undetermined_basis_and_two_distinct_candidates method_in_class constructor_detected destructor_detected error_sibling_of_declarator_does_not_trigger_macro_name_extraction zstd_attribute_macro_function_name_is_the_declarator_not_the_macros macro_call_shape_function_name_pcre2_priv forward_decl_class_is_flagged_definition_is_not in_class_method_prototype_is_flagged_out_of_line_def_is_not receiver_type_field_declaration_records_declared_type leveldb_export_db_class_not_function macro_struct_with_base_is_struct_named_bar_not_function file_node_has_correct_stable_key; do grep -qE \"^test .*$t .* ok$\" /tmp/cama-01.txt || { echo \"MISSING $t\"; exit 1; }; done && test \"$(grep -cE '^test extractor::tests::.* ok$' /tmp/cama-01.txt)\" -ge 77 && ! awk '/^#\\[cfg\\(test\\)\\]/{exit} {print}' crates/cpp-extractor/src/extractor.rs | grep -qE 'GUARDED_BY|LOCKS_EXCLUDED|EXCLUSIVE_LOCKS_REQUIRED|EXCLUSIVE_LOCK_FUNCTION|UNLOCK_FUNCTION|ASSERT_EXCLUSIVE_LOCK|is_ascii_uppercase|to_uppercase|is_uppercase' && test \"$(awk '/^#\\[cfg\\(test\\)\\]/{exit} {print}' crates/cpp-extractor/src/extractor.rs | grep -cE 'fn (macro_call_shape|error_child_name)\\(')\" = 2 && grep -qE '^const EXTRACTOR_NAME: &str = \"cpp-core:0.2.0\";$' crates/cpp-extractor/src/extractor.rs && test \"$(grep -c 'cpp-core:0.1.0' crates/cpp-extractor/src/extractor.rs)\" = 0",
        "cwd": "rust",
        "environment": "candidate tree",
        "inputs": "the WHOLE cpp-extractor test run (66 `extractor::tests` today — `cargo test -p repo-graph-cpp-extractor -- --list`, 2026-09-23 — + eleven NEW; the floor asserts ≥ 77 so no existing test vanishes; every existing test kept verbatim, twelve bound by name incl. `file_node_has_correct_stable_key`). Fixtures are the corpus lines VERBATIM (recovery is context-dependent: `class LOCKABLE Mutex {` and `class SCOPED_LOCKABLE MutexLock {` parse as a `function_definition` whose body is a `compound_statement`, so their members are `function_definition`/`declaration`/`labeled_statement`/`ERROR` children, not `field_declaration`s): leveldb 7ee830d db/db_impl.h:187/189 (in a minimal class with `port::Mutex mutex_;` and an in-body call), port/port_stdcxx.h:51-66, util/mutexlock.h:23-35, db/db_test.cc:43-63, util/env_posix.cc:499-516, db/db_impl.h:113-114; vcmi 93dd4ee launcher/gamepadHandler.h:16-29 (rule 10); synthetic probes for the no-evidence shapes and the `;`-terminated case. MARKER (SLICE_DOC §2.1 rule 2): a marked node's `metadata_json` carries all three of `identity: \"undetermined\"`, `basis: \"macro_recovery_ambiguous_identity\"` and `identity_candidates: [<displaced>, <declarator-slot>]`, ordered STRUCTURALLY — the identifier recovery displaced into the ERROR or the paired prototype first, the identifier in the declarator slot (today's node name) second; the order asserts nothing about which one is real. The rules: (1) `field_with_trailing_attribute_macro_naming_a_member_is_a_typed_data_member_not_a_node` — `SnapshotList snapshots_ GUARDED_BY(mutex_);` where `mutex_` is a data member: NO node, and the in-body `snapshots_.empty()` CALLS edge carries `\"receiverType\":\"SnapshotList\"` (RG-REQ-001-L01 as amended: 'a field whose trailing macro argument names a data member of the class is a data member'); (2) the pointer variant `WriteBatch* tmp_batch_ GUARDED_BY(mutex_);` → receiverType `WriteBatch`, no node, and `class_field_types` never records `GUARDED_BY` (today `descend_to_field_identifier` walks the `pointer_declarator` into the `function_declarator` and records `GUARDED_BY`, :2602-2620); (3) `definition_with_trailing_attribute_macro_is_stored_once_marked_with_both_identity_candidates` — port_stdcxx.h:51-66: exactly ONE node per member, named as today (`EXCLUSIVE_LOCK_FUNCTION`, `UNLOCK_FUNCTION`, `ASSERT_EXCLUSIVE_LOCK` — the extractor never picks), each carrying the marker with `identity_candidates` = [`Lock`,`EXCLUSIVE_LOCK_FUNCTION`] / [`Unlock`,`UNLOCK_FUNCTION`] / [`AssertHeld`,`ASSERT_EXCLUSIVE_LOCK`] (:59/:60 shape (2a) — the identifier inside the ERROR's `init_declarator`; :61 shape (2b) — the preceding `declaration(init_declarator(AssertHeld, ()))` with no `;`), no node named `Lock`; (4) `split_prototype_and_typeless_macro_definition_are_both_kept_and_the_definition_is_marked` — env_posix.cc:499-516: `Insert` (decl, :501, unmarked) AND `LOCKS_EXCLUDED` (:501-506, marked [`Insert`,`LOCKS_EXCLUDED`] — the immediately preceding prototype with only whitespace between is the candidate); likewise `Remove`; NO merge, NO rename; (5) `atomic_counter_class_yields_six_nodes_three_marked_and_no_field_phantom` — db_test.cc:43-63: exactly {`AtomicCounter` CONSTRUCTOR, `Increment`, `IncrementBy` (decl, unmarked), `LOCKS_EXCLUDED` ×3 marked [`IncrementBy`|`Read`|`Reset`, `LOCKS_EXCLUDED`]}, NO `GUARDED_BY` node (`count_` is a typed member); (6) `constructor_and_destructor_shapes_are_marked_with_both_candidates` — mutexlock.h:23-35: the :25 typeless definition marked [`MutexLock`,`EXCLUSIVE_LOCK_FUNCTION`] (its prototype a `declaration` inside the `public:` `labeled_statement`), the :28 node marked [`~MutexLock`,`UNLOCK_FUNCTION`] (its prototype an `ERROR` holding a `function_declarator` whose declarator is a `destructor_name` — the `~` kept), the deleted copy constructor present; (7) `ambiguous_shape_without_member_evidence_is_kept_and_marked_never_silently_renamed` — `class C { int MYAPI foo(Foo x); int seed_ MACRO(Foo); };` with no member named `Foo`/`x`: node `foo` marked [`MYAPI`,`foo`], node `MACRO` marked [`seed_`,`MACRO`] — today's names kept, both candidates recorded, nothing withheld; (8) `two_line_trailing_macro_on_a_prototype_stays_one_unmarked_declaration` — db_impl.h:113-114 `Recover`: one node, forward_decl, NO marker key; (9) `terminated_prototype_and_a_following_typeless_definition_are_not_paired` — `class T { public: void helper(); TEST_METHOD(A) { helper(); } };`: `helper` unmarked (its `;` is a real token), `TEST_METHOD` unmarked (today's reading, §8); (10) `q_object_body_macro_emits_nothing` — vcmi gamepadHandler.h:16-29: no node named `Q_OBJECT`; (11) `every_marked_node_carries_undetermined_basis_and_two_distinct_candidates` — across the fixtures of (3)–(7), every node carrying any of the three marker keys carries all three: `identity` = `undetermined`, `basis` = `macro_recovery_ambiguous_identity`, and exactly two distinct non-empty strings; no unmarked node carries any of them. Negated grep: no macro token literal and no case-class predicate in the non-test part (empty on HEAD); the two CPP-DECLARATORS-1 shape functions still present (2 on HEAD). The doc comment of `error_sibling_of_declarator_does_not_trigger_macro_name_extraction` corrected (its 'only reachable other ERROR' claim is false), assertion untouched. VERSION (D-REFRESH-STALE-1 = B; RG-REQ-001-L06 as amended by INPUT-9): the marker changes what a fresh extraction produces for content-unchanged files, which delta refresh copies forward, so `EXTRACTOR_NAME` is bumped `cpp-core:0.1.0` → `cpp-core:0.2.0` (extractor.rs:26, the only occurrence in the crate; `grep -c` = 1 on HEAD) and existing stores that serve C++ files show TOOLCHAIN-STALENESS-1's line until `rmap repo rebuild <path>`; a fresh index (CAM-A04's after-roots) is current."
      },
      "expected": "exit 0: on every macro recovery shape the extractor names nothing by choice — a field with member evidence becomes a typed member (no node), an ambiguous member is stored once, marked `identity: undetermined` with its basis and BOTH identity candidates (RG-REQ-001-L01 as amended; RG-REQ-002-L11's store-side part only — L11 stays PARTIAL until 1B, §0), a clear declaration stays unmarked; every pre-existing cpp-extractor test remains green (no behavior change for CPP-DECLARATORS-1's shapes, receiver typing, the forward-decl carrier, stable keys); the extractor's version constant is `cpp-core:0.2.0` and no `cpp-core:0.1.0` literal remains in the crate"
    },
    {
      "checkId": "CAM-A02",
      "obligationIds": [
        "RG-REQ-002-L11",
        "RG-REQ-001-L05",
        "P-CAM-02",
        "RG-REQ-001-L04",
        "RG-REQ-001-L07",
        "RG-REQ-001-L08",
        "RG-REQ-001-L09",
        "RG-REQ-001-L10",
        "RG-REQ-002-L01",
        "RG-REQ-002-L02",
        "RG-REQ-002-L03",
        "RG-REQ-002-L04",
        "RG-REQ-002-L05",
        "RG-REQ-002-L06",
        "RG-REQ-002-L07",
        "RG-REQ-002-L08",
        "RG-REQ-002-L09",
        "RG-REQ-002-L10"
      ],
      "owner": "builder",
      "method": {
        "kind": "command",
        "command": "cargo test -p repo-graph-storage --test agent_impl list_members_of_type 2>&1 | tee /tmp/cama-02a.txt | grep -E '^test result: ok\\.' && ! grep -E '^test .* FAILED$|^thread .* panicked at|^test result: FAILED' /tmp/cama-02a.txt && for t in list_members_of_type_returns_direct_members_only list_members_of_type_orders_focus_file_first_then_other_files_by_path_and_line list_members_of_type_marks_forward_declarations list_members_of_type_reads_the_identity_marker_and_a_partial_or_malformed_marker_is_an_error; do grep -qE \"^test .*$t .* ok$\" /tmp/cama-02a.txt || { echo \"MISSING $t\"; exit 1; }; done && cargo test -p repo-graph-agent --test explain_symbol 2>&1 | tee /tmp/cama-02b.txt | grep -E '^test result: ok\\.' && ! grep -E '^test .* FAILED$|^thread .* panicked at|^test result: FAILED' /tmp/cama-02b.txt && for t in explain_type_focus_emits_members_and_referenced_by explain_members_carry_the_undetermined_identity_marker_verbatim; do grep -qE \"^test .*$t .* ok$\" /tmp/cama-02b.txt || { echo \"MISSING $t\"; exit 1; }; done && cargo test -p repo-graph-rgr --lib presentation::explain 2>&1 | tee /tmp/cama-02c.txt | grep -E '^test result: ok\\.' && ! grep -E '^test .* FAILED$|^thread .* panicked at|^test result: FAILED' /tmp/cama-02c.txt && for t in explain_members_section_anchors_file_line_and_marks_decl member_row_with_identity_candidates_renders_the_undetermined_marker members_section_with_a_malformed_identity_marker_renders_the_unreadable_line members_section_with_a_partial_single_or_duplicate_candidate_marker_renders_the_unreadable_line; do grep -qE \"^test .*$t .* ok$\" /tmp/cama-02c.txt || { echo \"MISSING $t\"; exit 1; }; done && git diff --quiet HEAD -- crates/daemon-runtime crates/repo-index crates/repo-graph-livegraph crates/indexer crates/classification crates/trust crates/module-queries crates/repo-graph-seed crates/c-extractor crates/ts-extractor crates/python-extractor crates/java-extractor crates/rust-extractor crates/rgr/src/commands crates/rgr/src/presentation/seed.rs",
        "cwd": "rust",
        "environment": "candidate tree",
        "inputs": "the vertical from the store to the reader. STORAGE: `list_members_of_type` (storage/src/agent_impl.rs:1488-1580) reads `json_extract(metadata_json, '$.identity')`, `'$.identity_candidates'` and `'$.basis'` beside the existing `'$.forward_decl'`: all three absent → a determined member; all three present and well-formed (`identity` = `undetermined`, an array of exactly two distinct non-empty strings, a non-empty basis string) → `AgentMemberEntry` carries the undetermined identity as ONE all-or-nothing value (candidates + basis — the port never carries half a marker); any other state (one or two keys present, a non-array, fewer or more than two entries, a non-string, empty or duplicate entry, another `identity` value) is an `AgentStorageError` — the read fails, exactly as a present-but-unreadable `forward_decl` fails today (`list_members_of_type_marks_forward_declarations` asserts `is_err`, storage/tests/agent_impl.rs:1619-1637), never silently read as determined (RG-REQ-002-L04). AGENT: `explain/mod.rs:566-580` passes it through to `ExplainMemberItem` (dto/signal.rs:922-931), serialized as three flat keys `identity: \"undetermined\"`, `identity_candidates`, `identity_basis`, all ABSENT for a determined member (`skip_serializing_if`), so a determined member's JSON is byte-identical to today. RGR: `render_member_row` (explain_sections.rs:160-195) renders `<name> (<subtype>, identity undetermined — 2 candidates: <c1>, <c2>)  <path>:<line>` (`<name> (<subtype>, decl, identity undetermined — …)` when both apply; `<name> (identity undetermined — …)  <path>` when subtype is absent); the three keys absent/null → today's row byte-identical; any other state (a partial marker, a non-array, not exactly two distinct non-empty strings, a non-string basis, an `identity` value other than `undetermined`) returns `None`, so `render_members` (:306-337) renders the WHOLE section as its existing single `members unreadable on this snapshot` line (F-ETS-01's whole-section rule) — never a coerced or silently shorter row. The existing tests named stay green verbatim; the five `AgentMemberEntry {` literals (storage/src/agent_impl.rs:1556; agent/tests/explain_symbol.rs:513/521/584/618 — the sixth grep hit, agent/src/storage_port.rs:1253, is the struct definition) and the one `ExplainMemberItem {` literal (explain/mod.rs:570) gain the field; the test fakes that return members by clone (agent/tests/common/mod.rs:593, agent/src/aggregators/complexity_tests.rs:316) and the daemon-runtime spies that delegate (explain_serve_tests/spy.rs:301, orient_serve/tests.rs:556) need no edit. Byte-identity (git diff --quiet): daemon-runtime (the payload is serde of the agent DTO — no daemon-runtime file names `ExplainMemberItem`/`ExplainMembersEvidence`; `dispatch.rs` is not touched), repo-index and repo-graph-livegraph (the indexing pipeline and the LiveGraph serving path), the resolver, classification, trust, module-queries, the seed crate (`find` is not changed in this slice — §8), every other extractor, rgr's command layer and the seed renderer. Every RG-REQ-002 L01–L10 honesty obligation and RG-REQ-001-L04/L07–L10 are PRESERVED by leaving their machinery untouched (RG-REQ-001-L06's staleness comparison and its orient/check attachment are TOOLCHAIN-STALENESS-1's code in agent and daemon-runtime, untouched here; the slice's intended L06 movement — existing C++ stores reading stale — is CAM-A04's staleness part): the crates in the diff --quiet list carry the unknown-never-zero, coverage, degradation, label and overlay rules and the reproducibility/registry/is_test/generated-flag machinery; the rgr explain tests named keep the unreadable pattern (L04) for the new keys; a determined member's JSON is byte-identical (L02 surfaces agree)."
      },
      "expected": "exit 0: a marked member reaches the agent on `explain` (RG-REQ-002-L11's `explain` part only — L11 stays PARTIAL until 1B covers `find`, §0) with the `identity undetermined` marker, its basis and both candidates, a determined member renders exactly as before, a partial or malformed marker fails loudly (a storage error at the read, the unreadable section at the render) and is never coerced (no behavior change outside the marked rows); identity hand-back (RG-REQ-001-L05) unchanged — the node keeps today's name and stable key; every RG-REQ-002 L01–L10 rule and the RG-REQ-001 machinery outside the marked rows remain exactly as before (no behavior change)"
    },
    {
      "checkId": "CAM-A03",
      "obligationIds": [
        "P-CAM-03"
      ],
      "owner": "builder",
      "method": {
        "kind": "command",
        "command": "cargo fmt --check -p repo-graph-cpp-extractor -p repo-graph-storage -p repo-graph-agent -p repo-graph-rgr && cargo clippy -p repo-graph-cpp-extractor -p repo-graph-storage -p repo-graph-agent -p repo-graph-rgr --all-targets -- -D warnings > /tmp/cama-03.txt 2>&1; rc=$?; tail -3 /tmp/cama-03.txt; test $rc -eq 0",
        "cwd": "rust",
        "environment": "candidate tree",
        "inputs": "rustfmt and clippy (deny warnings, all targets) over the four touched crates"
      },
      "expected": "exit 0: no formatting or lint debt enters with the candidate"
    },
    {
      "checkId": "CAM-A04",
      "obligationIds": [
        "RG-REQ-001-L01",
        "RG-REQ-002-L11",
        "RG-REQ-001-L04",
        "RG-REQ-001-L02",
        "RG-REQ-001-L03",
        "RG-REQ-001-L07",
        "RG-REQ-001-L08",
        "P-CAM-01",
        "RG-REQ-001-L06",
        "RG-REQ-011-L06"
      ],
      "owner": "builder",
      "method": {
        "kind": "command",
        "command": "git cat-file -e HEAD:docs/assurance/TOOLCHAIN-STALENESS-1/implementation-review.json && git show HEAD:docs/assurance/TOOLCHAIN-STALENESS-1/implementation-review.json | python3 -c \"import json,sys; d=json.load(sys.stdin); sys.exit(0 if (d.get('workItemId'), d.get('result')) == ('TOOLCHAIN-STALENESS-1', 'accepted') else 1)\" && (cd rust && cargo build --release --bin rmap --bin rmapd > /tmp/cama-build.txt 2>&1; rc=$?; tail -1 /tmp/cama-build.txt; exit $rc) && test ! -e /private/tmp/CPP-ATTRIBUTE-MACRO-1A-leveldb-after && mkdir /private/tmp/CPP-ATTRIBUTE-MACRO-1A-leveldb-after && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/leveldb' && export RMAP_STATE_ROOT=/private/tmp/CPP-ATTRIBUTE-MACRO-1A-leveldb-after RMAP_SOCKET_PATH=/private/tmp/CPP-ATTRIBUTE-MACRO-1A-leveldb-after/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release:$PATH\" \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" index > /tmp/cama-ldb-index.txt 2>&1; echo $? > /tmp/cama-ldb-index.txt.rc ) && test \"$(cat /tmp/cama-ldb-index.txt.rc)\" = 0 && test ! -e /private/tmp/CPP-ATTRIBUTE-MACRO-1A-openxcom-after && mkdir /private/tmp/CPP-ATTRIBUTE-MACRO-1A-openxcom-after && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/OpenXcom' && export RMAP_STATE_ROOT=/private/tmp/CPP-ATTRIBUTE-MACRO-1A-openxcom-after RMAP_SOCKET_PATH=/private/tmp/CPP-ATTRIBUTE-MACRO-1A-openxcom-after/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release:$PATH\" \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" index > /tmp/cama-ox-index.txt 2>&1; echo $? > /tmp/cama-ox-index.txt.rc ) && test \"$(cat /tmp/cama-ox-index.txt.rc)\" = 0 && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/leveldb' && export RMAP_STATE_ROOT=/private/tmp/CPP-ATTRIBUTE-MACRO-1A-leveldb-after RMAP_SOCKET_PATH=/private/tmp/CPP-ATTRIBUTE-MACRO-1A-leveldb-after/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release:$PATH\" \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" explain 'leveldb::AtomicCounter' > /tmp/cama-ldb-explain.txt 2> /tmp/cama-ldb-explain.txt.err; echo $? > /tmp/cama-ldb-explain.txt.rc ) && test \"$(cat /tmp/cama-ldb-explain.txt.rc)\" = 0 && grep -qE '^Members \\(6\\)$' /tmp/cama-ldb-explain.txt && grep -qE '^  - LOCKS_EXCLUDED \\(METHOD, identity undetermined — 2 candidates: IncrementBy, LOCKS_EXCLUDED\\)  db/db_test\\.cc:47$' /tmp/cama-ldb-explain.txt && grep -qE '^  - LOCKS_EXCLUDED \\(METHOD, identity undetermined — 2 candidates: Read, LOCKS_EXCLUDED\\)  db/db_test\\.cc:51$' /tmp/cama-ldb-explain.txt && grep -qE '^  - LOCKS_EXCLUDED \\(METHOD, identity undetermined — 2 candidates: Reset, LOCKS_EXCLUDED\\)  db/db_test\\.cc:55$' /tmp/cama-ldb-explain.txt && grep -qE '^  - IncrementBy \\(METHOD, decl\\)  db/db_test\\.cc:47$' /tmp/cama-ldb-explain.txt && ! grep -q 'GUARDED_BY' /tmp/cama-ldb-explain.txt && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/leveldb' && export RMAP_STATE_ROOT=/private/tmp/CPP-ATTRIBUTE-MACRO-1A-leveldb-after RMAP_SOCKET_PATH=/private/tmp/CPP-ATTRIBUTE-MACRO-1A-leveldb-after/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release:$PATH\" \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" explain 'leveldb::DBImpl' > /tmp/cama-ldb-dbimpl.txt 2> /tmp/cama-ldb-dbimpl.txt.err; echo $? > /tmp/cama-ldb-dbimpl.txt.rc ) && test \"$(cat /tmp/cama-ldb-dbimpl.txt.rc)\" = 0 && grep -qE '^Members \\(69\\)$' /tmp/cama-ldb-dbimpl.txt && ! grep -q 'GUARDED_BY' /tmp/cama-ldb-dbimpl.txt && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/leveldb' && export RMAP_STATE_ROOT=/private/tmp/CPP-ATTRIBUTE-MACRO-1A-leveldb-after RMAP_SOCKET_PATH=/private/tmp/CPP-ATTRIBUTE-MACRO-1A-leveldb-after/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release:$PATH\" \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" explain 'leveldb::AtomicCounter' --json > /tmp/cama-ldb-explain.json 2> /tmp/cama-ldb-explain.json.err; echo $? > /tmp/cama-ldb-explain.json.rc ) && test \"$(cat /tmp/cama-ldb-explain.json.rc)\" = 0 && test -d /private/tmp/CPP-ATTRIBUTE-MACRO-1A-leveldb-after && test ! -e /private/tmp/CPP-ATTRIBUTE-MACRO-1A-leveldb-stale && M0=\"$(shasum -a 256 /private/tmp/CPP-ATTRIBUTE-MACRO-1-leveldb-before/registry.json /private/tmp/CPP-ATTRIBUTE-MACRO-1-leveldb-before/databases/*)\" && cp -R /private/tmp/CPP-ATTRIBUTE-MACRO-1-leveldb-before /private/tmp/CPP-ATTRIBUTE-MACRO-1A-leveldb-stale && sed -i '' 's#/private/tmp/CPP-ATTRIBUTE-MACRO-1-leveldb-before/#/private/tmp/CPP-ATTRIBUTE-MACRO-1A-leveldb-stale/#g' /private/tmp/CPP-ATTRIBUTE-MACRO-1A-leveldb-stale/registry.json && ! grep -q 'CPP-ATTRIBUTE-MACRO-1-leveldb-before' /private/tmp/CPP-ATTRIBUTE-MACRO-1A-leveldb-stale/registry.json && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/leveldb' && export RMAP_STATE_ROOT=/private/tmp/CPP-ATTRIBUTE-MACRO-1A-leveldb-stale RMAP_SOCKET_PATH=/private/tmp/CPP-ATTRIBUTE-MACRO-1A-leveldb-stale/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release:$PATH\" \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" orient > /tmp/cama-06-stale-orient.txt 2> /tmp/cama-06-stale-orient.txt.err; echo $? > /tmp/cama-06-stale-orient.txt.rc ) && test \"$(cat /tmp/cama-06-stale-orient.txt.rc)\" = 0 && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/leveldb' && export RMAP_STATE_ROOT=/private/tmp/CPP-ATTRIBUTE-MACRO-1A-leveldb-stale RMAP_SOCKET_PATH=/private/tmp/CPP-ATTRIBUTE-MACRO-1A-leveldb-stale/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release:$PATH\" \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" check > /tmp/cama-06-stale-check.txt 2> /tmp/cama-06-stale-check.txt.err; echo $? > /tmp/cama-06-stale-check.txt.rc ) && test \"$(cat /tmp/cama-06-stale-check.txt.rc)\" = 2 && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/leveldb' && export RMAP_STATE_ROOT=/private/tmp/CPP-ATTRIBUTE-MACRO-1A-leveldb-stale RMAP_SOCKET_PATH=/private/tmp/CPP-ATTRIBUTE-MACRO-1A-leveldb-stale/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release:$PATH\" \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" check --json > /tmp/cama-06-stale-check.json 2> /tmp/cama-06-stale-check.json.err; echo $? > /tmp/cama-06-stale-check.json.rc ) && test \"$(cat /tmp/cama-06-stale-check.json.rc)\" = 2 && ( cd '/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/leveldb' && export RMAP_STATE_ROOT=/private/tmp/CPP-ATTRIBUTE-MACRO-1A-leveldb-after RMAP_SOCKET_PATH=/private/tmp/CPP-ATTRIBUTE-MACRO-1A-leveldb-after/d.sock RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off && PATH=\"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release:$PATH\" \"/Users/apple/Documents/APLICATII BIJUTERIE/repo-graph/rust/target/release/rmap\" check --json > /tmp/cama-06-after-check.json 2> /tmp/cama-06-after-check.json.err; echo $? > /tmp/cama-06-after-check.json.rc ) && echo \"REPORTED after-root check exit $(cat /tmp/cama-06-after-check.json.rc)\" && grep -qE 'C\\+\\+ facts from cpp-core:0\\.1\\.0 \\(current cpp-core:0\\.2\\.0\\) — run rmap repo rebuild .+ to refresh' /tmp/cama-06-stale-orient.txt && grep -qE 'C\\+\\+ facts from cpp-core:0\\.1\\.0 \\(current cpp-core:0\\.2\\.0\\) — run rmap repo rebuild .+ to refresh' /tmp/cama-06-stale-check.txt && test \"$M0\" = \"$(shasum -a 256 /private/tmp/CPP-ATTRIBUTE-MACRO-1-leveldb-before/registry.json /private/tmp/CPP-ATTRIBUTE-MACRO-1-leveldb-before/databases/*)\" && python3 - <<'PY'\nimport sqlite3, glob, json, os\nBEFORE_LDB, BEFORE_OX = \"/private/tmp/CPP-ATTRIBUTE-MACRO-1-leveldb-before\", \"/private/tmp/CPP-ATTRIBUTE-MACRO-1-openxcom-before\"\nAFTER_LDB, AFTER_OX = \"/private/tmp/CPP-ATTRIBUTE-MACRO-1A-leveldb-after\", \"/private/tmp/CPP-ATTRIBUTE-MACRO-1A-openxcom-after\"\ndef db(root, before):\n    [path] = glob.glob(root + \"/databases/*.db\")\n    if before:\n        wal = path + \"-wal\"\n        assert not os.path.exists(wal) or os.path.getsize(wal) == 0, (\"manager before-store WAL is not empty: an immutable read would miss it — STOP\", wal)\n        return sqlite3.connect(\"file:\" + path + \"?immutable=1\", uri=True)\n    return sqlite3.connect(\"file:\" + path + \"?mode=ro\", uri=True)\ndef one(c, q, *a): return c.execute(q, a).fetchone()[0]\ndef norm(c):\n    uids = [r[0] for r in c.execute(\"select repo_uid from repos\")]\n    assert len(uids) == 1, (\"one repo per root\", uids)\n    ru = uids[0]\n    return lambda row: tuple(v.replace(ru, \"<repo>\") if isinstance(v, str) else v for v in row)\nBASIS = \"macro_recovery_ambiguous_identity\"\ndef member_items(v):\n    if isinstance(v, dict):\n        if \"name\" in v and \"file\" in v and \"forward_decl\" in v:\n            yield v\n        for x in v.values(): yield from member_items(x)\n    elif isinstance(v, list):\n        for x in v: yield from member_items(x)\nitems = list(member_items(json.load(open(\"/tmp/cama-ldb-explain.json\"))))\nmarked = sorted((i.get(\"name\"), tuple(i.get(\"identity_candidates\") or ()), i.get(\"identity\"), i.get(\"identity_basis\")) for i in items if any(k in i for k in (\"identity\", \"identity_candidates\", \"identity_basis\")))\nassert marked == sorted([(\"LOCKS_EXCLUDED\", (real, \"LOCKS_EXCLUDED\"), \"undetermined\", BASIS) for real in (\"IncrementBy\", \"Read\", \"Reset\")]), marked\nassert not [i for i in items if i.get(\"name\") == \"GUARDED_BY\"], \"field phantom in --json\"\nprint(\"json: the marker on exactly the three members,\", len(items), \"member items\")\nMAC = ('GUARDED_BY','LOCKS_EXCLUDED','EXCLUSIVE_LOCKS_REQUIRED','EXCLUSIVE_LOCK_FUNCTION','UNLOCK_FUNCTION','ASSERT_EXCLUSIVE_LOCK')\nPAIRS = [[\"db/db_test.cc\", 47, \"LOCKS_EXCLUDED\", \"IncrementBy\"], [\"db/db_test.cc\", 51, \"LOCKS_EXCLUDED\", \"Read\"], [\"db/db_test.cc\", 55, \"LOCKS_EXCLUDED\", \"Reset\"], [\"db/fault_injection_test.cc\", 150, \"LOCKS_EXCLUDED\", \"IsFilesystemActive\"], [\"db/fault_injection_test.cc\", 154, \"LOCKS_EXCLUDED\", \"SetFilesystemActive\"], [\"db/skiplist_test.cc\", 309, \"LOCKS_EXCLUDED\", \"Wait\"], [\"db/skiplist_test.cc\", 317, \"LOCKS_EXCLUDED\", \"Change\"], [\"helpers/memenv/memenv.cc\", 312, \"EXCLUSIVE_LOCKS_REQUIRED\", \"RemoveFileInternal\"], [\"port/port_stdcxx.h\", 59, \"EXCLUSIVE_LOCK_FUNCTION\", \"Lock\"], [\"port/port_stdcxx.h\", 60, \"UNLOCK_FUNCTION\", \"Unlock\"], [\"port/port_stdcxx.h\", 61, \"ASSERT_EXCLUSIVE_LOCK\", \"AssertHeld\"], [\"util/env_posix.cc\", 501, \"LOCKS_EXCLUDED\", \"Insert\"], [\"util/env_posix.cc\", 507, \"LOCKS_EXCLUDED\", \"Remove\"], [\"util/mutexlock.h\", 25, \"EXCLUSIVE_LOCK_FUNCTION\", \"MutexLock\"], [\"util/mutexlock.h\", 28, \"UNLOCK_FUNCTION\", \"~MutexLock\"]]\nB, A = db(BEFORE_LDB, True), db(AFTER_LDB, False)\nq_mac = \"select f.path, n.line_start, n.name, n.metadata_json from nodes n join files f on f.file_uid=n.file_uid where n.kind='SYMBOL' and n.name in (%s) order by 1,2\" % \",\".join(\"?\"*len(MAC))\nrb, ra = B.execute(q_mac, MAC).fetchall(), A.execute(q_mac, MAC).fetchall()\nassert len(rb) == 35, (\"before-root sanity\", len(rb))\nassert len(ra) == 15 and not [r for r in ra if r[2] == 'GUARDED_BY'], (\"expected exactly the 15 (2a)/(2b) rows, no field phantom\", len(ra), [r[:3] for r in ra if r[2]=='GUARDED_BY'][:5])\ngot = {(p, l): (n, json.loads(m or '{}')) for p, l, n, m in ra}\nfor p, l, mac, real in PAIRS:\n    n, md = got.get((p, l), (None, {}))\n    assert (n, md.get(\"identity\"), md.get(\"basis\"), md.get(\"identity_candidates\")) == (mac, \"undetermined\", BASIS, [real, mac]), (\"marker\", p, l, n, md, \"expected\", [real, mac])\nprint(\"15 rows keep today's name and carry the marker with the predicted candidate pairs\")\nMARKED = \"select count(*) from nodes where json_extract(metadata_json,'$.identity') is not null or json_extract(metadata_json,'$.identity_candidates') is not null or json_extract(metadata_json,'$.basis') is not null\"\nassert one(A, MARKED) == 15, (\"marked rows beyond the fifteen\", one(A, MARKED))\nassert one(B, MARKED) == 0\nNQ = \"select n.stable_key, n.kind, n.subtype, n.name, n.qualified_name, f.path, p.stable_key, n.line_start, n.col_start, n.line_end, n.col_end, n.signature, n.visibility, n.doc_comment, n.metadata_json from nodes n left join files f on f.file_uid=n.file_uid left join nodes p on p.node_uid=n.parent_node_uid\"\ndef nodes_by_key(c):\n    nm, out = norm(c), {}\n    for r in c.execute(NQ):\n        r = nm(r)\n        assert r[0] not in out, (\"duplicate stable key\", r[0])\n        out[r[0]] = (r[1:-1], json.loads(r[-1] or \"{}\"))\n    return out\nNB, NA = nodes_by_key(B), nodes_by_key(A)\nphantom = {k for k, (t, _) in NB.items() if t[0] == \"SYMBOL\" and t[2] == \"GUARDED_BY\"}\nassert len(phantom) == 20, len(phantom)\nassert set(NA) == set(NB) - phantom, (\"stable keys moved (repo-uid prefix normalized)\", sorted(set(NA) ^ (set(NB) - phantom))[:10])\nexpect_marker = {}\nfor p, l, mac, real in PAIRS:\n    [k] = [k for k, (t, _) in NB.items() if t[0] == \"SYMBOL\" and t[2] == mac and t[4] == p and t[6] == l]\n    expect_marker[k] = [real, mac]\nfor k in NA:\n    (tb, mb), (ta, ma) = NB[k], NA[k]\n    assert ta == tb, (\"node fields moved\", k, tb, ta)\n    want = dict(mb, identity=\"undetermined\", basis=BASIS, identity_candidates=expect_marker[k]) if k in expect_marker else mb\n    assert ma == want, (\"node metadata moved\", k, mb, ma)\nprint(\"every surviving node keeps its stable key and every field;\", len(NA), \"nodes; metadata moves only on the 15 marked keys\")\nfor qn, line in ((\"leveldb::AtomicCounter::IncrementBy\", 47), (\"leveldb::PosixLockTable::Insert\", 501), (\"leveldb::DBImpl::Recover\", 113)):\n    r = A.execute(\"select coalesce(json_extract(metadata_json,'$.forward_decl'),0), json_extract(metadata_json,'$.identity'), json_extract(metadata_json,'$.identity_candidates') from nodes where kind='SYMBOL' and qualified_name=? and line_start=?\", (qn, line)).fetchall(); assert r == [(1, None, None)], (qn, r)\nprint(\"prototypes kept as unmarked declarations\")\nTYPED = \"select count(*) from edges where type='CALLS' and json_extract(metadata_json,'$.receiver')=? and json_extract(metadata_json,'$.receiverType') is not null\"\nfor recv in (\"state_cv_\", \"file_map_\", \"blocks_\", \"snapshots_\", \"table_\", \"tmp_batch_\"):\n    print(\"REPORTED typed CALLS edges with receiver\", recv, one(B, TYPED, recv), \"->\", one(A, TYPED, recv))\nfor recv in (\"state_cv_\", \"file_map_\"):\n    assert one(B, TYPED, recv) == 0 and one(A, TYPED, recv) > 0, (\"in-body calls on a newly typed member carry no receiverType\", recv)\nassert one(A, \"select count(*) from edges where type='CALLS' and source_node_uid=target_node_uid and metadata_json like '%receiver%'\") == 0, \"receiver-bearing self-loop\"\nfor label, c in ((\"leveldb before\", B), (\"leveldb after\", A)):\n    print(label, \"nodes\", one(c, \"select count(*) from nodes\"), \"SYMBOL\", one(c, \"select count(*) from nodes where kind='SYMBOL'\"), \"METHOD\", one(c, \"select count(*) from nodes where subtype='METHOD'\"), \"edges\", one(c, \"select count(*) from edges\"), \"CALLS\", one(c, \"select count(*) from edges where type='CALLS'\"), \"unresolved\", one(c, \"select count(*) from unresolved_edges\"))\nassert one(A, \"select count(*) from nodes\") == one(B, \"select count(*) from nodes\") - 20, \"exactly the 20 field phantoms leave\"\nassert one(A, \"select count(*) from edges where type='CALLS'\") >= one(B, \"select count(*) from edges where type='CALLS'\") and one(A, \"select count(*) from unresolved_edges\") <= one(B, \"select count(*) from unresolved_edges\"), \"typed members may only add bindings\"\nFQ = \"select path, language, is_test, is_generated, is_excluded from files order by path\"\nassert B.execute(FQ).fetchall() == A.execute(FQ).fetchall(), \"leveldb file inventory (is_test/is_generated/is_excluded) differs\"\nEQ = \"select s.stable_key, t.stable_key, e.type, e.resolution, e.line_start, e.col_start, e.line_end, e.col_end, e.metadata_json, e.is_type_only from edges e left join nodes s on s.node_uid=e.source_node_uid left join nodes t on t.node_uid=e.target_node_uid\"\nUQ = \"select s.stable_key, u.target_key, u.type, u.resolution, u.line_start, u.col_start, u.line_end, u.col_end, u.metadata_json, u.category, u.classification, u.classifier_version, u.basis_code from unresolved_edges u left join nodes s on s.node_uid=u.source_node_uid\"\nVQ = \"select f.path, v.content_hash, v.ast_hash, v.parse_status, v.size_bytes, v.line_count from file_versions v join files f on f.file_uid=v.file_uid\"\nMQ = \"select target_stable_key, kind, value_json from measurements\"\nSQ = \"select f.path, s.import_bindings_json, s.package_dependencies_json, s.tsconfig_aliases_json from file_signals s join files f on f.file_uid=s.file_uid\"\nXQ = \"select s.stable_key, f.path, x.target_key, x.type, x.resolution, x.line_start, x.col_start, x.line_end, x.col_end, x.metadata_json from extraction_edges x left join nodes s on s.node_uid=x.source_node_uid left join files f on f.file_uid=x.source_file_uid\"\nTABLES = ((\"nodes\", NQ), (\"edges\", EQ), (\"unresolved_edges\", UQ), (\"files\", FQ), (\"file_versions\", VQ), (\"measurements\", MQ), (\"file_signals\", SQ), (\"extraction_edges\", XQ))\ndef dump(c):\n    nm = norm(c)\n    return [sorted((nm(r) for r in c.execute(q)), key=repr) for _, q in TABLES]\nOB, OA = db(BEFORE_OX, True), db(AFTER_OX, False)\ndb_, da_ = dump(OB), dump(OA)\nprint(\"openxcom before\", {t: len(x) for (t, _), x in zip(TABLES, db_)}, \"after\", {t: len(x) for (t, _), x in zip(TABLES, da_)})\nfor (name, _), xb, xa in zip(TABLES, db_, da_):\n    assert xb == xa, (\"OpenXcom \" + name + \" differ in a semantic field\", len(xb), len(xa), [r for r in xa if r not in set(xb)][:3])\nprint(\"CAM-A04 store oracles ok\")\nimport json\ndef walk(v):\n    if isinstance(v, dict):\n        yield v\n        for x in v.values(): yield from walk(x)\n    elif isinstance(v, list):\n        for x in v: yield from walk(x)\nenv = json.load(open(\"/tmp/cama-06-stale-check.json\"))\nts = env[\"value\"][\"toolchain_staleness\"]\nassert ts.get(\"state\") == \"stale\", (\"copied v0.19.0 store must read stale under the candidate\", ts)\nfams = ts[\"families\"]\ncpp = [f for f in fams if f.get(\"family\") == \"C++\"]\nassert cpp == [{\"family\": \"C++\", \"snapshot_version\": \"cpp-core:0.1.0\", \"current_version\": \"cpp-core:0.2.0\"}], (\"the C++ family entry\", fams)\nothers = [f for f in fams if f.get(\"family\") != \"C++\"]\nassert all(f.get(\"family\") == \"indexer\" for f in others), (\"no extractor family other than C++ may be stale on leveldb (C is unchanged)\", others)\nprint(\"REPORTED non-C++ stale families (an INDEXER_VERSION bump by TOOLCHAIN-STALENESS-1 itself, if any):\", others)\nconds = [d for d in walk(env) if d.get(\"code\") == \"TOOLCHAIN_STALENESS\"]\nassert conds and all(d.get(\"status\") == \"incomplete\" for d in conds), (\"TOOLCHAIN_STALENESS must be incomplete\", conds)\nfresh = json.load(open(\"/tmp/cama-06-after-check.json\"))[\"value\"][\"toolchain_staleness\"]\nassert fresh == {\"state\": \"current\"}, (\"a fresh index by the candidate is current\", fresh)\nprint(\"CAM-A04 staleness: copied store stale for C++ (cpp-core:0.1.0 -> cpp-core:0.2.0), TOOLCHAIN_STALENESS incomplete; fresh after-root current\")\nPY",
        "cwd": ".",
        "environment": "the candidate binary indexes /Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/leveldb (7ee830d) and /Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/OpenXcom (1edb0a5) into NEW isolated `-after` roots under RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off (stdio; builder-owned, deleted in CAM-A05); the manager's `-before` roots /private/tmp/CPP-ATTRIBUTE-MACRO-1-leveldb-before and /private/tmp/CPP-ATTRIBUTE-MACRO-1-openxcom-before (indexed 2026-09-23 by the v0.19.0 binary = this HEAD's rust tree) are READ with sqlite3 only (`?immutable=1`, after asserting their `-wal` is absent or empty so the immutable read misses nothing), never served, never reindexed; the builder's after-roots are read `mode=ro`. An after-root is created only when absent (`test ! -e` then `mkdir`): if one exists when CAM-A04 starts (a stopped earlier attempt), the check fails by design — inspect it, record what it is and why it is being discarded in build-progress.md, remove it deliberately, then rerun; never an unconditional delete; THEN, for the toolchain-staleness consequence (formerly a separate check, folded into CAM-A04 so the implementation round keeps its five checks CAM-A01..A05): a builder-owned COPY `/private/tmp/CPP-ATTRIBUTE-MACRO-1A-leveldb-stale` of the manager's leveldb before-root (v0.19.0 store: `toolchain_json` stamps `cpp-core:0.1.0`, `c-core:0.1.0`, `indexer:1.0.0`), created only when absent, its registry.json's absolute `db_path` rewritten to the copy before it is served (the manager's root is never served), served with RMAP_TRANSPORT=stdio RMAP_AUTO_ENRICH=off RMAP_AUTO_RETENTION=off; the manager's root is read only by `cp -R` and `shasum`, and its digests must be identical afterwards; deleted in CAM-A05",
        "inputs": "BEFORE (measured on the before-store): 35 SYMBOL rows named by a macro token — 20 shape-(1) `GUARDED_BY` field phantoms (benchmarks/db_bench.cc:348/349/358/359, db/db_impl.h:183/186/189/203, db/db_test.cc:62, db/skiplist_test.cc:326/327, helpers/memenv/memenv.cc:145/148/149/383, util/cache.cc:184/189/193/195, util/env_test.cc:144), 4 shape-(2a) and 11 shape-(2b) rows; no `identity_candidates` anywhere; `explain leveldb::AtomicCounter` renders `Members (7)` with `IncrementBy (METHOD, decl)  db/db_test.cc:47`, three bare `LOCKS_EXCLUDED (METHOD)` rows (:47/:51/:55) and `GUARDED_BY (METHOD, decl)  db/db_test.cc:62`; `explain leveldb::DBImpl` `Members (73)` (four `GUARDED_BY` decls among them); nodes 2255 / SYMBOL 2112 / METHOD 1053 / edges 4187 / CALLS 3488 / unresolved 6230; self-loops 0. AFTER (predicted from the inventory under §2.1; the product is the truth — a differing literal is a STOP-and-report with the construct): exactly 15 macro-named rows remain, each keeping today's name and carrying `identity: \"undetermined\"`, `basis: \"macro_recovery_ambiguous_identity\"` and `identity_candidates` = [displaced identifier, macro] as listed in the oracle (`IncrementBy`/`Read`/`Reset` + `LOCKS_EXCLUDED`; `IsFilesystemActive`/`SetFilesystemActive`; `Wait`/`Change`; `RemoveFileInternal` + `EXCLUSIVE_LOCKS_REQUIRED`; `Lock` + `EXCLUSIVE_LOCK_FUNCTION`, `Unlock` + `UNLOCK_FUNCTION`, `AssertHeld` + `ASSERT_EXCLUSIVE_LOCK`; `Insert`/`Remove`; `MutexLock` + `EXCLUSIVE_LOCK_FUNCTION`, `~MutexLock` + `UNLOCK_FUNCTION`); no other node carries any marker key (before: 0); the 20 field phantoms are gone (nodes exactly −20) and their members are typed for IN-BODY calls — `current_class` is set only inside the class-body walk (extractor.rs:812/:890) and read at :2075-2082, so calls in out-of-line definitions stay untyped: ASSERTED `state_cv_` (db/skiplist_test.cc:312 `state_cv_.Wait();`, :320 `state_cv_.Signal();`) and `file_map_` (helpers/memenv/memenv.cc:235 `if (file_map_.find(fname) == file_map_.end()) {`, inside `class InMemoryEnv`) go from 0 typed CALLS edges to > 0; REPORTED `blocks_` (memenv.cc `FileState`) and the out-of-line `snapshots_`/`table_`/`tmp_batch_` (db_impl.cc, cache.cc — expected unchanged); typing cannot remove a binding — the receiver-type stage only narrows an ambiguous pool and otherwise falls through to the unchanged unique-name rule (indexer/src/resolver.rs:1062-1100); the prototypes `IncrementBy` :47, `Insert` :501, `Recover` :113 stay unmarked declarations; `explain leveldb::AtomicCounter` `Members (6)` with `LOCKS_EXCLUDED (METHOD, identity undetermined — 2 candidates: IncrementBy, LOCKS_EXCLUDED)  db/db_test.cc:47` and the `Read`/`Reset` rows alike, `IncrementBy (METHOD, decl)`, no `GUARDED_BY`; `--json` carries the marker (`identity`, `identity_candidates`, `identity_basis`) on exactly the three `LOCKS_EXCLUDED` members and no `GUARDED_BY` member; `explain leveldb::DBImpl` `Members (69)`, no `GUARDED_BY`; CALLS edges ≥ before and unresolved ≤ before (typed members can only add bindings); self-loops 0; the `files` inventory (path, language, is_test, is_generated, is_excluded) identical before/after (RG-REQ-001-L07/L08 untouched); REPORTED totals. `explain leveldb::port::Mutex` is NOT a witness: leveldb has two `Mutex` classes (port_example.h:24, port_stdcxx.h:51) and the focus is ambiguous — RG-REQ-002-L11's field example names it and needs a text-only correction (§8). IDENTITY (RG-REQ-001-L04, P-CAM-02): every leveldb node except the 20 phantoms keeps its stable key and every field (kind, subtype, name, qualified name, file, parent, line/col span, signature, visibility, doc comment), and its `metadata_json` is unchanged except that the 15 marked keys gain exactly the three marker keys — keys compared with the store's own `<repo_uid>:` replaced by `<repo>` (repo_uid is a random ULID per registration, daemon-runtime/src/registry.rs:536; no other key renumbers — no surviving node shares a base key with a phantom, measured on the before-store); OpenXcom (no attribute macro anywhere): EVERY TABLE WRITTEN FROM EXTRACTOR OUTPUT — and the claim names exactly these eight — identical between the before and after stores as multisets: nodes (every field above plus `metadata_json`), edges (source and target stable key, type, resolution, line/col span, `metadata_json`, `is_type_only`), unresolved edges (source stable key, `target_key`, type, resolution, line/col span, `metadata_json`, category, classification, `classifier_version`, `basis_code`), files (path, language, `is_test`, `is_generated`, `is_excluded`), file_versions by path (`content_hash`, `ast_hash`, `parse_status`, `size_bytes`, `line_count` — the source of `compute_repo_summary`'s indexed/tracked-only counts, storage/src/agent_impl.rs:277-318), measurements (`target_stable_key`, kind, `value_json`), file_signals by path (import bindings, package dependencies, tsconfig aliases) and extraction_edges (source stable key and file, `target_key`, type, resolution, line/col span, `metadata_json`); excluded are only run-specific identity (`*_uid`, `snapshot_uid`; `repo_uid` normalized), timestamps (`observed_at`, `indexed_at`, `created_at`) and version-provenance strings (`extractor`, `measurements.source`); the `extractor` columns and the snapshot's `toolchain_json` move to `cpp-core:0.2.0` by design (§2.1 step 4; 21539 OpenXcom edges carry `cpp-core:0.1.0` in the before-store; no node or edge `metadata_json` names an extractor version). Before-store sizes, re-measured 2026-09-23: nodes 11274, edges 22514, unresolved 28219, files 733, file_versions 733, measurements 15861, file_signals 706, extraction_edges 49927. NOT compared, and not claimed: tables filled by later passes over these facts (module candidates and ownership, surfaces, boundaries, inferences, seeds, call-degree caches) — their inputs are the compared tables and their code is untouched (CAM-A02's diff --quiet), so their equality is INFERRED, not verified here. STALENESS (folded from the former CAM-A06): GATE (D-REFRESH-STALE-1's queue order: TOOLCHAIN-STALENESS-1 ships first): HEAD carries TOOLCHAIN-STALENESS-1's committed implementation-review record with `workItemId` TOOLCHAIN-STALENESS-1 and `result` accepted (docs/assurance/<ID>/implementation-review.json is the post-review record convention; it is committed only after the operator's acceptance) — accepted evidence, not a source string; absent on 76f23a58, so this slice cannot be implemented today. CONSEQUENCE under test (RG-REQ-001-L06 as amended by RG-BOOTSTRAP-INPUT-9, D-REFRESH-STALE-1/-2 option B): a store written by the previous binary and read by the candidate, whose running C++ extractor is `cpp-core:0.2.0` (§2.1 step 4), serves C++ facts from `cpp-core:0.1.0`, so `orient` and `check` render `C++ facts from cpp-core:0.1.0 (current cpp-core:0.2.0) — run rmap repo rebuild <path> to refresh` (the `<path>` slot matched as any non-empty text — its rendering is TOOLCHAIN-STALENESS-1's), `check` exits 2 with the `TOOLCHAIN_STALENESS` condition `incomplete`, and `check --json`'s `value.toolchain_staleness` is `{\"state\":\"stale\",\"families\":[…]}` holding exactly one C++ entry `{\"family\":\"C++\",\"snapshot_version\":\"cpp-core:0.1.0\",\"current_version\":\"cpp-core:0.2.0\"}`; no other extractor family (the C extractor is unchanged); an `indexer` entry is admitted and REPORTED (it would be TOOLCHAIN-STALENESS-1's own `INDEXER_VERSION` bump, not this slice's). Control: a fresh index by the candidate (CAM-A04's leveldb after-root) reads `{\"state\":\"current\"}`. The refresh-carried-forward and rebuild-clears cases are TOOLCHAIN-STALENESS-1's own verification (RG-REQ-001-L06's criterion) and are not repeated here."
      },
      "expected": "exit 0: on the corpus that motivated RC-4 no member is silently renamed and none is withheld — the twenty field phantoms become typed members, the fifteen ambiguous members are stored once, marked undetermined with their basis and both identity candidates and rendered to the agent on `explain` as something to investigate (RG-REQ-002-L11 PARTIAL — `find` is 1B's), every clear declaration is unchanged, every surviving node keeps its stable key and every field (no behavior change), no call binds to its own caller, bindings are only added, the file inventory (is_test/is_generated) is unchanged, and a corpus without the construct is identical in the eight tables written from extractor output (no behavior change); and the version bump reaches the agent — an existing C++ store read by the candidate states that its C++ facts come from `cpp-core:0.1.0` while the running extractor is `cpp-core:0.2.0`, names the rebuild, and makes `check` incomplete (exit 2); a fresh index is current; the manager's before-root is byte-identical"
    },
    {
      "checkId": "CAM-A05",
      "obligationIds": [
        "RG-REQ-011-L06",
        "P-CAM-03"
      ],
      "owner": "builder",
      "method": {
        "kind": "command",
        "command": "test \"$(grep -E '^registry-before [0-9a-f]{64}$' .agent-manager/slices/CPP-ATTRIBUTE-MACRO-1A/build-progress.md | tail -1 | cut -d' ' -f2)\" = \"$(shasum -a 256 \"$HOME/Library/Application Support/repo-graph/registry.json\" | cut -d' ' -f1)\" && PIDS=$(pgrep -x rmapd | paste -sd, -); { [ -z \"$PIDS\" ] || ! ps -o command= -p \"$PIDS\" | grep -v '/\\.local/bin/rmapd' | grep -q .; } && rm -rf /private/tmp/CPP-ATTRIBUTE-MACRO-1A-leveldb-after /private/tmp/CPP-ATTRIBUTE-MACRO-1A-openxcom-after /private/tmp/CPP-ATTRIBUTE-MACRO-1A-leveldb-stale && ! test -e /private/tmp/CPP-ATTRIBUTE-MACRO-1A-leveldb-after && ! test -e /private/tmp/CPP-ATTRIBUTE-MACRO-1A-openxcom-after && ! test -e /private/tmp/CPP-ATTRIBUTE-MACRO-1A-leveldb-stale && ls -d /private/tmp/CPP-ATTRIBUTE-MACRO-1-leveldb-before /private/tmp/CPP-ATTRIBUTE-MACRO-1-openxcom-before >/dev/null && ! git status --short | grep -vE '^[ MA?][ MA?] (rust/crates/cpp\\-extractor/src/extractor\\.rs|rust/crates/storage/src/agent_impl\\.rs|rust/crates/agent/src/storage_port\\.rs|rust/crates/agent/src/dto/signal\\.rs|rust/crates/agent/src/explain/mod\\.rs|rust/crates/agent/tests/explain_symbol\\.rs|rust/crates/rgr/src/presentation/explain_sections\\.rs|rust/crates/rgr/src/presentation/explain\\.rs)$' | grep -q . && git diff --check",
        "cwd": ".",
        "environment": "candidate tree, after every other check",
        "inputs": "the full sha256 of the operator registry recorded as `registry-before <digest>` in build-progress.md at step 0 equals the current one; no stray isolated daemon; the three builder-owned roots (the two `-after` roots and CAM-A04's `-stale` copy) removed at the end of the round, after every check that reads them; the two manager-owned `-before` roots kept; `git status --short` names only the eight candidate paths (the worktree holds no foreign untracked file at admission — the manager's duty); no trailing whitespace"
      },
      "expected": "exit 0: every proof was isolated (the operator's registry byte-identical — no behavior change outside the isolated roots), the tree holds only the allocated paths"
    }
  ]
}
-->

# CPP-ATTRIBUTE-MACRO-1A — a macro-recovered C++ member is typed when the evidence says field, and marked with both identity candidates otherwise

Status: SPECIFIED and GROUNDED 2026-09-23 on HEAD 438e0e28 (re-verified on b372172e and on 76f23a58, docs-only commits: `git diff --stat 438e0e28 HEAD -- rust` is empty) under RG-BOOTSTRAP-INPUT-8 (RC-4 of audit round seven; the re-cut of CPP-ATTRIBUTE-MACRO-1, whose five document reviews established that the recovery tree is symmetric and led to the human's certainty rule RG-REQ-002-L11 and the amended RG-REQ-001-L01). Track: audit round seven fix queue (certainty gate). CODE slice, index-time (a reindex changes macro-bearing C++ stores), vertical from the extractor to `explain`. Builder: claude / claude-opus-5-5 (human trial directive 2026-09-22); reviewer: codex / gpt-5.6-terra. Part 1B (inferred binding of calls through the candidate names, so `callers leveldb::port::Mutex::Lock` answers) follows as its own slice.

## 0. Requirements allocation (the machine-readable block above is binding; this section explains it)

**Implements:** RG-REQ-001-L01 as amended 2026-09-23 — "where tree-sitter recovery of a macro-bearing member leaves two parenthesised identifiers between the type and the body or terminator, the node is stored once, marked `identity: undetermined` with both candidates (RG-REQ-002-L11), never named by the macro alone and never dropped — a field whose trailing macro argument names a data member of the class is a data member" (CAM-A01, A02, A04). RG-REQ-001-L01 is the only obligation this slice COMPLETES; the allocation's `implements` holds it alone.

**Allocation rule (closes the class of F-CAM-L11-1 and F-CAM-L06-1):** `implements` lists only an obligation this slice completes; `changes` lists an authorized obligation whose observable behaviour this slice moves without completing it — a partial contribution, or a ratified consequence of the change; `preserves` lists only obligations whose behaviour stays exactly as before, each covered by a no-behavior-change oracle.

**RG-REQ-002-L11 coverage in this increment (stated, not hidden):** L11's criterion names, for the identity case, the store marker and the `explain` AND `find` renderings under CPP-ATTRIBUTE-MACRO-1. That slice is cut in two: 1A (this document) delivers the store marker and `explain`; 1B delivers `find`'s marker and the inferred call binding through the candidate names (§8). Until 1B ships, `find`, `callers` and `callees` render a marked node under its current name without the marker — today's behaviour, unchanged here, not a regression. L11's "default exclusion / flag inclusion" clause governs surfaces that aggregate EDGES; 1A adds no inferred edge, so it has no instance here. The allocation therefore lists L11 under `changes`, not `implements`: 1A's contribution is PARTIAL — candidates and reason recorded with the node-marking keys D-CERTAINTY-MARK-1 names (`identity: undetermined`, `identity_candidates`, `basis`) and rendered on `explain` as something to investigate, never guessed, never dropped, never counted as certain (CAM-A01, A02, A04). No 1A evidence may report L11 as implemented or passed; L11 moves to `implements` in 1B, whose checks cover `find` as well as `explain`, and only that evidence can carry an L11 PASS. Today leveldb's store holds 35 rows named by a macro token: 20 fields promoted to phantom method declarations, 15 members whose real name is lost behind the macro.

**Decision records (all named by the baseline manifest):** D-CERTAINTY-MARK-1 (the human, 2026-09-23); D-CAM-MARKER-BOUNDARY-1 (the operator, option A — the first document review's D-CAM-MEMBER-MARKER-BOUNDARY-1: the marker crosses the storage-port and agent→renderer boundaries as additive raw DTO fields decoded all-or-nothing, the `forward_decl` precedent; the human may override); D-REFRESH-STALE-1 (the human, option B — the first document review's D-CAM-REFRESH-1: no version-keyed invalidation; this slice bumps `EXTRACTOR_NAME` to `cpp-core:0.2.0` so every existing store that serves C++ files shows TOOLCHAIN-STALENESS-1's staleness line — `C++ facts from cpp-core:0.1.0 (current cpp-core:0.2.0) — run rmap repo rebuild <path> to refresh` — until rebuilt; RG-REQ-001-L06 as amended by RG-BOOTSTRAP-INPUT-9: "When the running binary's per-language extractor version or indexer version differs from the snapshot's recorded `toolchain_json`, `orient` and `check` shall render one line per stale toolchain family beside the index-basis line: `<family> facts from <snapshot_version> (current <current_version>) — run rmap repo rebuild <path> to refresh`", with "An extractor family is evaluated only when the served snapshot contains files of that family"). TOOLCHAIN-STALENESS-1 implements that line and ships FIRST (D-REFRESH-STALE-1's queue); this slice owns only the bump and proves its consequence (CAM-A04's staleness part). The gate is accepted evidence — TOOLCHAIN-STALENESS-1's committed implementation-review record with `result` accepted (§4, §5 step 0, CAM-A04) — not a source string. D-CAM-01's R1–R4 mechanics (withholding) are superseded by it: nothing is withheld here.

**Changes:** RG-REQ-002-L11 — partial, as above. RG-REQ-001-L06 — the ratified consequence of the version bump (D-REFRESH-STALE-1/-2 option B): an existing store that serves C++ files, read by the candidate, now states `C++ facts from cpp-core:0.1.0 (current cpp-core:0.2.0) — run rmap repo rebuild <path> to refresh` on `orient` and `check`, `check`'s `TOOLCHAIN_STALENESS` condition is `incomplete` (exit 2) and `check --json` carries the stale C++ family; a fresh index is current (CAM-A04's staleness part, on a builder-owned copy of the manager's leveldb before-root). The staleness machinery itself is TOOLCHAIN-STALENESS-1's and untouched here. Further REPORTED consequence on macro-bearing C++ corpora: the 20 field phantoms disappear and their members type their class's in-body calls (bindings may only be added — CAM-A04 asserts CALLS ≥ and unresolved ≤); the 15 members keep today's name and stable key (RG-REQ-001-L04/L05 unchanged) and gain `identity_candidates`; `explain <Type>` Members rows render the marker. Calls to the real names (`Lock`, `IncrementBy`'s body, …) stay as today — part 1B.

**Preserves:** RG-REQ-001-L02/L03 (no edge changes but typed members; self-loops 0; unresolved rows leave only by typed binding — CAM-A04), L04/L05 (names and keys unchanged: every surviving leveldb node's stable key and fields compared before/after — CAM-A04; a determined member's JSON byte-identical — CAM-A02), L07–L10 (untouched crates — CAM-A02; the `files` inventory, which carries `is_test`/`is_generated`, identical before/after — CAM-A04 for L07/L08), RG-REQ-002-L01–L10 (the honesty machinery untouched; the new field follows the unreadable-line pattern — CAM-A02), RG-REQ-011-L06 (isolation — CAM-A05; the manager's before-root byte-identical across the copy — CAM-A04).

| Id | Obligation | Proof |
|---|---|---|
| P-CAM-01 | a C++ corpus without the construct is identical in the eight tables written from extractor output (nodes with stable keys, edges with resolution and spans, unresolved edges, files, file_versions, measurements, file_signals, extraction_edges); later-pass tables not compared (INFERRED from untouched code); non-target extractors untouched | CAM-A04 (OpenXcom); CAM-A02 (diff --quiet) |
| P-CAM-02 | CPP-DECLARATORS-1's shapes (a)/(b), the `forward_decl` carrier, receiver typing, every existing cpp-extractor/agent/rgr test unchanged | CAM-A01 (twelve named tests; ≥ 77 floor; shape functions present); CAM-A02 (named existing tests) |
| P-CAM-03 | isolation, cleanup, hygiene | CAM-A03; CAM-A05; CAM-A04 (the manager's root digests) |

## 1. Problem (ROOT-CAUSED — RC-4 + addendum + five document reviews, 2026-09-23)

tree-sitter-cpp 0.23.4 has no derivation for `type ident MACRO(args)`: recovery moves the REAL declarator into an `ERROR` node that is a direct child of the member node between `type` and `declarator`, and lets the macro call fill the `declarator: function_declarator` slot. The extractor admits it through `has_function_declarator` (cpp-extractor/src/extractor.rs:1337-1345) at the class-body dispatch (:859-863), names it from the first direct-child `function_declarator` (:1648-1661), and `extract_function_name` (:2121-2194) matches neither CPP-DECLARATORS-1 shape — so the node is named by the macro token. Three recovery shapes on leveldb 7ee830d (every tree reproduced with `tree-sitter parse`):
- **(1) field + trailing macro** (20 rows: benchmarks/db_bench.cc:348/349/358/359, db/db_impl.h:183/186/189/203, db/db_test.cc:62, db/skiplist_test.cc:326/327, helpers/memenv/memenv.cc:145/148/149/383, util/cache.cc:184/189/193/195, util/env_test.cc:144): `db/db_impl.h:189  SnapshotList snapshots_ GUARDED_BY(mutex_);` → `field_declaration(type, ERROR(identifier snapshots_), declarator: function_declarator(GUARDED_BY, (mutex_)))` → phantom `leveldb::DBImpl::GUARDED_BY (decl)`; the pointer variant (`WriteBatch* tmp_batch_ GUARDED_BY(mutex_);`) puts the ERROR inside the `pointer_declarator` and records `GUARDED_BY` in `class_field_types`. The member's type is never recorded, so its in-body calls stay untyped.
- **(2a) definition + trailing macro, the real declarator inside the ERROR** (4 rows: port_stdcxx.h 59/60, db_test.cc 51/55): `void Lock() EXCLUSIVE_LOCK_FUNCTION() { mu_.lock(); }` → `function_definition(type, ERROR(init_declarator(Lock, ())), declarator: function_declarator(EXCLUSIVE_LOCK_FUNCTION, ()), body)` → a METHOD named `EXCLUSIVE_LOCK_FUNCTION`; `leveldb::port::Mutex::Lock` does not exist.
- **(2b) split: a prototype keeps the name, a typeless `function_definition` named by the macro takes the body** (11 rows: db_test.cc 47, fault_injection_test.cc 150/154, skiplist_test.cc 309/317, env_posix.cc 501/507, memenv.cc 312, port_stdcxx.h 61, mutexlock.h 25/28): `void IncrementBy(int count) LOCKS_EXCLUDED(mu_) {` → `field_declaration(fd(IncrementBy, (int count)))` (its `;` MISSING or absent) + `function_definition(declarator: fd(LOCKS_EXCLUDED, (mu_)), body)`. Inside a macro-decorated class (`class LOCKABLE Mutex`, `class SCOPED_LOCKABLE MutexLock` parse as a `function_definition` whose body is a `compound_statement`) the prototype takes other forms: port_stdcxx.h:61 `declaration(init_declarator(AssertHeld, ()))`; mutexlock.h:25 a `declaration` inside the `public:` `labeled_statement`; mutexlock.h:28 an `ERROR` holding `function_declarator(destructor_name ~MutexLock, ())`. Only the eight prototypes that are `field_declaration`s become nodes today (the unmarked `(decl)` rows); the other three prototypes produce no node.
The five document reviews of the first cut proved the tree is SYMMETRIC: `int MYAPI baz(Foo);` ≡ `int seed_ GUARDED_BY(mutex_);`, `void MYAPI() foo() {…}` ≡ `void Lock() EXCLUSIVE_LOCK_FUNCTION() {…}`, every parameter form ≡ a possible macro argument, and withholding a genuine declaration is an L01 completeness exception. The human ruled 2026-09-23: mark what the index cannot determine for the agent to investigate — the only evidence that IS decisive is a trailing macro whose argument names a data member of the class (a member name cannot also be a type in that class's declarations), which makes the construct a field.

## 2. Contract

### 2.1 The fix, at the cause — one evidence rule, one marker, nothing chosen
In the class-body member walk (:843-890) and the field pre-pass (:816-833):
1. **Field evidence** — a `field_declaration` whose (possibly pointer/reference/array-wrapped) declarator is a `function_declarator` F preceded by a single-identifier `ERROR` sibling, where F has ≥ 1 parameter and every parameter is an unnamed `parameter_declaration` whose `type` is a bare `type_identifier` naming a data member of this class (collected from the body's plain members in a first pass; the pointer variant never contributes `GUARDED_BY`), and no ERROR inside the parameter list: a DATA MEMBER named by the ERROR's identifier with the field's type — recorded in `class_field_types`, NO node.
2. **Marking** — every other macro recovery shape keeps TODAY's node (name, qualified name, kind, `forward_decl`, stable key unchanged — the extractor never picks) and gains three additive `metadata_json` keys, all or none (D-CERTAINTY-MARK-1's node mechanics): `identity: "undetermined"`, `basis: "macro_recovery_ambiguous_identity"` (the reason: tree-sitter's recovery of a macro-bearing member left two identifiers, either of which may be the name), and `identity_candidates: [<displaced>, <declarator-slot>]` — two distinct non-empty strings ordered STRUCTURALLY, never by a judgement of which is real: first the identifier recovery displaced, second the identifier in the `function_declarator`'s declarator slot (today's node name). The displaced identifier is: shape (2a) — the identifier inside the ERROR child's `function_declarator`/`init_declarator`; shape (2b) — the declarator identifier (of its `function_declarator`, or of an `init_declarator` whose value is an empty `argument_list`) of the immediately preceding prototype — a `field_declaration`/`declaration`, a `declaration` inside an access `labeled_statement`, or an `ERROR` holding a `function_declarator` — whose end is separated from the typeless definition's start by whitespace alone and whose `;` is MISSING or absent; shape (1) without field evidence — the ERROR's identifier. A destructor candidate keeps its `~` in every shape. A `;`-terminated prototype followed by a typeless definition is NOT paired (today's reading, §8).
3. **Vertical** — storage `list_members_of_type` reads the three keys into `AgentMemberEntry` as one all-or-nothing value (candidates + basis; absent = determined); a partial or malformed marker is an `AgentStorageError` — the read fails exactly as a present-but-unreadable `forward_decl` fails today, never read as determined; `ExplainMemberItem` serializes it as the flat keys `identity`, `identity_candidates`, `identity_basis`, all absent when determined (so determined members are byte-identical); rgr `render_member_row` renders `<name> (<subtype>[, decl], identity undetermined — 2 candidates: <c1>, <c2>)  <path>:<line>`; a partial or malformed marker on the wire makes `render_members` print its existing single `members unreadable on this snapshot` line for the whole section.
4. **Version** — `EXTRACTOR_NAME` `cpp-core:0.1.0` → `cpp-core:0.2.0` (extractor.rs:26, its only occurrence in the cpp-extractor crate): the marker changes the facts a fresh extraction produces for files whose content did not change; except when a changed-config widening re-extracts its scope, delta refresh copies hash-matched files forward and toolchain versions are not an invalidation input (RG-REQ-001-L06; D-REFRESH-STALE-1 = B, no invalidation), so the bump is what makes TOOLCHAIN-STALENESS-1's line fire — on every existing store whose served snapshot contains C++ files, and after any refresh by the new binary that copies a C++ file forward (the `carried_forward` provenance, D-REFRESH-STALE-2) — until `rmap repo rebuild <path>`. The other `cpp-core:0.1.0` literals in the tree (indexer/src/resolver.rs test fixtures from :2259, indexer/tests/call_binding_receiver.rs:48, rgr/src/presentation/imports.rs:556) are hand-built fixture values with no coupling to the constant — the resolver gates C/C++ provenance by the `cpp-core:` prefix (indexer/src/resolver.rs:58, "so the gate survives version bumps") — and stay untouched.
5. Nothing else: no resolver, invalidation, storage-schema, daemon, seed, `find`, `dead` (a withdrawn surface) or C-extractor change; no macro-name list; no case-class predicate; `error_sibling_of_declarator_does_not_trigger_macro_name_extraction`'s doc comment corrected, assertion untouched.

### 2.2 Evidence taxonomy
| Input | Outcome | Bound test / check |
|---|---|---|
| field + `GUARDED_BY(member)` (plain / pointer) | typed member; no node | CAM-A01 (1), (2); CAM-A04 (−20 nodes) |
| definition + trailing macro, displaced declarator in the ERROR | one node, today's name, marked undetermined [displaced, macro] + basis | CAM-A01 (3), (6); CAM-A04 |
| split prototype + typeless macro definition | prototype unmarked (a decl node only when it is a `field_declaration`); definition marked undetermined [prototype name, macro] + basis | CAM-A01 (3), (4), (5), (6); CAM-A04 |
| ambiguous shape without member evidence (`int MYAPI foo(Foo x)`, `int seed_ MACRO(Foo)`) | today's node, marked [ERROR identifier, fd identifier] | CAM-A01 (7) |
| two-line trailing macro on a prototype | one unmarked declaration | CAM-A01 (8) |
| `;`-terminated prototype + typeless definition | unpaired, unmarked (stated) | CAM-A01 (9) |
| `Q_OBJECT` | nothing | CAM-A01 (10) |
| marked member on `explain` / JSON / partial or malformed marker | marker row / `identity`+`identity_candidates`+`identity_basis` / storage error at the read, unreadable section at the render | CAM-A02; CAM-A04 |
| leveldb / OpenXcom | §2.3 / identical in the eight tables written from extractor output | CAM-A04 |
| an existing C++ store (copy of the v0.19.0 leveldb root) read by the candidate / a fresh index | `C++ facts from cpp-core:0.1.0 (current cpp-core:0.2.0)` line on `orient` and `check`, `TOOLCHAIN_STALENESS` incomplete, exit 2, the C++ family in `check --json` / `{"state":"current"}` | CAM-A04 |

### 2.3 Outward proof (leveldb, fresh isolated index by the candidate vs the manager's before-store)
Macro-named rows 35 → 15, all 15 keep today's name and carry `identity: undetermined`, the basis and the predicted pairs, no other node carries a marker key; nodes −20; in-body calls on the newly typed members carry `receiverType` (`state_cv_` at db/skiplist_test.cc:312/320, `file_map_` in `InMemoryEnv`: 0 → > 0 typed CALLS edges; out-of-line uses such as `snapshots_` in db_impl.cc stay untyped); `explain leveldb::AtomicCounter` `Members (7)` → `Members (6)`: `LOCKS_EXCLUDED (METHOD, identity undetermined — 2 candidates: IncrementBy, LOCKS_EXCLUDED)  db/db_test.cc:47` (and `Read`/`Reset` alike), `IncrementBy (METHOD, decl)`, no `GUARDED_BY`; `--json` carries the marker on the three `LOCKS_EXCLUDED` members; `explain leveldb::DBImpl` `Members (73)` → `Members (69)`; CALLS ≥ / unresolved ≤; self-loops 0; `files` inventory identical; every surviving node keeps its stable key (repo-uid prefix normalized) and every field, its metadata moving only on the 15 marked keys. OpenXcom identical in the eight tables written from extractor output (nodes with stable keys, edges with resolution and spans, unresolved edges, files, file_versions, measurements, file_signals, extraction_edges) — only run-specific identity, timestamps and version-provenance strings (`extractor`, `measurements.source`, `toolchain_json`) excluded, the last because they carry the bumped version by design; later-pass tables (modules, surfaces, boundaries, inferences, seeds) are not compared and not claimed. Staleness (CAM-A04): a copy of the v0.19.0 leveldb root read by the candidate renders the C++ staleness line on `orient` and `check`, `check` exits 2 with `TOOLCHAIN_STALENESS` incomplete, and the fresh after-root is current.

## 3. Regression watch
| Preserved | What would regress | Proof |
|---|---|---|
| RG-REQ-001-L02/L03 | an edge to a phantom; a self-loop; a row leaving unresolved without a typed binding | CAM-A04 |
| RG-REQ-001-L04/L05 | a renamed node or moved stable key | CAM-A01 (`file_node_has_correct_stable_key`; names asserted as today's); CAM-A04 (every surviving leveldb node's stable key and fields before = after; OpenXcom stable keys in the full projection) |
| RG-REQ-001-L06 (CHANGED, ratified) | the bump not reaching the agent: an existing C++ store reading current, or a fresh index reading stale | CAM-A04 |
| RG-REQ-001-L07/L08 | `is_test`/`is_generated` moving under an extractor change | CAM-A04 (`files` inventory identical, leveldb and OpenXcom); CAM-A02 (repo-index untouched) |
| RG-REQ-002-L01–L10 | a coerced default, a zero for unknown, a changed label | CAM-A02 (storage-error and unreadable-section tests; untouched crates) |
| P-CAM-01 | a corpus without the construct moving | CAM-A04 (OpenXcom, eight extractor-output tables) |
| P-CAM-02 | shape (a)/(b), `forward_decl`, receiver typing, any existing test | CAM-A01; CAM-A02 |
| RG-REQ-011-L06, P-CAM-03 | isolation, hygiene; the manager's roots never served or changed | CAM-A03; CAM-A04; CAM-A05 |

## 4. Stop conditions
Precondition (D-REFRESH-STALE-1's queue order: TOOLCHAIN-STALENESS-1 ships first), on ACCEPTED EVIDENCE, not a source string: unless `git show HEAD:docs/assurance/TOOLCHAIN-STALENESS-1/implementation-review.json` exists with `workItemId` TOOLCHAIN-STALENESS-1 and `result` accepted (the post-review record, committed only after the operator's acceptance — absent on 76f23a58), STOP before any edit: the version bump would change stored provenance with no accepted surface that reports it, and the §6 ship statement would be unproven. CAM-A04 re-asserts the gate first and proves the consequence on the candidate. An existing `-after` or `-stale` root when its check starts fails that check by design: inspect it, record what it is and why it is discarded, remove it deliberately, rerun — never an unconditional delete; the manager's `-before` roots are read `?immutable=1` (their `-wal` asserted empty) and are never served — the `-stale` copy's registry is rewritten to the copy before it is served. Frozen: node names, qualified names and stable keys of every existing node (the extractor never picks a candidate); the C extractor, the resolver, storage schema (the key is additive `metadata_json`), the daemon (`dispatch.rs` untouched — the payload is serde of the DTO), the seed crate and `find`, the grammar; no macro-name list; no lexical rule; no withholding. If a fourth recovery shape appears in the leveldb inventory, record its tree and STOP + DECISION_REQUIRED. If a §2.3 literal differs, STOP and report the construct. Nothing outside the eight candidate paths. STANDING HONESTY RULES. Unmet DoD → STOP. Do NOT commit. Run every check to completion in the FOREGROUND; end your turn only with the evidence object.

## 5. Validation (ORDERED; `build-progress.md` after EACH step)
0. On the CLEAN tree: record `git rev-parse HEAD`; `registry-before <full sha256>` in build-progress.md; verify the two `-before` roots exist (§7); verify the §4 precondition (`git show HEAD:docs/assurance/TOOLCHAIN-STALENESS-1/implementation-review.json` exists with `workItemId` TOOLCHAIN-STALENESS-1 and `result` accepted) — absent → STOP. 1. Failing tests FIRST (the eleven cpp-extractor tests, the storage/agent/rgr tests named in CAM-A02). 2. CAM-A01 → CAM-A02 → CAM-A03 — NEVER `cargo test --workspace`. 3. CAM-A04 (build, index the two after-roots — each created only when absent —, captures, store oracles, then — inside the same check — the staleness consequence on a copy of the leveldb before-root with the leveldb after-root as the current control). 4. CAM-A05 (deletes the two after-roots and the stale copy), hand-off with the evidence object (each check's outcome NESTED under `outcome`; `supportingEvidence` non-empty).

## 6. Definition of done
All five checks pass; §2.3 holds; the ship block (docs/ROADMAP.md, operator at closeout) states that existing stores that serve C++ files keep serving macro-named, unmarked members (and the 20 field phantoms) until `rmap repo rebuild <path>` — a refresh by the new binary does not fix copied-forward files — and that the running binary says so on `orient`/`check` (TOOLCHAIN-STALENESS-1's `C++ facts from cpp-core:0.1.0 (current cpp-core:0.2.0) — run rmap repo rebuild <path> to refresh`, proven by CAM-A04 — its captured line quoted in the report); the report states RG-REQ-002-L11 as PARTIAL (store marker + `explain`; `find` is 1B's), never as implemented; the report quotes, as code-under-analysis: `db/db_impl.h:189` BEFORE (`leveldb::DBImpl::GUARDED_BY … (decl)`) and AFTER (no row — `snapshots_`'s calls are all in out-of-line definitions in db_impl.cc and stay untyped), the typed-member witness `db/skiplist_test.cc:320` `state_cv_.Signal();` (its CALLS edge's `receiverType` before: absent; after: the member's declared type), `db/db_test.cc:47` BEFORE (`IncrementBy (METHOD, decl)` + `LOCKS_EXCLUDED (METHOD)`) and AFTER (the marked row verbatim), `port/port_stdcxx.h:59`'s row and its `identity`/`basis`/`identity_candidates` from the store, the leveldb totals before/after, and the OpenXcom identity line.

## 7. Corpus roots (pre-provisioned by the manager on 2026-09-23 with the installed v0.19.0 binary — this HEAD's rust tree)
- `/private/tmp/CPP-ATTRIBUTE-MACRO-1-leveldb-before` (`/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/leveldb` 7ee830d), `/private/tmp/CPP-ATTRIBUTE-MACRO-1-openxcom-before` (`/Users/apple/Documents/APLICATII BIJUTERIE/legacy-codebases/OpenXcom` 1edb0a5); READ with sqlite3 only (`?immutable=1`; the leveldb root has no `-wal`, OpenXcom's is 0 bytes — measured 2026-09-23), except that CAM-A04 copies the leveldb root with `cp -R` and asserts its digests unchanged. Their `registry.json` carries the ABSOLUTE `db_path` of the manager's store, so a copy is served only after that path is rewritten to the copy. The builder creates `/private/tmp/CPP-ATTRIBUTE-MACRO-1A-leveldb-after` and `/private/tmp/CPP-ATTRIBUTE-MACRO-1A-openxcom-after` with the candidate (same recipe, stdio, auto passes off) and `/private/tmp/CPP-ATTRIBUTE-MACRO-1A-leveldb-stale` (CAM-A04's copy), each only when absent, and deletes all three in CAM-A05. The retained roots under `~/repo-graph-retained/` are never served; the operator registry is read only for its digest.

## 8. Follow-ups (not this slice)
CPP-ATTRIBUTE-MACRO-1B: the resolver indexes each marked node under both candidate names so `mutex_.Lock()` binds INFERRED (RG-REQ-002-L11, RG-REQ-005-L02) and `callers leveldb::port::Mutex::Lock` answers with inferred callers; `find`'s seed rows (the `is_decl` column, migration 034) gain the identity marker; the focus header for a marked focus. `;`-terminated prototype + typeless macro definition (`TEST_METHOD(A) {…}`) keeps today's reading (a method named by the macro) — not marked here, a stated limit. RG-REQ-002-L11's field example `explain leveldb::port::Mutex` is ambiguous on leveldb (two `Mutex` classes: port_example.h:24, port_stdcxx.h:51) — text-only correction for the next bootstrap revision (use `explain leveldb::AtomicCounter`). duckdb (799ed64) carries the field shape 40 times (`DUCKDB_GUARDED_BY(lock)`, some with an initializer whose tree differs) — moves at its next reindex, reported then. Out-of-body calls on the newly typed members stay untyped (`current_class` is set only inside the class-body walk, extractor.rs:812/:890, and read at :2075-2082). Residual of another class, unchanged by this slice (RG-REQ-005-L02): `helpers/memenv/memenv.cc:235` `if (file_map_.find(fname) == file_map_.end()) {` — the `end()` call binds to `leveldb::Slice::end` by the unique-name rule before AND after (the new `receiverType` names a typedef with no indexed container, so the receiver-type stage finds no survivor and falls through, resolver.rs:1062-1100); a `std::map` call bound to a leveldb class is a unique-name fabrication for the call-binding queue, not for this slice. RG-REQ-002-L11 identity case on `find` (the marker on `find`'s rows) is 1B's — §0.

## 9. Baseline history
- INPUT-1 revised in PREP-2 iteration 2 (2026-09-23) for review-1's two findings: the staleness proof is folded into CAM-A04 (gate first, then the stale-copy `orient`/`check`/`check --json` assertions and the fresh-root control), so the allocation keeps the implementation packet's five checks CAM-A01..A05 with A05 immediately after A04 and no proof is dropped; the OpenXcom control compares EVERY table written from extractor output — adding file_versions (the source of the indexed/tracked-only counts), measurements, file_signals and extraction_edges to nodes/edges/unresolved/files — and every completeness claim now names exactly the tables its oracle compares (rule: a preservation claim names its compared set; later-pass tables are stated as not compared).
- INPUT-1 revised in PREP-2 iteration 1 (2026-09-23) for review-0's four findings: one allocation rule (§0) — `implements` only what the slice completes, `changes` what it moves without completing — so RG-REQ-002-L11 moves to `changes` (partial; an L11 PASS only on 1B's `find`+`explain` evidence) and RG-REQ-001-L06 moves from `preserves` to `changes` (the ratified staleness consequence of the bump), leaving CAM-A02's no-behavior-change set; the TOOLCHAIN-STALENESS-1 gate is its accepted, committed implementation-review record, not a source grep, and the new CAM-A06 proves on a copy of the v0.19.0 leveldb root that `orient`/`check` expose the stale C++ family with `check` incomplete (exit 2) while a fresh index is current; CAM-A04 compares every surviving leveldb node's stable key and fields (repo-uid prefix normalized — the uid is a random ULID) and the full semantic OpenXcom projection (stable keys, resolution, spans, all unresolved-edge fields), reads the manager's roots `?immutable=1` after asserting their WAL empty, and creates each after-root only when absent (no unconditional delete).
- INPUT-1 revised for the document item CPP-ATTRIBUTE-MACRO-1A-PREP-2 (2026-09-23): re-pinned under RG-BOOTSTRAP-INPUT-9 (RG-REQ-001-L06 amended — toolchain staleness); the first document item's two decisions answered by records — D-CAM-MARKER-BOUNDARY-1 (operator, option A) and D-REFRESH-STALE-1 (human, option B) — added as governance and to `requiredDecisionIds`; §2.1 step 4 (the version bump) and CAM-A01's version assertion added; ship block: existing stores need `rmap repo rebuild <path>`. Refined by the PREP-2 builder against the sources: §0 quotes L06's toolchain sentence and its served-family rule (the line fires only on stores serving C++ files); §2.1 step 4 carries L06's changed-config-widening exception and names the other `cpp-core:0.1.0` fixture literals as uncoupled (prefix gate, resolver.rs:58); the TOOLCHAIN-STALENESS-1-first precondition (absent on 76f23a58) added to §4 and §5 step 0; the OpenXcom projection states that it excludes the `extractor` column and `toolchain_json`, which move by design.
- INPUT-1 (2026-09-23): first admission baseline under RG-BOOTSTRAP-INPUT-8 (parents RG-REQ-001, RG-REQ-002, RG-REQ-011; decision record D-CERTAINTY-MARK-1 as governance). The first cut's five document items (CPP-ATTRIBUTE-MACRO-1-PREP…PREP-5) are the grounding record for the symmetric tree. Corrected by the document item CPP-ATTRIBUTE-MACRO-1A-PREP before first review: the stored marker carries `identity: undetermined` and `basis` beside the candidates (RG-REQ-001-L01's own words; D-CERTAINTY-MARK-1's node keys) and the candidate order is structural; a partial or malformed marker is a storage error at the read (as `forward_decl` is today) and the unreadable section at the render; CAM-A04's `--json` assertion made non-vacuous, the two unchecked `explain` exit codes checked, in-body typed-member witnesses and the `files` identity (L07/L08) added; the L11 1A/1B coverage split stated.
