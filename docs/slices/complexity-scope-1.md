# COMPLEXITY-SCOPE-1 — complexity centers rank the repository's own production code, and say what they excluded

Status: SPECIFIED (2026-09-12) · Track: audit round six, Q7 (HIGH; never worked — `files.is_generated` dead-false since f63a94e). CODE slice: indexer (`orchestrator.rs` generated sniff), classification (vendored predicate moved inward + `dependencies`), agent (`aggregators/complexity.rs` filter + excluded count), rgr (`orient_sections.rs` exclusion line). Requires reindex. Builder: Codex gpt-5.6-sol; reviewer: Codex gpt-5.6-terra.

## 0. Requirements allocation

**Implements:** RG-REQ-009-L01 (complexity centers rank production code; excluded count stated), RG-REQ-001-L08 (generated/vendored flags at index time), RG-REQ-003-L02 (headline scope), RG-REQ-002-L02 (the exclusion predicate shared across docs/seeds/complexity).

**Changes (pre-authorised, simulated in RC-9):** poco top-5 → `Foundation/src/*Encoding.cpp` (218 excluded); codegraph #1 → `src/mcp/tools.ts — handleExplore` (12 excluded); repo-graph's `tests/parity.rs` leaves the top-5 (2 excluded); `high_complexity_count` is the filtered count; vscode's `multilineModelWeights.ts` stays ranked (no persisted fact distinguishes it — an honest residual, NOT a defect of this slice). A populated `is_generated` also narrows the SEED corpus (`seed_impl.rs:81` already excludes generated files) — seeds/`find` move on repos with generated files; reported.

**Preserves (§3):** the LiveGraph complexity no-loss certificate (filter agent-side only), RG-REQ-003-L07 (budget invariance), RG-REQ-012-L04, `hotspots` (own flags), RG-REQ-008-L04 (docs vendored count — moves only if the vendored list widens, stated), RG-REQ-010-L06 (seed partition logic unchanged beyond the corpus), RG-REQ-001-L07 (`is_test` unchanged).

## 1. Problem (ROOT-CAUSED — RC-9)

The only filter on the complexity path is `complexity >= 20` (`agent_impl.rs:1638`); the SQL joins `files` but selects neither `is_test` (populated) nor `is_generated` (column exists; every construction site hard-codes `false` — `orchestrator.rs:333,551,841,1897,2036`, `compose.rs:1365,1450`; 0 rows on every store); `is_vendored_path`/`VENDORED_SEGMENTS` lives in an adapter crate (`daemon-runtime/src/handlers/quality/support.rs:33-52`) the core cannot import and lacks `dependencies`. Result: poco's top-5 are `dependencies/pcre2/*` + a Bison parser; codegraph's #1 is a tree-sitter `@generated` parser; repo-graph's #2 is a test file.

## 2. Contract

1. Index-time `is_generated`: a first-8-lines marker sniff (`@generated`, `Automatically generated`, `DO NOT EDIT`, `made by GNU Bison`, `generated by`) at the one site where content is in hand and `is_test` is computed (`orchestrator.rs:551`); the storage write and the seed consumer already exist. A repository-declared exclusion (`.gitattributes linguist-generated`) is NOT part of this slice (unratified input).
2. Vendored predicate: `VENDORED_SEGMENTS` gains `dependencies` (and `contrib`); `is_vendored_path` moves to an inner crate (classification) so agent can call it; daemon-runtime re-exports or calls the moved function — ONE definition.
3. Aggregator filter: `query_high_complexity_symbols` SELECTs `is_test`/`is_generated` (the files join is already there) into `AgentComplexityMeasurement`; `aggregate_with_threshold_cancellable` drops vendored/generated/test rows AFTER the storage read and BEFORE truncate; `high_complexity_count` from the filtered set; the excluded count carried in `HighComplexityEvidence`; both renderers print `N generated/vendored/test symbols excluded — --include-all`; `--include-all` restores the unfiltered ranking. The filter MUST NOT move into the storage query (the LiveGraph cert compares that read's full set).
4. Outward proof (simulated expectations): poco headline and section show Foundation encodings and `218 … excluded`; codegraph #1 `handleExplore`; repo-graph top-5 without `tests/`; vscode unchanged #1 with the exclusion line — stated as the residual.

## 3. Regression watch

| Preserved L | What would regress | Proof |
|---|---|---|
| LiveGraph complexity certificate | turns RED if the storage read is filtered | `daemon-runtime/src/orient_lg_decisions/complexity_cert.rs` tests green; the cert's comparison set unchanged |
| RG-REQ-003-L07, RG-REQ-012-L04 | numbers identical across budgets; monotonic length | `orient_density_tests.rs::rendered_numbers_are_budget_invariant_length_is_monotonic`, `budgets_change_length_never_numbers` |
| RG-REQ-003-L02 | symbol-named headline | `complexity_headline_names_symbols_not_file_rollup` |
| `hotspots` | unchanged (own `--exclude-vendored`) | `hotspots_command.rs` green; hotspots output byte-identical on a repo without generated files |
| RG-REQ-008-L04 | docs vendored count moves only with the widened list — STATE it | `docs_tests.rs::vendored_demoted_from_headline_and_listing_but_stated`; docs list before/after on poco |
| RG-REQ-010-L06, RG-REQ-010-L01 | seed corpus narrows only by newly generated files; ranking logic unchanged | `repo-graph-seed` tests green; `find` on leveldb byte-identical (no generated files) |
| RG-REQ-001-L07 | `is_test` computation untouched | `is_test_*_reproduction.rs` green |
| RG-REQ-001-L09 | determinism | parity harnesses green; the sniff is content-only |
| RG-REQ-012-L01 | exit codes | `exit_code_contract.rs` |

## 4. Stop conditions

Frozen: the storage read's set (cert), `is_test` rules, seed ranking formula, schema (column exists), wire (additive: excluded count), exit codes, `hotspots` flags. If the sniff misclassifies a hand-written file in the fixture corpus (a false generated), STOP and report — the marker list is narrowed, never widened by guesswork. No `.gitattributes` reading. STANDING HONESTY RULES. Do NOT commit.

## 5. Validation (ORDERED)

1. Failing tests FIRST: sniff (positive for each marker; negative for a hand-written file whose body mentions "generated"); aggregator filter with generated/vendored/test fixtures and the excluded count; `--include-all`; cert test unchanged.
2. `cargo test -p repo-graph-indexer`, `-p repo-graph-classification`, `-p repo-graph-agent`, `-p repo-graph-rgr --lib presentation::orient`, `-p repo-graph-daemon-runtime --lib orient_lg_decisions`.
3. Live proof: isolated index of codegraph (small; has `parser.c`) and repo-graph's own `rust/crates/storage/tests` fixture subtree or leveldb (no generated files → exclusion line `0 … excluded` or omitted — decide and state); worktree before-binary. poco/vscode OPERATOR-RUN from fresh isolated indexes (reindex required).
4. `build-N.md`.

## 6. Definition of done

§2.4 on codegraph + leveldb; §3 green; seed-corpus movement reported; gates green.

CORPUS PATHS: codegraph, leveldb, poco, vscode at ../legacy-codebases/<name>; repo-graph is THIS repo.
