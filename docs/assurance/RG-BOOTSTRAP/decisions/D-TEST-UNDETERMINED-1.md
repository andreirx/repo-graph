# D-TEST-UNDETERMINED-1 — Does "never from a filename alone" forbid the path conventions the routing rules use?

Raised: 2026-09-23 by the document review of TEST-EDGE-SCOPE-1A (reviewer codex gpt-6-sol): RG-REQ-001-L07 both endorses "the per-language conventions recorded in the routing rules" (which are path conventions: `__tests__`, `.test.`, `.spec.`, `/test/`, `/tests/` at `indexer/src/routing.rs:206-214`) and forbids evidence "from a filename alone"; TEST-EDGE-SCOPE-1A needs one more convention, `testsuite/` (poco: 788 CppUnit test files flagged as production at v0.19.0).
Resolved: 2026-09-26 by the HUMAN: "this is exactly the kind of thing that an agent can just resolve by itself if our heuristics show ambiguity — so pick whatever is easiest and just mark them for opening — with thoughtful wording like 'can't determine — open it and look inside'".

## Resolution (the easiest rule, applied by the operator under that ruling)
The routing path patterns (`indexer/src/routing.rs:206-214`: `__tests__`, `.test.`, `.spec.`, `test/`, `tests/`) are evidence, with `testsuite/` added; L07 enumerates them. A file whose path carries a test word as a whole word but matches no convention and no marker is test status UNDETERMINED: it stays in the production partition; partitioned surfaces state the count with "open them and look inside"; `explain <file>` says "test status: can't determine — open it and look inside". Measured on the v0.19.0 witness stores against the routing patterns plus `testsuite/`: poco 25 (the CppUnit framework headers `CppUnit/include/CppUnit/Test.h`, `TestCaller.h`, …), leveldb 4 (`db/c_test.c`, `util/testutil.cc`, `util/env_posix_test_helper.h`, `util/env_windows_test_helper.h`), django 5 (`django/contrib/admin/tests.py`, `django/contrib/staticfiles/testing.py`, …), kafka 28 (`connect/test-plugins/**`, scripts), nginx 1 (`src/misc/ngx_cpp_test_module.cpp`), FRAKTAG 1, amodx 0, vscode 144. Standing rule: definition questions of this class are decided by the operator as the easiest rule plus marking, not escalated.
