# D-PRB-RATE-1 — May PYTHON-RECEIVER-BINDING-1 claim RG-REQ-005-L02 and RG-REQ-002-L11 while trust and its rate consumers still count the newly INFERRED calls as resolved?

Raised: 2026-09-24 by the first document review of PYTHON-RECEIVER-BINDING-1 (reviewer codex gpt-6-sol). The requirements author had chosen the bounded reading (option C below: the persisted resolved-call aggregate `resolved_calls_total` → `snapshots.resolved_call_count` and the live `query_call_resolution_total` unchanged, django's aggregate staying 64,354) and listed the rate as a residual; the reviewer holds that L02's own words — "excluded from trust's resolved share and from `dead`" — and L11's "never counted among certain facts" leave no room for that residual.
Resolved: 2026-09-24 by the OPERATOR — option A. The human may override.

## Options
- **A (ratified by the operator)** — inside this slice: the resolved-call numerator (the orchestrator's `resolved_calls_total` tally at orchestrator.rs:1043 that feeds `persist_resolved_call_aggregate`, and the live `query_call_resolution_total` fallback at storage/src/call_resolution_reads.rs:205-210) counts only `static`/`dynamic` CALLS; the measured universe keeps the inferred calls (they are calls the index observed); every rate surface (`trust`'s calls-resolved share, `check`'s CALL_GRAPH_RELIABILITY summary, orient's reliability line) states the inferred count beside the rate ("40% resolved (+5,144 inferred name-only, not counted)"). Reward: L02 and L11 are verifiable end to end in one increment; no rate surface asserts a fabricated binding as resolved; the universe is stated (RG-REQ-002-L03). Risk: the rate computation's input and three rate surfaces move — allocated paths and renderer tests grow (the author's 35-path allocation already names trust, check and orient); django's calls-resolved share falls from 40% to roughly 32% and the band may cross a threshold — a true figure replacing a false one, stated in the ship block.
- **B** — reduce the numerator only, leave the universe and the surfaces silent. Reward: a smaller change. Risk: the inferred calls vanish from the rate's universe without a word — L11's remainder rule and L03's "counts state their universe" are violated.
- **C** — leave the rate unchanged and record it as a residual (CALL-RATE-INFERRED-1). Reward: the slice stays bounded to the relationship surfaces. Risk: trust and check keep reporting name-only guesses as resolved calls after the slice that removed them from every other surface — the fabrication class the audit ranked first survives on the headline; L02's text is contradicted by the slice that claims to implement it.

## Resolution
A. PYTHON-RECEIVER-BINDING-1's allocation carries the numerator change (orchestrator tally + live fallback), the universe statement and the three rate surfaces with paired human/JSON tests; the ship block names django's before/after share.
