# D-CIB-REVIEW-1 — The four decisions of CPP-INCLUDE-BASENAME-1's first document review

Raised: 2026-09-24 by the first document review of CPP-INCLUDE-BASENAME-1 (reviewer codex gpt-6-sol; decisions D-CIB-L06-1, D-CIB-REASON-1, D-CIB-REFRESH-1, D-CIB-COUNT-1; findings F-CIB-GATE, F-CIB-ORACLE, F-CIB-REFRESH).
Resolved: 2026-09-24 by the OPERATOR, each verified on the source and the before-stores. The human may override.

## 1. D-CIB-REFRESH-1 = A — refresh recomputes every binding; only extraction is copied forward
Verified: `indexer/src/orchestrator.rs:1005-1042` streams EVERY `extraction_edges` row of the snapshot (`query_extraction_edges_batch`) — copied-forward rows included (`storage/src/indexer_impl.rs:966-1020` copies `extraction_edges` with remapped source uids) — and calls `resolve_edges` against the full current index; `:1963-1967` builds the resolution context from copied + extracted + contract files. The slice document's §3 sentence ("a copied-forward include keeps its binding until rebuild") was FALSE and is withdrawn. The truth, to be written and tested: a refresh with the new binary re-resolves every include of the snapshot, so a unique-basename binding appears (and becomes ambiguous when a second file with that basename is added) on the next REFRESH, not only on rebuild; what is copied forward is the extraction (the include's spelled `target_key`, its line); the toolchain-staleness surface (RG-REQ-001-L06, TOOLCHAIN-STALENESS-1) reports the extraction version gap by its own rule and is not this slice's to restate. Test: a fixture indexed with one `foo.h`, refreshed after a second `foo.h` is added → the include moves from inferred (unique basename) to unresolved (`imports_ambiguous_basename`) on refresh. Rejected: B (retain old bindings until rebuild — new mechanism contradicting the re-resolution path and full-index parity); C (defer).

## 2. D-CIB-COUNT-1 = A, at the cause — trust counts the IMPORTS family
Verified: the `modules list` headline sums the shared `MODULES_LIST_UNRESOLVED_IMPORT_CATEGORIES` (all four categories: `imports_file_not_found`, `imports_wildcard`, `imports_ambiguous_suffix`, `imports_ambiguous_match`; `storage/src/trust_impl.rs:1361-1368`), while trust's `sum_unresolved_imports` (`trust/src/rules.rs:393-399`, doc "Sum unresolved counts for IMPORTS-family categories") reads the single key `imports_file_not_found` — a name that does not match what it does; its test `sum_unresolved_imports_picks_imports_family` (`:995`) asserts the narrow behaviour under the family name. The two surfaces already disagree wherever a non-legacy category exists: kafka 84 `imports_ambiguous_suffix`, poco 368 `imports_ambiguous_match`, grpc-java 5 + 3, petclinic 2 `imports_wildcard`, glamcrm 58 (before-stores, 2026-09-23/24). This slice's new `imports_ambiguous_basename` rows (nginx 299 → the 13 non-unique-basename rows) would widen it. Resolution: trust sums every IMPORTS-family category through the SAME shared set the headline uses (one definition, two readers); the test is renamed to what it now proves; a same-snapshot coherence test asserts trust's figure equals the headline's. `rust/crates/trust/src/rules.rs` and the module that exports the shared set join the allocation; the ship block names the trust movement on each corpus above. Rejected: labelling trust's narrower basis inline (states the universe but leaves two numbers for one fact where one reader is simply wrong); B (partial L02 with a gate — the fix is five lines); C (defer).

## 3. D-CIB-L06-1 = B — the INDEXER_VERSION bump is a reported consequence, not an allocated change
TOOLCHAIN-STALENESS-1 owns RG-REQ-001-L06 with a generic mechanism (the running versions compared with the snapshot's provenance) proven by its own version-gap fixture; every later slice that bumps `INDEXER_VERSION` (DEPS-GRADLE-CATALOG-1A, accepted with the same reading at 9755ba47) STATES the consequence in §0's reported consequences and in its ship block ("stores indexed before compiler-… report the indexer stale until rebuild") and does not allocate L06. Reward: one owner for the mechanism; no repeated obligation per bump. Risk: the reviewer of each slice sees the consequence as prose — it is prose because the obligation is TOOLCHAIN-STALENESS-1's. Rejected: A (a fifth parent and a manifest revision for a consequence another slice's mechanism already reports); C (defer).

## 4. D-CIB-REASON-1 = A — the reason reaches the agent-facing row
The packet says `implements RG-REQ-002-L11`; the candidate demoted it to `changes` because `rgr/src/presentation/imports.rs:92-99` renders resolution but not reason. Resolution: the vertical extends to `imports <file>`: an inferred include row renders `inferred: unique basename → <candidate path>` (human) and carries `{basis, candidates}` (JSON); the daemon handler and the storage read that carry the metadata join the allocation (the requirements author names them). Deep vertical slices (operator practice): a fact the index records is visible on a named surface in the same slice. Rejected: B (a follow-up while the row says only `inferred`); C (defer).

## Oracle corrections carried into the same re-admission
- F-CIB-GATE: every gate is a fail-closed link. The cargo helper's `&& set -o pipefail; cargo test …` breaks the `&&` chain at the `;` — the form is `&& ( set -o pipefail; cargo test … 2>&1 | tee LOG ) && grep …` (a subshell keeps the chain and the status). CIB-C05 verifies both prerequisite records' `result == accepted`, not their existence. (The manager's generator produced the broken form; every not-yet-admitted packet of this run is corrected the same way.)
- F-CIB-ORACLE: the before/after store comparison is `rg-store-diff.py` alone (every normalized field of files/nodes/edges/unresolved rows; `--expect edges:791 unresolved:791` on nginx; identity on leveldb and poco), with the moved rows' authorized additions asserted by a separate query; no hand-written three-field comparator.
- Witness: `src/core/nginx.c:8-9` includes headers that sit beside it (not the class). The failure witness is `src/core/ngx_config.h:22-34` — `#include <ngx_freebsd_config.h>`, `<ngx_linux_config.h>`, `<ngx_solaris_config.h>`, `<ngx_darwin_config.h>` — each unresolved (`imports_file_not_found`) on the before-store and each with exactly one indexed file of that basename under `src/os/unix/`.
