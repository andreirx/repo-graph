# D-CBR-XFILE-1 — operator decision (2026-09-14)

Raised by: the claude-opus-4-8 builder of CALL-BINDING-RECEIVER-1 (cycle 0 under baseline INPUT-2), as decision CBR-XFILE-1, with checks CBR-C12 and CBR-C17 honestly reported `failed`.

Question: the slice types an indirect receiver from the declaring `field_declaration` / local declaration. leveldb's witness call `versions_->Recover()` sits in db_impl.cc while `versions_` is declared in db_impl.h; the C++ extractor is per-file, so a header-declared member's type is not visible at the call site, and threading a resolve-time field-type index requires `indexer/src/orchestrator.rs` and the `ResolverIndex` shape — outside the three candidate paths. Options: A widen this slice's scope to build that index; B accept per-file typing here, amend CBR-C12 to assert same-file typing plus the honest unresolved remainder, and take cross-file typing as its own slice; C record the gap.

Decision: **Option B now, A as its own slice (CALL-BINDING-RECEIVER-2).** The ratified contract of this slice — an indirect receiver never binds to the caller's own class without evidence; binding only on evidence; the remainder counted — is fully met on a fresh leveldb index (receiver-bearing self-loops 155 → 0; call sites conserved 9413 = 9413; resolved 3261 → 3457 through in-file receiver typing; `callers DBImpl::Recover` = `DB::Open`; trust 37% → 39%; nginx and codegraph byte-stable). Shipping it removes the fabrication today. Cross-file receiver typing is a real product gain (header-declared members are the common C++ case — exactly the delegation wrappers users ask `callers`/`callees` about) and crosses a real seam (the resolver index built by the orchestrator), so it gets its own root-caused packet with the right candidate paths rather than a mid-slice widening. Option C is rejected: the gap is scheduled, not merely recorded.

Also decided: CBR-C17's oracle was wrong, not the fix — `rmap cycles` on two identical fresh indexes differs in the Snapshot id line and in the walk ring's entry point because the daemon iterates module-import edges without a stable order. The corrected check compares the snapshot-id-stripped cycle MEMBER SET; the nondeterministic walk is filed as CYCLES-WALK-DETERMINISM-1 (a determinism defect under RG-REQ-001, independent of this slice).

Consequence: oracle correction OC-2 (CBR-C12, CBR-C17 `expected`/command; §2.6 text; follow-ups named in §4) → baseline INPUT-3; the candidate is re-admitted unchanged.

Authority: operator (in-place-manager), local to this slice's validation text and the split of one ratified requirement's work into two slices; the requirement RG-REQ-005-L01/L02 stays as ratified. The human may override (e.g. choose A). Basis: docs/assurance/RG-BOOTSTRAP/human-authorization.md; D-TME-MOVEMENT-1 (report, don't predict); D-TME-VALIDATION-STALE-1 (oracles corrected, never bent).
