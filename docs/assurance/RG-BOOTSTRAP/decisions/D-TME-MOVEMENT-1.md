# D-TME-MOVEMENT-1 — human decision (2026-09-13): accept the seam fix; correct the predicted movement; root-cause the alias heuristic separately

Raised by: the TRUST-MODULE-EDGES-1 builder (claude-opus-4-8, implementation cycle 0) — STOP + DECISION_REQUIRED under the slice's §4 ("do not adjust rules to hold it"), after all allocated checks except the two outward-proof oracles passed.

Problem: the slice predicted (from RC-5) "Suspicious Modules 48 → 0; the alias-resolution downgrade disappears". With trust's fan counts re-sourced from the derived module edge set, repo-graph has 6 GENUINELY zero-connectivity module candidates (`rust/` umbrella directory, `tools`, `scripts`, two test-fixture directories, leaf crates `detectors` and `git`), kafka 5, FRAKTAG 3 (FRAKTAG has zero cross-module resolved imports: 185 intra of 210), hadoop 2, vcmi 2. `detect_alias_resolution_suspicion` fires at ≥ 3 zero-connectivity candidates — a Layer-2 heuristic calibrated when every module read zero — so the `alias_resolution_suspicion` downgrade still fires on repo-graph, kafka and FRAKTAG and clears only on hadoop and vcmi. RG-REQ-009-L02 as written ("a module with rendered edges shall never be listed as zero-connectivity") is met; the slice's movement text and checks TME-C05/TME-C06 encoded the manager's over-prediction.

Options presented (risk/reward in the manager's report): A accept the fix, correct the prediction, root-cause the heuristic in its own slice; B widen this slice to change the heuristic now; C revert.

Decision: **A** — human, 2026-09-13 ("ok for A"). Consequences: (1) the slice document's §0 "Changes", §2.4, §6 and the TME-C05/TME-C06 expected oracles are corrected to the measured truth (repo-graph 48 → 6 genuinely disconnected, named; alias downgrade persists where ≥ 3 remain; FRAKTAG connected 0 by fact; hadoop/vcmi lose the downgrade); re-baselined as TRUST-MODULE-EDGES-1-INPUT-3 with a fresh review; (2) the implementation resumes on the preserved candidate; (3) ALIAS-SUSPICION-1 is filed as a root-caused follow-up (the ≥ 3 threshold cannot distinguish unresolved imports from genuine isolation).
