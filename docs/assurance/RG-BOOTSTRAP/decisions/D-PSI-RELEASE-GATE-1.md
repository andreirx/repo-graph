# D-PSI-RELEASE-GATE-1 — What keeps newly INFERRED import edges from being read as certain by consumers that do not yet partition by resolution?

Raised: 2026-09-23 by the document item PYTHON-SUBMODULE-IMPORT-1-PREP-2 (reviewer codex gpt-6-sol, F-PSI-CERTAINTY / D-PSI-04) and VERIFIED by the manager on the source: of the storage crate's IMPORTS reads, only the module-edge derivation (`crud/module_edges_support.rs get_resolved_imports_for_snapshot`, `resolution = 'static'`) and a few trust reads predicate on `resolution`; `queries.rs` (cycles `find_cycles` :1254-1302, path traversal :1634-1790, map dependency reads :2874-3034), `agent_impl.rs` (boundary and file-import reads :635/:691/:1469/:1623), `agent_cycle_labeling.rs` (:342/:564) and `trust_impl.rs` (:710/:716/:856) select every IMPORTS edge. Once PYTHON-SUBMODULE-IMPORT-1 (and later CPP-INCLUDE-BASENAME-1) persist `resolution = 'inferred'` IMPORTS edges, those readers would present them as certain — against RG-REQ-002-L01/L02/L11 and RG-REQ-004-L12 (default = static/dynamic production; `--include-inferred`).
Resolved: 2026-09-24 by the OPERATOR — option B. The human may override.

## Options
- **A** — complete every affected consumer partition inside PYTHON-SUBMODULE-IMPORT-1. Reward: the inferred fact is honest on every surface the moment the slice lands. Risk: the slice grows from a resolver stage plus `explain` into cycles, path, map, boundary, trust and their renderers — the exact scope the ratified TEST-EDGE-SCOPE-1 (RG-REQ-004-L12) already owns for the test/resolution partition; two slices would rewrite the same readers.
- **B (ratified by the operator)** — a release gate: PYTHON-SUBMODULE-IMPORT-1 may be implemented and committed, but no release is cut until TEST-EDGE-SCOPE-1B has shipped with its scope set to EVERY IMPORTS reader named above (partition by the importing file's `is_test` and by resolution class; default = static/dynamic production; `--include-tests` / `--include-inferred`; the inferred remainder stated on each surface; JSON partition counts). The gate is recorded in the release-cut checklist (agent-manager `scripts/repo-graph-release-cut.sh` preconditions) and in docs/ROADMAP.md. Reward: one slice owns the reader partition (no duplicated rewrite); the served binary (the operator's installed release) never carries the interim; the queue order the human ratified (… → TEST-EDGE-SCOPE-1 → … → CPP-INCLUDE-BASENAME-1) already places 1B before the include slice that adds the second inferred-import family. Risk: main carries an interim in which a freshly built (unreleased) binary would show inferred imports as certain on cycles/path/map/boundary — bounded to developers running from source, stated in ROADMAP; the release cannot happen before 1B.
- **C** — a time-bounded interim ratified in the catalog. Reward: nothing B does not give. Risk: a catalog amendment for a sequencing fact.

## Resolution
B. TEST-EDGE-SCOPE-1B's allocation shall list every IMPORTS reader above; the release-cut precondition "TEST-EDGE-SCOPE-1B accepted at HEAD" is added to `scripts/repo-graph-release-cut.sh` by the operator before the next cut; PYTHON-SUBMODULE-IMPORT-1's §8 and ship block state the gate.
