# Requirements-catalog corrections queue (RG-BOOTSTRAP)

A catalog edit invalidates the bootstrap baseline and every not-yet-approved slice manifest (CLAUDE.md lesson 2026-09-14), so corrections found mid-queue are QUEUED here and applied together in the next bootstrap baseline revision (RG-BOOTSTRAP-INPUT-n+1: standalone Terra pass over the diff + operator-authored approval, one commit). Each entry: id, date, file, the current text, the corrected text, the evidence, the owner, status.

| id | date | file | current text | correction | evidence | owner | status |
|---|---|---|---|---|---|---|---|
| CC-1 | 2026-09-19 | docs/requirements/rg-req-004-module-model-and-cycles.md (RG-REQ-004-L02) | "derivation is query-time, never persisted" | Two module-edge populations exist and must both be named: (a) index-time PERSISTED MODULE→MODULE IMPORTS rows (`indexer/src/orchestrator.rs:1350-1447 create_module_edges`, Phase 4, directory MODULE nodes) that `find_cycles(snapshot_uid, "module")` and `module_import_edges` run over; (b) the query-time pure derivation `classification/src/module_edges.rs::derive_module_dependency_edges` over `module_candidates`/ownership that `modules list`/`deps`/`trust` use. The L either ratifies one population as THE module edge set (then (a) or (b) is a defect to remove) or states both with their identity spaces (RG-REQ-004-L11 / RC-5 already records the identity-space divergence). Decision for the human at the revision. | D-ECH-002; SQLite provenance investigation 2026-09-19 | next bootstrap revision | queued |
| CC-2 | 2026-09-19 | docs/slices/module-aggregation-1.md:105-110 (D4) and code doc comments rust/crates/repo-graph-livegraph/src/lib.rs:2249-2252, 2283-2287 | "package / path-alias / dynamic / re-export NOT captured" | tsconfig `paths` aliases (`AstImportTsconfigPathResolved`) and LITERAL dynamic imports (`AstDynamicImportResolved`) ARE captured by the in-memory cross-partition overlay (`rebuild_xpart_overlay`, lib.rs:1232-1344; `file_import_edges` lib.rs:1428-1432 pushes every overlay edge into the SCC universe). Still uncaptured: package/workspace-external specifiers, non-literal `import(expr)`, re-exports, targets not in the resident inventory. Correct the D4 wording and the two doc comments (a slice doc is a pinned source in several manifests, so it is queued here, not edited in place). | D-ECH-002; LiveGraph ingest investigation 2026-09-19 | TS-ALIAS-RESOLUTION-1 packet (touches this scope) or the next bootstrap revision, whichever is first | applied 2026-09-20 (RG-BOOTSTRAP-INPUT-4) |
| CC-3 | 2026-09-19 | docs/slices/explain-livegraph-1.md:125 ("CALLEES/IMPORTS/cycles sections become LG-first leaves by direct reuse of the migrated drilldown answers") and the served-route test it ratified (`daemon-runtime/src/explain_coherence_served_tests.rs::explain_cycles_serves_live_cycles_from_livegraph`) | the explain cycles leaf is rebuilt from `module_import_cycles` and labelled `{livegraph}` | SUPERSEDED for the FOCUS cycles value by D-ECH-002 / EXPLAIN-CYCLES-HONEST-1 A-1: the LiveGraph route cannot reproduce the verified walk, so the focus cycle reads delegate to SQLite and the leaf is labelled `sqlite` + `LiveGraphRenderUnsupported`; the test is renamed to its new identity with inverted assertions (A-2). The spec sentence stays as history; a note pointing at D-ECH-002 belongs in explain-livegraph-1.md at the next revision of that pinned source. | D-ECH-002; ECH-IR-001 (fourth admission, codex gpt-5.6-terra) | next bootstrap revision | applied 2026-09-20 (RG-BOOTSTRAP-INPUT-4) |
| CC-4 | 2026-09-19 | docs/requirements/rg-req-011-daemon-and-store-lifecycle.md (completeness) | no L states that a query abandons its work at the next cooperative checkpoint when the client disconnects (DAEMON-CANCEL-1/3 are shipped contracts: `find_cycles_cancellable`, the `*_cancellable` port methods, `AgentCancelCheck`) | add an L: "A read whose client has disconnected shall stop at the next cooperative checkpoint and return a named cancelled error; heavy phases (SCC traversal, per-cycle fan-out, labeling) carry checkpoints; verification: the `*_cancellable` tests incl. `focus_cycle_reads_cancellable_return_cancelled_at_the_checkpoint_before_labeling`" — until then a cancellation finding can only bind to a preservation obligation (ECH-IR-002 → P-ECH-06) | ECH-IR-002 (fourth admission review); EXPLAIN-CYCLES-HONEST-1 A-2 | next bootstrap revision | queued |
| CC-5 | 2026-09-19 | CLAUDE.md (repo-graph) § End-of-Slice Procedure — "Test (always, before handoff): cargo build/fmt/clippy/test in rust/, the smoke scripts AND the isolated live rmap dogfood" | read by the Q5 reviewer as a BUILDER duty (F-DEP-01, P-DEP-05 refinement-required) although the allocated round forbids the builder's `cargo test --workspace` | Under the requirements-assurance paradigm the end-of-slice Test phase is the OPERATOR gate suite (`agent-manager/scripts/repo-graph-gates.sh`: fmt, clippy --workspace --all-targets -D warnings, per-crate tests, consolidation witness, release bins, `dogfood-isolated.sh`) run after review acceptance and before the commit, logged to a file and asserted separately; builders run the allocated per-crate checks; the smoke protocol runs at release cuts/audits. Add that sentence to the section (a pinned governance input in every manifest — apply at the next bootstrap revision). | Q5 second admission review F-DEP-01; the process rule in agent-manager CLAUDE.md (chunked per-crate gates, operator suite after approval) | next bootstrap revision | applied 2026-09-20 (RG-BOOTSTRAP-INPUT-4) |

## CC-6 (2026-09-21) — RG-REQ-009-L02's verification criterion names a test that no longer exists (needs the next bootstrap revision; text-only)

`docs/requirements/rg-req-009-quality-signals-trust-reliability.md:62` binds `trust_tests.rs::suspicious_modules_state_basis_and_point_at_stats`; TRUST-MODULE-EDGES-1 renamed it under D-TME-TEST-NAME-1 to `suspicious_modules_basis_in_reader_frame_no_internal_wording` (rust/crates/rgr/src/presentation/trust_tests.rs:84). Found 2026-09-21 by the read-only ALIAS-SUSPICION-1 grounding. Correction: replace the name.

## CC-7 (2026-09-21) — RG-REQ-009-L02's evidence line predates Q2 (needs the next bootstrap revision; text-only)

`…rg-req-009…md:64` still reads "NOT MET — RC-5 … Queue Q2 TRUST-MODULE-EDGES-1" although Q2 shipped in df08725 (2026-09-13): fans now come from the derived module edge set (`rgr/tests/trust_module_edges_seam.rs`); the structural half is MET and the residual is exactly RC-11 (`detect_alias_resolution_suspicion` still counts genuine isolation — ALIAS-SUSPICION-1). Correction: an evidence line naming the shipped commit and the RC-11 residual.

- CC-6 and CC-7 APPLIED 2026-09-21 as part of RG-BOOTSTRAP-INPUT-5 (the re-baseline that carries the human's ratification of RG-REQ-005-L03's wording, D-PSB-002).

## CC-8 (2026-09-21) — RG-REQ-008-L01's verification criterion carried estimated field numbers and a contradictory byte-stable list (needs the next bootstrap revision; text-only)

`docs/requirements/rg-req-008-documentation-inventory.md:50` read "hadoop 23 → ~545, django 670 → ~674, buildroot 4 → ~77; django/kafka/grpc-java/langchain4j/vscode/storybook/gstreamer/repo-graph byte-stable" — the numbers were RC-8's estimates (buildroot's 233 `board/**/readme.txt` were not counted; `readme` by stem admits them anywhere) and django appeared both as moving and as byte-stable. Found 2026-09-21 by the manager's pre-packet simulation of the ratified rule over 17 checkouts (`scratchpad dd1-sim.py`; the walk reproduces today's product inventory exactly on every corpus without `MAP.md` sidecars). Correction: the measured expectations over the enumerated 17 checkouts (leveldb, FRAKTAG, repo-graph, grpc-java, django, buildroot, kafka, hadoop, langchain4j, vscode, storybook, gstreamer, poco, codegraph, OpenXcom, spring-petclinic, mempalace), the exact byte-stable set, the matching semantics the numbers assume (stem case-insensitive, extension byte-for-byte as today), and the one known non-prose admission (`debian/install`). The requirement statement is untouched; whether extension matching should become case-insensitive (`README.TXT`, `README.MD`) is an open question for the human. Standalone review pass 1 (codex gpt-5.6-terra) REFINE: the case behaviour was implicit and the corpus was not enumerated — both corrected before pass 2.

- CC-8 and the human's D-SD1-001 (RG-REQ-010-L10 wording) APPLIED 2026-09-21 as part of RG-BOOTSTRAP-INPUT-6.

## CC-9 (2026-09-22) — RG-REQ-008-L07's field example lists django's two root files in the opposite order of the frozen ranking (needs the next bootstrap revision; text-only)

`docs/requirements/rg-req-008-documentation-inventory.md` L07 criterion: "django `read: README.rst, CONTRIBUTING.rst, docs/`". `select_orientation_docs` (daemon-runtime/src/modules_method.rs:419-429, MODULES-METHOD-1, pinned by `orientation_root_first_ranking`) ranks root-first then path-ASC, so the product prints `read: CONTRIBUTING.rst, README.rst, docs/ — or the tree.` (today, with both files invisible, `read: docs/ — or the tree.`). Found 2026-09-22 by the DOCS-DISCOVERY-1 grounding. Correction: reorder the example. The requirement statement ("root first") is untouched and does not order root files among themselves.

- D-DD1-003 (2026-09-22, human): RG-REQ-008-L01's statement now says stem AND extension match case-insensitively; CC-8's field numbers revised accordingly (django 616 → 622 with `extras/README.TXT`; langchain4j +1 `langchain4j-milvus/README.MD`, no longer byte-stable — both admitted through the STEM branch: a stem is the name minus a recognised documentation extension, and the extension is now recognised case-insensitively; no uppercase-extension file exists inside any docs tree in the 17 corpora). Carried by RG-BOOTSTRAP-INPUT-7. The human also ruled the `license` stem question closed (no `license` stem; root `LICENSE.txt` stays outside the inventory) — recorded, no catalog change.

## CC-10 (2026-09-23) — RG-REQ-008-L05 and L08 contradicted each other for a document whose bytes cannot be read (three document reviews of DOCS-UNREADABLE-DECODE-1) → L08 amended: `content_hash` always present, `null` when unread; L05 names it (D-DU-CC10; applied in RG-BOOTSTRAP-INPUT-8).

## CC-11 (2026-09-23) — the catalog had no rule for facts the index cannot determine; four slices stalled between guessing, withholding and deferring → RG-REQ-002-L11 in the human's words, with cross-references in RG-REQ-001-L01, RG-REQ-005-L02, RG-REQ-006-L04/L11, RG-REQ-008-L08 (D-CERTAINTY-MARK-1; INPUT-8).

## CC-12 (2026-09-23) — RG-REQ-004 had no test-scope statement although `files.is_test` is a stored fact and the module-edge derivation never reads it → RG-REQ-004-L12 (human: option A; D-TEST-SCOPE-1; INPUT-8).

## CC-13 (2026-09-23) — RG-REQ-006 never said whose build block declares for which Gradle module, nor that catalog aliases are declarations → RG-REQ-006-L13 (human: option A, two slices; D-GRADLE-DECLARED-1; INPUT-8).

## CC-14 (2026-09-23) — RG-REQ-006-L04's Python clause named neither the submodule rule nor its certainty (PSI-R01/PSI-D02) and L11 forbade single-segment matching outright → amended as INFERRED forms under L11 (INPUT-8). The TS clause is not amended (its parenthesis names an open human ruling; the ratified no-go stands).

## CC-15 (2026-09-23) — RG-REQ-001-L06 said which COMMIT a snapshot describes but not which TOOLCHAIN produced it; except when a changed-config widening re-extracts its scope, delta refresh copies forward files whose content hashes match (toolchain versions are not an invalidation input), so an extractor upgrade never reached existing stores and nothing said so → L06 amended with the toolchain-staleness line and the explicit-rebuild remedy (human: option B, D-REFRESH-STALE-1; INPUT-9).

## CC-16 (2026-09-23) — the INPUT-9 draft compared the running toolchain with the stamp on the latest snapshot, but a refresh stamps the RUNNING toolchain while copying old facts forward (orchestrator.rs:1812-1821), so the line would vanish after the first refresh → L06 amended: the refresh stamp carries, under `carried_forward`, the set of every version that may have produced the copied-forward facts; a family is stale when the running version differs from any of them (human: option B, D-REFRESH-STALE-2; the set shape is an operator amendment after review pass 4; INPUT-9).
