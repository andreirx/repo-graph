# CPP-INCLUDE-ROOTS-1 — oracle corrections (docs/MANAGER.md § Oracle corrections)

| id | date | check(s) | old → new | evidence | approver | slice digest before → after |
|---|---|---|---|---|---|---|
| OC-1 | 2026-09-18 | CIR-C09, CIR-C10 | `R=count(*) FROM edges WHERE type='IMPORTS'` → FILE→FILE IMPORTS only (JOIN nodes s,t kind='FILE'), plus `ME` module-level IMPORTS reported as `module_edges=`; `RB`/`TB` sed anchored (`^BEFORE resolved=…`; `TB` first left as ` total=…` — unanchored, found by PREP-3 review F-CIR-001 — then anchored to `^BEFORE .* total=([0-9]+) module_edges=.*`, amendment applied in INPUT-2 before its approval); expected text says the module edges are counted separately | builder cycle 1 (build-progress.md, D-CIR-C10-ORACLE): total 15882 ≠ 15405 because MODULE→MODULE IMPORTS grew 15 → 492 while FILE→FILE + unresolved = 15390 before AND after; retained store: IMPORTS = 1687 FILE→FILE + 15 MODULE→MODULE (manager, sqlite read-only 2026-09-18); greedy sed captured `poco_prefixed_unresolved=` | in-place-manager | sha256:693e23e5dd5f3c79e50a4badc27459b19d0e03b39b2214a9efa8f02b7ea14784 → sha256:d131b78de32d37d96148cdf79c9a5156073a0548dbdbec0aac6ea601b2816f07 |
| OC-2 | 2026-09-18 | CIR-C10 (+ §1, §2.6, packet examples) | witness `Net/src/HTTPClientSession.cpp → Foundation/include/Poco/Exception.h` → `Net/src/AbstractHTTPRequestHandler.cpp:20 #include "Poco/Exception.h"` → same header | `grep -n '^#include' Net/src/HTTPClientSession.cpp` lists only Poco/Net/* and other Foundation headers (no Poco/Exception.h); `sed -n 20p Net/src/AbstractHTTPRequestHandler.cpp` = `#include "Poco/Exception.h"`; 25 Net/src files include it directly; builder: 402 resolved edges to that header after vs 0 before | in-place-manager | (same edit) |

Both corrections change only oracle text and explanatory prose: no allocation set, check-id set, requirement text or candidate path changes. The runtime binds the allocation digest, so both are carried as CPP-INCLUDE-ROOTS-1-INPUT-2 (OC-1a amended INPUT-2 in place before approval) (TD-022).

| OC-1a | 2026-09-18 | CIR-C10 | `TB=$(sed -E 's/.* total=([0-9]+) .*/\1/' …)` → `TB=$(sed -E 's/^BEFORE .* total=([0-9]+) module_edges=.*/\1/' …)` | PREP-3 review F-CIR-001 (codex gpt-5.6-terra): OC-1 claimed both extractions anchored; TB was not | in-place-manager | sha256:d131b78de32d37d96148cdf79c9a5156073a0548dbdbec0aac6ea601b2816f07 → sha256:48f7625c9e17443b1a3ea41c8ae7e86db8921fa41a7df35d4a5abb1d1ad0399e |
